# Правила тарификации для Yandex Monitoring

Чтобы рассчитать стоимость использования сервиса, ознакомьтесь с тарифами в этом разделе.

Цены на продукты сервиса также доступны в [Прайс-листе](https://yandex.cloud/ru/price-list).


Все цены в рублях и тенге указаны с НДС, все цены в долларах — без НДС.

## Из чего складывается стоимость использования Monitoring {#rules}

На данный момент в рамках сервиса Monitoring тарифицируется запись пользовательских метрик через [Monitoring API](api-ref/index.md) и запись любых метрик через [Prometheus Remote API](operations/prometheus/index.md), а также чтение любых метрик через [Monitoring API](api-ref/index.md).

Запись значений метрик ресурсов Yandex Cloud и чтение метрик через Prometheus Remote API не тарифицируются.

Особенности тарификации:
* После записи или чтения первых 50 млн значений через Monitoring API стоимость записи снижается. См. [Цены для региона Россия](#prices).
* Запись метрик ресурсов Yandex Cloud, собираемых автоматически, не тарифицируется.
* Чтение метрик при помощи интерфейса Monitoring и при помощи консоли Yandex Cloud не тарифицируется.
* Входящий и исходящий трафик в Monitoring не тарифицируются.

### Пример расчета стоимости {#example}


{% list tabs group=pricing %}

- Расчет в рублях {#prices-rub}

  **Пример 1**
  
  Стоимость пользования сервисом Monitoring в течение 30 дней при записи 20 метрик с частотой **1 значение в минуту** через Monitoring API:
  
  > 20 × 1 × (60 × 24 × 30) = 864&nbsp;000 значений = 0,864 млн значений
  > 0,864 × 0,32 = {% calc [currency=RUB] 0,864 × 0,32 %} = {% calc [currency=RUB] round((0,864 × 0,32) × 100) / 100 %}
  > 
  > Итого: {% calc [currency=RUB] round((0,864 × 0,32) × 100) / 100 %}
  
  Где:
  
  * 20 — количество метрик.
  * 1 — количество записанных значений в минуту.
  * (60 × 24 × 30) — количество минут в 30 днях.
  * 0,32 — стоимость записи 1 млн значений.
  
  **Пример 2**
  
  Стоимость экспорта 100 метрик из Monitoring в собственную инсталляцию системы мониторинга Prometheus с периодом сбора **15 секунд** в течение 30 дней через Monitoring API:
  
  > 100 × (60 / 15) × (60 × 24 × 30) = 17&nbsp;280&nbsp;000 значений = 17,28 млн значений
  > 17,28 × 7,686 ₽ = {% calc [currency=RUB] round((17,28 × 7.686) × 100) / 100 %}
  > 
  > Итого: {% calc [currency=RUB] round((17,28 × 7.686) × 100) / 100 %}
  
  Где:
  
  * 100 — количество метрик.
  * (60 / 15) — количество считанных значений в минуту.
  * (60 × 24 × 30) — количество минут в 30 днях.
  * 7,686 ₽ — стоимость чтения 1 млн значений (до 50 млн значений).

- Расчет в тенге {#prices-kzt}

  **Пример 1**
  
  Стоимость пользования сервисом Monitoring в течение 30 дней при записи 20 метрик с частотой **1 значение в минуту** через Monitoring API:
  
  > 20 × 1 × (60 × 24 × 30) = 864&nbsp;000 значений = 0,864 млн значений
  > 0,864 × 1,63 = {% calc [currency=KZT] 0,864 × 1,63 %} = {% calc [currency=KZT] round((0,864 × 1,63) × 100) / 100 %}
  > 
  > Итого: {% calc [currency=KZT] round((0,864 × 1,63) × 100) / 100 %}
  
  Где:
  
  * 20 — количество метрик.
  * 1 — количество записанных значений в минуту.
  * (60 × 24 × 30) — количество минут в 30 днях.
  * 1,63 — стоимость записи 1 млн значений.
  
  **Пример 2**
  
  Стоимость экспорта 100 метрик из Monitoring в собственную инсталляцию системы мониторинга Prometheus с периодом сбора **15 секунд** в течение 30 дней через Monitoring API:
  
  > 100 × (60 / 15) × (60 × 24 × 30) = 17&nbsp;280&nbsp;000 значений = 17,28 млн значений
  > 17,28 × 38,43 ₸ = {% calc [currency=KZT] round((17,28 × 38.43) × 100) / 100 %}
  > 
  > Итого: {% calc [currency=KZT] round((17,28 × 38.43) × 100) / 100 %}
  
  Где:
  
  * 100 — количество метрик.
  * (60 / 15) — количество считанных значений в минуту.
  * (60 × 24 × 30) — количество минут в 30 днях.
  * 38,43 ₸ — стоимость чтения 1 млн значений (до 50 млн значений).

{% endlist %}







## Цены для региона Россия {#prices}



{% note info %}

Цены на ресурсы Yandex Cloud в разных регионах различаются. Подробнее о доступных регионах в разделе [Регионы](../overview/concepts/region.md).

Валюта, которой можно оплачивать ресурсы, зависит от юридического лица, с которым пользователь заключил договор. Подробнее о регистрации аккаунта в разделе [Регистрация аккаунта в Yandex Cloud](../billing/quickstart/index.md).

{% endnote %}

Минимальная единица тарификации — 1 значение метрики. Стоимость округляется до копейки, тиына или цента.


Например, стоимость записи первых 86 400 значений составит `(86 400 значений / 1 млн) × 0,32 ₽ = {% calc [currency=RUB] 86400 / 1000000 × 0.32 %}` и будет округлена до `{% calc [currency=RUB] round((86400 / 1000000 × 0.32) × 100 ) / 100 %}`. При этом стоимость записи 87 000 значений составит `(87 000 значений / 1 млн) × 0,32 ₽ = {% calc [currency=RUB] 87000 / 1000000 × 0.32 %}` и будет округлена до `{% calc [currency=RUB] round((87000 / 1000000 × 0.32) × 100 ) / 100 %}`. Где `0,32 ₽` — цена за 1 млн значений (при записи до 50 млн значений).

| Услуга                                                                                                   | Цена              | Ед. тарификации | Действует с   | Действует до |
| -------------------------------------------------------------------------------------------------------- | ----------------- | --------------- | ------------- | ------------ |
| Monium Prometheus. Запись метрик через Prometheus Remote API, от 0 до 50 единицы тарификации в месяц     | Не тарифицируется | 1 млн значений  | 1 января 2026 | —            |
| Monium Prometheus. Запись метрик через Prometheus Remote API, от 50 до 10000 единицы тарификации в месяц | 2,3058 ₽          | 1 млн значений  | 1 января 2026 | —            |
| Monium Prometheus. Запись метрик через Prometheus Remote API, от 10000 единицы тарификации в месяц       | 0,6588 ₽          | 1 млн значений  | 1 января 2026 | —            |
| Monium. Запись пользовательских метрик через API                                                         | 0,32 ₽            | 1 млн значений  | 4 марта 2026  | —            |
| Monium. Чтение любых метрик через API, от 0 до 50 единицы тарификации в месяц                            | 7,686 ₽           | 1 млн значений  | 1 января 2026 | —            |
| Monium. Чтение любых метрик через API, от 50 единицы тарификации в месяц                                 | 4,6116 ₽          | 1 млн значений  | 1 января 2026 | —            |
| Monium. Экспорт метрик в формате Prometheus через API, от 0 до 50 единицы тарификации в месяц            | 7,686 ₽           | 1 млн значений  | 1 января 2026 | —            |
| Monium. Экспорт метрик в формате Prometheus через API, от 50 единицы тарификации в месяц                 | 4,6116 ₽          | 1 млн значений  | 1 января 2026 | —            |





_© 2025 Linux Foundation. Все права защищены. Linux Foundation зарегистрировала товарные знаки и использует товарные знаки. Список товарных знаков Linux Foundation см. на странице [Trademark Usage](https://www.linuxfoundation.org/legal/trademark-usage)._
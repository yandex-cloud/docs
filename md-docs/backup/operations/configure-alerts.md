[Документация Yandex Cloud](../../index.md) > [Yandex Cloud Backup](../index.md) > [Пошаговые инструкции](index.md) > Настроить алерты Monium

# Настроить алерты Yandex Monium для ресурсов Yandex Cloud Backup

_Алерт_ — набор последовательных именованных [запросов](../../monium/concepts/data-model.md#queries), которые вычисляются один раз в минуту. Полученное значение запроса сравнивается с заданными пороговыми значениями. Если порог достигнут, Monium переводит алерт в [статус](../../monium/concepts/alerting/alert.md#alert-statuses) `Alarm` или `Warning` и оповещает пользователя по [каналу уведомления](../../monium/concepts/alerting/notification-channel.md).

Вы можете [настроить](../../monium/operations/alert/create-alert.md) алерт на любую из метрик [Yandex Cloud Backup](../index.md). Рекомендуется создать алерты на следующие события:

* Доступна новая версия [агента Cloud Backup](../concepts/agent.md).
* Агент Cloud Backup недоступен.
* Неудачная операция резервного копирования.

{% note info %}

Если метрика еще ни разу не поступала в Monium, система не сможет предложить выбрать эту метрику в выпадающем меню конструктора запросов. В этом случае введите запрос в режиме кода на [языке запросов](../../monium/concepts/querying.md).

{% endnote %}


### Доступные версии агентов {#version-available}

Чтобы создать алерт для уведомлений о доступных версиях агента:

{% list tabs group=instructions %}

- Консоль управления {#console}

  1. На главной странице [Monium](https://monium.yandex.cloud) слева выберите раздел **Алерты**.
  1. Вверху справа нажмите **Создать алерт** → **Пользовательский**.
  1. В поле **Название** укажите `agent_available_new_version`
  1. В блоке **Настройки алерта**:
      1. Включите режим кода, нажав ![image](../../_assets/console-icons/code.svg).
      1. Введите запрос на языке запросов:

          ```text
          {project = "folder__<идентификатор_каталога>", service = "__backup__", cluster = "default", resource_type = "agent", name = "agent_version_available", resource_name = "<имя_ВМ>"}
          ```

          Где:

          * `project` — идентификатор каталога, в котором находится ВМ. Идентификатор каталога можно [скопировать](../../resource-manager/operations/folder/get-id.md) в [консоли управления](https://console.yandex.cloud) на панели сверху.
          * `service` — имя сервиса.
          * `cluster` — окружение.
          * `resource_type` — тип ресурса.
          * `name` — имя метрики.
          * `resource_name` — имя виртуальной машины. Чтобы выбрать все ВМ, вместо имени ВМ укажите `"*"`.

  1. В блоке **Условия срабатывания** укажите:
      * **Функция сравнения** — `Не равно`.
      * **Warning** — удалите значение.
      * **Alarm** — `0`.

  1. В блоке **Уведомления** добавьте нужные каналы уведомлений.
  1. Нажмите **Создать алерт**.

{% endlist %}


### Агент недоступен {#agent-offline}

Чтобы создать алерт для уведомлений о недоступности агента:

{% list tabs group=instructions %}

- Консоль управления {#console}

  1. На главной странице [Monium](https://monium.yandex.cloud) слева выберите раздел **Алерты**.
  1. Вверху справа нажмите **Создать алерт** → **Пользовательский**.
  1. В поле **Название** укажите `agent_status_offline`
  1. В блоке **Настройки алерта**:
      1. Включите режим кода, нажав ![image](../../_assets/console-icons/code.svg).
      1. Введите запрос на языке запросов:

          ```text
          {project = "folder__<идентификатор_каталога>", service = "__backup__", cluster = "default", resource_type = "agent", name = "agent_alive", resource_name = "<имя_ВМ>"}
          ```

          Где:

          * `project` — идентификатор каталога, в котором находится ВМ. Идентификатор каталога можно [скопировать](../../resource-manager/operations/folder/get-id.md) в [консоли управления](https://console.yandex.cloud) на панели сверху.
          * `service` — имя сервиса.
          * `cluster` — окружение.
          * `resource_type` — тип ресурса.
          * `name` — имя метрики.
          * `resource_name` — имя виртуальной машины. Чтобы выбрать все ВМ, вместо имени ВМ укажите `"*"`.

  1. В блоке **Условия срабатывания** укажите:
      * **Функция сравнения** — `Равно`.
      * **Warning** — удалите значение.
      * **Alarm** — `0`.

  1. В блоке **Уведомления** добавьте нужные каналы уведомлений.
  1. Нажмите **Создать алерт**.

{% endlist %}


### Неудачная операция бэкапа {#operation-failure}

Чтобы создать алерт для уведомлений о неудачных операциях бэкапа:

{% list tabs group=instructions %}

- Консоль управления {#console}

  1. На главной странице [Monium](https://monium.yandex.cloud) слева выберите раздел **Алерты**.
  1. Вверху справа нажмите **Создать алерт** → **Пользовательский**.
  1. В поле **Название** укажите `agent_operation_failure`
  1. В блоке **Настройки алерта**:
      1. Включите режим кода, нажав ![image](../../_assets/console-icons/code.svg).
      1. Введите запрос на языке запросов:

          ```text
          {project = "folder__<идентификатор_каталога>", service = "__backup__", cluster = "default", resource_type = "backup", name = "BackupError", action = "CompleteFailed", resource_name = "<имя_ВМ>"}
          ```

          Где:

          * `project` — идентификатор каталога, в котором находится ВМ. Идентификатор каталога можно [скопировать](../../resource-manager/operations/folder/get-id.md) в [консоли управления](https://console.yandex.cloud) на панели сверху.
          * `service` — имя сервиса.
          * `cluster` — окружение.
          * `resource_type` — тип ресурса.
          * `name` — имя метрики.
          * `action` — тип действия.
          * `resource_name` — имя виртуальной машины. Чтобы выбрать все ВМ, вместо имени ВМ укажите `"*"`.

  1. В блоке **Условия срабатывания** укажите:
      * **Функция сравнения** — `Больше`.
      * **Warning** — удалите значение.
      * **Alarm** — `0`.

  1. В блоке **Уведомления** добавьте нужные каналы уведомлений.
  1. Нажмите **Создать алерт**.

{% endlist %}


#### Полезные ссылки {#see-also}

* [Посмотреть статистику резервного копирования](get-stats.md)
* [Справочник метрик Yandex Monitoring](../metrics.md)
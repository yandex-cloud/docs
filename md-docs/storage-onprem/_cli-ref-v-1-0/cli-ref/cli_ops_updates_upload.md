[Документация Yandex Cloud](../../../index.md) > [storage-onprem/_cli-ref-v-1-0](../index.md) > ops > updates > upload

# cli ops updates upload

Upload and unpack an update archive

```
cli ops updates upload <archive-file> [flags]
```

## Options

```
      --ca-bundle string   Path to a CA certificate bundle for the S3 upload endpoint
  -h, --help               help for upload
      --no-verify-ssl      Disable TLS certificate verification for the S3 upload endpoint
      --wait               Wait for task completion
```

## Options inherited from parent commands

```
  -c, --config-dir string   path to configuration directory
      --debug               enable debug mode
      --insecure            use if console has self-signed certificate
  -p, --profile string      configuration profile
```

## See also

* [cli ops updates](cli_ops_updates.md)	 — Manage updates
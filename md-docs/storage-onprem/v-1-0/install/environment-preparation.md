# Подготовка окружения

Здесь и далее рассматривается пример установки стенда с названием `my-onprem` со следующими параметрами:

* Четыре хоста, репликация x3.
    Три хоста нужны для минимальной отказоустойчивости, а четвертый – для поддержки производительности при отказе одного из хостов.

* Internal-подсеть `10.0.0.0/24`.
    Эта сеть будет использоваться для доступа с установочного хоста.

* Data-подсеть `10.0.1.0/24`.
* Management-подсеть `10.0.2.0/24`.

{% note info %}

В примерах описан случай с IPv4, но также возможно использование IPv6.

{% endnote %}


## Установочный хост {#installation-host}

Минимальные требования к установочному хосту:

* Сетевой доступ к хостам стенда.
* 15 GiB свободного места для архивов поставки.
* Linux или macOS с bash 5.2 для выполнения скриптов установки.

На установочный хост необходимо любым образом загрузить и распаковать архив `install-kit.tar.gz`, включенный в поставку.


# Хосты стенда {#booth-hosts}

Перед установкой стенда выполните минимальную предварительную настройку хостов:

* Установите Ubuntu 22.04 на все серверы.
* Настройте доступ по SSH с установочного хоста.
* Проверьте, что хосты подключены к internal-, data- и management-подсетям (см. [Требования к системе](../concepts/architecture/system-requirements.md)).
* Разметьте NVMe-диск: выделите отдельный раздел для метаданных хранилища (см. [Требования к системе](../concepts/architecture/system-requirements.md)).

Настройте сеть так, чтобы были выполнены следующие условия:

* Все серверы кластера должны быть связаны между собой так, чтобы можно было создать сеть на основе VxLAN mesh.
* Маршрутизация (routing и policy routing) должна быть настроена так, чтобы трафик из других интерфейсов не попадал во внутреннюю VxLAN-сеть.
* Ответы на запросы к адресам management и data (например, S3 API) должны отправляться с того же интерфейса, с которого поступил запрос, — независимо от таблицы маршрутизации. Для этого не требуется настраивать default-маршрут. Отсутствие default-маршрута не должно мешать корректной работе.

Если вы используете типовую схему с bonding (агрегация каналов через LACP/Failover) и разделением на три сегмента (internal, mgmt, data):

* Используйте TUI-утилиту lanconfig для настройки сети. Lanconfig отображает состояние каналов (link, LLDP, LACP), позволяет проверить доступность (ping, DNS), а также показывает состояние IPMI.
* Настройте IP-адреса, соберите bonding-интерфейсы, проверьте и настройте IPMI.

Обеспечьте стабильную связность между всеми серверами и корректную маршрутизацию в каждом из сегментов: internal, data и management.

![connectivity-status](../_assets/install/connectivity-status.png)

В результате настройки:

* Появится конфигурационный файл `/etc/netconf.yaml`, отражающий эти изменения. Этот файл не используется, он нужен только для сохранения статуса утилиты.
* Будет сгенерирован конфигурационный файл `/etc/netplan/50-lanconfig.yaml` для netplan.
* Появится файл `/etc/default/vxlan_peers` со списком Peers — internal-адресов всех серверов.

В `/etc/default/vxlan_node` укажите уникальный номер сервера в формате `NODEID=<номер>`. Нумерация должна начинаться с `0`.

VXLAN применяется через networkd-dispatcher `/etc/networkd-dispatcher/routable.d/50-vxlan`.


#### Что дальше? {#whats-next}

* [Настройка параметров инсталляции](setup-install-params.md)
* [Шаги установки](installation-steps.md)
* [Устранение неисправностей при установке](../troubleshooting/installation-errors.md)
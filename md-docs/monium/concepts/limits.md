[Документация Yandex Cloud](../../index.md) > [Monium](../index.md) > Концепции > Квоты и лимиты

# Квоты и лимиты в Monium

В Monium действуют ограничения (квоты и лимиты) на ряд ресурсов.

* [_Квоты_](https://console.yandex.cloud/cloud?section=quotas) — организационные ограничения, которые можно изменить по запросу в техническую поддержку.
* _Лимиты_ — технические ограничения, обусловленные особенностями архитектуры Yandex Cloud. Изменить лимиты невозможно.

## Посмотреть действующие квоты {#view-quotas}

{% list tabs group=instructions %}

- Интерфейс Monium {#console}
  
  1. На главной странице [Monium](https://monium.yandex.cloud) слева выберите **Квоты**.
  1. Слева вверху выберите нужный проект или оставьте текущий.
  1. На вкладке **Квоты проекта** посмотрите значения установленных квот по группам ресурсов.
  1. Чтобы посмотреть квоты по отдельным шардам, на странице **Квоты** перейдите на вкладку **Распределение по шардам**.
  1. Для каждого шарда приведены значения:
     * **Используется** — текущее потребление ресурса в шарде.
     * **Лимит** — максимальное значение квоты для шарда.
     * **Утилизация** — доля использованной квоты (отношение потребления к лимиту). Показывает, насколько близко текущее потребление к квоте.

{% endlist %}

## Изменить квоты {#change-quotas}

{% list tabs group=instructions %}

- Интерфейс Monium {#console}
  
  1. Убедитесь, что ваш аккаунт имеет роль [monium.admin](../security/index.md#monium-admin) или [monium.editor](../security/index.md#monium-editor).
  1. На главной странице [Monium](https://monium.yandex.cloud) слева выберите **Квоты**.
  1. Слева вверху выберите нужный проект или оставьте текущий.
  1. На вкладке **Квоты проекта** в нужной группе квот нажмите ![image](../../_assets/console-icons/pencil.svg) **Изменить объём квот**.
  1. В открывшемся окне введите новые значения для квот этой группы.
  1. Нажмите **Отправить**.

{% endlist %}

Квоты можно увеличивать и уменьшать.

Увеличение квоты подтверждается автоматически, но только до определенного предела. Если требуется большее значение, система предложит обратиться в [поддержку](https://center.yandex.cloud/support) для дальнейшего согласования.

Уменьшение квоты помогает ограничить неожиданный рост потребления. Например, после релиза приложение начало записывает слишком много логов. В этом случае сначала можно снизить квоту, а затем выяснить причину, откатить релиз или внести исправление. Это поможет контролировать расходы.

## Квотируемые ресурсы {#quota-resources}

* **Метрики**:
  * `Metrics Write` — скорость записи точек в метрики шарда, в точках в секунду. При исчерпании квоты сервис начнет случайным образом отбрасывать точки для сохраняемых метрик. На графиках могут появиться пропуски.
  * `Stored Metrics` — количество уникальных метрик (временных рядов). При исчерпании квоты новые метрики не будут сохраняться. Запись данных в уже существующие метрики продолжится.
* **Алерты**:
  * `Alerts Count` — количество алертов и подалертов в проекте. При исчерпании квоты вы не сможете создать новые алерты и подалерты.
* **Логи**:
  * `Logs Write` — поток записываемых логов, [мебибайт](https://ru.wikipedia.org/wiki/Мебибайт) в секунду. При исчерпании квоты сервис начнет случайным образом отбрасывать входящие логи.
* **Трейсы**:
  * `Spans Write` — поток записываемых спанов, мебибайт в секунду. При исчерпании квоты сервис начнет случайным образом отбрасывать входящие спаны. Из-за этого часть трейсов может собираться не полностью.

## Квоты и лимиты {#limits-quotas}

### Квоты {#default-quotas}

В таблице приведены значения квот, которые установлены по умолчанию в Monium для нового проекта.

#|
|| Вид ограничения | Значение ||
|| Скорость записи точек в метрики шарда, `Metrics Write` | 20 млн/c ||
|| Количество уникальных метрик (временных рядов), `Stored Metrics` | 200 млн ||
|| Количество алертов в проекте, `Alerts Count` | 30 000 ||
|| Поток записываемых логов, `Logs Write` | 150 МиБ/с ||
|| Поток записываемых спанов, `Spans Write` | 150 МиБ/с ||
|#

### Лимиты метрик и алертов {#limits-metrics-alerts}

**Общие лимиты**

Вид ограничения | Значение
----- | -----
Количество меток для одной метрики<br/>(включая обязательные) | 16
Количество значений одной метки | 1 000

**Лимиты API**

Вид ограничения | Значение
----- | -----
Максимальное количество метрик, возвращаемое в API-запросе на выгрузку данных | 500
Максимальное количество метрик, передаваемое в API-запросе на загрузку данных | 10 000
Минимальное количество точек, возвращаемое в API-запросе на выгрузку данных | 10
Максимальное количество точек, возвращаемое в API-запросе на выгрузку данных | 10 000
Минимальный интервал времени, за который возможно выгрузить метрики | 1 секунда

**Лимиты алертинга**

Вид ограничения | Значение
----- | -----
Количество метрик для одного алерта | 1 000
Количество SMS на один номер в сутки | 100

### Лимиты логов {#limits-logs}

Вид ограничения | Значение
--------------- | --------
Максимальный размер одного батча с логами | 8 МБ
Максимальное время начала лога в прошлом (`time_unix_nano`) | 24 часа назад
Максимальное время лога в будущем (`time_unix_nano`) | 5 минут вперед
Максимальная длина имени [метки (label)](data-model.md#label) | 64 байта
Максимальная длина значения метки | 256 байт
Максимальная длина имени [метаинформации (meta)](data-model.md#meta) | 400 байт
Максимальная длина значения метаинформации | 1 МБ

{% note info %}

Допустимы батчи с логами размером до 16 МБ, но такой прием не гарантируется.

{% endnote %}

### Лимиты трейсов {#limits-traces}

**Лимиты хранения**

Вид ограничения | Значение
--------------- | --------
Время хранения данных по всем спанам | 4 дня

**Лимиты записи**

Вид ограничения | Значение
--------------- | --------
Максимальный размер одного батча со спанами | 4 МБ
Максимальное время начала спана в прошлом (`startTimeUnixNano`) | 24 часа назад
Максимальное время окончания спана в будущем (`endTimeUnixNano`) | 1 час вперед
Максимальная длина имени спана (`name`) | 2048 символов
Максимальное количество атрибутов спана (`attributes`) | 512
Максимальная длина названия атрибута | 256 символов
Максимальная длина значения атрибута | 2 097 152 символа
Максимальное количество событий в спане (`events`) | 256

**Лимиты чтения**

Вид ограничения | Значение
--------------- | --------
Максимальное количество спанов, отображаемых в интерфейсе просмотра трейса | 100 000

Для просмотра трейсов большего размера разбейте их на несколько трейсов и объедините с помощью ссылок [Span Links](https://opentelemetry.io/docs/concepts/signals/traces/#span-links).
### Can I use SQL queries to generate a dataset? {#sql-to-create-dataset}

You can directly query a database via the dataset creation interface using [SQL queries](../../datalens/dataset/settings.md#sql-request-in-datatset).

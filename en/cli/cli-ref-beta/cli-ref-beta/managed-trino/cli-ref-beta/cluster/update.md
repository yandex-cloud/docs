---
editable: false
noIndex: true
---

# yc beta managed-trino cluster update

Updates configuration of the specified Trino cluster.

#### Command Usage

Syntax:

`yc beta managed-trino cluster update <CLUSTER-ID>`

#### Flags

#|
||Flag | Description ||
|| `--cluster-id` | `string`

ID of the Trino cluster. ||
|| `--deletion-protection` | Deletion Protection inhibits deletion of the cluster. ||
|| `--description` | `string`

Description of the Trino cluster. ||
|| `--labels` | `map<string><string>`

Custom labels for the Trino cluster as '' key:value '' pairs. For example: {"env": "prod"}. ||
|| `--logging` | `shorthand/json`

Cloud logging configuration.

Shorthand Syntax:

```hcl
{
  destination = folder-id=string | log-group-id=string,
  enabled = boolean,
  min-level = TRACE|DEBUG|INFO|WARN|ERROR|FATAL
}
```

JSON Syntax:

```json
{
  "destination": {
    "folder-id": "string",
    "log-group-id": "string"
  },
  "enabled": "boolean",
  "min-level": "TRACE|DEBUG|INFO|WARN|ERROR|FATAL"
}
```

Fields:

```
enabled -> (boolean)
  Logs generated by the Trino components are delivered to Cloud Logging.
min-level -> (struct)
  Minimum log entry level. See [LogLevel.Level] for details.
destination -> (oneof<folder-id|log-group-id>)
  Oneof destination field
  folder-id -> (string)
    Logs should be written to default log group for specified folder.
  log-group-id -> (string)
    Logs should be written to log group resolved by ID.
``` ||
|| `--maintenance-window` | `shorthand/json`

Window of maintenance operations.

Shorthand Syntax:

```hcl
{
  policy = anytime={} | weekly-maintenance-window={
    day = MON|TUE|WED|THU|FRI|SAT|SUN,
    hour = integer
  }
}
```

JSON Syntax:

```json
{
  "policy": {
    "anytime": {},
    "weekly-maintenance-window": {
      "day": "MON|TUE|WED|THU|FRI|SAT|SUN",
      "hour": "integer"
    }
  }
}
```

Fields:

```
policy -> (oneof<anytime|weekly-maintenance-window>)
  Oneof policy field
  anytime -> (struct)
  weekly-maintenance-window -> (struct)
    day -> (struct)
    hour -> (integer)
      Hour of the day in UTC.
``` ||
|| `--name` | `string`

Name of the Trino cluster. The name must be unique within the folder. ||
|| `--network-spec` | `shorthand/json`

Network related configuration options.

Shorthand Syntax:

```hcl
{
  private-access = {
    enabled = boolean
  },
  security-group-ids = string,...
}
```

JSON Syntax:

```json
{
  "private-access": {
    "enabled": "boolean"
  },
  "security-group-ids": [
    "string", ...
  ]
}
```

Fields:

```
private-access -> (struct)
  Private access configuration for secure connectivity to the cluster.
  enabled -> (boolean)
    Enables access to the cluster only via private endpoint.
security-group-ids -> ([]string)
  User security groups.
``` ||
|| `--service-account-id` | `string`

Service account used to access Cloud resources. ||
|| `--trino` | `shorthand/json`

Configuration of Trino components.

Shorthand Syntax:

```hcl
{
  access-control = {
    catalog-session-properties = [
      {
        allow = NO|YES,
        catalog = {
          match-by = ids={
            any = string,...
          } | name-regexp=string | names={
            any = string,...
          }
        },
        description = string,
        groups = string,...,
        property = {
          match-by = name-regexp=string | names={
            any = string,...
          }
        },
        users = string,...
      }, ...
    ],
    catalogs = [
      {
        catalog = {
          match-by = ids={
            any = string,...
          } | name-regexp=string | names={
            any = string,...
          }
        },
        description = string,
        groups = string,...,
        permission = NONE|ALL|READ_ONLY,
        users = string,...
      }, ...
    ],
    functions = [
      {
        catalog = {
          match-by = ids={
            any = string,...
          } | name-regexp=string | names={
            any = string,...
          }
        },
        description = string,
        function = {
          match-by = name-regexp=string | names={
            any = string,...
          }
        },
        groups = string,...,
        privileges = [
          EXECUTE|GRANT_EXECUTE|OWNERSHIP, ...
        ],
        schema = {
          match-by = name-regexp=string | names={
            any = string,...
          }
        },
        users = string,...
      }, ...
    ],
    procedures = [
      {
        catalog = {
          match-by = ids={
            any = string,...
          } | name-regexp=string | names={
            any = string,...
          }
        },
        description = string,
        groups = string,...,
        privileges = [
          EXECUTE, ...
        ],
        procedure = {
          match-by = name-regexp=string | names={
            any = string,...
          }
        },
        schema = {
          match-by = name-regexp=string | names={
            any = string,...
          }
        },
        users = string,...
      }, ...
    ],
    queries = [
      {
        description = string,
        groups = string,...,
        privileges = [
          VIEW|EXECUTE|KILL, ...
        ],
        query-owners = string,...,
        users = string,...
      }, ...
    ],
    schemas = [
      {
        catalog = {
          match-by = ids={
            any = string,...
          } | name-regexp=string | names={
            any = string,...
          }
        },
        description = string,
        groups = string,...,
        owner = NO|YES,
        schema = {
          match-by = name-regexp=string | names={
            any = string,...
          }
        },
        users = string,...
      }, ...
    ],
    system-session-properties = [
      {
        allow = NO|YES,
        description = string,
        groups = string,...,
        property = {
          match-by = name-regexp=string | names={
            any = string,...
          }
        },
        users = string,...
      }, ...
    ],
    tables = [
      {
        catalog = {
          match-by = ids={
            any = string,...
          } | name-regexp=string | names={
            any = string,...
          }
        },
        columns = [
          {
            access = NONE|ALL,
            mask = string,
            name = string
          }, ...
        ],
        description = string,
        filter = string,
        groups = string,...,
        privileges = [
          SELECT|INSERT|DELETE|UPDATE|OWNERSHIP|GRANT_SELECT, ...
        ],
        schema = {
          match-by = name-regexp=string | names={
            any = string,...
          }
        },
        table = {
          match-by = name-regexp=string | names={
            any = string,...
          }
        },
        users = string,...
      }, ...
    ]
  },
  coordinator-config = {
    resources = {
      resource-preset-id = string
    }
  },
  resource-management = {
    query = {
      properties = {key=string, key=...}
    },
    resource-groups = {
      cpu-quota-period = string,
      root-groups = [
        {
          hard-concurrency-limit = integer,
          hard-cpu-limit = string,
          max-queued = integer,
          name = string,
          scheduling-policy = FAIR|WEIGHTED|WEIGHTED_FAIR|QUERY_PRIORITY,
          scheduling-weight = integer,
          soft-concurrency-limit = integer,
          soft-cpu-limit = string,
          soft-memory-limit = string,
          sub-groups = <ResourceGroupConfig>
        }, ...
      ],
      selectors = [
        {
          client-tags = string,...,
          group = string,
          query-type = SELECT|EXPLAIN|DESCRIBE|INSERT|UPDATE|MERGE|DELETE|ANALYZE|DATA_DEFINITION|ALTER_TABLE_EXECUTE,
          source = string,
          user = string,
          user-group = string
        }, ...
      ]
    }
  },
  retry-policy = {
    additional-properties = {key=string, key=...},
    exchange-manager = {
      additional-properties = {key=string, key=...},
      storage = {
        type = service-s3={}
      }
    },
    policy = QUERY|TASK
  },
  tls = {
    trusted-certificates = string,...
  },
  version = string,
  worker-config = {
    resources = {
      resource-preset-id = string
    },
    scale-policy = {
      scale-type = auto-scale={
        max-count = integer,
        min-count = integer
      } | fixed-scale={
        count = integer
      }
    }
  }
}
```

JSON Syntax:

```json
{
  "access-control": {
    "catalog-session-properties": [
      {
        "allow": "NO|YES",
        "catalog": {
          "match-by": {
            "ids": {
              "any": [
                "string", ...
              ]
            },
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "description": "string",
        "groups": [
          "string", ...
        ],
        "property": {
          "match-by": {
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "users": [
          "string", ...
        ]
      }, ...
    ],
    "catalogs": [
      {
        "catalog": {
          "match-by": {
            "ids": {
              "any": [
                "string", ...
              ]
            },
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "description": "string",
        "groups": [
          "string", ...
        ],
        "permission": "NONE|ALL|READ_ONLY",
        "users": [
          "string", ...
        ]
      }, ...
    ],
    "functions": [
      {
        "catalog": {
          "match-by": {
            "ids": {
              "any": [
                "string", ...
              ]
            },
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "description": "string",
        "function": {
          "match-by": {
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "groups": [
          "string", ...
        ],
        "privileges": [
          "EXECUTE|GRANT_EXECUTE|OWNERSHIP", ...
        ],
        "schema": {
          "match-by": {
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "users": [
          "string", ...
        ]
      }, ...
    ],
    "procedures": [
      {
        "catalog": {
          "match-by": {
            "ids": {
              "any": [
                "string", ...
              ]
            },
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "description": "string",
        "groups": [
          "string", ...
        ],
        "privileges": [
          "EXECUTE", ...
        ],
        "procedure": {
          "match-by": {
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "schema": {
          "match-by": {
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "users": [
          "string", ...
        ]
      }, ...
    ],
    "queries": [
      {
        "description": "string",
        "groups": [
          "string", ...
        ],
        "privileges": [
          "VIEW|EXECUTE|KILL", ...
        ],
        "query-owners": [
          "string", ...
        ],
        "users": [
          "string", ...
        ]
      }, ...
    ],
    "schemas": [
      {
        "catalog": {
          "match-by": {
            "ids": {
              "any": [
                "string", ...
              ]
            },
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "description": "string",
        "groups": [
          "string", ...
        ],
        "owner": "NO|YES",
        "schema": {
          "match-by": {
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "users": [
          "string", ...
        ]
      }, ...
    ],
    "system-session-properties": [
      {
        "allow": "NO|YES",
        "description": "string",
        "groups": [
          "string", ...
        ],
        "property": {
          "match-by": {
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "users": [
          "string", ...
        ]
      }, ...
    ],
    "tables": [
      {
        "catalog": {
          "match-by": {
            "ids": {
              "any": [
                "string", ...
              ]
            },
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "columns": [
          {
            "access": "NONE|ALL",
            "mask": "string",
            "name": "string"
          }, ...
        ],
        "description": "string",
        "filter": "string",
        "groups": [
          "string", ...
        ],
        "privileges": [
          "SELECT|INSERT|DELETE|UPDATE|OWNERSHIP|GRANT_SELECT", ...
        ],
        "schema": {
          "match-by": {
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "table": {
          "match-by": {
            "name-regexp": "string",
            "names": {
              "any": [
                "string", ...
              ]
            }
          }
        },
        "users": [
          "string", ...
        ]
      }, ...
    ]
  },
  "coordinator-config": {
    "resources": {
      "resource-preset-id": "string"
    }
  },
  "resource-management": {
    "query": {
      "properties": {
        "<key>": "string", ...
      }
    },
    "resource-groups": {
      "cpu-quota-period": "string",
      "root-groups": [
        {
          "hard-concurrency-limit": "integer",
          "hard-cpu-limit": "string",
          "max-queued": "integer",
          "name": "string",
          "scheduling-policy": "FAIR|WEIGHTED|WEIGHTED_FAIR|QUERY_PRIORITY",
          "scheduling-weight": "integer",
          "soft-concurrency-limit": "integer",
          "soft-cpu-limit": "string",
          "soft-memory-limit": "string",
          "sub-groups": "<ResourceGroupConfig>"
        }, ...
      ],
      "selectors": [
        {
          "client-tags": [
            "string", ...
          ],
          "group": "string",
          "query-type": "SELECT|EXPLAIN|DESCRIBE|INSERT|UPDATE|MERGE|DELETE|ANALYZE|DATA_DEFINITION|ALTER_TABLE_EXECUTE",
          "source": "string",
          "user": "string",
          "user-group": "string"
        }, ...
      ]
    }
  },
  "retry-policy": {
    "additional-properties": {
      "<key>": "string", ...
    },
    "exchange-manager": {
      "additional-properties": {
        "<key>": "string", ...
      },
      "storage": {
        "type": {
          "service-s3": {}
        }
      }
    },
    "policy": "QUERY|TASK"
  },
  "tls": {
    "trusted-certificates": [
      "string", ...
    ]
  },
  "version": "string",
  "worker-config": {
    "resources": {
      "resource-preset-id": "string"
    },
    "scale-policy": {
      "scale-type": {
        "auto-scale": {
          "max-count": "integer",
          "min-count": "integer"
        },
        "fixed-scale": {
          "count": "integer"
        }
      }
    }
  }
}
```

Fields:

```
access-control -> (struct)
  Configuration for access control, specifying the fine-grained access rules.
  catalog-session-properties -> ([]struct)
    Catalog session property access control rules.
    allow -> (struct)
      Whether the rule allows setting the property.
    catalog -> (struct)
      Catalog matcher specifying what catalogs the rule is applied to.
      match-by -> (oneof<ids|name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Catalog name regexp the rule is applied to.
        ids -> (struct)
          Catalog IDs rule is applied to.
          any -> ([]string)
        names -> (struct)
          Catalog names rule is applied to.
          any -> ([]string)
    description -> (string)
      Rule description.
    groups -> ([]string)
      IAM group IDs the rule is applied to.
    property -> (struct)
      Property matcher specifying what properties the rule is applied to.
      match-by -> (oneof<name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Property name regexp the rule is applied to.
        names -> (struct)
          Property names the rule is applied to.
          any -> ([]string)
    users -> ([]string)
      IAM user IDs the rule is applied to.
  catalogs -> ([]struct)
    Catalog access control rules.
    catalog -> (struct)
      Catalog matcher specifying what catalogs the rule is applied to.
      match-by -> (oneof<ids|name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Catalog name regexp the rule is applied to.
        ids -> (struct)
          Catalog IDs rule is applied to.
          any -> ([]string)
        names -> (struct)
          Catalog names rule is applied to.
          any -> ([]string)
    description -> (string)
      Rule description.
    groups -> ([]string)
      IAM group IDs the rule is applied to.
    permission -> (struct)
      Permission granted by the rule.
    users -> ([]string)
      IAM user IDs the rule is applied to.
  functions -> ([]struct)
    Function access control rules.
    catalog -> (struct)
      Catalog matcher specifying what catalogs the rule is applied to.
      match-by -> (oneof<ids|name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Catalog name regexp the rule is applied to.
        ids -> (struct)
          Catalog IDs rule is applied to.
          any -> ([]string)
        names -> (struct)
          Catalog names rule is applied to.
          any -> ([]string)
    description -> (string)
      Rule description.
    function -> (struct)
      Function matcher specifying what functions the rule is applied to.
      match-by -> (oneof<name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Function name regexp the rule is applied to.
        names -> (struct)
          Function names the rule is applied to.
          any -> ([]string)
    groups -> ([]string)
      IAM group IDs the rule is applied to.
    privileges -> ([]struct)
      Privileges granted by the rule.
    schema -> (struct)
      Schema matcher specifying what schema the rule is applied to.
      match-by -> (oneof<name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Schema name regexp the rule is applied to.
        names -> (struct)
          Schema names the rule is applied to.
          any -> ([]string)
    users -> ([]string)
      IAM user IDs the rule is applied to.
  procedures -> ([]struct)
    Procedures access control rules.
    catalog -> (struct)
      Catalog matcher specifying what catalogs the rule is applied to.
      match-by -> (oneof<ids|name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Catalog name regexp the rule is applied to.
        ids -> (struct)
          Catalog IDs rule is applied to.
          any -> ([]string)
        names -> (struct)
          Catalog names rule is applied to.
          any -> ([]string)
    description -> (string)
      Rule description.
    groups -> ([]string)
      IAM group IDs the rule is applied to.
    privileges -> ([]struct)
      Privileges granted by the rule.
    procedure -> (struct)
      Procedure matcher specifying what functions the rule is applied to.
      match-by -> (oneof<name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Procedure name regexp the rule is applied to.
        names -> (struct)
          Procedure names the rule is applied to.
          any -> ([]string)
    schema -> (struct)
      Schema matcher specifying what schema the rule is applied to.
      match-by -> (oneof<name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Schema name regexp the rule is applied to.
        names -> (struct)
          Schema names the rule is applied to.
          any -> ([]string)
    users -> ([]string)
      IAM user IDs the rule is applied to.
  queries -> ([]struct)
    Queries access control rules.
    description -> (string)
      Rule description.
    groups -> ([]string)
      IAM group IDs the rule is applied to.
    privileges -> ([]struct)
      Privileges granted by the user.
    query-owners -> ([]string)
      Owners of queries the rule is applied to. Cannot be combined with EXECUTE privilege.
    users -> ([]string)
      IAM user IDs the rule is applied to.
  schemas -> ([]struct)
    Schema access control rules.
    catalog -> (struct)
      Catalog matcher specifying what catalogs the rule is applied to.
      match-by -> (oneof<ids|name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Catalog name regexp the rule is applied to.
        ids -> (struct)
          Catalog IDs rule is applied to.
          any -> ([]string)
        names -> (struct)
          Catalog names rule is applied to.
          any -> ([]string)
    description -> (string)
      Rule description.
    groups -> ([]string)
      IAM group IDs the rule is applied to.
    owner -> (struct)
      Ownership granted by the rule.
    schema -> (struct)
      Schema matcher specifying what schemas the rule is applied to.
      match-by -> (oneof<name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Schema name regexp the rule is applied to.
        names -> (struct)
          Schema names the rule is applied to.
          any -> ([]string)
    users -> ([]string)
      IAM user IDs the rule is applied to.
  system-session-properties -> ([]struct)
    System session property access control rules.
    allow -> (struct)
      Whether the rule allows setting the property.
    description -> (string)
      Rule description.
    groups -> ([]string)
      IAM group IDs the rule is applied to.
    property -> (struct)
      Property matcher specifying what properties the rule is applied to.
      match-by -> (oneof<name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Property name regexp the rule is applied to.
        names -> (struct)
          Property names the rule is applied to.
          any -> ([]string)
    users -> ([]string)
      IAM user IDs the rule is applied to.
  tables -> ([]struct)
    Table access control rules.
    catalog -> (struct)
      Catalog matcher specifying what catalogs the rule is applied to.
      match-by -> (oneof<ids|name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Catalog name regexp the rule is applied to.
        ids -> (struct)
          Catalog IDs rule is applied to.
          any -> ([]string)
        names -> (struct)
          Catalog names rule is applied to.
          any -> ([]string)
    columns -> ([]struct)
      Column rules.
      access -> (struct)
        Column access mode.
      mask -> (string)
        SQL expression mask to evaluate instead of original column values. Mask should have the same type as original column.
      name -> (string)
        Column name.
    description -> (string)
      Rule description.
    filter -> (string)
      Boolean SQL expression to filter table rows for particular user.
    groups -> ([]string)
      IAM group IDs the rule is applied to.
    privileges -> ([]struct)
      Permission granted by the rule.
    schema -> (struct)
      Schema matcher specifying what schemas the rule is applied to.
      match-by -> (oneof<name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Schema name regexp the rule is applied to.
        names -> (struct)
          Schema names the rule is applied to.
          any -> ([]string)
    table -> (struct)
      Table matcher specifying what tables the rule is applied to.
      match-by -> (oneof<name-regexp|names>)
        Oneof match-by field
        name-regexp -> (string)
          Table name regexp the rule is applied to.
        names -> (struct)
          Table names the rule is applied to.
          any -> ([]string)
    users -> ([]string)
      IAM user IDs the rule is applied to.
coordinator-config -> (struct)
  Configuration for the coordinator, specifying computational resources and other settings.
  resources -> (struct)
    Configuration for computational resources assigned to the coordinator instance.
    resource-preset-id -> (string)
      ID of the preset for computational resources allocated to a instance (e.g., CPU, memory, etc.).
resource-management -> (struct)
  Configuration for cluster resource management, specifying the resource groups.
  query -> (struct)
    Query management confiugration.
    properties -> (map[string,string])
      Query properties.
  resource-groups -> (struct)
    Resource groups configuration.
    cpu-quota-period -> (string)
      Period for CPU quota calculations.
    root-groups -> ([]struct)
      Root resource groups configuration.
      hard-concurrency-limit -> (integer)
        Maximum number of queries that can run concurrently.
      hard-cpu-limit -> (string)
        Maximum CPU time per period this group can use.
      max-queued -> (integer)
        Maximum number of queued queries.
      name -> (string)
        Resource group name.
      scheduling-policy -> (struct)
        Policy for selecting queued queries and sub-group eligibility.
      scheduling-weight -> (integer)
        Weight for weighted and weighted_fair scheduling policies.
      soft-concurrency-limit -> (integer)
        Number of concurrently running queries after which new queries only run if peer groups are below soft limits.
      soft-cpu-limit -> (string)
        Maximum CPU time per period this group can use before applying penalty to running queries (requires hard_cpu_limit).
      soft-memory-limit -> (string)
        Maximum amount of distributed memory this group can use.
      sub-groups -> ([]struct)
        List of sub-groups.
    selectors -> ([]struct)
      Selector rules for routing queries to resource groups.
      client-tags -> ([]string)
        Tags that must all be present in the query's client tags.
      group -> (string)
        Target resource group name.
      query-type -> (struct)
        Type of query to match.
      source -> (string)
        Regex to match against query source.
      user -> (string)
        Regex to match against username.
      user-group -> (string)
        Regex to match against user groups.
retry-policy -> (struct)
  Configuration for retry policy, specifying the spooling storage destination and other settings.
  additional-properties -> (map[string,string])
    Additional properties.
  exchange-manager -> (struct)
    Configuration for exchange manager.
    additional-properties -> (map[string,string])
      Additional properties.
    storage -> (struct)
      Storage for spooled data by exchange manager.
      type -> (oneof<service-s3>)
        Oneof type field
        service-s3 -> (struct)
          Use service side s3 bucket for exchange manager.
  policy -> (struct)
    Retry policy level.
tls -> (struct)
  Configuration for TLS.
  trusted-certificates -> ([]string)
    Trusted CA-certificates. Each element should contain single self-signed CA-certificate or chain of CA-certificates where first certificate is the leaf and last certificate is the self-signed root.
version -> (string)
  Trino version. Format: "Number".
worker-config -> (struct)
  Configuration for worker nodes, including scaling policy and computational resources.
  resources -> (struct)
    Configuration for computational resources for worker instances.
    resource-preset-id -> (string)
      ID of the preset for computational resources allocated to a instance (e.g., CPU, memory, etc.).
  scale-policy -> (struct)
    Configuration for scaling policy for worker instances.
    scale-type -> (oneof<auto-scale|fixed-scale>)
      Oneof scale-type field
      fixed-scale -> (struct)
        A fixed scaling policy that specifies a fixed number of worker instances.
        count -> (integer)
          Specifies the number of worker instances.
      auto-scale -> (struct)
        A scaling policy that dynamically adjusts the number of worker instances based on the cluster's workload. The system automatically increases or decreases the number of instances within the defined range.
        max-count -> (integer)
          Maximum number of worker instances.
        min-count -> (integer)
          Minimum number of worker instances.
``` ||
|| `--async` | Display information about the operation in progress, without waiting for the operation to complete. ||
|#

#### Global Flags

#|
||Flag | Description ||
|| `--profile` | `string`

Set the custom profile. ||
|| `--region` | `string`

Set the region. ||
|| `--debug` | Debug logging. ||
|| `--debug-grpc` | Debug gRPC logging. Very verbose, used for debugging connection problems. ||
|| `--no-user-output` | Disable printing user intended output to stderr. ||
|| `--pager` | `string`

Set the custom pager. ||
|| `--format` | `string`

Set the output format: text, yaml, json, table, summary \|\| summary[name, instance.id, instance.disks[0].size]. ||
|| `--retry` | `int`

Enable gRPC retries. By default, retries are enabled with maximum 5 attempts.
Pass 0 to disable retries. Pass any negative value for infinite retries.
Even infinite retries are capped with 2 minutes timeout. ||
|| `--timeout` | `string`

Set the timeout. ||
|| `--token` | `string`

Set the IAM token to use. ||
|| `--impersonate-service-account-id` | `string`

Set the ID of the service account to impersonate. ||
|| `--no-browser` | Disable opening browser for authentication. ||
|| `--query` | `string`

Query to select values from the response using jq syntax ||
|| `-h`, `--help` | Display help for the command. ||
|#
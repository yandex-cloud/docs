---
editable: false
noIndex: true
sourcePath: en/_cli-ref-beta/cli-ref-beta/managed-trino/cli-ref-beta/cluster/create.md
---

# yc beta managed-trino cluster create

Creates a Trino Cluster resource.

#### Command Usage

Syntax: 

`yc beta managed-trino cluster create <FOLDER-ID>`

#### Flags

| Flag | Description |
|----|----|
|`-r`,`--request-file`|<b>`string`</b><br/>Path to a request file.|
|`--example-json`|Generates a JSON template of the request.<br/><br/>The template can be customized and used as input for the command.<br/><br/>Usage example:<br/>1. Generate template: yc beta compute instance create --example-json > request.json<br/>2. Edit the template: vim request.json<br/>3. Run with template: yc beta compute instance create -r request.json|
|`--example-yaml`|Generates a YAML template of the request.<br/><br/>The template can be customized and used as input for the command.<br/><br/>Usage example:<br/>1. Generate template: yc beta compute instance create --example-yaml > request.yaml<br/>2. Edit the template: vim request.yaml<br/>3. Run with template: yc beta compute instance create -r request.yaml|
|`--deletion-protection`|Deletion Protection inhibits deletion of the cluster.|
|`--description`|<b>`string`</b><br/>Description of the Trino cluster.|
|`--folder-id`|<b>`string`</b><br/>ID of the folder to create Trino cluster in.|
|`--labels`|<b>`stringToString`</b><br/>Custom labels for the Trino cluster as '' key:value '' pairs.<br/>For example: {"env": "prod"}.|
|`--logging`|<b>`shorthand/json`</b><br/>Cloud logging configuration.<br/><br/>Example:|
|`--logging`|<b>`destination={folder-id=value},`</b><br/>enabled=true, min-level=TRACE<br/><br/>Shorthand Syntax:<br/>{destination={folder-id=str \| log-group-id=str}, enabled=bool, min-level=TRACE\|DEBUG\|INFO\|WARN\|ERROR\|FATAL}<br/>Fields:<br/>enabled      bool                                     — Logs generated by the Trino components are delivered to Cloud Logging.<br/>min-level    enum<DEBUG\|ERROR\|FATAL\|INFO\|TRACE\|WARN>  — Minimum log entry level.<br/><br/>See [LogLevel.Level] for details.<br/>destination  oneof<folder-id\|log-group-id>            — Oneof destination field<br/>folder-id     string  — Logs should be written to default log group for specified folder.<br/>log-group-id  string  — Logs should be written to log group resolved by ID.<br/>|
|`--maintenance-window`|<b>`shorthand/json`</b><br/>Window of maintenance operations.<br/><br/>Example:<br/>--maintenance-window policy={anytime={}}<br/><br/>Shorthand Syntax:<br/>{policy={anytime={} \| weekly-maintenance-window={day=MON\|TUE\|WED\|THU\|FRI\|SAT\|SUN, hour=int}}}<br/>Fields:<br/>policy  oneof<anytime\|weekly-maintenance-window>  — Oneof policy field<br/>anytime                    struct  —<br/>weekly-maintenance-window  struct  —<br/>day   enum<FRI\|MON\|SAT\|SUN\|THU\|TUE\|WED>  —<br/>hour  int                                — Hour of the day in UTC.<br/>|
|`--name`|<b>`string`</b><br/>Name of the Trino cluster. The name must be unique within the folder.|
|`--network`|<b>`shorthand/json`</b><br/>Network related configuration options.<br/><br/>Example:|
|`--network`|<b>`security-group-ids=value,`</b><br/>subnet-ids=value<br/><br/>Shorthand Syntax:<br/>{security-group-ids=str,..., subnet-ids=str,...}<br/>Fields:<br/>security-group-ids  []string  — User security groups.<br/>subnet-ids          []string  — IDs of VPC network subnets where instances of the cluster are attached.<br/>|
|`--service-account-id`|<b>`string`</b><br/>Service account used to access Cloud resources.|
|`--trino`|<b>`shorthand/json`</b><br/>Configuration of Trino components.<br/><br/>Example:|
|`--trino`|<b>`access-control={catalog-session-properties=[{allow=NO,`</b><br/>catalog={match-by={ids={any=value}}}, description=value, groups=value, property={match-by={name-regexp=value}}, users=value}], catalogs=[{catalog={match-by={ids={any=value}}}, description=value, groups=value, permission=NONE, users=value}], functions=[{catalog={match-by={ids={any=value}}}, description=value, function={match-by={name-regexp=value}}, groups=value, privileges=EXECUTE, schema={match-by={name-regexp=value}}, users=value}], procedures=[{catalog={match-by={ids={any=value}}}, description=value, groups=value, privileges=EXECUTE, procedure={match-by={name-regexp=value}}, schema={match-by={name-regexp=value}}, users=value}], queries=[{description=value, groups=value, privileges=VIEW, query-owners=value, users=value}], schemas=[{catalog={match-by={ids={any=value}}}, description=value, groups=value, owner=NO, schema={match-by={name-regexp=value}}, users=value}], system-session-properties=[{allow=NO, description=value, groups=value, property={match-by={name-regexp=value}}, users=value}], tables=[{catalog={match-by={ids={any=value}}}, columns=[{access=NONE, mask=value, name=value}], description=value, filter=value, groups=value, privileges=SELECT, schema={match-by={name-regexp=value}}, table={match-by={name-regexp=value}}, users=value}]}, catalogs=[{connector={type={clickhouse={additional-properties={key=value}, connection={type={connection-manager={connection-id=value, connection-properties={key=value}, database=value}}}}}}, description=value, labels={key=value}, name=value}], coordinator-config={resources={resource-preset-id=value}}, resource-management={query={properties={key=value}}, resource-groups={cpu-quota-period=value, root-groups=[{hard-concurrency-limit=1, hard-cpu-limit=value, max-queued=1, name=value, scheduling-policy=FAIR, scheduling-weight=1, soft-concurrency-limit=1, soft-cpu-limit=value, soft-memory-limit=value, sub-groups=<Trino_V1_ResourceGroupConfig>}], selectors=[{client-tags=value, group=value, query-type=SELECT, source=value, user=value, user-group=value}]}}, retry-policy={additional-properties={key=value}, exchange-manager={additional-properties={key=value}, storage={type={service-s3={}}}}, policy=QUERY}, tls={trusted-certificates=value}, version=value, worker-config={resources={resource-preset-id=value}, scale-policy={scale-type={auto-scale={max-count=1, min-count=1}}}}<br/><br/>Shorthand Syntax:<br/>{access-control={catalog-session-properties=[{allow=NO\|YES, catalog={match-by={ids={any=str,...} \| name-regexp=str \| names={any=str,...}}}, description=str, groups=str,..., property={match-by={name-regexp=str \| names={any=str,...}}}, users=str,...},...], catalogs=[{catalog={match-by={ids={any=str,...} \| name-regexp=str \| names={any=str,...}}}, description=str, groups=str,..., permission=NONE\|ALL\|READ_ONLY, users=str,...},...], functions=[{catalog={match-by={ids={any=str,...} \| name-regexp=str \| names={any=str,...}}}, description=str, function={match-by={name-regexp=str \| names={any=str,...}}}, groups=str,..., privileges=EXECUTE\|GRANT_EXECUTE\|OWNERSHIP,..., schema={match-by={name-regexp=str \| names={any=str,...}}}, users=str,...},...], procedures=[{catalog={match-by={ids={any=str,...} \| name-regexp=str \| names={any=str,...}}}, description=str, groups=str,..., privileges=EXECUTE,..., procedure={match-by={name-regexp=str \| names={any=str,...}}}, schema={match-by={name-regexp=str \| names={any=str,...}}}, users=str,...},...], queries=[{description=str, groups=str,..., privileges=VIEW\|EXECUTE\|KILL,..., query-owners=str,..., users=str,...},...], schemas=[{catalog={match-by={ids={any=str,...} \| name-regexp=str \| names={any=str,...}}}, description=str, groups=str,..., owner=NO\|YES, schema={match-by={name-regexp=str \| names={any=str,...}}}, users=str,...},...], system-session-properties=[{allow=NO\|YES, description=str, groups=str,..., property={match-by={name-regexp=str \| names={any=str,...}}}, users=str,...},...], tables=[{catalog={match-by={ids={any=str,...} \| name-regexp=str \| names={any=str,...}}}, columns=[{access=NONE\|ALL, mask=str, name=str},...], description=str, filter=str, groups=str,..., privileges=SELECT\|INSERT\|DELETE\|UPDATE\|OWNERSHIP\|GRANT_SELECT,..., schema={match-by={name-regexp=str \| names={any=str,...}}}, table={match-by={name-regexp=str \| names={any=str,...}}}, users=str,...},...]}, catalogs=[{connector={type={clickhouse={additional-properties={key=str, key=...}, connection={type={connection-manager={connection-id=str, connection-properties={key=str, key=...}, database=str} \| on-premise={connection-url=str, password=str, user-name=str}}}} \| delta-lake={additional-properties={key=str, key=...}, filesystem={type={external-s3={aws-access-key=str, aws-endpoint=str, aws-region=str, aws-secret-key=str} \| s3={}}}, metastore={type={hive={connection={uri=str}}}}} \| hive={additional-properties={key=str, key=...}, filesystem={type={external-s3={aws-access-key=str, aws-endpoint=str, aws-region=str, aws-secret-key=str} \| s3={}}}, metastore={type={hive={connection={uri=str}}}}} \| hudi={additional-properties={key=str, key=...}, filesystem={type={external-s3={aws-access-key=str, aws-endpoint=str, aws-region=str, aws-secret-key=str} \| s3={}}}, metastore={type={hive={connection={uri=str}}}}} \| iceberg={additional-properties={key=str, key=...}, filesystem={type={external-s3={aws-access-key=str, aws-endpoint=str, aws-region=str, aws-secret-key=str} \| s3={}}}, metastore={type={hive={connection={uri=str}}}}} \| mysql={additional-properties={key=str, key=...}, connection={type={connection-manager={connection-id=str, connection-properties={key=str, key=...}} \| on-premise={connection-url=str, password=str, user-name=str}}}} \| oracle={additional-properties={key=str, key=...}, connection={type={on-premise={connection-url=str, password=str, user-name=str}}}} \| postgresql={additional-properties={key=str, key=...}, connection={type={connection-manager={connection-id=str, connection-properties={key=str, key=...}, database=str} \| on-premise={connection-url=str, password=str, user-name=str}}}} \| sqlserver={additional-properties={key=str, key=...}, connection={type={on-premise={connection-url=str, password=str, user-name=str}}}} \| tpcds={additional-properties={key=str, key=...}} \| tpch={additional-properties={key=str, key=...}}}}, description=str, labels={key=str, key=...}, name=str},...], coordinator-config={resources={resource-preset-id=str}}, resource-management={query={properties={key=str, key=...}}, resource-groups={cpu-quota-period=str, root-groups=[{hard-concurrency-limit=int, hard-cpu-limit=str, max-queued=int, name=str, scheduling-policy=FAIR\|WEIGHTED\|WEIGHTED_FAIR\|QUERY_PRIORITY, scheduling-weight=int, soft-concurrency-limit=int, soft-cpu-limit=str, soft-memory-limit=str, sub-groups=\<ResourceGroupConfig\>},...], selectors=[{client-tags=str,..., group=str, query-type=SELECT\|EXPLAIN\|DESCRIBE\|INSERT\|UPDATE\|MERGE\|DELETE\|ANALYZE\|DATA_DEFINITION\|ALTER_TABLE_EXECUTE, source=str, user=str, user-group=str},...]}}, retry-policy={additional-properties={key=str, key=...}, exchange-manager={additional-properties={key=str, key=...}, storage={type={service-s3={}}}}, policy=QUERY\|TASK}, tls={trusted-certificates=str,...}, version=str, worker-config={resources={resource-preset-id=str}, scale-policy={scale-type={auto-scale={max-count=int, min-count=int} \| fixed-scale={count=int}}}}}<br/>Fields:<br/>access-control       struct              — Configuration for access control, specifying the fine-grained access rules.<br/>catalog-session-properties  []struct  — Catalog session property access control rules.<br/>allow        enum<NO\|YES>  required  — Whether the rule allows setting the property.<br/>catalog      struct                  — Catalog matcher specifying what catalogs the rule is applied to.<br/>match-by  oneof<ids\|name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Catalog name regexp the rule is applied to.<br/>ids          struct  — Catalog IDs rule is applied to.<br/>any  []string  —<br/>names        struct  — Catalog names rule is applied to.<br/>any  []string  —<br/>description  string                  — Rule description.<br/>groups       []string                — IAM group IDs the rule is applied to.<br/>property     struct                  — Property matcher specifying what properties the rule is applied to.<br/>match-by  oneof<name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Property name regexp the rule is applied to.<br/>names        struct  — Property names the rule is applied to.<br/>any  []string  —<br/>users        []string                — IAM user IDs the rule is applied to.<br/>catalogs                    []struct  — Catalog access control rules.<br/>catalog      struct                              — Catalog matcher specifying what catalogs the rule is applied to.<br/>match-by  oneof<ids\|name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Catalog name regexp the rule is applied to.<br/>ids          struct  — Catalog IDs rule is applied to.<br/>any  []string  —<br/>names        struct  — Catalog names rule is applied to.<br/>any  []string  —<br/>description  string                              — Rule description.<br/>groups       []string                            — IAM group IDs the rule is applied to.<br/>permission   enum<ALL\|NONE\|READ_ONLY>  required  — Permission granted by the rule.<br/>users        []string                            — IAM user IDs the rule is applied to.<br/>functions                   []struct  — Function access control rules.<br/>catalog      struct    — Catalog matcher specifying what catalogs the rule is applied to.<br/>match-by  oneof<ids\|name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Catalog name regexp the rule is applied to.<br/>ids          struct  — Catalog IDs rule is applied to.<br/>any  []string  —<br/>names        struct  — Catalog names rule is applied to.<br/>any  []string  —<br/>description  string    — Rule description.<br/>function     struct    — Function matcher specifying what functions the rule is applied to.<br/>match-by  oneof<name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Function name regexp the rule is applied to.<br/>names        struct  — Function names the rule is applied to.<br/>any  []string  —<br/>groups       []string  — IAM group IDs the rule is applied to.<br/>privileges   []int     — Privileges granted by the rule.<br/>schema       struct    — Schema matcher specifying what schema the rule is applied to.<br/>match-by  oneof<name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Schema name regexp the rule is applied to.<br/>names        struct  — Schema names the rule is applied to.<br/>any  []string  —<br/>users        []string  — IAM user IDs the rule is applied to.<br/>procedures                  []struct  — Procedures access control rules.<br/>catalog      struct    — Catalog matcher specifying what catalogs the rule is applied to.<br/>match-by  oneof<ids\|name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Catalog name regexp the rule is applied to.<br/>ids          struct  — Catalog IDs rule is applied to.<br/>any  []string  —<br/>names        struct  — Catalog names rule is applied to.<br/>any  []string  —<br/>description  string    — Rule description.<br/>groups       []string  — IAM group IDs the rule is applied to.<br/>privileges   []int     — Privileges granted by the rule.<br/>procedure    struct    — Procedure matcher specifying what functions the rule is applied to.<br/>match-by  oneof<name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Procedure name regexp the rule is applied to.<br/>names        struct  — Procedure names the rule is applied to.<br/>any  []string  —<br/>schema       struct    — Schema matcher specifying what schema the rule is applied to.<br/>match-by  oneof<name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Schema name regexp the rule is applied to.<br/>names        struct  — Schema names the rule is applied to.<br/>any  []string  —<br/>users        []string  — IAM user IDs the rule is applied to.<br/>queries                     []struct  — Queries access control rules.<br/>description   string    — Rule description.<br/>groups        []string  — IAM group IDs the rule is applied to.<br/>privileges    []int     — Privileges granted by the user.<br/>query-owners  []string  — Owners of queries the rule is applied to.<br/>Cannot be combined with EXECUTE privilege.<br/>users         []string  — IAM user IDs the rule is applied to.<br/>schemas                     []struct  — Schema access control rules.<br/>catalog      struct                  — Catalog matcher specifying what catalogs the rule is applied to.<br/>match-by  oneof<ids\|name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Catalog name regexp the rule is applied to.<br/>ids          struct  — Catalog IDs rule is applied to.<br/>any  []string  —<br/>names        struct  — Catalog names rule is applied to.<br/>any  []string  —<br/>description  string                  — Rule description.<br/>groups       []string                — IAM group IDs the rule is applied to.<br/>owner        enum<NO\|YES>  required  — Ownership granted by the rule.<br/>schema       struct                  — Schema matcher specifying what schemas the rule is applied to.<br/>match-by  oneof<name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Schema name regexp the rule is applied to.<br/>names        struct  — Schema names the rule is applied to.<br/>any  []string  —<br/>users        []string                — IAM user IDs the rule is applied to.<br/>system-session-properties   []struct  — System session property access control rules.<br/>allow        enum<NO\|YES>  required  — Whether the rule allows setting the property.<br/>description  string                  — Rule description.<br/>groups       []string                — IAM group IDs the rule is applied to.<br/>property     struct                  — Property matcher specifying what properties the rule is applied to.<br/>match-by  oneof<name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Property name regexp the rule is applied to.<br/>names        struct  — Property names the rule is applied to.<br/>any  []string  —<br/>users        []string                — IAM user IDs the rule is applied to.<br/>tables                      []struct  — Table access control rules.<br/>catalog      struct    — Catalog matcher specifying what catalogs the rule is applied to.<br/>match-by  oneof<ids\|name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Catalog name regexp the rule is applied to.<br/>ids          struct  — Catalog IDs rule is applied to.<br/>any  []string  —<br/>names        struct  — Catalog names rule is applied to.<br/>any  []string  —<br/>columns      []struct  — Column rules.<br/>access  enum<ALL\|NONE>  required  — Column access mode.<br/>mask    string                    — SQL expression mask to evaluate instead of original column values.<br/>Mask should have the same type as original column.<br/>name    string          required  — Column name.<br/>description  string    — Rule description.<br/>filter       string    — Boolean SQL expression to filter table rows for particular user.<br/>groups       []string  — IAM group IDs the rule is applied to.<br/>privileges   []int     — Permission granted by the rule.<br/>schema       struct    — Schema matcher specifying what schemas the rule is applied to.<br/>match-by  oneof<name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Schema name regexp the rule is applied to.<br/>names        struct  — Schema names the rule is applied to.<br/>any  []string  —<br/>table        struct    — Table matcher specifying what tables the rule is applied to.<br/>match-by  oneof<name-regexp\|names>  — Oneof match-by field<br/>name-regexp  string  — Table name regexp the rule is applied to.<br/>names        struct  — Table names the rule is applied to.<br/>any  []string  —<br/>users        []string  — IAM user IDs the rule is applied to.<br/>catalogs             []struct            — List of catalogs that enable integration with various data sources.<br/>Each catalog defines a connection to an external data source that Trino can query.<br/>connector    struct              required  — Connector backing this catalog.<br/>type  oneof<clickhouse\|delta-lake\|hive\|hudi\|iceberg\|mysql\|oracle\|postgresql\|sqlserver\|tpcds\|tpch>  — Oneof type field<br/>hive        struct  — Hive connector configuration.<br/>additional-properties  map[string,string]            — Additional properties.<br/>filesystem             struct              required  — File system configuration.<br/>type  oneof<external-s3\|s3>  — Oneof type field<br/>s3           struct  —<br/>external-s3  struct  —<br/>aws-access-key  string  required  —<br/>aws-endpoint    string  required  —<br/>aws-region      string  required  —<br/>aws-secret-key  string  required  —<br/>metastore              struct              required  — Metastore configuration.<br/>type  oneof\<hive\>  — Oneof type field<br/>hive  struct  —<br/>connection  oneof\<uri\>  — Oneof connection field<br/>uri  string  — URI or cluster ID of the Hive Metastore.<br/>iceberg     struct  — Iceberg connector configuration.<br/>additional-properties  map[string,string]            — Additional properties.<br/>filesystem             struct              required  — File system configuration.<br/>type  oneof<external-s3\|s3>  — Oneof type field<br/>s3           struct  —<br/>external-s3  struct  —<br/>aws-access-key  string  required  —<br/>aws-endpoint    string  required  —<br/>aws-region      string  required  —<br/>aws-secret-key  string  required  —<br/>metastore              struct              required  — Metastore configuration.<br/>type  oneof\<hive\>  — Oneof type field<br/>hive  struct  —<br/>connection  oneof\<uri\>  — Oneof connection field<br/>uri  string  — URI or cluster ID of the Hive Metastore.<br/>delta-lake  struct  — Delta Lake connector configuration.<br/>additional-properties  map[string,string]            — Additional properties.<br/>filesystem             struct              required  — File system configuration.<br/>type  oneof<external-s3\|s3>  — Oneof type field<br/>s3           struct  —<br/>external-s3  struct  —<br/>aws-access-key  string  required  —<br/>aws-endpoint    string  required  —<br/>aws-region      string  required  —<br/>aws-secret-key  string  required  —<br/>metastore              struct              required  — Metastore configuration.<br/>type  oneof\<hive\>  — Oneof type field<br/>hive  struct  —<br/>connection  oneof\<uri\>  — Oneof connection field<br/>uri  string  — URI or cluster ID of the Hive Metastore.<br/>postgresql  struct  — PostgreSQL connector configuration.<br/>additional-properties  map[string,string]  — Additional properties.<br/>connection             struct              — Connection configuration.<br/>type  oneof<connection-manager\|on-premise>  — Oneof type field<br/>on-premise          struct  —<br/>connection-url  string  required  — Connection to the Postgresql.<br/>password        string  required  — Password of the Postgresql user.<br/>user-name       string  required  — Name of the Postgresql user.<br/>connection-manager  struct  —<br/>connection-id          string              required  — Connection ID.<br/>connection-properties  map[string,string]            — Additional connection properties.<br/>database               string              required  — Database.<br/>clickhouse  struct  — ClickHouse connector configuration.<br/>additional-properties  map[string,string]  — Additional properties.<br/>connection             struct              — Connection configuration.<br/>type  oneof<connection-manager\|on-premise>  — Oneof type field<br/>on-premise          struct  —<br/>connection-url  string  required  — Connection to the Clickhouse.<br/>password        string  required  — Password of the Clickhouse user.<br/>user-name       string  required  — Name of the Clickhouse user.<br/>connection-manager  struct  —<br/>connection-id          string              required  — Connection ID.<br/>connection-properties  map[string,string]            — Additional connection properties.<br/>database               string              required  — Database.<br/>tpch        struct  — TPC-H connector for synthetic benchmarking.<br/>additional-properties  map[string,string]  — Additional properties.<br/>tpcds       struct  — TPC-DS connector for synthetic benchmarking.<br/>additional-properties  map[string,string]  — Additional properties.<br/>oracle      struct  — Oracle connector configuration for connecting to Oracle Database instances.<br/>additional-properties  map[string,string]  — Additional properties.<br/>connection             struct              — Connection configuration.<br/>type  oneof\<on-premise\>  — Oneof type field<br/>on-premise  struct  —<br/>connection-url  string  required  — Connection to the Oracle.<br/>password        string  required  — Password of the Oracle user.<br/>user-name       string  required  — Name of the Oracle user.<br/>sqlserver   struct  — SQLServer connector configuration for connecting to SQLServer Database instances.<br/>additional-properties  map[string,string]  — Additional properties.<br/>connection             struct              — Connection configuration.<br/>type  oneof\<on-premise\>  — Oneof type field<br/>on-premise  struct  —<br/>connection-url  string  required  — Connection to the SQLServer.<br/>password        string  required  — Password of the SQLServer user.<br/>user-name       string  required  — Name of the SQLServer user.<br/>hudi        struct  — Hudi connector configuration.<br/>additional-properties  map[string,string]            — Additional properties.<br/>filesystem             struct              required  — File system configuration.<br/>type  oneof<external-s3\|s3>  — Oneof type field<br/>s3           struct  —<br/>external-s3  struct  —<br/>aws-access-key  string  required  —<br/>aws-endpoint    string  required  —<br/>aws-region      string  required  —<br/>aws-secret-key  string  required  —<br/>metastore              struct              required  — Metastore configuration.<br/>type  oneof\<hive\>  — Oneof type field<br/>hive  struct  —<br/>connection  oneof\<uri\>  — Oneof connection field<br/>uri  string  — URI or cluster ID of the Hive Metastore.<br/>mysql       struct  — MySQL connector configuration for connecting to MySQL Database instances.<br/>additional-properties  map[string,string]  — Additional properties.<br/>connection             struct              — Connection configuration.<br/>type  oneof<connection-manager\|on-premise>  — Oneof type field<br/>on-premise          struct  —<br/>connection-url  string  required  — Connection to the MySQL.<br/>password        string  required  — Password of the MySQL user.<br/>user-name       string  required  — Name of the MySQL user.<br/>connection-manager  struct  —<br/>connection-id          string              required  — Connection ID.<br/>connection-properties  map[string,string]            — Additional connection properties.<br/>description  string                        — Description of the catalog.<br/>labels       map[string,string]            — Labels associated with the catalog.<br/>name         string              required  — Name of the catalog.<br/>Must be unique within a Trino cluster.<br/>coordinator-config   struct    required  — Configuration for the coordinator, specifying computational resources and other settings.<br/>resources  struct  required  — Configuration for computational resources assigned to the coordinator instance.<br/>resource-preset-id  string  required  — ID of the preset for computational resources allocated to a instance (e.g., CPU, memory, etc.).<br/>resource-management  struct              — Configuration for cluster resource management, specifying the resource groups.<br/>query            struct  — Query management confiugration.<br/>properties  map[string,string]  — Query properties.<br/>resource-groups  struct  — Resource groups configuration.<br/>cpu-quota-period  string    — Period for CPU quota calculations.<br/>root-groups       []struct  — Root resource groups configuration.<br/>hard-concurrency-limit  int                                                         — Maximum number of queries that can run concurrently.<br/>hard-cpu-limit          string                                                      — Maximum CPU time per period this group can use.<br/>max-queued              int                                                         — Maximum number of queued queries.<br/>name                    string                                            required  — Resource group name.<br/>scheduling-policy       enum<FAIR\|QUERY_PRIORITY\|WEIGHTED\|WEIGHTED_FAIR>            — Policy for selecting queued queries and sub-group eligibility.<br/>scheduling-weight       int                                                         — Weight for weighted and weighted_fair scheduling policies.<br/>soft-concurrency-limit  int                                                         — Number of concurrently running queries after which new queries only run if peer groups are below soft limits.<br/>soft-cpu-limit          string                                                      — Maximum CPU time per period this group can use before applying penalty to running queries (requires hard_cpu_limit).<br/>soft-memory-limit       string                                                      — Maximum amount of distributed memory this group can use.<br/>sub-groups              []struct                                                    — List of sub-groups.<br/>… []struct (recursive)<br/>selectors         []struct  — Selector rules for routing queries to resource groups.<br/>client-tags  []string                                                                                                        — Tags that must all be present in the query's client tags.<br/>group        string                                                                                                required  — Target resource group name.<br/>query-type   enum<ALTER_TABLE_EXECUTE\|ANALYZE\|DATA_DEFINITION\|DELETE\|DESCRIBE\|EXPLAIN\|INSERT\|MERGE\|SELECT\|UPDATE>            — Type of query to match.<br/>source       string                                                                                                          — Regex to match against query source.<br/>user         string                                                                                                          — Regex to match against username.<br/>user-group   string                                                                                                          — Regex to match against user groups.<br/>retry-policy         struct              — Configuration for retry policy, specifying the spooling storage destination and other settings.<br/>additional-properties  map[string,string]  — Additional properties.<br/>exchange-manager       struct              — Configuration for exchange manager.<br/>additional-properties  map[string,string]  — Additional properties.<br/>storage                struct              —<br/>type  oneof\<service-s3\>  — Oneof type field<br/>service-s3  struct  — Use service side s3 bucket for exchange manager.<br/>policy                 enum<QUERY\|TASK>    — Retry policy level.<br/>tls                  struct              — Configuration for TLS.<br/>trusted-certificates  []string  — Trusted CA-certificates. Each element should contain single self-signed CA-certificate or<br/>chain of CA-certificates where first certificate is the leaf and last certificate is the self-signed root.<br/>version              string              — Trino version.<br/>Format: "Number".<br/>worker-config        struct    required  — Configuration for worker nodes, including scaling policy and computational resources.<br/>resources     struct  required  — Configuration for computational resources for worker instances.<br/>resource-preset-id  string  required  — ID of the preset for computational resources allocated to a instance (e.g., CPU, memory, etc.).<br/>scale-policy  struct  required  — Configuration for scaling policy for worker instances.<br/>scale-type  oneof<auto-scale\|fixed-scale>  — Oneof scale-type field<br/>fixed-scale  struct  — A fixed scaling policy that specifies a fixed number of worker instances.<br/>count  int  — Specifies the number of worker instances.<br/>auto-scale   struct  — A scaling policy that dynamically adjusts the number of worker instances<br/>based on the cluster's workload. The system automatically increases or<br/>decreases the number of instances within the defined range.<br/>max-count  int  —<br/>min-count  int  —<br/>|
|`--async`|Display information about the operation in progress, without waiting for the operation to complete.|

#### Global Flags

| Flag | Description |
|----|----|
|`--profile`|<b>`string`</b><br/>Set the custom profile.|
|`--region`|<b>`string`</b><br/>Set the region.|
|`--debug`|Debug logging.|
|`--debug-grpc`|Debug gRPC logging. Very verbose, used for debugging connection problems.|
|`--no-user-output`|Disable printing user intended output to stderr.|
|`--pager`|<b>`string`</b><br/>Set the custom pager.|
|`--format`|<b>`string`</b><br/>Set the output format: text, yaml, json, table, json-rest.|
|`--retry`|<b>`int`</b><br/>Enable gRPC retries. By default, retries are enabled with maximum 5 attempts.<br/>Pass 0 to disable retries. Pass any negative value for infinite retries.<br/>Even infinite retries are capped with 2 minutes timeout.|
|`--timeout`|<b>`string`</b><br/>Set the timeout.|
|`--token`|<b>`string`</b><br/>Set the IAM token to use.|
|`--impersonate-service-account-id`|<b>`string`</b><br/>Set the ID of the service account to impersonate.|
|`--no-browser`|Disable opening browser for authentication.|
|`--query`|<b>`string`</b><br/>Query to select values from the response using jq syntax|
|`-h`,`--help`|Display help for the command.|

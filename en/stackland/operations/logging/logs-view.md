# Viewing logs

To view cluster logs:

1. Add **Query**.
1. On the **Explore** tab, select **Logs**.
1. Select the log delivery service of your choice (`fluentbit` by default) and click **Show logs**. This will open the log page.

On the log page, you can set up filters to show the log levels, containers, namespaces, and other things you need.

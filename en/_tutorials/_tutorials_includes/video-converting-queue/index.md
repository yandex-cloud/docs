# Converting a video to a GIF in Python


You will create a video converter using [FFmpeg](https://ffmpeg.org/) and [{{ message-queue-full-name }}](../../../message-queue/). This tutorial is intended for Linux and macOS users.



You can create an infrastructure for a video converter with FFmpeg and {{ message-queue-full-name }} using the following tools:

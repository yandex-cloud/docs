Wait for the DNS records to get updated (this may take several hours).

Check if the website is accessible: use the new URL, `cdn.example.com`, to open it. You should be redirected to the `https://cdn.example.com` website with a TLS certificate from {{ certificate-manager-name }} already connected and content sourced from {{ cdn-name }}.
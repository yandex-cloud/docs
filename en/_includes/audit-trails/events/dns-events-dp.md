Event name | Description
--- | ---
`ProcessDnsQuery` | Execution result of a DNS query from a Cloud DNS client

{% note info %}

If the action was performed by one of the {{ yandex-cloud }} infrastructure services or a support employee, the special `cloud.yandex` value will be specified in the `client_ip` and `server_ip` fields in the `details` section.

{% endnote %}
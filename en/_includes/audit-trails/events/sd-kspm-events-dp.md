Event name | Description
--- | ---
`AddCluster` | Adding a cluster
`DeleteCluster` | Deleting a cluster
`TriggerAdmissionControl` | Triggering of {{ kspm-name }} when checking newly created and updated resources
`TriggerInventoryControlBatch` | Triggering of {{ kspm-name }} when checking the configuration
`TriggerInventoryControlEvent` | Triggering of {{ kspm-name }} when checking the configuration by event
`TriggerInventoryControlSnapshot` | Triggering of {{ kspm-name }} when checking the configuration by snapshot
`TriggerRuntimeControl` | Triggering of {{ kspm-name }} when monitoring workload security
`TriggerRuntimeProcessInfo` | Triggering of {{ kspm-name }} when getting process information

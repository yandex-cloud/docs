You can manually [download the executable archive](../../cli/operations/install-cli.md#bin) and install the CLI.

For example, this may be useful if the target device does not have access to `https://{{ s3-storage-host-cli }}`. Download the archive containing the CLI executable on a different device, then transfer the archive to the target device using a method approved by your organization.

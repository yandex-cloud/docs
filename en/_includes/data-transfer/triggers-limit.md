To avoid errors on the target endpoint for _{{dt-type-repl}}_ and _{{dt-type-copy-repl}}_ transfers, deactivate trigger transfer at the transfer initiation stage and reactivate it at the completion stage.


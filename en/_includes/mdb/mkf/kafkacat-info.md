{% note info %}

`kafkacat` is a deprecated tool that only works with {{ KF }} cluster versions below 4.0. For clusters running {{ KF }} 4.0 or later, use [kafkactl](../../../managed-kafka/operations/connect/clients.md#kafkactl).

{% endnote %}

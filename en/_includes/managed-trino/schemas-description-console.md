Select format for the schemas:
* **{{ ui-key.yacloud.trino.rbac-catalog-match-by-name }}**: Select schema names.
* **{{ ui-key.yacloud.trino.rbac-catalog-match-by-name-regexp }}**: Enter a regular expression for schema names.
* **{{ ui-key.yacloud.trino.rbac-catalog-match-by-empty }}**: The `<schema_name>` field may contain any value.

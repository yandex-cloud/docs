Available versions: 3.1 and 4.0.

{% note warning %}

To integrate a {{ metastore-name }} cluster with [{{ mtr-full-name }}](../../managed-trino/index.yaml) and [{{ msp-full-name }}](../../managed-spark/index.yaml), you need version {{ metastore.integration-version }}.

If required, you can upgrade 3.1 to 4.0, but you cannot downgrade 4.0 to 3.1.

{% endnote %}

A federated user can go directly to the dashboard of their choice using for authentication a link in the following format:

```html
{{ link-datalens-main }}/<dashboard_ID>?__yc_federation_id=<federation_ID>
```

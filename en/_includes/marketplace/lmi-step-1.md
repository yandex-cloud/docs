Get the subscription template ID (`license_template_id`).

You can find the subscription template ID in the {{ marketplace-name }} partner dashboard. Use the ID in the product code to manage the subscription.
{% note warning %}

There must be no collisions between the UIDs of the {{ oslogin }} profile and the OS system profiles. If you set the UID manually, use values in the range from `1002` to `2^63 - 1` to avoid such collisions.

{% endnote %}
Under **{{ ui-key.yacloud_org.organization.security-settings.PolicyResultCard.section_title }}**, select an action to apply to the authentication event that matches the policy criteria.

Currently, you can only use authentication policies to deny authentication.
You can learn the public IP address of a VM from the **{{ ui-key.yacloud.compute.instance.overview.label_public-ipv4 }}** field of the network interface under **{{ ui-key.yacloud.compute.instance.overview.section_network }}** on the VM page in the [management console]({{ link-console-main }}) or by using the `yc compute instance get <VM_name>` [YC CLI](../../cli/quickstart.md) command.
The VM’s operating system must have the appropriate GPU drivers installed when adding a GPU. For such VMs, we recommend using one of the [GPU-specific images](../../compute/concepts/gpus.md#os).

You can also [install the drivers](../../compute/operations/vm-operate/install-nvidia-drivers.md) on another standard image manually.
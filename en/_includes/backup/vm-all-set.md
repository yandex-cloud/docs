Once you install the agent, the {{ backup-name }} VM will be added to **{{ ui-key.yacloud.iam.folder.dashboard.label_backup }}** in the **{{ ui-key.yacloud.backup.label_instances }}** tab of the **{{ ui-key.yacloud.backup.label_connected-resources }}** section and associated with a [backup policy](../../backup/concepts/policy.md).

If the VM is not automatically associated with the policy, [associate](../../backup/operations/policy-vm/attach-and-detach-vm.md) it manually.

To check that the API gateway labels were updated, use the [management console]({{ link-console-main }}) or this [CLI](../../cli/index.yaml) command:

```bash
yc serverless api-gateway get <API_gateway_name>
```
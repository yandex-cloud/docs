
The `search-api.viewer` role was used by an API that is now deprecated. The role enables viewing {{ search-api }} quotas, as well as information on the relevant cloud and folder.

The `search-api.viewer` role should no longer be used.
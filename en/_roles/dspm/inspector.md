The `dspm.inspector` role enables creating {{ dspm-name }} data sources using the specified {{ yandex-cloud }} resources. To create a {{ dspm-name }} data source, assign this role to a user for the appropriate cloud resource.

The `dspm.inspector` role is deprecated and no longer in use.

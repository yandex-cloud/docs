The `datalens.collections.creator` is assigned for a collection and enables viewing it as well as creating objects within it without access to any other objects residing within the collection. In the {{ datalens-name }} UI, this role is referred to as `Creator in collection`. We recommend assigning this role only via the {{ datalens-name }} UI.

This role includes the `datalens.collections.visitor` permissions.

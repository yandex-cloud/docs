The `datalens.sharedEntries.viewer` role is assigned for a common object and enables viewing it and its access permissions. In the {{ datalens-name }} UI, this role is referred to as `Viewer`. We recommend assigning this role only via the {{ datalens-name }} UI.

This role includes the `datalens.sharedEntries.limitedViewer` permissions.

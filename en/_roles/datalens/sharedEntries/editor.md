The `datalens.sharedEntries.editor` role is assigned for a common object and enables editing and viewing it, as well as viewing its access permissions. In the {{ datalens-name }} UI, this role is referred to as `Editor`. We recommend assigning this role only via the {{ datalens-name }} UI.

This role includes the `datalens.sharedEntries.viewer` permissions.

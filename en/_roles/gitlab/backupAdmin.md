The `gitlab.backupAdmin` role enables completely managing {{ mgl-name }} [instance](../../managed-gitlab/concepts/index.md#instance) backups. Users with this role can create and delete backups, restore instances from them, as well as download backups, including {{ GL }} secrets from such backups.

This role includes the `gitlab.backupRestorer` and `gitlab.backupDownloader` permissions.

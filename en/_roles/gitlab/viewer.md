The `gitlab.viewer` role enables viewing info on the {{ mgl-name }} [instances](../../managed-gitlab/concepts/index.md#instance) and [quotas](../../managed-gitlab/concepts/limits.md#quotas).

This role includes the `gitlab.auditor` permissions.

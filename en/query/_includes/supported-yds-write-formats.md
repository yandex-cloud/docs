{{ yds-short-name }} only lets you write data as a byte stream, which is processed by the receiving side.

When writing to {{ yds-short-name }}, file format and compression algorithm settings are not applied.


openapi: 3.0.0
info:
  title: Virtual Private Cloud API
  description: This API reference is organized by resource. Actions are performed
    by sending HTTP requests to resource URLs or making RPC calls. For more information
    about API architecture, see [API Concepts](/docs/api-design-guide/).
  version: v1
  x-y-docUri: api-ref
  x-y-baseUrl: https://vpc.api.cloud.yandex.net
paths:
  /vpc/v1/addresses/{addressId}:
    get:
      tags:
      - Address
      summary: Returns the specified Address resource.
      description: To get the list of all available Address resources, make a [list](/docs/vpc/api-ref/Address/list)
        request.
      operationId: AddressGet
      parameters:
      - name: addressId
        in: path
        description: |-
          Required.
          ID of the Address resource to return.

          To get Address resource ID make a [list](/docs/vpc/api-ref/Address/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Address'
      x-y-navtitle: get
    delete:
      tags:
      - Address
      summary: Deletes the specified address.
      operationId: AddressDelete
      parameters:
      - name: addressId
        in: path
        description: |-
          Required.
          ID of the address to delete.

          To get a address ID make a [list](/docs/vpc/api-ref/Address/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteAddressMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - Address
      summary: Updates the specified address.
      operationId: AddressUpdate
      parameters:
      - name: addressId
        in: path
        description: |-
          Required.
          ID of the address to update.

          To get the address ID make a [list](/docs/vpc/api-ref/Address/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAddressRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateAddressMetadata'
      x-y-operation-response: '[Address](/docs/vpc/api-ref/Address#representation)'
  /vpc/v1/addresses:byValue:
    get:
      tags:
      - Address
      summary: Returns the specified Address resource by a given value.
      description: To get the list of all available Address resources, make a [list](/docs/vpc/api-ref/Address/list)
        request.
      operationId: AddressGetByValue
      parameters:
      - name: externalIpv4Address
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Address'
      x-y-navtitle: getByValue
  /vpc/v1/addresses:
    get:
      tags:
      - Address
      summary: Retrieves the list of Address resources in the specified folder.
      operationId: AddressList
      parameters:
      - name: folderId
        in: query
        description: |-
          Required.
          ID of the folder to list addresses in.

          To get the folder ID use a [list](/docs/resource-manager/api-ref/Folder/list) request.

          The maximum string length in characters is 50.
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return. If the number of available
          results is larger than `page_size`, the service returns a [nextPageToken](/docs/vpc/api-ref/Address/list#responses)
          that can be used to get the next page of results in subsequent list requests.
          Default value: 100.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. To get the next page of results, set `page_token` to the
          [nextPageToken](/docs/vpc/api-ref/Address/list#responses) returned by a previous list request.

          The maximum string length in characters is 100.
        schema:
          type: string
      - name: filter
        in: query
        description: |-
          A filter expression that filters Address listed in the response.

          The expression must specify:
          1. The field name. Currently you can use filtering only on [Address.name](/docs/vpc/api-ref/Address#representation) field.
          2. An `=` operator.
          3. The value in double quotes (`"`). Must be 3-63 characters long and match the regular expression `[a-z][-a-z0-9]{1,61}[a-z0-9]`.
          Example of a filter: `name=my-address`.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListAddressesResponse'
      x-y-navtitle: list
    post:
      tags:
      - Address
      summary: Creates an address in the specified folder and network.
      operationId: AddressCreate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAddressRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateAddressMetadata'
      x-y-operation-response: '[Address](/docs/vpc/api-ref/Address#representation)'
  /vpc/v1/addresses/{addressId}/operations:
    get:
      tags:
      - Address
      summary: List operations for the specified address.
      operationId: AddressListOperations
      parameters:
      - name: addressId
        in: path
        description: |-
          Required.
          ID of the address to list operations for.

          To get a address ID make a [list](/docs/vpc/api-ref/Address/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return. If the number of available
          results is larger than [pageSize](/docs/vpc/api-ref/Address/listOperations#query_params), the service returns a [nextPageToken](/docs/vpc/api-ref/Address/listOperations#responses)
          that can be used to get the next page of results in subsequent list requests.
          Default value: 100.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. To get the next page of results, set [pageToken](/docs/vpc/api-ref/Address/listOperations#query_params) to the
          [nextPageToken](/docs/vpc/api-ref/Address/listOperations#responses) returned by a previous list request.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListAddressOperationsResponse'
      x-y-navtitle: listOperations
  /vpc/v1/addresses/{addressId}:move:
    post:
      tags:
      - Address
      summary: Move an address to another folder
      operationId: AddressMove
      parameters:
      - name: addressId
        in: path
        description: |-
          Required.
          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveAddressRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: move
      x-y-operation-metadata:
        $ref: '#/components/schemas/MoveAddressMetadata'
      x-y-operation-response: '[Address](/docs/vpc/api-ref/Address#representation)'
  /vpc/v1/gateways/{gatewayId}:
    get:
      tags:
      - Gateway
      summary: Returns the specified Gateway resource.
      description: To get the list of all available Gateway resources, make a [list](/docs/vpc/api-ref/Gateway/list)
        request.
      operationId: GatewayGet
      parameters:
      - name: gatewayId
        in: path
        description: |-
          Required.
          ID of the Gateway resource to return.

          To get Gateway resource ID make a [list](/docs/vpc/api-ref/Gateway/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Gateway'
      x-y-navtitle: get
    delete:
      tags:
      - Gateway
      summary: Deletes the specified gateway.
      operationId: GatewayDelete
      parameters:
      - name: gatewayId
        in: path
        description: |-
          Required.
          ID of the gateway to delete.

          To get a gateway ID make a [list](/docs/vpc/api-ref/Gateway/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteGatewayMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - Gateway
      summary: Updates the specified gateway.
      operationId: GatewayUpdate
      parameters:
      - name: gatewayId
        in: path
        description: |-
          Required.
          ID of the gateway to update.

          To get the gateway ID make a [list](/docs/vpc/api-ref/Gateway/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateGatewayRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateGatewayMetadata'
      x-y-operation-response: '[Gateway](/docs/vpc/api-ref/Gateway#representation)'
  /vpc/v1/gateways:
    get:
      tags:
      - Gateway
      summary: Retrieves the list of Gateway resources in the specified folder.
      operationId: GatewayList
      parameters:
      - name: folderId
        in: query
        description: |-
          Required.
          ID of the folder to list gateways in.

          To get the folder ID use a [list](/docs/resource-manager/api-ref/Folder/list) request.

          The maximum string length in characters is 50.
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return. If the number of available
          results is larger than `page_size`, the service returns a [nextPageToken](/docs/vpc/api-ref/Gateway/list#responses)
          that can be used to get the next page of results in subsequent list requests.
          Default value: 100.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. To get the next page of results, set `page_token` to the
          [nextPageToken](/docs/vpc/api-ref/Gateway/list#responses) returned by a previous list request.

          The maximum string length in characters is 100.
        schema:
          type: string
      - name: filter
        in: query
        description: |-
          A filter expression that filters Gateway listed in the response.

          The expression must specify:
          1. The field name. Currently you can use filtering only on [Gateway.name](/docs/vpc/api-ref/Gateway#representation) field.
          2. An `=` operator.
          3. The value in double quotes (`"`). Must be 3-63 characters long and match the regular expression `[a-z][-a-z0-9]{1,61}[a-z0-9]`.
          Example of a filter: `name=my-gateway`.

          The maximum string length in characters is 1000.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListGatewaysResponse'
      x-y-navtitle: list
    post:
      tags:
      - Gateway
      summary: Creates a gateway in the specified folder.
      operationId: GatewayCreate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateGatewayRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateGatewayMetadata'
      x-y-operation-response: '[Gateway](/docs/vpc/api-ref/Gateway#representation)'
  /vpc/v1/gateways/{gatewayId}/operations:
    get:
      tags:
      - Gateway
      summary: List operations for the specified gateway.
      operationId: GatewayListOperations
      parameters:
      - name: gatewayId
        in: path
        description: |-
          Required.
          ID of the gateway to list operations for.

          To get a gateway ID make a [list](/docs/vpc/api-ref/Gateway/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return. If the number of available
          results is larger than [pageSize](/docs/vpc/api-ref/Gateway/listOperations#query_params), the service returns a [nextPageToken](/docs/vpc/api-ref/Gateway/listOperations#responses)
          that can be used to get the next page of results in subsequent list requests.
          Default value: 100.

          Acceptable values are 0 to 1000, inclusive.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. To get the next page of results, set [pageToken](/docs/vpc/api-ref/Gateway/listOperations#query_params) to the
          [nextPageToken](/docs/vpc/api-ref/Gateway/listOperations#responses) returned by a previous list request.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListGatewayOperationsResponse'
      x-y-navtitle: listOperations
  /vpc/v1/gateways/{gatewayId}:move:
    post:
      tags:
      - Gateway
      summary: Move a gateway to another folder
      operationId: GatewayMove
      parameters:
      - name: gatewayId
        in: path
        description: |-
          Required.
          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveGatewayRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: move
      x-y-operation-metadata:
        $ref: '#/components/schemas/MoveGatewayMetadata'
      x-y-operation-response: '[Gateway](/docs/vpc/api-ref/Gateway#representation)'
  /vpc/v1/networks/{networkId}:
    get:
      tags:
      - Network
      summary: Returns the specified Network resource.
      description: Get the list of available Network resources by making a [list](/docs/vpc/api-ref/Network/list)
        request.
      operationId: NetworkGet
      parameters:
      - name: networkId
        in: path
        description: |-
          Required.
          ID of the Network resource to return.
          To get the network ID, use a [list](/docs/vpc/api-ref/Network/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Network'
      x-y-navtitle: get
    delete:
      tags:
      - Network
      summary: Deletes the specified network.
      operationId: NetworkDelete
      parameters:
      - name: networkId
        in: path
        description: |-
          Required.
          ID of the Network resource to update.
          To get the network ID, use a [list](/docs/vpc/api-ref/Network/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteNetworkMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - Network
      summary: |-
        Updates the specified network.
        Method starts an asynchronous operation that can be cancelled while it is in progress.
      operationId: NetworkUpdate
      parameters:
      - name: networkId
        in: path
        description: |-
          Required.
          ID of the Network resource to update.
          To get the network ID use a [list](/docs/vpc/api-ref/Network/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateNetworkRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateNetworkMetadata'
      x-y-operation-response: '[Network](/docs/vpc/api-ref/Network#representation)'
  /vpc/v1/networks:
    get:
      tags:
      - Network
      summary: Retrieves the list of Network resources in the specified folder.
      operationId: NetworkList
      parameters:
      - name: folderId
        in: query
        description: |-
          Required.
          ID of the folder to list networks in.
          To get the folder ID, use a [list](/docs/resource-manager/api-ref/Folder/list) request.

          The maximum string length in characters is 50.
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return. If the number of available
          results is larger than [pageSize](/docs/vpc/api-ref/Network/list#query_params),
          the service returns a [nextPageToken](/docs/vpc/api-ref/Network/list#responses)
          that can be used to get the next page of results in subsequent list requests. Default value: 100.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. To get the next page of results, set [pageToken](/docs/vpc/api-ref/Network/list#query_params) to the
          [nextPageToken](/docs/vpc/api-ref/Network/list#responses) returned by a previous list request.

          The maximum string length in characters is 100.
        schema:
          type: string
      - name: filter
        in: query
        description: |-
          A filter expression that filters resources listed in the response.
          The expression must specify:
          1. The field name. Currently you can use filtering only on the [Network.name](/docs/vpc/api-ref/Network#representation) field.
          2. An `=` operator.
          3. The value in double quotes (`"`). Must be 3-63 characters long and match the regular expression `[a-z][-a-z0-9]{1,61}[a-z0-9]`.

          The maximum string length in characters is 1000.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListNetworksResponse'
      x-y-navtitle: list
    post:
      tags:
      - Network
      summary: |-
        Creates a network in the specified folder using the data specified in the request.
        Method starts an asynchronous operation that can be cancelled while it is in progress.
      operationId: NetworkCreate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateNetworkRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateNetworkMetadata'
      x-y-operation-response: '[Network](/docs/vpc/api-ref/Network#representation)'
  /vpc/v1/networks/{networkId}/subnets:
    get:
      tags:
      - Network
      summary: Lists subnets from the specified network.
      operationId: NetworkListSubnets
      parameters:
      - name: networkId
        in: path
        description: |-
          Required.
          ID of the Network resource to list subnets for.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page that should be returned. If the number of available
          results is larger than [pageSize](/docs/vpc/api-ref/Network/listSubnets#query_params),
          the service returns a [nextPageToken](/docs/vpc/api-ref/Network/listSubnets#responses)
          that can be used to get the next page of results in subsequent list requests. Default value: 100.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. Set [pageToken](/docs/vpc/api-ref/Network/listSubnets#query_params)
          to the [nextPageToken](/docs/vpc/api-ref/Network/listSubnets#responses)
          returned by a previous list request to get the next page of results.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListNetworkSubnetsResponse'
      x-y-navtitle: listSubnets
  /vpc/v1/networks/{networkId}/security_groups:
    get:
      tags:
      - Network
      summary: Lists security groups from the specified network.
      operationId: NetworkListSecurityGroups
      parameters:
      - name: networkId
        in: path
        description: |-
          Required.
          ID of the Network resource to list security groups for.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page that should be returned. If the number of available
          results is larger than [pageSize](/docs/vpc/api-ref/Network/listSecurityGroups#query_params),
          the service returns a [nextPageToken](/docs/vpc/api-ref/Network/listSecurityGroups#responses)
          that can be used to get the next page of results in subsequent list requests. Default value: 100.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. Set [pageToken](/docs/vpc/api-ref/Network/listSecurityGroups#query_params)
          to the [nextPageToken](/docs/vpc/api-ref/Network/listSecurityGroups#responses)
          returned by a previous list request to get the next page of results.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListNetworkSecurityGroupsResponse'
      x-y-navtitle: listSecurityGroups
  /vpc/v1/networks/{networkId}/route_tables:
    get:
      tags:
      - Network
      summary: Lists route tables from the specified network.
      operationId: NetworkListRouteTables
      parameters:
      - name: networkId
        in: path
        description: |-
          Required.
          ID of the Network resource to list route tables for.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page that should be returned. If the number of available
          results is larger than [pageSize](/docs/vpc/api-ref/Network/listRouteTables#query_params),
          the service returns a [nextPageToken](/docs/vpc/api-ref/Network/listRouteTables#responses)
          that can be used to get the next page of results in subsequent list requests. Default value: 100.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. Set [pageToken](/docs/vpc/api-ref/Network/listRouteTables#query_params)
          to the [nextPageToken](/docs/vpc/api-ref/Network/listRouteTables#responses)
          returned by a previous list request to get the next page of results.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListNetworkRouteTablesResponse'
      x-y-navtitle: listRouteTables
  /vpc/v1/networks/{networkId}/operations:
    get:
      tags:
      - Network
      summary: Lists operations for the specified network.
      operationId: NetworkListOperations
      parameters:
      - name: networkId
        in: path
        description: |-
          Required.
          ID of the Network resource to list operations for.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page that should be returned. If the number of available
          results is larger than [pageSize](/docs/vpc/api-ref/Network/listOperations#query_params), the service returns a [nextPageToken](/docs/vpc/api-ref/Network/listOperations#responses)
          that can be used to get the next page of results in subsequent list requests. Default value: 100.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. To get the next page of results, set [pageToken](/docs/vpc/api-ref/Network/listOperations#query_params) to the
          [nextPageToken](/docs/vpc/api-ref/Network/listOperations#responses) returned by a previous list request.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListNetworkOperationsResponse'
      x-y-navtitle: listOperations
  /vpc/v1/networks/{networkId}:move:
    post:
      tags:
      - Network
      summary: Move network to another folder.
      operationId: NetworkMove
      parameters:
      - name: networkId
        in: path
        description: |-
          Required.
          ID of the Network resource to move.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveNetworkRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: move
      x-y-operation-metadata:
        $ref: '#/components/schemas/MoveNetworkMetadata'
      x-y-operation-response: '[Network](/docs/vpc/api-ref/Network#representation)'
  /vpc/v1/routeTables/{routeTableId}:
    get:
      tags:
      - RouteTable
      summary: Returns the specified RouteTable resource.
      description: To get the list of available RouteTable resources, make a [list](/docs/vpc/api-ref/RouteTable/list)
        request.
      operationId: RouteTableGet
      parameters:
      - name: routeTableId
        in: path
        description: |-
          Required.
          ID of the RouteTable resource to return.
          To get the route table ID use a [list](/docs/vpc/api-ref/RouteTable/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteTable'
      x-y-navtitle: get
    delete:
      tags:
      - RouteTable
      summary: Deletes the specified route table.
      operationId: RouteTableDelete
      parameters:
      - name: routeTableId
        in: path
        description: |-
          Required.
          ID of the route table to delete.
          To get the route table ID use a [list](/docs/vpc/api-ref/RouteTable/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteRouteTableMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - RouteTable
      summary: |-
        Updates the specified route table.
        Method starts an asynchronous operation that can be cancelled while it is in progress.
      operationId: RouteTableUpdate
      parameters:
      - name: routeTableId
        in: path
        description: |-
          Required.
          ID of the RouteTable resource to update.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRouteTableRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateRouteTableMetadata'
      x-y-operation-response: '[RouteTable](/docs/vpc/api-ref/RouteTable#representation)'
  /vpc/v1/routeTables:
    get:
      tags:
      - RouteTable
      summary: Retrieves the list of RouteTable resources in the specified folder.
      operationId: RouteTableList
      parameters:
      - name: folderId
        in: query
        description: |-
          Required.
          ID of the folder that the route table belongs to.
          To get the folder ID use a [list](/docs/resource-manager/api-ref/Folder/list) request.

          The maximum string length in characters is 50.
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return. If the number of available
          results is larger than [pageSize](/docs/vpc/api-ref/RouteTable/list#query_params),
          the service returns a [nextPageToken](/docs/vpc/api-ref/RouteTable/list#responses)
          that can be used to get the next page of results in subsequent list requests. Default value: 100.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. To get the next page of results, set [pageToken](/docs/vpc/api-ref/RouteTable/list#query_params) to the
          [nextPageToken](/docs/vpc/api-ref/RouteTable/list#responses) returned by a previous list request.

          The maximum string length in characters is 100.
        schema:
          type: string
      - name: filter
        in: query
        description: |-
          A filter expression that filters resources listed in the response.
          The expression must specify:
          1. The field name. Currently you can use filtering only on [RouteTable.name](/docs/vpc/api-ref/RouteTable#representation) field.
          2. An `=` operator.
          3. The value in double quotes (`"`). Must be 3-63 characters long and match the regular expression `[a-z][-a-z0-9]{1,61}[a-z0-9]`.

          The maximum string length in characters is 1000.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListRouteTablesResponse'
      x-y-navtitle: list
    post:
      tags:
      - RouteTable
      summary: |-
        Creates a route table in the specified folder and network.
        Method starts an asynchronous operation that can be cancelled while it is in progress.
      operationId: RouteTableCreate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRouteTableRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateRouteTableMetadata'
      x-y-operation-response: '[RouteTable](/docs/vpc/api-ref/RouteTable#representation)'
  /vpc/v1/routeTables/{routeTableId}/operations:
    get:
      tags:
      - RouteTable
      summary: List operations for the specified route table.
      operationId: RouteTableListOperations
      parameters:
      - name: routeTableId
        in: path
        description: |-
          Required.
          ID of the RouteTable resource to list operations for.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page that should be returned. If the number of available
          results is larger than [pageSize](/docs/vpc/api-ref/RouteTable/listOperations#query_params), the service returns a [nextPageToken](/docs/vpc/api-ref/RouteTable/listOperations#responses)
          that can be used to get the next page of results in subsequent list requests. Default value: 100.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. To get the next page of results, set [pageToken](/docs/vpc/api-ref/RouteTable/listOperations#query_params) to the
          [nextPageToken](/docs/vpc/api-ref/RouteTable/listOperations#responses) returned by a previous list request.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListRouteTableOperationsResponse'
      x-y-navtitle: listOperations
  /vpc/v1/routeTables/{routeTableId}:move:
    post:
      tags:
      - RouteTable
      summary: Move route table to another folder.
      operationId: RouteTableMove
      parameters:
      - name: routeTableId
        in: path
        description: |-
          Required.
          ID of the RouteTable resource to move.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveRouteTableRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: move
      x-y-operation-metadata:
        $ref: '#/components/schemas/MoveRouteTableMetadata'
      x-y-operation-response: '[RouteTable](/docs/vpc/api-ref/RouteTable#representation)'
  /vpc/v1/securityGroups/{securityGroupId}:
    get:
      tags:
      - SecurityGroup
      operationId: SecurityGroupGet
      parameters:
      - name: securityGroupId
        in: path
        description: Required.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SecurityGroup'
      x-y-navtitle: get
    delete:
      tags:
      - SecurityGroup
      operationId: SecurityGroupDelete
      parameters:
      - name: securityGroupId
        in: path
        description: Required.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteSecurityGroupMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - SecurityGroup
      operationId: SecurityGroupUpdate
      parameters:
      - name: securityGroupId
        in: path
        description: Required.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSecurityGroupRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateSecurityGroupMetadata'
      x-y-operation-response: '[SecurityGroup](/docs/vpc/api-ref/SecurityGroup#representation)'
  /vpc/v1/securityGroups:
    get:
      tags:
      - SecurityGroup
      operationId: SecurityGroupList
      parameters:
      - name: folderId
        in: query
        description: Required.
        schema:
          type: string
      - name: pageSize
        in: query
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        schema:
          type: string
      - name: filter
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSecurityGroupsResponse'
      x-y-navtitle: list
    post:
      tags:
      - SecurityGroup
      operationId: SecurityGroupCreate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSecurityGroupRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateSecurityGroupMetadata'
      x-y-operation-response: '[SecurityGroup](/docs/vpc/api-ref/SecurityGroup#representation)'
  /vpc/v1/securityGroups/{securityGroupId}/rules:
    patch:
      tags:
      - SecurityGroup
      operationId: SecurityGroupUpdateRules
      parameters:
      - name: securityGroupId
        in: path
        description: Required.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSecurityGroupRulesRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: updateRules
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateSecurityGroupMetadata'
      x-y-operation-response: '[SecurityGroup](/docs/vpc/api-ref/SecurityGroup#representation)'
  /vpc/v1/securityGroups/{securityGroupId}/rules/{ruleId}:
    patch:
      tags:
      - SecurityGroup
      summary: update rule description or labels
      operationId: SecurityGroupUpdateRule
      parameters:
      - name: securityGroupId
        in: path
        description: Required.
        required: true
        schema:
          type: string
      - name: ruleId
        in: path
        description: Required.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSecurityGroupRuleRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: updateRule
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateSecurityGroupRuleMetadata'
      x-y-operation-response: '`SecurityGroupRule`'
  /vpc/v1/securityGroups/{securityGroupId}:move:
    post:
      tags:
      - SecurityGroup
      operationId: SecurityGroupMove
      parameters:
      - name: securityGroupId
        in: path
        description: Required.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveSecurityGroupRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: move
      x-y-operation-metadata:
        $ref: '#/components/schemas/MoveSecurityGroupMetadata'
      x-y-operation-response: '[SecurityGroup](/docs/vpc/api-ref/SecurityGroup#representation)'
  /vpc/v1/securityGroups/{securityGroupId}/operations:
    get:
      tags:
      - SecurityGroup
      operationId: SecurityGroupListOperations
      parameters:
      - name: securityGroupId
        in: path
        description: Required.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSecurityGroupOperationsResponse'
      x-y-navtitle: listOperations
  /vpc/v1/subnets/{subnetId}:
    get:
      tags:
      - Subnet
      summary: Returns the specified Subnet resource.
      description: To get the list of available Subnet resources, make a [list](/docs/vpc/api-ref/Subnet/list)
        request.
      operationId: SubnetGet
      parameters:
      - name: subnetId
        in: path
        description: |-
          Required.
          ID of the Subnet resource to return.
          To get the subnet ID use a [list](/docs/vpc/api-ref/Subnet/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subnet'
      x-y-navtitle: get
    delete:
      tags:
      - Subnet
      summary: Deletes the specified subnet.
      operationId: SubnetDelete
      parameters:
      - name: subnetId
        in: path
        description: |-
          Required.
          ID of the subnet to delete.
          To get the subnet ID use a [list](/docs/vpc/api-ref/Subnet/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteSubnetMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - Subnet
      summary: |-
        Updates the specified subnet.
        Method starts an asynchronous operation that can be cancelled while it is in progress.
      operationId: SubnetUpdate
      parameters:
      - name: subnetId
        in: path
        description: |-
          Required.
          ID of the Subnet resource to update.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSubnetRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateSubnetMetadata'
      x-y-operation-response: '[Subnet](/docs/vpc/api-ref/Subnet#representation)'
  /vpc/v1/subnets:
    get:
      tags:
      - Subnet
      summary: Retrieves the list of Subnet resources in the specified folder.
      operationId: SubnetList
      parameters:
      - name: folderId
        in: query
        description: |-
          Required.
          ID of the folder to list subnets in.
          To get the folder ID use a [list](/docs/resource-manager/api-ref/Folder/list) request.

          The maximum string length in characters is 50.
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return. If the number of available
          results is larger than [pageSize](/docs/vpc/api-ref/Subnet/list#query_params),
          the service returns a [nextPageToken](/docs/vpc/api-ref/Subnet/list#responses)
          that can be used to get the next page of results in subsequent list requests. Default value: 100.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. To get the next page of results, set [pageToken](/docs/vpc/api-ref/Subnet/list#query_params) to the
          [nextPageToken](/docs/vpc/api-ref/Subnet/list#responses) returned by a previous list request.

          The maximum string length in characters is 100.
        schema:
          type: string
      - name: filter
        in: query
        description: |-
          A filter expression that filters resources listed in the response.
          The expression must specify:
          1. The field name. Currently you can use filtering only on [Subnet.name](/docs/vpc/api-ref/Subnet#representation) field.
          2. An `=` operator.
          3. The value in double quotes (`"`). Must be 3-63 characters long and match the regular expression `[a-z][-a-z0-9]{1,61}[a-z0-9]`.

          The maximum string length in characters is 1000.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSubnetsResponse'
      x-y-navtitle: list
    post:
      tags:
      - Subnet
      summary: |-
        Creates a subnet in the specified folder and network.
        Method starts an asynchronous operation that can be cancelled while it is in progress.
      operationId: SubnetCreate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSubnetRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateSubnetMetadata'
      x-y-operation-response: '[Subnet](/docs/vpc/api-ref/Subnet#representation)'
  /vpc/v1/subnets/{subnetId}:add-cidr-blocks:
    post:
      tags:
      - Subnet
      summary: |-
        Adds CIDR blocks to the specified subnet.
        Method starts an asynchronous operation that can be cancelled while it is in progress.
      operationId: SubnetAddCidrBlocks
      parameters:
      - name: subnetId
        in: path
        description: |-
          Required.
          ID of the Subnet resource that is being updated.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddSubnetCidrBlocksRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: addCidrBlocks
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateSubnetMetadata'
      x-y-operation-response: '[Subnet](/docs/vpc/api-ref/Subnet#representation)'
  /vpc/v1/subnets/{subnetId}:remove-cidr-blocks:
    post:
      tags:
      - Subnet
      summary: |-
        Removes CIDR blocks from the specified subnet.
        Method starts an asynchronous operation that can be cancelled while it is in progress.
      operationId: SubnetRemoveCidrBlocks
      parameters:
      - name: subnetId
        in: path
        description: |-
          Required.
          ID of the Subnet resource that is being updated.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemoveSubnetCidrBlocksRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: removeCidrBlocks
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateSubnetMetadata'
      x-y-operation-response: '[Subnet](/docs/vpc/api-ref/Subnet#representation)'
  /vpc/v1/subnets/{subnetId}/operations:
    get:
      tags:
      - Subnet
      summary: List operations for the specified subnet.
      operationId: SubnetListOperations
      parameters:
      - name: subnetId
        in: path
        description: |-
          Required.
          ID of the Subnet resource to list operations for.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page that should be returned. If the number of available
          results is larger than [pageSize](/docs/vpc/api-ref/Subnet/listOperations#query_params), the service returns a [nextPageToken](/docs/vpc/api-ref/Subnet/listOperations#responses)
          that can be used to get the next page of results in subsequent list requests. Default value: 100.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token. To get the next page of results, set [pageToken](/docs/vpc/api-ref/Subnet/listOperations#query_params) to the
          [nextPageToken](/docs/vpc/api-ref/Subnet/listOperations#responses) returned by a previous list request.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSubnetOperationsResponse'
      x-y-navtitle: listOperations
  /vpc/v1/subnets/{subnetId}:move:
    post:
      tags:
      - Subnet
      summary: Move subnet to another folder.
      operationId: SubnetMove
      parameters:
      - name: subnetId
        in: path
        description: |-
          Required.
          ID of the Subnet resource to move.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveSubnetRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: move
      x-y-operation-metadata:
        $ref: '#/components/schemas/MoveSubnetMetadata'
      x-y-operation-response: '[Subnet](/docs/vpc/api-ref/Subnet#representation)'
  /vpc/v1/subnets/{subnetId}/addresses:
    get:
      tags:
      - Subnet
      summary: List used addresses in specified subnet.
      operationId: SubnetListUsedAddresses
      parameters:
      - name: subnetId
        in: path
        description: Required.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        schema:
          type: string
      - name: filter
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListUsedAddressesResponse'
      x-y-navtitle: listUsedAddresses
components:
  schemas:
    AddSubnetCidrBlocksRequest:
      type: object
      properties:
        v4CidrBlocks:
          description: |-
            CIDR block.
            The range of internal addresses that should be added to this subnet.
            For example, 10.0.0.0/22 or 192.168.0.0/24.
            Minimum subnet size is /28, maximum subnet size is /16.
          type: array
          items:
            type: string
    Address:
      description: An Address resource. For more information, see [Address](/docs/vpc/concepts/address).
      type: object
      allOf:
      - type: object
        properties:
          id:
            description: ID of the address. Generated at creation time.
            type: string
          folderId:
            description: ID of the folder that the address belongs to.
            type: string
          createdAt:
            description: "Creation timestamp.\n\nString in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt)
              text format. The range of possible values is from \n`0001-01-01T00:00:00Z`
              to `9999-12-31T23:59:59.999999999Z`, i.e. from 0 to 9 digits for fractions
              of a second.\n\nTo work with values in this field, use the APIs described
              in the \n[Protocol Buffers reference](https://developers.google.com/protocol-buffers/docs/reference/overview).
              \nIn some languages, built-in datetime utilities do not support nanosecond
              precision (9 digits)."
            type: string
            format: date-time
          name:
            description: |-
              Name of the address.
              The name is unique within the folder.
            type: string
          description:
            description: Description of the address.
            type: string
          labels:
            description: Resource labels as `key:value` pairs.
            type: object
            additionalProperties:
              type: string
          reserved:
            description: Specifies if address is reserved or not.
            type: boolean
            format: boolean
          used:
            description: Specifies if address is used or not.
            type: boolean
            format: boolean
          type:
            $ref: '#/components/schemas/AddressType'
            description: Type of the IP address.
          ipVersion:
            $ref: '#/components/schemas/AddressIpVersion'
            description: Vervion of the IP address.
          deletionProtection:
            description: Specifies if address protected from deletion.
            type: boolean
            format: boolean
      - title: address
        type: object
        maxProperties: 1
        properties:
          externalIpv4Address:
            $ref: '#/components/schemas/ExternalIpv4Address'
    AddressIpVersion:
      description: |2-
         - IPV4: IPv4 address.
         - IPV6: IPv6 address.
      type: string
      enum:
      - IPV4
      - IPV6
    AddressRequirements:
      type: object
      properties:
        ddosProtectionProvider:
          description: DDoS protection provider ID.
          type: string
        outgoingSmtpCapability:
          description: Capability to send SMTP traffic.
          type: string
    AddressType:
      description: |2-
         - INTERNAL: Internal IP address.
         - EXTERNAL: Public IP address.
      type: string
      enum:
      - INTERNAL
      - EXTERNAL
    CidrBlocks:
      type: object
      properties:
        v4CidrBlocks:
          type: array
          items:
            type: string
        v6CidrBlocks:
          type: array
          items:
            type: string
    CreateAddressMetadata:
      type: object
      properties:
        addressId:
          description: ID of the address that is being created.
          type: string
    CreateAddressRequest:
      type: object
      allOf:
      - type: object
        properties:
          folderId:
            description: |-
              Required.
              ID of the folder to create a address in.

              To get a folder ID make a [list](/docs/resource-manager/api-ref/Folder/list) request.

              The maximum string length in characters is 50.
            type: string
          name:
            description: |-
              Name of the address.
              The name must be unique within the folder.

              Value must match the regular expression `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
            type: string
          description:
            description: |-
              Description of the address.

              The maximum string length in characters is 256.
            type: string
          labels:
            description: |-
              Address labels as `key:value` pairs.

              No more than 64 per resource.
              The string length in characters for each key must be 1-63.
              Each key must match the regular expression `` [a-z][-_0-9a-z]* ``.
              The maximum string length in characters for each value is 63.
              Each value must match the regular expression `` [-_0-9a-z]* ``.
            type: object
            additionalProperties:
              type: string
          deletionProtection:
            description: Specifies if address protected from deletion.
            type: boolean
            format: boolean
      - title: address_spec
        type: object
        maxProperties: 1
        properties:
          externalIpv4AddressSpec:
            $ref: '#/components/schemas/ExternalIpv4AddressSpec'
    CreateGatewayMetadata:
      type: object
      properties:
        gatewayId:
          description: ID of the gateway that is being created.
          type: string
    CreateGatewayRequest:
      type: object
      allOf:
      - type: object
        properties:
          folderId:
            description: |-
              Required.
              ID of the folder to create a gateway in.

              To get a folder ID make a [list](/docs/resource-manager/api-ref/Folder/list) request.

              The maximum string length in characters is 50.
            type: string
          name:
            description: |-
              Name of the gateway.
              The name must be unique within the folder.

              Value must match the regular expression `` |[a-z]([-a-z0-9]{0,61}[a-z0-9])? ``.
            type: string
          description:
            description: |-
              Description of the gateway.

              The maximum string length in characters is 256.
            type: string
          labels:
            description: |-
              Gateway labels as `key:value` pairs.

              No more than 64 per resource.
              The string length in characters for each key must be 1-63.
              Each key must match the regular expression `` [a-z][-_./\@0-9a-z]* ``.
              The maximum string length in characters for each value is 63.
              Each value must match the regular expression `` [-_./\@0-9a-z]* ``.
            type: object
            additionalProperties:
              type: string
      - title: gateway
        type: object
        maxProperties: 1
        properties:
          sharedEgressGatewaySpec:
            $ref: '#/components/schemas/SharedEgressGatewaySpec'
    CreateNetworkMetadata:
      type: object
      properties:
        networkId:
          description: ID of the Network that is being created.
          type: string
    CreateNetworkRequest:
      type: object
      properties:
        folderId:
          description: |-
            Required.
            ID of the folder for this request to create a network in.
            To get the folder ID, use a [list](/docs/resource-manager/api-ref/Folder/list) request.

            The maximum string length in characters is 50.
          type: string
        name:
          description: |-
            Name of the network.
            The name must be unique within the folder.

            Value must match the regular expression `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            Description of the network.

            The maximum string length in characters is 256.
          type: string
        labels:
          description: |-
            Resource labels as `` key:value `` pairs.

            No more than 64 per resource.
            The string length in characters for each key must be 1-63.
            Each key must match the regular expression `` [a-z][-_0-9a-z]* ``.
            The maximum string length in characters for each value is 63.
            Each value must match the regular expression `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
    CreateRouteTableMetadata:
      type: object
      properties:
        routeTableId:
          description: ID of the route table that is being created.
          type: string
    CreateRouteTableRequest:
      type: object
      properties:
        folderId:
          description: |-
            Required.
            ID of the folder that the route table belongs to.
            To get the folder ID use a [list](/docs/resource-manager/api-ref/Folder/list) request.

            The maximum string length in characters is 50.
          type: string
        name:
          description: |-
            Name of the route table.
            The name must be unique within the folder.

            Value must match the regular expression `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            Description of the route table.

            The maximum string length in characters is 256.
          type: string
        labels:
          description: |-
            Resource labels, `` key:value `` pairs.

            No more than 64 per resource.
            The string length in characters for each key must be 1-63.
            Each key must match the regular expression `` [a-z][-_0-9a-z]* ``.
            The maximum string length in characters for each value is 63.
            Each value must match the regular expression `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        networkId:
          description: |-
            Required.
            ID of the network the route table belongs to.

            The maximum string length in characters is 50.
          type: string
        staticRoutes:
          description: List of static routes.
          type: array
          items:
            $ref: '#/components/schemas/StaticRoute'
    CreateSecurityGroupMetadata:
      type: object
      properties:
        securityGroupId:
          type: string
    CreateSecurityGroupRequest:
      type: object
      properties:
        folderId:
          description: Required.
          type: string
        name:
          type: string
        description:
          type: string
        labels:
          type: object
          additionalProperties:
            type: string
        networkId:
          description: Required.
          type: string
        ruleSpecs:
          type: array
          items:
            $ref: '#/components/schemas/SecurityGroupRuleSpec'
    CreateSubnetMetadata:
      type: object
      properties:
        subnetId:
          description: ID of the subnet that is being created.
          type: string
    CreateSubnetRequest:
      type: object
      properties:
        folderId:
          description: |-
            Required.
            ID of the folder to create a subnet in.
            To get folder ID use a [list](/docs/resource-manager/api-ref/Folder/list) request.

            The maximum string length in characters is 50.
          type: string
        name:
          description: |-
            Name of the subnet.
            The name must be unique within the folder.

            Value must match the regular expression `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            Description of the subnet.

            The maximum string length in characters is 256.
          type: string
        labels:
          description: |-
            Resource labels, `` key:value `` pairs.

            No more than 64 per resource.
            The string length in characters for each key must be 1-63.
            Each key must match the regular expression `` [a-z][-_0-9a-z]* ``.
            The maximum string length in characters for each value is 63.
            Each value must match the regular expression `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        networkId:
          description: |-
            Required.
            ID of the network to create subnet in.

            The maximum string length in characters is 50.
          type: string
        zoneId:
          description: |-
            Required.
            ID of the availability zone where the subnet resides.
            To get a list of available zones, use the [list](/docs/compute/api-ref/Zone/list) request.

            The maximum string length in characters is 50.
          type: string
        v4CidrBlocks:
          description: |-
            Required.
            CIDR block.
            The range of internal addresses that are defined for this subnet.
            This field can be set only at Subnet resource creation time and cannot be changed.
            For example, 10.0.0.0/22 or 192.168.0.0/24.
            Minimum subnet size is /28, maximum subnet size is /16.
          type: array
          items:
            type: string
        routeTableId:
          description: |-
            ID of route table the subnet is linked to.

            The maximum string length in characters is 50.
          type: string
        dhcpOptions:
          $ref: '#/components/schemas/DhcpOptions_in_CreateSubnetRequest_dhcpOptions'
    DeleteAddressMetadata:
      type: object
      properties:
        addressId:
          description: ID of the address that is being deleted.
          type: string
    DeleteGatewayMetadata:
      type: object
      properties:
        gatewayId:
          description: ID of the gateway that is being deleted.
          type: string
    DeleteNetworkMetadata:
      type: object
      properties:
        networkId:
          description: ID of the network that is being deleted.
          type: string
    DeleteRouteTableMetadata:
      type: object
      properties:
        routeTableId:
          description: ID of the RouteTable resource that is being deleted.
          type: string
    DeleteSecurityGroupMetadata:
      type: object
      properties:
        securityGroupId:
          type: string
    DeleteSubnetMetadata:
      type: object
      properties:
        subnetId:
          description: ID of the Subnet resource that is being deleted.
          type: string
    DhcpOptions:
      type: object
      properties:
        domainNameServers:
          type: array
          items:
            type: string
        domainName:
          type: string
        ntpServers:
          type: array
          items:
            type: string
    Empty:
      description: Empty JSON object `` {} ``.
      type: object
      maxProperties: 0
    ExternalIpv4Address:
      type: object
      properties:
        address:
          description: Value of address.
          type: string
        zoneId:
          description: Availability zone from which the address will be allocated.
          type: string
        requirements:
          $ref: '#/components/schemas/AddressRequirements_in_ExternalIpv4Address_requirements'
    ExternalIpv4AddressSpec:
      type: object
      properties:
        address:
          description: Value of address.
          type: string
        zoneId:
          description: Availability zone from which the address will be allocated.
          type: string
        requirements:
          $ref: '#/components/schemas/AddressRequirements_in_ExternalIpv4AddressSpec_requirements'
    FieldMask:
      description: |-
        A comma-separated names off ALL fields to be updated.
        Only the specified fields will be changed. The others will be left untouched.
        If the field is specified in `` updateMask `` and no value for that field was sent in the request,
        the field's value will be reset to the default. The default value for most fields is null or 0.

        If `` updateMask `` is not sent in the request, all fields' values will be updated.
        Fields specified in the request will be updated to provided values.
        The rest of the fields will be reset to the default.
      type: string
    Gateway:
      description: A Gateway resource. For more information, see [Gateway](/docs/vpc/concepts/gateway).
      type: object
      allOf:
      - type: object
        properties:
          id:
            description: ID of the gateway. Generated at creation time.
            type: string
          folderId:
            description: ID of the folder that the gateway belongs to.
            type: string
          createdAt:
            description: "Creation timestamp.\n\nString in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt)
              text format. The range of possible values is from \n`0001-01-01T00:00:00Z`
              to `9999-12-31T23:59:59.999999999Z`, i.e. from 0 to 9 digits for fractions
              of a second.\n\nTo work with values in this field, use the APIs described
              in the \n[Protocol Buffers reference](https://developers.google.com/protocol-buffers/docs/reference/overview).
              \nIn some languages, built-in datetime utilities do not support nanosecond
              precision (9 digits)."
            type: string
            format: date-time
          name:
            description: |-
              Name of the gateway.
              The name is unique within the folder.
            type: string
          description:
            description: Description of the gateway.
            type: string
          labels:
            description: Resource labels as `key:value` pairs.
            type: object
            additionalProperties:
              type: string
      - title: gateway
        type: object
        maxProperties: 1
        properties:
          sharedEgressGateway:
            $ref: '#/components/schemas/SharedEgressGateway'
    ListAddressOperationsResponse:
      type: object
      properties:
        operations:
          description: List of operations for the specified address.
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextPageToken:
          description: |-
            Token for getting the next page of the list. If the number of results is greater than
            the specified [pageSize](/docs/vpc/api-ref/Address/listOperations#query_params), use `next_page_token` as the value
            for the [pageToken](/docs/vpc/api-ref/Address/listOperations#query_params) parameter in the next list request.

            Each subsequent page will have its own `next_page_token` to continue paging through the results.
          type: string
    ListAddressesResponse:
      type: object
      properties:
        addresses:
          description: List of addresses.
          type: array
          items:
            $ref: '#/components/schemas/Address'
        nextPageToken:
          description: |-
            Token for getting the next page of the list. If the number of results is greater than
            the specified [pageSize](/docs/vpc/api-ref/Address/list#query_params), use `next_page_token` as the value
            for the [pageToken](/docs/vpc/api-ref/Address/list#query_params) parameter in the next list request.

            Each subsequent page will have its own `next_page_token` to continue paging through the results.
          type: string
    ListGatewayOperationsResponse:
      type: object
      properties:
        operations:
          description: List of operations for the specified gateway.
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextPageToken:
          description: |-
            Token for getting the next page of the list. If the number of results is greater than
            the specified [pageSize](/docs/vpc/api-ref/Gateway/listOperations#query_params), use `next_page_token` as the value
            for the [pageToken](/docs/vpc/api-ref/Gateway/listOperations#query_params) parameter in the next list request.

            Each subsequent page will have its own `next_page_token` to continue paging through the results.
          type: string
    ListGatewaysResponse:
      type: object
      properties:
        gateways:
          description: List of gateways.
          type: array
          items:
            $ref: '#/components/schemas/Gateway'
        nextPageToken:
          description: |-
            Token for getting the next page of the list. If the number of results is greater than
            the specified [pageSize](/docs/vpc/api-ref/Gateway/list#query_params), use `next_page_token` as the value
            for the [pageToken](/docs/vpc/api-ref/Gateway/list#query_params) parameter in the next list request.

            Each subsequent page will have its own `next_page_token` to continue paging through the results.
          type: string
    ListNetworkOperationsResponse:
      type: object
      properties:
        operations:
          description: List of operations for the specified network.
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextPageToken:
          description: |-
            This token allows you to get the next page of results for list requests. If the number of results
            is larger than [pageSize](/docs/vpc/api-ref/Network/listOperations#query_params), use the [nextPageToken](/docs/vpc/api-ref/Network/listOperations#responses) as the value
            for the [pageToken](/docs/vpc/api-ref/Network/listOperations#query_params) query parameter in the next list request.
            Each subsequent list request will have its own [nextPageToken](/docs/vpc/api-ref/Network/listOperations#responses) to continue paging through the results.
          type: string
    ListNetworkRouteTablesResponse:
      type: object
      properties:
        routeTables:
          description: List of route tables that belong to the network which is specified
            in the request.
          type: array
          items:
            $ref: '#/components/schemas/RouteTable'
        nextPageToken:
          description: |-
            This token allows you to get the next page of results for list requests. If the number of results
            is larger than [pageSize](/docs/vpc/api-ref/Network/listRouteTables#query_params), use
            the [nextPageToken](/docs/vpc/api-ref/Network/listRouteTables#responses) as the value
            for the [pageToken](/docs/vpc/api-ref/Network/listRouteTables#query_params) query parameter
            in the next list request. Subsequent list requests will have their own
            [nextPageToken](/docs/vpc/api-ref/Network/listRouteTables#responses) to continue paging through the results.
          type: string
    ListNetworkSecurityGroupsResponse:
      type: object
      properties:
        securityGroups:
          description: List of security groups that belong to the network which is
            specified in the request.
          type: array
          items:
            $ref: '#/components/schemas/SecurityGroup'
        nextPageToken:
          description: |-
            This token allows you to get the next page of results for list requests. If the number of results
            is larger than [pageSize](/docs/vpc/api-ref/Network/listSecurityGroups#query_params), use
            the [nextPageToken](/docs/vpc/api-ref/Network/listSecurityGroups#responses) as the value
            for the [pageToken](/docs/vpc/api-ref/Network/listSecurityGroups#query_params) query parameter
            in the next list request. Subsequent list requests will have their own
            [nextPageToken](/docs/vpc/api-ref/Network/listSecurityGroups#responses) to continue paging through the results.
          type: string
    ListNetworkSubnetsResponse:
      type: object
      properties:
        subnets:
          description: List of subnets that belong to the network which is specified
            in the request.
          type: array
          items:
            $ref: '#/components/schemas/Subnet'
        nextPageToken:
          description: |-
            This token allows you to get the next page of results for list requests. If the number of results
            is larger than [pageSize](/docs/vpc/api-ref/Network/listSubnets#query_params), use
            the [nextPageToken](/docs/vpc/api-ref/Network/listSubnets#responses) as the value
            for the [pageToken](/docs/vpc/api-ref/Network/listSubnets#query_params) query parameter
            in the next list request. Subsequent list requests will have their own
            [nextPageToken](/docs/vpc/api-ref/Network/listSubnets#responses) to continue paging through the results.
          type: string
    ListNetworksResponse:
      type: object
      properties:
        networks:
          description: List of Network resources.
          type: array
          items:
            $ref: '#/components/schemas/Network'
        nextPageToken:
          description: |-
            This token allows you to get the next page of results for list requests. If the number of results
            is larger than [pageSize](/docs/vpc/api-ref/Network/list#query_params), use
            the [nextPageToken](/docs/vpc/api-ref/Network/list#responses) as the value
            for the [pageToken](/docs/vpc/api-ref/Network/list#query_params) query parameter
            in the next list request. Subsequent list requests will have their own
            [nextPageToken](/docs/vpc/api-ref/Network/list#responses) to continue paging through the results.
          type: string
    ListRouteTableOperationsResponse:
      type: object
      properties:
        operations:
          description: List of operations for the specified RouteTable resource.
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextPageToken:
          description: |-
            This token allows you to get the next page of results for list requests. If the number of results
            is larger than [pageSize](/docs/vpc/api-ref/RouteTable/listOperations#query_params), use the [nextPageToken](/docs/vpc/api-ref/RouteTable/listOperations#responses) as the value
            for the [pageToken](/docs/vpc/api-ref/RouteTable/listOperations#query_params) query parameter in the next list request.
            Each subsequent list request will have its own [nextPageToken](/docs/vpc/api-ref/RouteTable/listOperations#responses) to continue paging through the results.
          type: string
    ListRouteTablesResponse:
      type: object
      properties:
        routeTables:
          description: List of RouteTable resources.
          type: array
          items:
            $ref: '#/components/schemas/RouteTable'
        nextPageToken:
          description: |-
            This token allows you to get the next page of results for list requests. If the number of results
            is larger than [pageSize](/docs/vpc/api-ref/RouteTable/list#query_params), use
            the [nextPageToken](/docs/vpc/api-ref/RouteTable/list#responses) as the value
            for the [pageToken](/docs/vpc/api-ref/RouteTable/list#query_params) query parameter
            in the next list request. Subsequent list requests will have their own
            [nextPageToken](/docs/vpc/api-ref/RouteTable/list#responses) to continue paging through the results.
          type: string
    ListSecurityGroupOperationsResponse:
      type: object
      properties:
        operations:
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextPageToken:
          type: string
    ListSecurityGroupsResponse:
      type: object
      properties:
        securityGroups:
          type: array
          items:
            $ref: '#/components/schemas/SecurityGroup'
        nextPageToken:
          type: string
    ListSubnetOperationsResponse:
      type: object
      properties:
        operations:
          description: List of operations for the specified Subnet resource.
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextPageToken:
          description: |-
            This token allows you to get the next page of results for list requests. If the number of results
            is larger than [pageSize](/docs/vpc/api-ref/Subnet/listOperations#query_params), use the [nextPageToken](/docs/vpc/api-ref/Subnet/listOperations#responses) as the value
            for the [pageToken](/docs/vpc/api-ref/Subnet/listOperations#query_params) query parameter in the next list request.
            Each subsequent list request will have its own [nextPageToken](/docs/vpc/api-ref/Subnet/listOperations#responses) to continue paging through the results.
          type: string
    ListSubnetsResponse:
      type: object
      properties:
        subnets:
          description: List of Subnet resources.
          type: array
          items:
            $ref: '#/components/schemas/Subnet'
        nextPageToken:
          description: |-
            This token allows you to get the next page of results for list requests. If the number of results
            is larger than [pageSize](/docs/vpc/api-ref/Subnet/list#query_params), use
            the [nextPageToken](/docs/vpc/api-ref/Subnet/list#responses) as the value
            for the [pageToken](/docs/vpc/api-ref/Subnet/list#query_params) query parameter
            in the next list request. Subsequent list requests will have their own
            [nextPageToken](/docs/vpc/api-ref/Subnet/list#responses) to continue paging through the results.
          type: string
    ListUsedAddressesResponse:
      type: object
      properties:
        addresses:
          type: array
          items:
            $ref: '#/components/schemas/UsedAddress'
        nextPageToken:
          type: string
    MoveAddressMetadata:
      type: object
      properties:
        addressId:
          type: string
    MoveAddressRequest:
      type: object
      properties:
        destinationFolderId:
          description: |-
            Required.
            The maximum string length in characters is 50.
          type: string
    MoveGatewayMetadata:
      type: object
      properties:
        gatewayId:
          type: string
    MoveGatewayRequest:
      type: object
      properties:
        destinationFolderId:
          description: |-
            Required.
            The maximum string length in characters is 50.
          type: string
    MoveNetworkMetadata:
      type: object
      properties:
        networkId:
          description: ID of the network that is being moved.
          type: string
    MoveNetworkRequest:
      type: object
      properties:
        destinationFolderId:
          description: |-
            Required.
            ID of the destination folder.

            The maximum string length in characters is 50.
          type: string
    MoveRouteTableMetadata:
      type: object
      properties:
        routeTableId:
          description: ID of the RouteTable resource that is being moved.
          type: string
    MoveRouteTableRequest:
      type: object
      properties:
        destinationFolderId:
          description: |-
            Required.
            ID of the destination folder.

            The maximum string length in characters is 50.
          type: string
    MoveSecurityGroupMetadata:
      type: object
      properties:
        securityGroupId:
          type: string
    MoveSecurityGroupRequest:
      type: object
      properties:
        destinationFolderId:
          description: Required.
          type: string
    MoveSubnetMetadata:
      type: object
      properties:
        subnetId:
          description: ID of the Subnet resource that is being moved.
          type: string
    MoveSubnetRequest:
      type: object
      properties:
        destinationFolderId:
          description: |-
            Required.
            ID of the destination folder.

            The maximum string length in characters is 50.
          type: string
    Network:
      description: A Network resource. For more information, see [Networks](/docs/vpc/concepts/network).
      type: object
      properties:
        id:
          description: ID of the network.
          type: string
        folderId:
          description: ID of the folder that the network belongs to.
          type: string
        createdAt:
          description: "Creation timestamp in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt)
            text format.\n\nString in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt)
            text format. The range of possible values is from \n`0001-01-01T00:00:00Z`
            to `9999-12-31T23:59:59.999999999Z`, i.e. from 0 to 9 digits for fractions
            of a second.\n\nTo work with values in this field, use the APIs described
            in the \n[Protocol Buffers reference](https://developers.google.com/protocol-buffers/docs/reference/overview).
            \nIn some languages, built-in datetime utilities do not support nanosecond
            precision (9 digits)."
          type: string
          format: date-time
        name:
          description: |-
            Name of the network.
            The name is unique within the folder. 3-63 characters long.
          type: string
        description:
          description: Optional description of the network. 0-256 characters long.
          type: string
        labels:
          description: Resource labels as `` key:value `` pairs. Maximum of 64 per
            resource.
          type: object
          additionalProperties:
            type: string
        defaultSecurityGroupId:
          description: ID of default security group for network.
          type: string
    Operation:
      description: An Operation resource. For more information, see [Operation](/docs/api-design-guide/concepts/operation).
      type: object
      allOf:
      - type: object
        properties:
          id:
            description: ID of the operation.
            type: string
          description:
            description: Description of the operation. 0-256 characters long.
            type: string
          createdAt:
            description: "Creation timestamp.\n\nString in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt)
              text format. The range of possible values is from \n`0001-01-01T00:00:00Z`
              to `9999-12-31T23:59:59.999999999Z`, i.e. from 0 to 9 digits for fractions
              of a second.\n\nTo work with values in this field, use the APIs described
              in the \n[Protocol Buffers reference](https://developers.google.com/protocol-buffers/docs/reference/overview).
              \nIn some languages, built-in datetime utilities do not support nanosecond
              precision (9 digits)."
            type: string
            format: date-time
          createdBy:
            description: ID of the user or service account who initiated the operation.
            type: string
          modifiedAt:
            description: "The time when the Operation resource was last modified.\n\nString
              in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format. The
              range of possible values is from \n`0001-01-01T00:00:00Z` to `9999-12-31T23:59:59.999999999Z`,
              i.e. from 0 to 9 digits for fractions of a second.\n\nTo work with values
              in this field, use the APIs described in the \n[Protocol Buffers reference](https://developers.google.com/protocol-buffers/docs/reference/overview).
              \nIn some languages, built-in datetime utilities do not support nanosecond
              precision (9 digits)."
            type: string
            format: date-time
          done:
            description: |-
              If the value is `false`, it means the operation is still in progress.
              If `true`, the operation is completed, and either `error` or `response` is available.
            type: boolean
            format: boolean
          metadata:
            description: |-
              Service-specific metadata associated with the operation.
              It typically contains the ID of the target resource that the operation is performed on.
              Any method that returns a long-running operation should document the metadata type, if any.
            type: object
      - title: result
        type: object
        maxProperties: 1
        properties:
          error:
            $ref: '#/components/schemas/rpcStatus'
            description: The error result of the operation in case of failure or cancellation.
          response:
            description: |-
              The normal response of the operation in case of success.
              If the original method returns no data on success, such as Delete,
              the response is [google.protobuf.Empty](https://developers.google.com/protocol-buffers/docs/reference/google.protobuf#empty).
              If the original method is the standard Create/Update,
              the response should be the target resource of the operation.
              Any method that returns a long-running operation should document the response type, if any.
            type: object
    PortRange:
      type: object
      properties:
        fromPort:
          description: Acceptable values are 0 to 65535, inclusive.
          type: string
          format: int64
        toPort:
          description: Acceptable values are 0 to 65535, inclusive.
          type: string
          format: int64
    Reference:
      type: object
      properties:
        referrer:
          $ref: '#/components/schemas/Referrer_in_Reference_referrer'
        type:
          $ref: '#/components/schemas/ReferenceType_in_Reference_type'
    ReferenceType:
      type: string
      enum:
      - MANAGED_BY
      - USED_BY
    Referrer:
      type: object
      properties:
        type:
          description: |-
            * `type = compute.instance, id = <instance id>`
            * `type = compute.instanceGroup, id = <instanceGroup id>`
            * `type = loadbalancer.networkLoadBalancer, id = <networkLoadBalancer id>`
            * `type = managed-kubernetes.cluster, id = <cluster id>`
            * `type = managed-mysql.cluster, id = <cluster id>`
          type: string
        id:
          type: string
    RemoveSubnetCidrBlocksRequest:
      type: object
      properties:
        v4CidrBlocks:
          description: |-
            CIDR block.
            The range of internal addresses that are removed from this subnet.
          type: array
          items:
            type: string
    RouteTable:
      description: A RouteTable resource. For more information, see [Static Routes](/docs/vpc/concepts/static-routes).
      type: object
      properties:
        id:
          description: ID of the route table.
          type: string
        folderId:
          description: ID of the folder that the route table belongs to.
          type: string
        createdAt:
          description: "Creation timestamp in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt)
            text format.\n\nString in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt)
            text format. The range of possible values is from \n`0001-01-01T00:00:00Z`
            to `9999-12-31T23:59:59.999999999Z`, i.e. from 0 to 9 digits for fractions
            of a second.\n\nTo work with values in this field, use the APIs described
            in the \n[Protocol Buffers reference](https://developers.google.com/protocol-buffers/docs/reference/overview).
            \nIn some languages, built-in datetime utilities do not support nanosecond
            precision (9 digits)."
          type: string
          format: date-time
        name:
          description: Name of the route table. The name is unique within the project.
            3-63 characters long.
          type: string
        description:
          description: Optional description of the route table. 0-256 characters long.
          type: string
        labels:
          description: Resource labels as `` key:value `` pairs. Maximum of 64 per
            resource.
          type: object
          additionalProperties:
            type: string
        networkId:
          description: ID of the network the route table belongs to.
          type: string
        staticRoutes:
          description: List of static routes.
          type: array
          items:
            $ref: '#/components/schemas/StaticRoute'
    SecurityGroup:
      type: object
      properties:
        id:
          type: string
        folderId:
          type: string
        createdAt:
          description: "String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt)
            text format. The range of possible values is from \n`0001-01-01T00:00:00Z`
            to `9999-12-31T23:59:59.999999999Z`, i.e. from 0 to 9 digits for fractions
            of a second.\n\nTo work with values in this field, use the APIs described
            in the \n[Protocol Buffers reference](https://developers.google.com/protocol-buffers/docs/reference/overview).
            \nIn some languages, built-in datetime utilities do not support nanosecond
            precision (9 digits)."
          type: string
          format: date-time
        name:
          type: string
        description:
          type: string
        labels:
          type: object
          additionalProperties:
            type: string
        networkId:
          type: string
        status:
          $ref: '#/components/schemas/SecurityGroupStatus_in_SecurityGroup_status'
        rules:
          type: array
          items:
            $ref: '#/components/schemas/SecurityGroupRule'
        defaultForNetwork:
          type: boolean
          format: boolean
    SecurityGroupRule:
      type: object
      allOf:
      - type: object
        properties:
          id:
            type: string
          description:
            type: string
          labels:
            type: object
            additionalProperties:
              type: string
          direction:
            $ref: '#/components/schemas/SecurityGroupRuleDirection'
            description: Required.
          ports:
            $ref: '#/components/schemas/PortRange'
          protocolName:
            description: |-
              null value means any protocol
              values from https://www.iana.org/assignments/protocol-numbers/protocol-numbers.xhtml
            type: string
          protocolNumber:
            type: string
            format: int64
      - title: target
        type: object
        maxProperties: 1
        properties:
          cidrBlocks:
            $ref: '#/components/schemas/CidrBlocks'
          securityGroupId:
            type: string
          predefinedTarget:
            type: string
    SecurityGroupRuleDirection:
      type: string
      enum:
      - INGRESS
      - EGRESS
    SecurityGroupRuleSpec:
      type: object
      allOf:
      - type: object
        properties:
          description:
            type: string
          labels:
            type: object
            additionalProperties:
              type: string
          direction:
            $ref: '#/components/schemas/SecurityGroupRuleDirection'
            description: Required.
          ports:
            $ref: '#/components/schemas/PortRange'
      - title: protocol
        type: object
        maxProperties: 1
        properties:
          protocolName:
            type: string
          protocolNumber:
            type: string
            format: int64
      - title: target
        type: object
        maxProperties: 1
        properties:
          cidrBlocks:
            $ref: '#/components/schemas/CidrBlocks'
          securityGroupId:
            type: string
          predefinedTarget:
            type: string
    SecurityGroupStatus:
      description: ' - UPDATING: updating is a long operation because we must update
        all instances in SG'
      type: string
      enum:
      - CREATING
      - ACTIVE
      - UPDATING
      - DELETING
    SharedEgressGateway:
      description: Shared Egress Gateway configuration
      type: object
      properties: {}
    SharedEgressGatewaySpec:
      type: object
      properties: {}
    StaticRoute:
      description: A StaticRoute resource. For more information, see [Static Routes](/docs/vpc/concepts/static-routes).
      type: object
      allOf:
      - type: object
        properties:
          labels:
            description: Resource labels as `` key:value `` pairs. Maximum of 64 per
              resource.
            type: object
            additionalProperties:
              type: string
      - title: destination
        type: object
        maxProperties: 1
        properties:
          destinationPrefix:
            description: Destination subnet in CIDR notation
            type: string
      - title: next_hop
        type: object
        maxProperties: 1
        properties:
          nextHopAddress:
            description: Next hop IP address
            type: string
          gatewayId:
            description: Next hop gateway id
            type: string
    Subnet:
      description: A Subnet resource. For more information, see [Subnets](/docs/vpc/concepts/subnets).
      type: object
      properties:
        id:
          description: ID of the subnet.
          type: string
        folderId:
          description: ID of the folder that the subnet belongs to.
          type: string
        createdAt:
          description: "Creation timestamp in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt)
            text format.\n\nString in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt)
            text format. The range of possible values is from \n`0001-01-01T00:00:00Z`
            to `9999-12-31T23:59:59.999999999Z`, i.e. from 0 to 9 digits for fractions
            of a second.\n\nTo work with values in this field, use the APIs described
            in the \n[Protocol Buffers reference](https://developers.google.com/protocol-buffers/docs/reference/overview).
            \nIn some languages, built-in datetime utilities do not support nanosecond
            precision (9 digits)."
          type: string
          format: date-time
        name:
          description: Name of the subnet. The name is unique within the project.
            3-63 characters long.
          type: string
        description:
          description: Optional description of the subnet. 0-256 characters long.
          type: string
        labels:
          description: Resource labels as `` key:value `` pairs. Maximum of 64 per
            resource.
          type: object
          additionalProperties:
            type: string
        networkId:
          description: ID of the network the subnet belongs to.
          type: string
        zoneId:
          description: ID of the availability zone where the subnet resides.
          type: string
        v4CidrBlocks:
          description: |-
            CIDR block.
            The range of internal addresses that are defined for this subnet.
            This field can be set only at Subnet resource creation time and cannot be changed.
            For example, 10.0.0.0/22 or 192.168.0.0/24.
            Minimum subnet size is /28, maximum subnet size is /16.
          type: array
          items:
            type: string
        v6CidrBlocks:
          description: IPv6 not available yet.
          type: array
          items:
            type: string
        routeTableId:
          description: ID of route table the subnet is linked to.
          type: string
        dhcpOptions:
          $ref: '#/components/schemas/DhcpOptions_in_Subnet_dhcpOptions'
    UpdateAddressMetadata:
      type: object
      properties:
        addressId:
          description: ID of the Address that is being updated.
          type: string
    UpdateAddressRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateAddressRequest_updateMask'
        name:
          description: |-
            New name for the address.
            The name must be unique within the folder.

            Value must match the regular expression `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            New description of the address.

            The maximum string length in characters is 256.
          type: string
        labels:
          description: |-
            Address labels as `key:value` pairs.

            Existing set of labels is completely replaced by the provided set, so if you just want
            to add or remove a label:
            1. Get the current set of labels with a [get](/docs/vpc/api-ref/Address/get) request.
            2. Add or remove a label in this set.
            3. Send the new set in this field.

            No more than 64 per resource.
            The string length in characters for each key must be 1-63.
            Each key must match the regular expression `` [a-z][-_0-9a-z]* ``.
            The maximum string length in characters for each value is 63.
            Each value must match the regular expression `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        reserved:
          description: Specifies if address is reserved or not.
          type: boolean
          format: boolean
        deletionProtection:
          description: Specifies if address protected from deletion.
          type: boolean
          format: boolean
    UpdateGatewayMetadata:
      type: object
      properties:
        gatewayId:
          description: ID of the Gateway that is being updated.
          type: string
    UpdateGatewayRequest:
      type: object
      allOf:
      - type: object
        properties:
          updateMask:
            $ref: '#/components/schemas/FieldMask'
            description: Field mask that specifies which attributes of the Gateway
              should be updated.
          name:
            description: |-
              New name for the gateway.
              The name must be unique within the folder.

              Value must match the regular expression `` |[a-z]([-a-z0-9]{0,61}[a-z0-9])? ``.
            type: string
          description:
            description: |-
              New description of the gateway.

              The maximum string length in characters is 256.
            type: string
          labels:
            description: |-
              Gateway labels as `key:value` pairs.

              Existing set of labels is completely replaced by the provided set, so if you just want
              to add or remove a label:
              1. Get the current set of labels with a [get](/docs/vpc/api-ref/Gateway/get) request.
              2. Add or remove a label in this set.
              3. Send the new set in this field.

              No more than 64 per resource.
              The string length in characters for each key must be 1-63.
              Each key must match the regular expression `` [a-z][-_./\@0-9a-z]* ``.
              The maximum string length in characters for each value is 63.
              Each value must match the regular expression `` [-_./\@0-9a-z]* ``.
            type: object
            additionalProperties:
              type: string
      - title: gateway
        type: object
        maxProperties: 1
        properties:
          sharedEgressGatewaySpec:
            $ref: '#/components/schemas/SharedEgressGatewaySpec'
    UpdateNetworkMetadata:
      type: object
      properties:
        networkId:
          description: ID of the Network resource that is being updated.
          type: string
    UpdateNetworkRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateNetworkRequest_updateMask'
        name:
          description: |-
            Name of the network.
            The name must be unique within the folder.

            Value must match the regular expression `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            Description of the network.

            The maximum string length in characters is 256.
          type: string
        labels:
          description: |-
            Resource labels as `` key:value `` pairs.

            No more than 64 per resource.
            The string length in characters for each key must be 1-63.
            Each key must match the regular expression `` [a-z][-_0-9a-z]* ``.
            The maximum string length in characters for each value is 63.
            Each value must match the regular expression `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
    UpdateRouteTableMetadata:
      type: object
      properties:
        routeTableId:
          description: ID of the RouteTable resource that is being updated.
          type: string
    UpdateRouteTableRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateRouteTableRequest_updateMask'
        name:
          description: |-
            Name of the route table.
            The name must be unique within the folder.

            Value must match the regular expression `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            Description of the route table.

            The maximum string length in characters is 256.
          type: string
        labels:
          description: |-
            Resource labels as `` key:value `` pairs.

            No more than 64 per resource.
            The string length in characters for each key must be 1-63.
            Each key must match the regular expression `` [a-z][-_0-9a-z]* ``.
            The maximum string length in characters for each value is 63.
            Each value must match the regular expression `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        staticRoutes:
          description: List of static routes.
          type: array
          items:
            $ref: '#/components/schemas/StaticRoute'
    UpdateSecurityGroupMetadata:
      type: object
      properties:
        securityGroupId:
          type: string
        addedRuleIds:
          type: array
          items:
            type: string
    UpdateSecurityGroupRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateSecurityGroupRequest_updateMask'
        name:
          type: string
        description:
          type: string
        labels:
          type: object
          additionalProperties:
            type: string
        ruleSpecs:
          description: all existing rules will be replaced with given list
          type: array
          items:
            $ref: '#/components/schemas/SecurityGroupRuleSpec'
    UpdateSecurityGroupRuleMetadata:
      type: object
      properties:
        securityGroupId:
          type: string
        ruleId:
          type: string
    UpdateSecurityGroupRuleRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateSecurityGroupRuleRequest_updateMask'
        description:
          type: string
        labels:
          type: object
          additionalProperties:
            type: string
    UpdateSecurityGroupRulesRequest:
      type: object
      properties:
        deletionRuleIds:
          type: array
          items:
            type: string
        additionRuleSpecs:
          type: array
          items:
            $ref: '#/components/schemas/SecurityGroupRuleSpec'
    UpdateSubnetMetadata:
      type: object
      properties:
        subnetId:
          description: ID of the Subnet resource that is being updated.
          type: string
    UpdateSubnetRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateSubnetRequest_updateMask'
        name:
          description: |-
            Name of the subnet.
            The name must be unique within the folder.

            Value must match the regular expression `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            Description of the subnet.

            The maximum string length in characters is 256.
          type: string
        labels:
          description: |-
            Resource labels as `` key:value `` pairs.

            No more than 64 per resource.
            The string length in characters for each key must be 1-63.
            Each key must match the regular expression `` [a-z][-_0-9a-z]* ``.
            The maximum string length in characters for each value is 63.
            Each value must match the regular expression `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        routeTableId:
          description: |-
            ID of route table the subnet is linked to.

            The maximum string length in characters is 50.
          type: string
        dhcpOptions:
          $ref: '#/components/schemas/DhcpOptions_in_UpdateSubnetRequest_dhcpOptions'
    UsedAddress:
      type: object
      properties:
        address:
          type: string
        ipVersion:
          $ref: '#/components/schemas/v1IpVersion_in_UsedAddress_ipVersion'
        references:
          type: array
          items:
            $ref: '#/components/schemas/Reference'
    rpcStatus:
      description: The error result of the operation in case of failure or cancellation.
      type: object
      properties:
        code:
          description: Error code. An enum value of [google.rpc.Code](https://github.com/googleapis/googleapis/blob/master/google/rpc/code.proto).
          type: integer
          format: int32
        message:
          description: An error message.
          type: string
        details:
          description: A list of messages that carry the error details.
          type: array
          items:
            type: object
    v1IpVersion:
      type: string
      enum:
      - IPV4
      - IPV6
    DhcpOptions_in_CreateSubnetRequest_dhcpOptions:
      type: object
      properties:
        domainNameServers:
          type: array
          items:
            type: string
        domainName:
          type: string
        ntpServers:
          type: array
          items:
            type: string
    AddressRequirements_in_ExternalIpv4Address_requirements:
      description: Parameters of the allocated address, for example DDoS Protection.
      type: object
      properties:
        ddosProtectionProvider:
          description: DDoS protection provider ID.
          type: string
        outgoingSmtpCapability:
          description: Capability to send SMTP traffic.
          type: string
    AddressRequirements_in_ExternalIpv4AddressSpec_requirements:
      description: Parameters of the allocated address, for example DDoS Protection.
      type: object
      properties:
        ddosProtectionProvider:
          description: DDoS protection provider ID.
          type: string
        outgoingSmtpCapability:
          description: Capability to send SMTP traffic.
          type: string
    Referrer_in_Reference_referrer:
      type: object
      properties:
        type:
          description: |-
            * `type = compute.instance, id = <instance id>`
            * `type = compute.instanceGroup, id = <instanceGroup id>`
            * `type = loadbalancer.networkLoadBalancer, id = <networkLoadBalancer id>`
            * `type = managed-kubernetes.cluster, id = <cluster id>`
            * `type = managed-mysql.cluster, id = <cluster id>`
          type: string
        id:
          type: string
    ReferenceType_in_Reference_type:
      type: string
      enum:
      - MANAGED_BY
      - USED_BY
    SecurityGroupStatus_in_SecurityGroup_status:
      description: ' - UPDATING: updating is a long operation because we must update
        all instances in SG'
      type: string
      enum:
      - CREATING
      - ACTIVE
      - UPDATING
      - DELETING
    DhcpOptions_in_Subnet_dhcpOptions:
      type: object
      properties:
        domainNameServers:
          type: array
          items:
            type: string
        domainName:
          type: string
        ntpServers:
          type: array
          items:
            type: string
    FieldMask_in_UpdateAddressRequest_updateMask:
      description: |-
        Field mask that specifies which attributes of the Address should be updated.

        A comma-separated names off ALL fields to be updated.
        Only the specified fields will be changed. The others will be left untouched.
        If the field is specified in `` updateMask `` and no value for that field was sent in the request,
        the field's value will be reset to the default. The default value for most fields is null or 0.

        If `` updateMask `` is not sent in the request, all fields' values will be updated.
        Fields specified in the request will be updated to provided values.
        The rest of the fields will be reset to the default.
      type: string
    FieldMask_in_UpdateNetworkRequest_updateMask:
      description: |-
        Field mask that specifies which fields of the Network resource are going to be updated.

        A comma-separated names off ALL fields to be updated.
        Only the specified fields will be changed. The others will be left untouched.
        If the field is specified in `` updateMask `` and no value for that field was sent in the request,
        the field's value will be reset to the default. The default value for most fields is null or 0.

        If `` updateMask `` is not sent in the request, all fields' values will be updated.
        Fields specified in the request will be updated to provided values.
        The rest of the fields will be reset to the default.
      type: string
    FieldMask_in_UpdateRouteTableRequest_updateMask:
      description: |-
        Field mask that specifies which fields of the RouteTable resource are going to be updated.

        A comma-separated names off ALL fields to be updated.
        Only the specified fields will be changed. The others will be left untouched.
        If the field is specified in `` updateMask `` and no value for that field was sent in the request,
        the field's value will be reset to the default. The default value for most fields is null or 0.

        If `` updateMask `` is not sent in the request, all fields' values will be updated.
        Fields specified in the request will be updated to provided values.
        The rest of the fields will be reset to the default.
      type: string
    FieldMask_in_UpdateSecurityGroupRequest_updateMask:
      description: |-
        A comma-separated names off ALL fields to be updated.
        Only the specified fields will be changed. The others will be left untouched.
        If the field is specified in `` updateMask `` and no value for that field was sent in the request,
        the field's value will be reset to the default. The default value for most fields is null or 0.

        If `` updateMask `` is not sent in the request, all fields' values will be updated.
        Fields specified in the request will be updated to provided values.
        The rest of the fields will be reset to the default.
      type: string
    FieldMask_in_UpdateSecurityGroupRuleRequest_updateMask:
      description: |-
        A comma-separated names off ALL fields to be updated.
        Only the specified fields will be changed. The others will be left untouched.
        If the field is specified in `` updateMask `` and no value for that field was sent in the request,
        the field's value will be reset to the default. The default value for most fields is null or 0.

        If `` updateMask `` is not sent in the request, all fields' values will be updated.
        Fields specified in the request will be updated to provided values.
        The rest of the fields will be reset to the default.
      type: string
    FieldMask_in_UpdateSubnetRequest_updateMask:
      description: |-
        Field mask that specifies which fields of the Subnet resource are going to be updated.

        A comma-separated names off ALL fields to be updated.
        Only the specified fields will be changed. The others will be left untouched.
        If the field is specified in `` updateMask `` and no value for that field was sent in the request,
        the field's value will be reset to the default. The default value for most fields is null or 0.

        If `` updateMask `` is not sent in the request, all fields' values will be updated.
        Fields specified in the request will be updated to provided values.
        The rest of the fields will be reset to the default.
      type: string
    DhcpOptions_in_UpdateSubnetRequest_dhcpOptions:
      type: object
      properties:
        domainNameServers:
          type: array
          items:
            type: string
        domainName:
          type: string
        ntpServers:
          type: array
          items:
            type: string
    v1IpVersion_in_UsedAddress_ipVersion:
      type: string
      enum:
      - IPV4
      - IPV6
tags:
- name: Address
  description: A set of methods for managing Address resources.
- name: Gateway
- name: Network
  description: A set of methods for managing Network resources.
- name: RouteTable
  description: A set of methods for managing RouteTable resources.
- name: SecurityGroup
- name: Subnet
  description: A set of methods for managing Subnet resources.

openapi: 3.0.0
info:
  title: Managed Service for MySQL API
  description: This API reference is organized by resource. Actions are performed
    by sending HTTP requests to resource URLs or making RPC calls. For more information
    about API architecture, see [API Concepts](/docs/api-design-guide/).
  version: v1
  x-y-docUri: api-ref
  x-y-baseUrl: https://mdb.api.cloud.yandex.net
paths:
  /managed-mysql/v1/backups/{backupId}:
    get:
      tags:
      - Backup
      summary: Retrieves information about the specified backup.
      operationId: BackupGet
      parameters:
      - name: backupId
        in: path
        description: |-
          Required.
          ID of the backup to return information about.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Backup/list) request (lists all backups in a folder) or a [listBackups](/docs/managed-mysql/api-ref/Cluster/listBackups) request (lists all backups for an existing cluster).
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Backup'
      x-y-navtitle: get
  /managed-mysql/v1/backups:
    get:
      tags:
      - Backup
      summary: Retrieves the list of backups in a folder.
      description: To list backups for an existing cluster, make a [listBackups](/docs/managed-mysql/api-ref/Cluster/listBackups)
        request.
      operationId: BackupList
      parameters:
      - name: folderId
        in: query
        description: |-
          Required.
          ID of the folder to list backups in.

          To get this ID, make a [list](/docs/resource-manager/api-ref/Folder/list) request.

          The maximum string length in characters is 50.
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return.

          If the number of available results is larger than [pageSize](/docs/managed-mysql/api-ref/Backup/list#query_params), the API returns a [nextPageToken](/docs/managed-mysql/api-ref/Backup/list#responses) that can be used to get the next page of results in the subsequent [list](/docs/managed-mysql/api-ref/Backup/list) requests.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token that can be used to iterate through multiple pages of results.

          To get the next page of results, set [pageToken](/docs/managed-mysql/api-ref/Backup/list#query_params) to the [nextPageToken](/docs/managed-mysql/api-ref/Backup/list#responses) returned by the previous [list](/docs/managed-mysql/api-ref/Backup/list) request.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListBackupsResponse'
      x-y-navtitle: list
  /managed-mysql/v1/clusters/{clusterId}:
    get:
      tags:
      - Cluster
      summary: Retrieves information about a cluster.
      operationId: ClusterGet
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to return information about.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cluster'
      x-y-navtitle: get
    delete:
      tags:
      - Cluster
      summary: Deletes a cluster.
      operationId: ClusterDelete
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to delete.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteClusterMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - Cluster
      summary: Updates a cluster.
      operationId: ClusterUpdate
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to update.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateClusterRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters:
    get:
      tags:
      - Cluster
      summary: Retrieves the list of clusters in a folder.
      operationId: ClusterList
      parameters:
      - name: folderId
        in: query
        description: |-
          Required.
          ID of the folder to list clusters in.

          To get this ID, make a [list](/docs/resource-manager/api-ref/Folder/list) request.

          The maximum string length in characters is 50.
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return.

          If the number of available results is larger than [pageSize](/docs/managed-mysql/api-ref/Cluster/list#query_params), the API returns a [nextPageToken](/docs/managed-mysql/api-ref/Cluster/list#responses) that can be used to get the next page of results in the subsequent [list](/docs/managed-mysql/api-ref/Cluster/list) requests.

          Acceptable values are 0 to 1000, inclusive.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token that can be used to iterate through multiple pages of results.

          To get the next page of results, set [pageToken](/docs/managed-mysql/api-ref/Cluster/list#query_params) to the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/list#responses) returned by the previous [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 100.
        schema:
          type: string
      - name: filter
        in: query
        description: |-
          A filter expression that selects clusters listed in the response.

          The expression must specify:
          1. The field name. Currently you can only use filtering with the [Cluster.name](/docs/managed-mysql/api-ref/Cluster#representation) field.
          2. An `=` operator.
          3. The value in double quotes (`"`). Must be 1-63 characters long and match the regular expression `[a-zA-Z0-9_-]+`.

          The maximum string length in characters is 1000.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListClustersResponse'
      x-y-navtitle: list
    post:
      tags:
      - Cluster
      summary: Creates a cluster in a folder.
      operationId: ClusterCreate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateClusterRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:start:
    post:
      tags:
      - Cluster
      summary: Starts a cluster.
      operationId: ClusterStart
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to start.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: start
      x-y-operation-metadata:
        $ref: '#/components/schemas/StartClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:stop:
    post:
      tags:
      - Cluster
      summary: Stops a cluster.
      operationId: ClusterStop
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to stop.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: stop
      x-y-operation-metadata:
        $ref: '#/components/schemas/StopClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:move:
    post:
      tags:
      - Cluster
      summary: Moves a cluster to a folder.
      operationId: ClusterMove
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to move.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveClusterRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: move
      x-y-operation-metadata:
        $ref: '#/components/schemas/MoveClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:backup:
    post:
      tags:
      - Cluster
      summary: Creates a backup for a cluster.
      description: To get information about a backup, make a [get](/docs/managed-mysql/api-ref/Backup/get)
        request.
      operationId: ClusterBackup
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to back up.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: backup
      x-y-operation-metadata:
        $ref: '#/components/schemas/BackupClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters:restore:
    post:
      tags:
      - Cluster
      summary: Restores a backup to a new cluster.
      description: See [the documentation](/docs/managed-mysql/concepts/backup) for
        details.
      operationId: ClusterRestore
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RestoreClusterRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: restore
      x-y-operation-metadata:
        $ref: '#/components/schemas/RestoreClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:rescheduleMaintenance:
    post:
      tags:
      - Cluster
      summary: Reschedules planned maintenance operation.
      operationId: ClusterRescheduleMaintenance
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to reschedule the maintenance operation for.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RescheduleMaintenanceRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: rescheduleMaintenance
      x-y-operation-metadata:
        $ref: '#/components/schemas/RescheduleMaintenanceMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:startFailover:
    post:
      tags:
      - Cluster
      summary: Starts a manual failover for a cluster.
      operationId: ClusterStartFailover
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to start failover for.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartClusterFailoverRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: startFailover
      x-y-operation-metadata:
        $ref: '#/components/schemas/StartClusterFailoverMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:logs:
    get:
      tags:
      - Cluster
      summary: Retrieves logs for a cluster.
      description: Alternatively, logs can be streamed using [streamLogs](/docs/managed-mysql/api-ref/Cluster/streamLogs).
      operationId: ClusterListLogs
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to request logs for.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: columnFilter
        in: query
        description: |-
          Columns from the logs table to request.
          If no columns are specified, complete log records are returned.
        schema:
          type: array
          items:
            type: string
      - name: serviceType
        in: query
        description: The log type.
        schema:
          $ref: '#/components/schemas/ListClusterLogsRequestServiceType'
      - name: fromTime
        in: query
        description: |-
          Start timestamp for the logs request.
          The logs in the response will be within [fromTime](/docs/managed-mysql/api-ref/Cluster/listLogs#query_params) to [toTime](/docs/managed-mysql/api-ref/Cluster/listLogs#query_params) range.

          String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
        schema:
          type: string
          format: date-time
      - name: toTime
        in: query
        description: |-
          End timestamp for the logs request.
          The logs in the response will be within [fromTime](/docs/managed-mysql/api-ref/Cluster/listLogs#query_params) to [toTime](/docs/managed-mysql/api-ref/Cluster/listLogs#query_params) range.

          String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
        schema:
          type: string
          format: date-time
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return.

          If the number of available results is larger than [pageSize](/docs/managed-mysql/api-ref/Cluster/listLogs#query_params), the API returns a [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#responses) that can be used to get the next page of results in the subsequent [listLogs](/docs/managed-mysql/api-ref/Cluster/listLogs) requests.

          Acceptable values are 0 to 1000, inclusive.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token that can be used to iterate through multiple pages of results.

          To get the next page of results, set [pageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#query_params) to the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#responses) returned by the previous [listLogs](/docs/managed-mysql/api-ref/Cluster/listLogs) request.

          The maximum string length in characters is 100.
        schema:
          type: string
      - name: alwaysNextPageToken
        in: query
        description: |-
          Option that controls the behavior of result pagination.
          If it is set to `true`, then [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#responses) will always be returned, even if the current page is empty.
        schema:
          type: boolean
          format: boolean
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListClusterLogsResponse'
      x-y-navtitle: listLogs
  /managed-mysql/v1/clusters/{clusterId}:stream_logs:
    get:
      tags:
      - Cluster
      summary: Retrieves a log stream for a cluster.
      description: This method is similar to [listLogs](/docs/managed-mysql/api-ref/Cluster/listLogs),
        but uses server-side streaming, which allows for the `tail -f` command semantics.
      operationId: ClusterStreamLogs
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to stream logs for.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: columnFilter
        in: query
        description: |-
          Columns from the logs table to request.
          If no columns are specified, complete log records are returned.
        schema:
          type: array
          items:
            type: string
      - name: serviceType
        in: query
        description: The log type.
        schema:
          $ref: '#/components/schemas/StreamClusterLogsRequestServiceType'
      - name: fromTime
        in: query
        description: |-
          Start timestamp for the logs request.

          String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
        schema:
          type: string
          format: date-time
      - name: toTime
        in: query
        description: |-
          End timestamp for the logs request.
          If this field is not set, all existing log records beginning from [fromTime](/docs/managed-mysql/api-ref/Cluster/streamLogs#query_params) will be returned first, and then the new records will be returned as they appear.

          In essence it has `tail -f` command semantics.

          String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
        schema:
          type: string
          format: date-time
      - name: recordToken
        in: query
        description: |-
          Record token that can be used to control logs streaming.

          Set [recordToken](/docs/managed-mysql/api-ref/Cluster/streamLogs#query_params) to the `nextRecordToken`, returned by the previous [streamLogs](/docs/managed-mysql/api-ref/Cluster/streamLogs) request to start streaming from the next log record.

          The maximum string length in characters is 100.
        schema:
          type: string
      - name: filter
        in: query
        description: |-
          A filter expression that selects clusters logs listed in the response.

          The expression must specify:
          1. The field name. Currently filtering can be applied to the [LogRecord.logs.hostname] field.
          2. An `=` operator.
          3. The value in double quotes (`"`). Must be 3-63 characters long and match the regular expression `[a-z][-a-z0-9]{1,61}[a-z0-9]`.
          Examples of a filter: `message.hostname='node1.db.cloud.yandex.net'`

          The maximum string length in characters is 1000.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StreamLogRecord'
      x-y-navtitle: streamLogs
  /managed-mysql/v1/clusters/{clusterId}/operations:
    get:
      tags:
      - Cluster
      summary: Retrieves a list of operations for a cluster.
      operationId: ClusterListOperations
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to list operations for.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return.

          If the number of available results is larger than [pageSize](/docs/managed-mysql/api-ref/Cluster/listOperations#query_params), the API returns a [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listOperations#responses) that can be used to get the next page of results in the subsequent [listOperations](/docs/managed-mysql/api-ref/Cluster/listOperations) requests.

          Acceptable values are 0 to 1000, inclusive.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token that can be used to iterate through multiple pages of results.

          To get the next page of results, set [pageToken](/docs/managed-mysql/api-ref/Cluster/listOperations#query_params) to the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listOperations#responses) returned by the previous [listOperations](/docs/managed-mysql/api-ref/Cluster/listOperations) request.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListClusterOperationsResponse'
      x-y-navtitle: listOperations
  /managed-mysql/v1/clusters/{clusterId}/backups:
    get:
      tags:
      - Cluster
      summary: Retrieves a list of backups for a cluster.
      description: To list all backups in a folder, make a [list](/docs/managed-mysql/api-ref/Backup/list)
        request.
      operationId: ClusterListBackups
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to list backups for.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return.

          If the number of available results is larger than [pageSize](/docs/managed-mysql/api-ref/Cluster/listBackups#query_params), the API returns a [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listBackups#responses) that can be used to get the next page of results in the subsequent [listBackups](/docs/managed-mysql/api-ref/Cluster/listBackups) requests.

          The maximum value is 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token that can be used to iterate through multiple pages of results.

          To get the next page of results, set [pageToken](/docs/managed-mysql/api-ref/Cluster/listBackups#query_params) to the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listBackups#responses) returned by the previous [listBackups](/docs/managed-mysql/api-ref/Cluster/listBackups) request.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListClusterBackupsResponse'
      x-y-navtitle: listBackups
  /managed-mysql/v1/clusters/{clusterId}/hosts:
    get:
      tags:
      - Cluster
      summary: Retrieves a list of hosts for a cluster.
      operationId: ClusterListHosts
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to list hosts for.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return.

          If the number of available results is larger than [pageSize](/docs/managed-mysql/api-ref/Cluster/listHosts#query_params), the API returns a [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listHosts#responses) that can be used to get the next page of results in the subsequent [listHosts](/docs/managed-mysql/api-ref/Cluster/listHosts) requests.

          Acceptable values are 0 to 1000, inclusive.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token that can be used to iterate through multiple pages of results.

          To get the next page of results, set [pageToken](/docs/managed-mysql/api-ref/Cluster/listHosts#query_params) to the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listHosts#responses) returned by the previous [listHosts](/docs/managed-mysql/api-ref/Cluster/listHosts) request.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListClusterHostsResponse'
      x-y-navtitle: listHosts
  /managed-mysql/v1/clusters/{clusterId}/hosts:batchCreate:
    post:
      tags:
      - Cluster
      summary: Adds new hosts in a cluster.
      operationId: ClusterAddHosts
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to add hosts to.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddClusterHostsRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: addHosts
      x-y-operation-metadata:
        $ref: '#/components/schemas/AddClusterHostsMetadata'
      x-y-operation-response: Empty
  /managed-mysql/v1/clusters/{clusterId}/hosts:batchUpdate:
    post:
      tags:
      - Cluster
      summary: Updates the specified hosts.
      operationId: ClusterUpdateHosts
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the MySQL cluster to update hosts in.
          To get the MySQL cluster ID, use a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateClusterHostsRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: updateHosts
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateClusterHostsMetadata'
      x-y-operation-response: Empty
  /managed-mysql/v1/clusters/{clusterId}/hosts:batchDelete:
    post:
      tags:
      - Cluster
      summary: Deletes the specified hosts for a cluster.
      operationId: ClusterDeleteHosts
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to delete hosts from.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteClusterHostsRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: deleteHosts
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteClusterHostsMetadata'
      x-y-operation-response: Empty
  /managed-mysql/v1/clusters/{clusterId}/databases/{databaseName}:
    get:
      tags:
      - Database
      summary: Retrieves information about the specified database.
      operationId: DatabaseGet
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster that the database belongs to.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: databaseName
        in: path
        description: |-
          Required.
          Name of the database to return information about.

          To get this name, make a [list](/docs/managed-mysql/api-ref/Database/list) request.

          The maximum string length in characters is 63.
          Value must match the regular expression `` [a-zA-Z0-9_-]* ``.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Database'
      x-y-navtitle: get
    delete:
      tags:
      - Database
      summary: Deletes a database from a cluster.
      operationId: DatabaseDelete
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to delete the database from.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: databaseName
        in: path
        description: |-
          Required.
          Name of the database to delete.

          To get this name, make a [list](/docs/managed-mysql/api-ref/Database/list) request.

          The maximum string length in characters is 63.
          Value must match the regular expression `` [a-zA-Z0-9_-]* ``.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteDatabaseMetadata'
      x-y-operation-response: Empty
  /managed-mysql/v1/clusters/{clusterId}/databases:
    get:
      tags:
      - Database
      summary: Retrieves the list of databases in a cluster.
      operationId: DatabaseList
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to list databases in.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return.

          If the number of available results is larger than [pageSize](/docs/managed-mysql/api-ref/Database/list#query_params), the API returns a [nextPageToken](/docs/managed-mysql/api-ref/Database/list#responses) that can be used to get the next page of results in the subsequent [list](/docs/managed-mysql/api-ref/Database/list) requests.

          Acceptable values are 0 to 1000, inclusive.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token that can be used to iterate through multiple pages of results.

          To get the next page of results, set [pageToken](/docs/managed-mysql/api-ref/Database/list#query_params) to the [nextPageToken](/docs/managed-mysql/api-ref/Database/list#responses) returned by the previous [list](/docs/managed-mysql/api-ref/Database/list) request.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListDatabasesResponse'
      x-y-navtitle: list
    post:
      tags:
      - Database
      summary: Creates a new database in a cluster.
      operationId: DatabaseCreate
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to create the database in.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDatabaseRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateDatabaseMetadata'
      x-y-operation-response: '[Database](/docs/managed-mysql/api-ref/Database#representation)'
  /managed-mysql/v1/resourcePresets/{resourcePresetId}:
    get:
      tags:
      - ResourcePreset
      summary: Retrieves information about a resource preset.
      operationId: ResourcePresetGet
      parameters:
      - name: resourcePresetId
        in: path
        description: |-
          Required.
          ID of the resource preset to return information about.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/ResourcePreset/list) request.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourcePreset'
      x-y-navtitle: get
  /managed-mysql/v1/resourcePresets:
    get:
      tags:
      - ResourcePreset
      summary: Retrieves the list of available resource presets.
      operationId: ResourcePresetList
      parameters:
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return.

          If the number of available results is larger than [pageSize](/docs/managed-mysql/api-ref/ResourcePreset/list#query_params), the API returns a [nextPageToken](/docs/managed-mysql/api-ref/ResourcePreset/list#responses) that can be used to get the next page of results in the subsequent [list](/docs/managed-mysql/api-ref/ResourcePreset/list) requests.

          Acceptable values are 0 to 1000, inclusive.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token that can be used to iterate through multiple pages of results.

          To get the next page of results, set [pageToken](/docs/managed-mysql/api-ref/ResourcePreset/list#query_params) to the [nextPageToken](/docs/managed-mysql/api-ref/ResourcePreset/list#responses) returned by the previous [list](/docs/managed-mysql/api-ref/ResourcePreset/list) request.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListResourcePresetsResponse'
      x-y-navtitle: list
  /managed-mysql/v1/clusters/{clusterId}/users/{userName}:
    get:
      tags:
      - User
      summary: Retrieves information about the specified user.
      operationId: UserGet
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster the user belongs to.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: userName
        in: path
        description: |-
          Required.
          Name of the user to return information about.

          To get this name, make a [list](/docs/managed-mysql/api-ref/User/list) request.

          The maximum string length in characters is 63.
          Value must match the regular expression `` [a-zA-Z0-9_]* ``.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
      x-y-navtitle: get
    delete:
      tags:
      - User
      summary: Deletes a user in a cluster.
      operationId: UserDelete
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to delete the user from.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: userName
        in: path
        description: |-
          Required.
          Name of the user to delete.

          To get this name, make a [list](/docs/managed-mysql/api-ref/User/list) request.

          The maximum string length in characters is 63.
          Value must match the regular expression `` [a-zA-Z0-9_]* ``.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteUserMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - User
      summary: Updates a user in a cluster.
      operationId: UserUpdate
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to update the user in.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: userName
        in: path
        description: |-
          Required.
          Name of the user to update.

          To get this name, make a [list](/docs/managed-mysql/api-ref/User/list) request.

          The maximum string length in characters is 63.
          Value must match the regular expression `` [a-zA-Z0-9_]* ``.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateUserMetadata'
      x-y-operation-response: '[User](/docs/managed-mysql/api-ref/User#representation)'
  /managed-mysql/v1/clusters/{clusterId}/users:
    get:
      tags:
      - User
      summary: Retrieves the list of users in a cluster.
      operationId: UserList
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to list the users in.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          The maximum number of results per page to return.

          If the number of available results is larger than [pageSize](/docs/managed-mysql/api-ref/User/list#query_params), the API returns a [nextPageToken](/docs/managed-mysql/api-ref/User/list#responses) that can be used to get the next page of results in the subsequent [list](/docs/managed-mysql/api-ref/User/list) requests.

          Acceptable values are 0 to 1000, inclusive.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Page token that can be used to iterate through multiple pages of results.

          To get the next page of results, set [pageToken](/docs/managed-mysql/api-ref/User/list#query_params) to the [nextPageToken](/docs/managed-mysql/api-ref/User/list#responses) returned by the previous [list](/docs/managed-mysql/api-ref/User/list) request.

          The maximum string length in characters is 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListUsersResponse'
      x-y-navtitle: list
    post:
      tags:
      - User
      summary: Creates a user in a cluster.
      operationId: UserCreate
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to create the user in.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateUserMetadata'
      x-y-operation-response: '[User](/docs/managed-mysql/api-ref/User#representation)'
  /managed-mysql/v1/clusters/{clusterId}/users/{userName}:grantPermission:
    post:
      tags:
      - User
      summary: Grants permission to access a database to a user in a cluster.
      operationId: UserGrantPermission
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to grant permission to the user in.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: userName
        in: path
        description: |-
          Required.
          Name of the user to grant permission to.

          To get this name, make a [list](/docs/managed-mysql/api-ref/User/list) request.

          The maximum string length in characters is 63.
          Value must match the regular expression `` [a-zA-Z0-9_]* ``.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GrantUserPermissionRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: grantPermission
      x-y-operation-metadata:
        $ref: '#/components/schemas/GrantUserPermissionMetadata'
      x-y-operation-response: '[User](/docs/managed-mysql/api-ref/User#representation)'
  /managed-mysql/v1/clusters/{clusterId}/users/{userName}:revokePermission:
    post:
      tags:
      - User
      summary: Revokes permission to access a database from a user in a cluster.
      operationId: UserRevokePermission
      parameters:
      - name: clusterId
        in: path
        description: |-
          Required.
          ID of the cluster to revoke permission from the user in.

          To get this ID, make a [list](/docs/managed-mysql/api-ref/Cluster/list) request.

          The maximum string length in characters is 50.
        required: true
        schema:
          type: string
      - name: userName
        in: path
        description: |-
          Required.
          Name of the user to revoke permission from.

          To get this name, make a [list](/docs/managed-mysql/api-ref/User/list) request.

          The maximum string length in characters is 63.
          Value must match the regular expression `` [a-zA-Z0-9_]* ``.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RevokeUserPermissionRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: revokePermission
      x-y-operation-metadata:
        $ref: '#/components/schemas/RevokeUserPermissionMetadata'
      x-y-operation-response: '[User](/docs/managed-mysql/api-ref/User#representation)'
components:
  schemas:
    Access:
      type: object
      properties:
        dataLens:
          description: |-
            Allows access from DataLens.

            See [the documentation](/docs/managed-mysql/operations/datalens-connect) for details.
          type: boolean
          format: boolean
        webSql:
          description: |-
            Allows SQL queries to the cluster databases from management console.

            See [the documentation](/docs/managed-mysql/operations/web-sql-query) for details.
          type: boolean
          format: boolean
        dataTransfer:
          description: Allow access for DataTransfer.
          type: boolean
          format: boolean
    AddClusterHostsMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster to which the hosts are being added.
          type: string
        hostNames:
          description: Names of hosts that are being added.
          type: array
          items:
            type: string
    AddClusterHostsRequest:
      type: object
      properties:
        hostSpecs:
          description: |-
            Required.
            Configuration of the newly added hosts.

            Must contain at least one element.
          type: array
          items:
            $ref: '#/components/schemas/HostSpec'
    AnytimeMaintenanceWindow:
      type: object
      properties: {}
    Backup:
      description: |-
        An object that represents MySQL backup.

        See [the documentation](/docs/managed-mysql/concepts/backup) for details.
      type: object
      properties:
        id:
          description: |-
            Required.
            ID of the backup.
          type: string
        folderId:
          description: ID of the folder that the backup belongs to.
          type: string
        createdAt:
          description: |-
            Creation timestamp (the time when the backup operation was completed).

            String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
          type: string
          format: date-time
        sourceClusterId:
          description: ID of the cluster that the backup was created for.
          type: string
        startedAt:
          description: |-
            Start timestamp (the time when the backup operation was started).

            String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
          type: string
          format: date-time
    BackupClusterMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster that is being backed up.
          type: string
    Cluster:
      description: |-
        An object that represents MySQL cluster.

        See [the documentation](/docs/managed-mysql/concepts) for details.
      type: object
      properties:
        id:
          description: |-
            ID of the cluster.

            This ID is assigned by the platform at the time of creation.
          type: string
        folderId:
          description: ID of the folder that the cluster belongs to.
          type: string
        createdAt:
          description: |-
            Creation timestamp of the cluster.

            String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
          type: string
          format: date-time
        name:
          description: Name of the cluster.
          type: string
        description:
          description: Description of the cluster.
          type: string
        labels:
          description: Custom labels for the cluster as `key:value` pairs.
          type: object
          additionalProperties:
            type: string
        environment:
          $ref: '#/components/schemas/ClusterEnvironment_in_Cluster_environment'
        monitoring:
          description: Monitoring systems data that is relevant to the cluster.
          type: array
          items:
            $ref: '#/components/schemas/Monitoring'
        config:
          $ref: '#/components/schemas/ClusterConfig_in_Cluster_config'
        networkId:
          description: ID of the network that the cluster belongs to.
          type: string
        health:
          $ref: '#/components/schemas/ClusterHealth_in_Cluster_health'
        status:
          $ref: '#/components/schemas/ClusterStatus_in_Cluster_status'
        maintenanceWindow:
          $ref: '#/components/schemas/MaintenanceWindow_in_Cluster_maintenanceWindow'
        plannedOperation:
          $ref: '#/components/schemas/MaintenanceOperation_in_Cluster_plannedOperation'
        securityGroupIds:
          description: Effective list of security group IDs applied to the cluster.
          type: array
          items:
            type: string
        deletionProtection:
          description: This option prevents unintended deletion of the cluster.
          type: boolean
          format: boolean
        hostGroupIds:
          description: Host groups hosting VMs of the cluster.
          type: array
          items:
            type: string
    ClusterConfig:
      type: object
      allOf:
      - type: object
        properties:
          version:
            description: Version of MySQL used in the cluster.
            type: string
          resources:
            $ref: '#/components/schemas/Resources'
            description: Resource preset for the cluster hosts.
          backupWindowStart:
            $ref: '#/components/schemas/TimeOfDay'
            description: Time to start the daily backup, in the UTC timezone.
          access:
            $ref: '#/components/schemas/Access'
            description: Access policy for external services.
          performanceDiagnostics:
            $ref: '#/components/schemas/PerformanceDiagnostics'
            description: Configuration of the performance diagnostics service.
          backupRetainPeriodDays:
            description: |-
              Retention policy of automated backups.

              Acceptable values are 7 to 60, inclusive.
            type: integer
            format: int64
      - title: mysql_config
        type: object
        maxProperties: 1
        properties:
          mysqlConfig_5_7:
            $ref: '#/components/schemas/MysqlConfigSet5_7'
            description: Configuration of a MySQL 5.7 server.
          mysqlConfig_8_0:
            $ref: '#/components/schemas/MysqlConfigSet8_0'
            description: Configuration of a MySQL 8.0 server.
    ClusterEnvironment:
      description: |2-
         - PRODUCTION: Environment for stable versions of your apps.
        A conservative update policy is in effect: only bug fixes are applied during regular maintenance.
         - PRESTABLE: Environment for testing, including the Managed Service for MySQL itself.
        This environment gets new features, improvements, and bug fixes in the first place, compared to the production environment.
        However, not every update ensures backward compatibility.
      type: string
      enum:
      - PRODUCTION
      - PRESTABLE
    ClusterHealth:
      description: |2-
         - HEALTH_UNKNOWN: Health of the cluster is unknown (`health` for every host in the cluster is `UNKNOWN`).
         - ALIVE: Cluster is alive and well (`health` for every host in the cluster is `ALIVE`).
         - DEAD: Cluster is inoperable (`health` for every host in the cluster is `DEAD`).
         - DEGRADED: Cluster is degraded (`health` for at least one host in the cluster is not `ALIVE`).
      type: string
      enum:
      - HEALTH_UNKNOWN
      - ALIVE
      - DEAD
      - DEGRADED
      default: HEALTH_UNKNOWN
    ClusterStatus:
      description: |2-
         - STATUS_UNKNOWN: Cluster state is unknown.
         - CREATING: Cluster is being created.
         - RUNNING: Cluster is running normally.
         - ERROR: Cluster encountered a problem and cannot operate.
         - UPDATING: Cluster is being updated.
         - STOPPING: Cluster is stopping.
         - STOPPED: Cluster is stopped.
         - STARTING: Cluster is starting.
      type: string
      enum:
      - STATUS_UNKNOWN
      - CREATING
      - RUNNING
      - ERROR
      - UPDATING
      - STOPPING
      - STOPPED
      - STARTING
      default: STATUS_UNKNOWN
    ConfigSpec:
      type: object
      allOf:
      - type: object
        properties:
          version:
            description: |-
              Version of MySQL used in the cluster.

              Possible values: `5.7`, `8.0`.
            type: string
          resources:
            $ref: '#/components/schemas/Resources'
            description: Resource preset for the cluster hosts.
          backupWindowStart:
            $ref: '#/components/schemas/TimeOfDay'
            description: Time to start the daily backup, in the UTC timezone.
          access:
            $ref: '#/components/schemas/Access'
            description: |-
              Access policy for external services.

              If the specific services need to access the cluster, then set the necessary values in this policy.
          performanceDiagnostics:
            $ref: '#/components/schemas/PerformanceDiagnostics'
            description: Configuration of the performance diagnostics service.
          backupRetainPeriodDays:
            description: |-
              Retention policy of automated backups.

              Acceptable values are 7 to 60, inclusive.
            type: integer
            format: int64
      - title: mysql_config
        type: object
        maxProperties: 1
        properties:
          mysqlConfig_5_7:
            $ref: '#/components/schemas/MysqlConfig5_7'
            description: Configuration for a MySQL 5.7 cluster.
          mysqlConfig_8_0:
            $ref: '#/components/schemas/MysqlConfig8_0'
            description: Configuration for a MySQL 8.0 cluster.
    ConnectionLimits:
      type: object
      properties:
        maxQuestionsPerHour:
          description: |-
            The maximum permitted number of user questions per hour.

            The minimum value is 0.
          type: integer
          format: int64
        maxUpdatesPerHour:
          description: |-
            The maximum permitted number of user updates per hour.

            The minimum value is 0.
          type: integer
          format: int64
        maxConnectionsPerHour:
          description: |-
            The maximum permitted number of simultaneous client connections per hour.

            The minimum value is 0.
          type: integer
          format: int64
        maxUserConnections:
          description: |-
            The maximum number of simultaneous connections permitted to any given MySQL user account.

            The minimum value is 0.
          type: integer
          format: int64
    CreateClusterMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster that is being created.
          type: string
    CreateClusterRequest:
      type: object
      properties:
        folderId:
          description: |-
            Required.
            ID of the folder to create the cluster in.

            To get this ID, make a [list](/docs/resource-manager/api-ref/Folder/list) request.

            The maximum string length in characters is 50.
          type: string
        name:
          description: |-
            Required.
            Name of the cluster. The name must be unique within the folder.

            The maximum string length in characters is 63.
            Value must match the regular expression `` [a-zA-Z0-9_-]* ``.
          type: string
        description:
          description: |-
            Description of the cluster.

            The maximum string length in characters is 256.
          type: string
        labels:
          description: |-
            Custom labels for the cluster as `key:value` pairs.

            No more than 64 per resource.
            The string length in characters for each key must be 1-63.
            Each key must match the regular expression `` [a-z][-_0-9a-z]* ``.
            The maximum string length in characters for each value is 63.
            Each value must match the regular expression `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        environment:
          $ref: '#/components/schemas/ClusterEnvironment_in_CreateClusterRequest_environment'
        configSpec:
          $ref: '#/components/schemas/ConfigSpec_in_CreateClusterRequest_configSpec'
        databaseSpecs:
          description: Configuration of databases in the cluster.
          type: array
          items:
            $ref: '#/components/schemas/DatabaseSpec'
        userSpecs:
          description: Configuration of database users in the cluster.
          type: array
          items:
            $ref: '#/components/schemas/UserSpec'
        hostSpecs:
          description: Configuration of hosts in the cluster.
          type: array
          items:
            $ref: '#/components/schemas/HostSpec'
        networkId:
          description: |-
            ID of the network to create the cluster in.

            The maximum string length in characters is 50.
          type: string
        securityGroupIds:
          description: List of security group IDs to apply to the cluster.
          type: array
          items:
            type: string
        deletionProtection:
          description: This option prevents unintended deletion of the cluster.
          type: boolean
          format: boolean
        hostGroupIds:
          description: Host groups hosting VMs of the cluster.
          type: array
          items:
            type: string
    CreateDatabaseMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster the database is being created in.
          type: string
        databaseName:
          description: Name of the database that is being created.
          type: string
    CreateDatabaseRequest:
      type: object
      properties:
        databaseSpec:
          $ref: '#/components/schemas/DatabaseSpec_in_CreateDatabaseRequest_databaseSpec'
    CreateUserMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster the user is being created in.
          type: string
        userName:
          description: Name of the user that is being created.
          type: string
    CreateUserRequest:
      type: object
      properties:
        userSpec:
          $ref: '#/components/schemas/UserSpec_in_CreateUserRequest_userSpec'
    Database:
      description: |-
        An object that represents MySQL database.

        See [the documentation](/docs/managed-mysql/operations/databases) for details.
      type: object
      properties:
        name:
          description: Name of the database.
          type: string
        clusterId:
          description: ID of the cluster that the database belongs to.
          type: string
    DatabaseSpec:
      type: object
      properties:
        name:
          description: |-
            Required.
            Name of the database.

            The maximum string length in characters is 63.
            Value must match the regular expression `` [a-zA-Z0-9_-]* ``.
          type: string
    DeleteClusterHostsMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster from which the hosts are being deleted.
          type: string
        hostNames:
          description: Names of hosts that are being deleted.
          type: array
          items:
            type: string
    DeleteClusterHostsRequest:
      type: object
      properties:
        hostNames:
          description: |-
            Required.
            Names of hosts to delete.

            To get these names, make a [listHosts](/docs/managed-mysql/api-ref/Cluster/listHosts) request.

            Must contain at least one element.
            The maximum string length in characters for each value is 253.
          type: array
          items:
            type: string
    DeleteClusterMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster that is being deleted.
          type: string
    DeleteDatabaseMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster the database is being deleted from.
          type: string
        databaseName:
          description: Name of the database that is being deleted.
          type: string
    DeleteUserMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster the user is being deleted from.
          type: string
        userName:
          description: Name of the user that is being deleted.
          type: string
    Empty:
      description: Empty JSON object `` {} ``.
      type: object
      maxProperties: 0
    FieldMask:
      description: |-
        A comma-separated names off ALL fields to be updated.
        Only the specified fields will be changed. The others will be left untouched.
        If the field is specified in `` updateMask `` and no value for that field was sent in the request,
        the field's value will be reset to the default. The default value for most fields is null or 0.

        If `` updateMask `` is not sent in the request, all fields' values will be updated.
        Fields specified in the request will be updated to provided values.
        The rest of the fields will be reset to the default.
      type: string
    GlobalPermission:
      description: |2-
         - REPLICATION_CLIENT: Enables use of the `SHOW MASTER STATUS`, `SHOW SLAVE STATUS`, and `SHOW BINARY LOGS` statements.
         - REPLICATION_SLAVE: Enables the account to request updates that have been made to databases on the master server,
        using the `SHOW SLAVE HOSTS`, `SHOW RELAYLOG EVENTS` and `SHOW BINLOG EVENTS` statements.
         - PROCESS: Enables display of information about the the statements currently being performed by sessions (the set of threads executing within the server).

        The privilege enables use of `SHOW PROCESSLIST` or `mysqladmin` processlist to see threads belonging to other users.
        You can always see your own threads. The `PROCESS` privilege also enables use of `SHOW ENGINE`.
      type: string
      enum:
      - REPLICATION_CLIENT
      - REPLICATION_SLAVE
      - PROCESS
    GrantUserPermissionMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster the user is being granted a permission in.
          type: string
        userName:
          description: Name of the user that is being granted a permission.
          type: string
    GrantUserPermissionRequest:
      type: object
      properties:
        permission:
          $ref: '#/components/schemas/Permission_in_GrantUserPermissionRequest_permission'
    Host:
      type: object
      properties:
        name:
          description: |-
            Name of the host.

            This name is assigned by the platform at the time of creation.
            The name is unique across all MDB hosts that exist on the platform, as it defines the FQDN of the host.
          type: string
        clusterId:
          description: ID of the cluster the host belongs to.
          type: string
        zoneId:
          description: ID of the availability zone where the host resides.
          type: string
        resources:
          $ref: '#/components/schemas/Resources_in_Host_resources'
        role:
          $ref: '#/components/schemas/HostRole_in_Host_role'
        health:
          $ref: '#/components/schemas/HostHealth_in_Host_health'
        services:
          description: List of services provided by the host.
          type: array
          items:
            $ref: '#/components/schemas/Service'
        subnetId:
          description: ID of the subnet that the host belongs to.
          type: string
        assignPublicIp:
          description: Flag that shows if public IP address is assigned to the host
            so that the host can be accessed from the internet.
          type: boolean
          format: boolean
        replicationSource:
          description: Name of the host to be used as the replication source for cascading
            replication.
          type: string
        backupPriority:
          description: |-
            Host backup priority.

            Acceptable values are 0 to 100, inclusive.
          type: string
          format: int64
        priority:
          description: |-
            Host master promotion priority.

            Acceptable values are 0 to 100, inclusive.
          type: string
          format: int64
    HostHealth:
      description: |2-
         - HEALTH_UNKNOWN: Health of the host is unknown.
         - ALIVE: Host is performing all its functions normally.
         - DEAD: Host is inoperable, and cannot perform any of its essential functions.
         - DEGRADED: Host is degraded, and can perform only some of its essential functions.
         - READONLY: Host is alive, but in read-only mode.
      type: string
      enum:
      - HEALTH_UNKNOWN
      - ALIVE
      - DEAD
      - DEGRADED
      - READONLY
      default: HEALTH_UNKNOWN
    HostRole:
      description: |2-
         - ROLE_UNKNOWN: Role of the host is unknown.
         - MASTER: Host is the master.
         - REPLICA: Host is a replica.
      type: string
      enum:
      - ROLE_UNKNOWN
      - MASTER
      - REPLICA
      default: ROLE_UNKNOWN
    HostSpec:
      type: object
      properties:
        zoneId:
          description: |-
            ID of the availability zone where the host resides.

            To get a list of available zones, make the [list](/docs/compute/api-ref/Zone/list) request.

            The maximum string length in characters is 50.
          type: string
        subnetId:
          description: |-
            ID of the subnet to assign to the host.

            This subnet should be a part of the cluster network (the network ID is specified in the [ClusterService.CreateClusterRequest.network_id]).

            The maximum string length in characters is 50.
          type: string
        assignPublicIp:
          description: |-
            Option that enables public IP address for the host so that the host can be accessed from the internet.

            After a host has been created, this setting cannot be changed.
            To remove an assigned public IP address, or to assign a public IP address to a host without one, recreate the host with the appropriate `assignPublicIp` value set.

            Possible values:
            * `false` - don't assign a public IP address to the host.
            * `true` - assign a public IP address to the host.
          type: boolean
          format: boolean
        replicationSource:
          description: '`name` of the host to be used as the replication source (for
            cascading replication).'
          type: string
        backupPriority:
          description: |-
            Host backup priority

            Acceptable values are 0 to 100, inclusive.
          type: string
          format: int64
        priority:
          description: |-
            Host master promotion priority

            Acceptable values are 0 to 100, inclusive.
          type: string
          format: int64
    ListBackupsResponse:
      type: object
      properties:
        backups:
          description: List of backups.
          type: array
          items:
            $ref: '#/components/schemas/Backup'
        nextPageToken:
          description: |-
            The token that can be used to get the next page of results.

            If the number of results is larger than [pageSize](/docs/managed-mysql/api-ref/Backup/list#query_params), use the [nextPageToken](/docs/managed-mysql/api-ref/Backup/list#responses) as the value for the [pageToken](/docs/managed-mysql/api-ref/Backup/list#query_params) in the subsequent [list](/docs/managed-mysql/api-ref/Backup/list) request to iterate through multiple pages of results.

            Each of the subsequent [list](/docs/managed-mysql/api-ref/Backup/list) requests should use the [nextPageToken](/docs/managed-mysql/api-ref/Backup/list#responses) value returned by the previous request to continue paging through the results.

            The maximum string length in characters is 100.
          type: string
    ListClusterBackupsResponse:
      type: object
      properties:
        backups:
          description: List of the cluster backups.
          type: array
          items:
            $ref: '#/components/schemas/Backup'
        nextPageToken:
          description: |-
            The token that can be used to get the next page of results.

            If the number of results is larger than [pageSize](/docs/managed-mysql/api-ref/Cluster/listBackups#query_params), use the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listBackups#responses) as the value for the [pageToken](/docs/managed-mysql/api-ref/Cluster/listBackups#query_params) in the subsequent [listBackups](/docs/managed-mysql/api-ref/Cluster/listBackups) request to iterate through multiple pages of results.

            Each of the subsequent [listBackups](/docs/managed-mysql/api-ref/Cluster/listBackups) requests should use the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listBackups#responses) value returned by the previous request to continue paging through the results.
          type: string
    ListClusterHostsResponse:
      type: object
      properties:
        hosts:
          description: List of hosts in the cluster.
          type: array
          items:
            $ref: '#/components/schemas/Host'
        nextPageToken:
          description: |-
            The token that can be used to get the next page of results.

            If the number of results is larger than [pageSize](/docs/managed-mysql/api-ref/Cluster/listHosts#query_params), use the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listHosts#responses) as the value for the [pageToken](/docs/managed-mysql/api-ref/Cluster/listHosts#query_params) in the subsequent [listHosts](/docs/managed-mysql/api-ref/Cluster/listHosts) request to iterate through multiple pages of results.

            Each of the subsequent [listHosts](/docs/managed-mysql/api-ref/Cluster/listHosts) requests should use the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listHosts#responses) value returned by the previous request to continue paging through the results.
          type: string
    ListClusterLogsRequestServiceType:
      description: |2-
         - MYSQL_ERROR: MySQL error log.
         - MYSQL_GENERAL: MySQL general query log.
         - MYSQL_SLOW_QUERY: MySQL slow query log.
         - MYSQL_AUDIT: MySQL audit log.
      type: string
      enum:
      - MYSQL_ERROR
      - MYSQL_GENERAL
      - MYSQL_SLOW_QUERY
      - MYSQL_AUDIT
    ListClusterLogsResponse:
      type: object
      properties:
        logs:
          description: Requested log records.
          type: array
          items:
            $ref: '#/components/schemas/LogRecord'
        nextPageToken:
          description: |-
            The token that can be used to get the next page of results.

            If the number of results is larger than [pageSize](/docs/managed-mysql/api-ref/Cluster/listLogs#query_params), use the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#responses) as the value for the [pageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#query_params) in the subsequent [listLogs](/docs/managed-mysql/api-ref/Cluster/listLogs) request to iterate through multiple pages of results.

            Each of the subsequent [listLogs](/docs/managed-mysql/api-ref/Cluster/listLogs) requests should use the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#responses) value returned by the previous request to continue paging through the results.

            This value is interchangeable with `nextRecordToken` from [streamLogs](/docs/managed-mysql/api-ref/Cluster/streamLogs) method.
          type: string
    ListClusterOperationsResponse:
      type: object
      properties:
        operations:
          description: List of operations in the cluster.
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextPageToken:
          description: |-
            The token that can be used to get the next page of results.

            If the number of results is larger than [pageSize](/docs/managed-mysql/api-ref/Cluster/listOperations#query_params), use the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listOperations#responses) as the value for the [pageToken](/docs/managed-mysql/api-ref/Cluster/listOperations#query_params) in the subsequent [listOperations](/docs/managed-mysql/api-ref/Cluster/listOperations) request to iterate through multiple pages of results.

            Each of the subsequent [listOperations](/docs/managed-mysql/api-ref/Cluster/listOperations) requests should use the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listOperations#responses) value returned by the previous request to continue paging through the results.
          type: string
    ListClustersResponse:
      type: object
      properties:
        clusters:
          description: List of clusters.
          type: array
          items:
            $ref: '#/components/schemas/Cluster'
        nextPageToken:
          description: |-
            The token that can be used to get the next page of results.

            If the number of results is larger than [pageSize](/docs/managed-mysql/api-ref/Cluster/list#query_params), use the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/list#responses) as the value for the [pageToken](/docs/managed-mysql/api-ref/Cluster/list#query_params) in the subsequent [list](/docs/managed-mysql/api-ref/Cluster/list) request to iterate through multiple pages of results.

            Each of the subsequent [list](/docs/managed-mysql/api-ref/Cluster/list) requests should use the [nextPageToken](/docs/managed-mysql/api-ref/Cluster/list#responses) value returned by the previous request to continue paging through the results.
          type: string
    ListDatabasesResponse:
      type: object
      properties:
        databases:
          description: List of databases.
          type: array
          items:
            $ref: '#/components/schemas/Database'
        nextPageToken:
          description: |-
            The token that can be used to get the next page of results.

            If the number of results is larger than [pageSize](/docs/managed-mysql/api-ref/Database/list#query_params), use the [nextPageToken](/docs/managed-mysql/api-ref/Database/list#responses) as the value for the [pageToken](/docs/managed-mysql/api-ref/Database/list#query_params) in the subsequent [list](/docs/managed-mysql/api-ref/Database/list) request to iterate through multiple pages of results.

            Each of the subsequent [list](/docs/managed-mysql/api-ref/Database/list) requests should use the [nextPageToken](/docs/managed-mysql/api-ref/Database/list#responses) value returned by the previous request to continue paging through the results.
          type: string
    ListResourcePresetsResponse:
      type: object
      properties:
        resourcePresets:
          description: List of resource presets.
          type: array
          items:
            $ref: '#/components/schemas/ResourcePreset'
        nextPageToken:
          description: |-
            The token that can be used to get the next page of results.

            If the number of results is larger than [pageSize](/docs/managed-mysql/api-ref/ResourcePreset/list#query_params), use the [nextPageToken](/docs/managed-mysql/api-ref/ResourcePreset/list#responses) as the value for the [pageToken](/docs/managed-mysql/api-ref/ResourcePreset/list#query_params) in the subsequent [list](/docs/managed-mysql/api-ref/ResourcePreset/list) request to iterate through multiple pages of results.

            Each of the subsequent [list](/docs/managed-mysql/api-ref/ResourcePreset/list) requests should use the [nextPageToken](/docs/managed-mysql/api-ref/ResourcePreset/list#responses) value returned by the previous request to continue paging through the results.

            The maximum string length in characters is 100.
          type: string
    ListUsersResponse:
      type: object
      properties:
        users:
          description: List of users.
          type: array
          items:
            $ref: '#/components/schemas/User'
        nextPageToken:
          description: |-
            The token that can be used to get the next page of results.

            If the number of results is larger than [pageSize](/docs/managed-mysql/api-ref/User/list#query_params), use the [nextPageToken](/docs/managed-mysql/api-ref/User/list#responses) as the value for the [pageToken](/docs/managed-mysql/api-ref/User/list#query_params) in the subsequent [list](/docs/managed-mysql/api-ref/User/list) request to iterate through multiple pages of results.

            Each of the subsequent [list](/docs/managed-mysql/api-ref/User/list) requests should use the [nextPageToken](/docs/managed-mysql/api-ref/User/list#responses) value returned by the previous request to continue paging through the results.
          type: string
    LogRecord:
      description: A single log record.
      type: object
      properties:
        timestamp:
          description: |-
            Timestamp of the log record.

            String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
          type: string
          format: date-time
        message:
          description: Contents of the log record.
          type: object
          additionalProperties:
            type: string
    MaintenanceOperation:
      description: A planned maintenance operation.
      type: object
      properties:
        info:
          description: |-
            Information about this maintenance operation.

            The maximum string length in characters is 256.
          type: string
        delayedUntil:
          description: |-
            Time until which this maintenance operation is delayed.

            String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
          type: string
          format: date-time
    MaintenanceWindow:
      description: Configuration of a maintenance window in a MySQL cluster.
      type: object
      allOf:
      - title: policy
        type: object
        maxProperties: 1
        properties:
          anytime:
            $ref: '#/components/schemas/AnytimeMaintenanceWindow'
            description: Maintenance operation can be scheduled anytime.
          weeklyMaintenanceWindow:
            $ref: '#/components/schemas/WeeklyMaintenanceWindow'
            description: Maintenance operation can be scheduled on a weekly basis.
    Monitoring:
      description: Cluster-related monitoring system data.
      type: object
      properties:
        name:
          description: Name of the monitoring system.
          type: string
        description:
          description: Description of the monitoring system.
          type: string
        link:
          description: Link to the monitoring system charts for the cluster.
          type: string
    MoveClusterMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster that is being moved.
          type: string
        sourceFolderId:
          description: ID of the source folder.
          type: string
        destinationFolderId:
          description: ID of the destination folder.
          type: string
    MoveClusterRequest:
      type: object
      properties:
        destinationFolderId:
          description: |-
            Required.
            ID of the destination folder.

            To get this ID, make a [list](/docs/resource-manager/api-ref/Folder/list) request.

            The maximum string length in characters is 50.
          type: string
    MysqlConfig5_7:
      description: Options and structure of `MysqlConfig5_7` reflects MySQL 5.7 configuration
        file.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Size of the InnoDB buffer pool used for caching table and index data.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size) for details.

            The minimum value is 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            The maximum permitted number of simultaneous client connections.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_connections) for details.

            Acceptable values are 10 to 16384, inclusive.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Time that it takes to process a query before it is considered slow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_long_query_time) for details.

            Acceptable values are 0 to 3600, inclusive.
          type: number
          format: double
        generalLog:
          description: |-
            Enable writing of general query log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_general_log) for details.
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Enable writing of audit log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/mysql-security-excerpt/5.6/en/audit-log-options-variables.html#option_mysqld_audit-log) for details.
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Server SQL mode of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/sql-mode.html#sql-mode-setting) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig5_7SQLMode'
        maxAllowedPacket:
          description: |-
            The maximum size in bytes of one packet.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_allowed_packet) for details.

            Acceptable values are 1024 to 1073741824, inclusive.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig5_7AuthPlugin_in_MysqlConfig5_7_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Transaction log flush behaviour.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Max time in seconds for a transaction to wait for a row lock.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout for details.

            Acceptable values are 1 to 28800, inclusive.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig5_7TransactionIsolation_in_MysqlConfig5_7_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Print information about deadlocks in error log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks for details.
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            The number of seconds to wait for more data from a connection before aborting the read.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_read_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            The number of seconds to wait for a block to be written to a connection before aborting the write.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_write_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            The maximum permitted result length in bytes for the GROUP_CONCAT() function.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_group_concat_max_len for details.

            Acceptable values are 4 to 33554432, inclusive.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            The maximum size of internal in-memory temporary tables.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_tmp_table_size for details.

            Acceptable values are 1024 to 536870912, inclusive.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            This variable sets the maximum size to which user-created MEMORY tables are permitted to grow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_heap_table_size for details.

            Acceptable values are 16384 to 536870912, inclusive.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            The servers default time zone.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-options.html#option_mysqld_default-time-zone for details.
          type: string
        characterSetServer:
          description: |-
            The servers default character set.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_character_set_server for details.
          type: string
        collationServer:
          description: |-
            The server default collation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_collation_server for details.
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Enables InnoDB adaptive hash index.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index for details.
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Enables the NUMA interleave memory policy for allocation of the InnoDB buffer pool.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_numa_interleave for details.
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            The size in bytes of the buffer that InnoDB uses to write to the log files on disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_buffer_size for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            The size in bytes of the single InnoDB Redo log file.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_file_size for details.

            Acceptable values are 268435456 to 4294967296, inclusive.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity_max for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            The number of I/O threads for read operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_read_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            The number of I/O threads for write operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_write_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            The number of background threads devoted to the InnoDB purge operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_purge_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Defines the maximum number of threads permitted inside of InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_thread_concurrency for details.

            Acceptable values are 0 to 1000, inclusive.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Limits the max size of InnoDB temp tablespace.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path for details.

            Acceptable values are 0 to 107374182400, inclusive.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            A number of threads the server should cache for reuse.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_cache_size) for details.

            Acceptable values are 10 to 10000, inclusive.
          type: integer
          format: int64
        threadStack:
          description: |-
            The stack size for each thread. The default is large enough for normal operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_stack) for details.

            Acceptable values are 131072 to 16777216, inclusive.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            The minimum size of the buffer that is used for plain index scans, range index scans, and joins that do not use indexes and thus perform full table scans.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_join_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Each session that must perform a sort allocates a buffer of this size.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_sort_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            The number of table definitions that can be stored in the definition cache.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_definition_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            The number of open tables for all threads.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            The number of open tables cache instances.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache_instances) for details.

            Acceptable values are 1 to 32, inclusive.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Determines whether the server enables certain nonstandard behaviors for default values and NULL-value handling in TIMESTAMP columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp) for details.
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_increment) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_offset) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Controls how often the MySQL server synchronizes the binary log to disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_sync_binlog) for details.

            Acceptable values are 0 to 4096, inclusive.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            The size of the cache to hold changes to the binary log during a transaction.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_cache_size) for details.

            Acceptable values are 4096 to 67108864, inclusive.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Controls how many microseconds the binary log commit waits before synchronizing the binary log file to disk.

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay) for details.

            Acceptable values are 0 to 50000, inclusive.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig5_7BinlogRowImage_in_MysqlConfig5_7_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            When enabled, it causes the server to write informational log events such as row query log events into its binary log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events) for details.
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            The number of replica acknowledgments the source must receive per transaction before proceeding.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count) for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig5_7SlaveParallelType_in_MysqlConfig5_7_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Sets the number of applier threads for executing replication transactions in parallel.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-replica.html#sysvar_slave_parallel_workers) for details.

            Acceptable values are 0 to 64, inclusive.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            The size of the binary log to hold.

            Acceptable values are 1073741824 to 1099511627776, inclusive.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            The number of seconds the server waits for activity on an interactive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_interactive_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            The number of seconds the server waits for activity on a noninteractive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_wait_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = ON' to prevent users from reading stale data.

            Acceptable values are 600 to 432000, inclusive.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = OFF'.
            Should be less than mdb_offline_mode_enable_lag value.

            Acceptable values are 60 to 86400, inclusive.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            The limit on memory consumption for the range optimizer.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size) for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        slowQueryLog:
          description: |-
            Manages slow query log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_slow_query_log) for details.
          type: boolean
          format: boolean
        slowQueryLogAlwaysWriteTime:
          description: |-
            Query execution time, after which query to be logged unconditionally, that is, `log_slow_rate_limit`` will not apply to it.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#slow_query_log_always_write_time) for details.
          type: number
          format: double
        logSlowRateType:
          $ref: '#/components/schemas/MysqlConfig5_7LogSlowRateType_in_MysqlConfig5_7_logSlowRateType'
        logSlowRateLimit:
          description: |-
            Specifies what fraction of session/query should be logged. Logging is enabled for every nth session/query.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_rate_limit) for details.

            Acceptable values are 1 to 1000, inclusive.
          type: integer
          format: int64
        logSlowSpStatements:
          description: |-
            When TRUE, statements executed by stored procedures are logged to the slow log.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_sp_statements) for details.
          type: boolean
          format: boolean
        logSlowFilter:
          description: |-
            Filters the slow log by the query's execution plan.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_filter) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig5_7LogSlowFilterType'
        mdbPriorityChoiceMaxLag:
          description: |-
            Replication lag threshold (seconds) which allows replica to be promoted to master while executing "switchover from".
            Should be less than mdb_offline_mode_disable_lag.

            Acceptable values are 0 to 86400, inclusive.
          type: integer
          format: int64
        innodbPageSize:
          description: |-
            Specifies the page size for InnoDB tablespaces.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_page_size).

            Acceptable values are 4096 to 65536, inclusive.
          type: integer
          format: int64
        innodbOnlineAlterLogMaxSize:
          description: |-
            The limit in bytes on the size of the temporary log files used during online DDL operations

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_online_alter_log_max_size).

            Acceptable values are 65536 to 107374182400, inclusive.
          type: integer
          format: int64
        innodbFtMinTokenSize:
          description: |-
            Minimum length of words that are stored in an InnoDB FULLTEXT index

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_ft_min_token_size).

            Acceptable values are 0 to 16, inclusive.
          type: integer
          format: int64
        innodbFtMaxTokenSize:
          description: |-
            Maximum length of words that are stored in an InnoDB FULLTEXT index

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_ft_max_token_size).

            Acceptable values are 10 to 84, inclusive.
          type: integer
          format: int64
        lowerCaseTableNames:
          description: |-
            Table names storage and comparison strategy

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_lower_case_table_names).

            Acceptable values are 0 to 1, inclusive.
          type: integer
          format: int64
        showCompatibility_56:
          description: |-
            Manages MySQL 5.6 compatibility

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_show_compatibility_56).
          type: boolean
          format: boolean
        maxSpRecursionDepth:
          description: |-
            The number of times that any given stored procedure may be called recursively.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_sp_recursion_depth).

            Acceptable values are 0 to 255, inclusive.
          type: integer
          format: int64
        innodbCompressionLevel:
          description: |-
            The level of zlib compression to use for InnoDB compressed tables and indexes.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_compression_level).

            Acceptable values are 0 to 9, inclusive.
          type: integer
          format: int64
        binlogTransactionDependencyTracking:
          $ref: '#/components/schemas/MysqlConfig5_7BinlogTransactionDependencyTracking_in_MysqlConfig5_7_binlogTransactionDependencyTracking'
    MysqlConfig5_7AuthPlugin:
      description: |2-
         - MYSQL_NATIVE_PASSWORD: Using [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/5.7/en/native-pluggable-authentication.html).
         - SHA256_PASSWORD: Using [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/5.7/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    MysqlConfig5_7BinlogRowImage:
      type: string
      enum:
      - FULL
      - MINIMAL
      - NOBLOB
    MysqlConfig5_7BinlogTransactionDependencyTracking:
      type: string
      enum:
      - COMMIT_ORDER
      - WRITESET
      - WRITESET_SESSION
    MysqlConfig5_7LogSlowFilterType:
      type: string
      enum:
      - FULL_SCAN
      - FULL_JOIN
      - TMP_TABLE
      - TMP_TABLE_ON_DISK
      - FILESORT
      - FILESORT_ON_DISK
    MysqlConfig5_7LogSlowRateType:
      type: string
      enum:
      - SESSION
      - QUERY
    MysqlConfig5_7SQLMode:
      type: string
      enum:
      - ALLOW_INVALID_DATES
      - ANSI_QUOTES
      - ERROR_FOR_DIVISION_BY_ZERO
      - HIGH_NOT_PRECEDENCE
      - IGNORE_SPACE
      - NO_AUTO_VALUE_ON_ZERO
      - NO_BACKSLASH_ESCAPES
      - NO_ENGINE_SUBSTITUTION
      - NO_UNSIGNED_SUBTRACTION
      - NO_ZERO_DATE
      - NO_ZERO_IN_DATE
      - NO_FIELD_OPTIONS
      - NO_KEY_OPTIONS
      - NO_TABLE_OPTIONS
      - ONLY_FULL_GROUP_BY
      - PAD_CHAR_TO_FULL_LENGTH
      - PIPES_AS_CONCAT
      - REAL_AS_FLOAT
      - STRICT_ALL_TABLES
      - STRICT_TRANS_TABLES
      - ANSI
      - TRADITIONAL
      - DB2
      - MAXDB
      - MSSQL
      - MYSQL323
      - MYSQL40
      - ORACLE
      - POSTGRESQL
      - NO_AUTO_CREATE_USER
      - NO_DIR_IN_CREATE
    MysqlConfig5_7SlaveParallelType:
      type: string
      enum:
      - DATABASE
      - LOGICAL_CLOCK
    MysqlConfig5_7TransactionIsolation:
      type: string
      enum:
      - READ_COMMITTED
      - REPEATABLE_READ
      - SERIALIZABLE
    MysqlConfig8_0:
      description: Options and structure of `MysqlConfig8_0` reflects MySQL 8.0 configuration
        file.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Size of the InnoDB buffer pool used for caching table and index data.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size) for details.

            The minimum value is 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            The maximum permitted number of simultaneous client connections.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_connections) for details.

            Acceptable values are 10 to 16384, inclusive.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Time that it takes to process a query before it is considered slow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_long_query_time) for details.
          type: number
          format: double
        generalLog:
          description: |-
            Enable writing of general query log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_general_log) for details.
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Enable writing of audit log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/audit-log-reference.html#audit-log-options-variables) for details.
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Server SQL mode of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/sql-mode.html#sql-mode-setting) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig8_0SQLMode'
        maxAllowedPacket:
          description: |-
            The maximum size in bytes of one packet.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_allowed_packet) for details.

            Acceptable values are 1024 to 1073741824, inclusive.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig8_0AuthPlugin_in_MysqlConfig8_0_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Transaction log flush behaviour.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Max time in seconds for a transaction to wait for a row lock.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout for details.

            Acceptable values are 1 to 28800, inclusive.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig8_0TransactionIsolation_in_MysqlConfig8_0_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Print information about deadlocks in error log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks for details.
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            The number of seconds to wait for more data from a connection before aborting the read.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_read_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            The number of seconds to wait for a block to be written to a connection before aborting the write.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_write_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            The maximum permitted result length in bytes for the GROUP_CONCAT() function.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_group_concat_max_len for details.

            Acceptable values are 4 to 33554432, inclusive.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            The maximum size of internal in-memory temporary tables.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_tmp_table_size for details.

            Acceptable values are 1024 to 536870912, inclusive.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            This variable sets the maximum size to which user-created MEMORY tables are permitted to grow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_heap_table_size for details.

            Acceptable values are 16384 to 536870912, inclusive.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            The servers default time zone.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-options.html#option_mysqld_default-time-zone for details.
          type: string
        characterSetServer:
          description: |-
            The servers default character set.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_character_set_server for details.
          type: string
        collationServer:
          description: |-
            The server default collation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_collation_server for details.
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Enables InnoDB adaptive hash index.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index for details.
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Enables the NUMA interleave memory policy for allocation of the InnoDB buffer pool.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_numa_interleave for details.
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            The size in bytes of the buffer that InnoDB uses to write to the log files on disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_buffer_size for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            The size in bytes of the single InnoDB Redo log file.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_file_size for details.

            Acceptable values are 268435456 to 4294967296, inclusive.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity_max for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            The number of I/O threads for read operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_read_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            The number of I/O threads for write operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_write_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            The number of background threads devoted to the InnoDB purge operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_purge_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Defines the maximum number of threads permitted inside of InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_thread_concurrency for details.

            Acceptable values are 0 to 1000, inclusive.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Limits the max size of InnoDB temp tablespace.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path for details.

            Acceptable values are 0 to 107374182400, inclusive.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            How many threads the server should cache for reuse.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_cache_size) for details.

            Acceptable values are 10 to 10000, inclusive.
          type: integer
          format: int64
        threadStack:
          description: |-
            The stack size for each thread. The default is large enough for normal operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_stack) for details.

            Acceptable values are 131072 to 16777216, inclusive.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            The minimum size of the buffer that is used for plain index scans, range index scans, and joins that do not use indexes and thus perform full table scans.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_join_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Each session that must perform a sort allocates a buffer of this size.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_sort_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            The number of table definitions that can be stored in the definition cache.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_definition_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            The number of open tables for all threads.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            The number of open tables cache instances.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache_instances) for details.

            Acceptable values are 1 to 32, inclusive.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Determines whether the server enables certain nonstandard behaviors for default values and NULL-value handling in TIMESTAMP columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp) for details.
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_increment) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_offset) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Controls how often the MySQL server synchronizes the binary log to disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_sync_binlog) for details.

            Acceptable values are 0 to 4096, inclusive.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            The size of the cache to hold changes to the binary log during a transaction.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_cache_size) for details.

            Acceptable values are 4096 to 67108864, inclusive.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Controls how many microseconds the binary log commit waits before synchronizing the binary log file to disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay) for details.

            Acceptable values are 0 to 50000, inclusive.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig8_0BinlogRowImage_in_MysqlConfig8_0_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            When enabled, it causes the server to write informational log events such as row query log events into its binary log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events) for details.
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            The number of replica acknowledgments the source must receive per transaction before proceeding.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count) for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig8_0SlaveParallelType_in_MysqlConfig8_0_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Sets the number of applier threads for executing replication transactions in parallel.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_slave_parallel_workers) for details.

            Acceptable values are 0 to 64, inclusive.
          type: integer
          format: int64
        regexpTimeLimit:
          description: |-
            The time limit for regular expression matching operations performed by REGEXP_LIKE and similar functions.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_regexp_time_limit) for details.

            Acceptable values are 0 to 1048576, inclusive.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            The size of the binary log to hold.

            Acceptable values are 1073741824 to 1099511627776, inclusive.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            The number of seconds the server waits for activity on an interactive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_interactive_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            The number of seconds the server waits for activity on a noninteractive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_wait_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = ON' to prevent users from reading stale data.

            Acceptable values are 600 to 432000, inclusive.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = OFF'.
            Should be less than mdb_offline_mode_enable_lag.

            Acceptable values are 60 to 86400, inclusive.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            The limit on memory consumption for the range optimizer.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size) for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        slowQueryLog:
          description: |-
            Manages slow query log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_slow_query_log) for details.
          type: boolean
          format: boolean
        slowQueryLogAlwaysWriteTime:
          description: |-
            Query execution time, after which query to be logged unconditionally, that is, `log_slow_rate_limit` will not apply to it.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#slow_query_log_always_write_time) for details.
          type: number
          format: double
        logSlowRateType:
          $ref: '#/components/schemas/MysqlConfig8_0LogSlowRateType_in_MysqlConfig8_0_logSlowRateType'
        logSlowRateLimit:
          description: |-
            Specifies what fraction of session/query should be logged. Logging is enabled for every nth session/query.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_rate_limit) for details.

            Acceptable values are 1 to 1000, inclusive.
          type: integer
          format: int64
        logSlowSpStatements:
          description: |-
            When TRUE, statements executed by stored procedures are logged to the slow log.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_sp_statements) for details.
          type: boolean
          format: boolean
        logSlowFilter:
          description: |-
            Filters the slow log by the query's execution plan.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_filter) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig8_0LogSlowFilterType'
        mdbPriorityChoiceMaxLag:
          description: |-
            Replication lag threshold (seconds) which allows replica to be promoted to master while executing "switchover from".
            Should be less than mdb_offline_mode_disable_lag.

            Acceptable values are 0 to 86400, inclusive.
          type: integer
          format: int64
        innodbPageSize:
          description: |-
            Specifies the page size for InnoDB tablespaces.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_page_size).

            Acceptable values are 4096 to 65536, inclusive.
          type: integer
          format: int64
        innodbOnlineAlterLogMaxSize:
          description: |-
            The limit in bytes on the size of the temporary log files used during online DDL operations

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_online_alter_log_max_size) for details.

            Acceptable values are 65536 to 107374182400, inclusive.
          type: integer
          format: int64
        innodbFtMinTokenSize:
          description: |-
            Minimum length of words that are stored in an InnoDB FULLTEXT index

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_ft_min_token_size) for details.

            Acceptable values are 0 to 16, inclusive.
          type: integer
          format: int64
        innodbFtMaxTokenSize:
          description: |-
            Maximum length of words that are stored in an InnoDB FULLTEXT index

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_ft_max_token_size) for details.

            Acceptable values are 10 to 84, inclusive.
          type: integer
          format: int64
        lowerCaseTableNames:
          description: |-
            Table names storage and comparison strategy

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_lower_case_table_names) for details.

            Acceptable values are 0 to 1, inclusive.
          type: integer
          format: int64
        maxSpRecursionDepth:
          description: |-
            The number of times that any given stored procedure may be called recursively.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_sp_recursion_depth).

            Acceptable values are 0 to 255, inclusive.
          type: integer
          format: int64
        innodbCompressionLevel:
          description: |-
            The level of zlib compression to use for InnoDB compressed tables and indexes.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_compression_level).

            Acceptable values are 0 to 9, inclusive.
          type: integer
          format: int64
        binlogTransactionDependencyTracking:
          $ref: '#/components/schemas/MysqlConfig8_0BinlogTransactionDependencyTracking_in_MysqlConfig8_0_binlogTransactionDependencyTracking'
    MysqlConfig8_0AuthPlugin:
      description: |2-
         - MYSQL_NATIVE_PASSWORD: Using [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/native-pluggable-authentication.html).
         - CACHING_SHA2_PASSWORD: Using [Caching SHA-2 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/caching-sha2-pluggable-authentication.html).
         - SHA256_PASSWORD: Using [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    MysqlConfig8_0BinlogRowImage:
      type: string
      enum:
      - FULL
      - MINIMAL
      - NOBLOB
    MysqlConfig8_0BinlogTransactionDependencyTracking:
      type: string
      enum:
      - COMMIT_ORDER
      - WRITESET
      - WRITESET_SESSION
    MysqlConfig8_0LogSlowFilterType:
      type: string
      enum:
      - FULL_SCAN
      - FULL_JOIN
      - TMP_TABLE
      - TMP_TABLE_ON_DISK
      - FILESORT
      - FILESORT_ON_DISK
    MysqlConfig8_0LogSlowRateType:
      type: string
      enum:
      - SESSION
      - QUERY
    MysqlConfig8_0SQLMode:
      type: string
      enum:
      - ALLOW_INVALID_DATES
      - ANSI_QUOTES
      - ERROR_FOR_DIVISION_BY_ZERO
      - HIGH_NOT_PRECEDENCE
      - IGNORE_SPACE
      - NO_AUTO_VALUE_ON_ZERO
      - NO_BACKSLASH_ESCAPES
      - NO_ENGINE_SUBSTITUTION
      - NO_UNSIGNED_SUBTRACTION
      - NO_ZERO_DATE
      - NO_ZERO_IN_DATE
      - ONLY_FULL_GROUP_BY
      - PAD_CHAR_TO_FULL_LENGTH
      - PIPES_AS_CONCAT
      - REAL_AS_FLOAT
      - STRICT_ALL_TABLES
      - STRICT_TRANS_TABLES
      - TIME_TRUNCATE_FRACTIONAL
      - ANSI
      - TRADITIONAL
      - NO_DIR_IN_CREATE
    MysqlConfig8_0SlaveParallelType:
      type: string
      enum:
      - DATABASE
      - LOGICAL_CLOCK
    MysqlConfig8_0TransactionIsolation:
      type: string
      enum:
      - READ_COMMITTED
      - REPEATABLE_READ
      - SERIALIZABLE
    MysqlConfigSet5_7:
      type: object
      properties:
        effectiveConfig:
          $ref: '#/components/schemas/MysqlConfig5_7_in_MysqlConfigSet5_7_effectiveConfig'
        userConfig:
          $ref: '#/components/schemas/MysqlConfig5_7_in_MysqlConfigSet5_7_userConfig'
        defaultConfig:
          $ref: '#/components/schemas/MysqlConfig5_7_in_MysqlConfigSet5_7_defaultConfig'
    MysqlConfigSet8_0:
      type: object
      properties:
        effectiveConfig:
          $ref: '#/components/schemas/MysqlConfig8_0_in_MysqlConfigSet8_0_effectiveConfig'
        userConfig:
          $ref: '#/components/schemas/MysqlConfig8_0_in_MysqlConfigSet8_0_userConfig'
        defaultConfig:
          $ref: '#/components/schemas/MysqlConfig8_0_in_MysqlConfigSet8_0_defaultConfig'
    Operation:
      description: An Operation resource. For more information, see [Operation](/docs/api-design-guide/concepts/operation).
      type: object
      allOf:
      - type: object
        properties:
          id:
            description: ID of the operation.
            type: string
          description:
            description: Description of the operation. 0-256 characters long.
            type: string
          createdAt:
            description: |-
              Creation timestamp.

              String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
            type: string
            format: date-time
          createdBy:
            description: ID of the user or service account who initiated the operation.
            type: string
          modifiedAt:
            description: |-
              The time when the Operation resource was last modified.

              String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
            type: string
            format: date-time
          done:
            description: |-
              If the value is `false`, it means the operation is still in progress.
              If `true`, the operation is completed, and either `error` or `response` is available.
            type: boolean
            format: boolean
          metadata:
            description: |-
              Service-specific metadata associated with the operation.
              It typically contains the ID of the target resource that the operation is performed on.
              Any method that returns a long-running operation should document the metadata type, if any.
            type: object
      - title: result
        type: object
        maxProperties: 1
        properties:
          error:
            $ref: '#/components/schemas/rpcStatus'
            description: The error result of the operation in case of failure or cancellation.
          response:
            description: |-
              The normal response of the operation in case of success.
              If the original method returns no data on success, such as Delete,
              the response is [google.protobuf.Empty](https://developers.google.com/protocol-buffers/docs/reference/google.protobuf#empty).
              If the original method is the standard Create/Update,
              the response should be the target resource of the operation.
              Any method that returns a long-running operation should document the response type, if any.
            type: object
    PerformanceDiagnostics:
      type: object
      properties:
        enabled:
          description: Flag that shows if performance statistics gathering is enabled
            for the cluster.
          type: boolean
          format: boolean
        sessionsSamplingInterval:
          description: |-
            Interval (in seconds) for `my_session` sampling.

            Acceptable values are 1 to 86400, inclusive.
          type: string
          format: int64
        statementsSamplingInterval:
          description: |-
            Interval (in seconds) for `my_statements` sampling.

            Acceptable values are 1 to 86400, inclusive.
          type: string
          format: int64
    Permission:
      type: object
      properties:
        databaseName:
          description: Name of the database that the permission grants access to.
          type: string
        roles:
          description: |-
            Required.
            Roles granted to the user within the database.

            See [the documentation](/docs/managed-mysql/operations/grant) for details.

            The minimum number of elements is 1.
          type: array
          items:
            $ref: '#/components/schemas/PermissionPrivilege'
    PermissionPrivilege:
      description: |2-
         - ALL_PRIVILEGES: All privileges that can be made available to the user.
         - ALTER: Altering tables.
         - ALTER_ROUTINE: Altering stored routines and functions.
         - CREATE: Creating tables or indexes.
         - CREATE_ROUTINE: Creating stored routines.
         - CREATE_TEMPORARY_TABLES: Creating temporary tables.
         - CREATE_VIEW: Creating views.
         - DELETE: Deleting tables.
         - DROP: Removing tables or views.
         - EVENT: Creating, altering, dropping, or displaying events for the Event Scheduler.
         - EXECUTE: Executing stored routines.
         - INDEX: Creating and removing indexes.
         - INSERT: Inserting rows into the database.
         - LOCK_TABLES: Using `LOCK TABLES` statement for tables available with `SELECT` privilege.
         - SELECT: Selecting rows from tables.

        Some `SELECT` statements can be allowed without the `SELECT` privilege. All statements that read column values require the `SELECT` privilege.

        See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/privileges-provided.html#priv_select) for details.
         - SHOW_VIEW: Using the `SHOW CREATE VIEW` statement. Also needed for views used with `EXPLAIN`.
         - TRIGGER: Creating, removing, executing, or displaying triggers for a table.
         - UPDATE: Updating rows in the database.
         - REFERENCES: Creation of a foreign key constraint for the parent table.
      type: string
      enum:
      - ALL_PRIVILEGES
      - ALTER
      - ALTER_ROUTINE
      - CREATE
      - CREATE_ROUTINE
      - CREATE_TEMPORARY_TABLES
      - CREATE_VIEW
      - DELETE
      - DROP
      - EVENT
      - EXECUTE
      - INDEX
      - INSERT
      - LOCK_TABLES
      - SELECT
      - SHOW_VIEW
      - TRIGGER
      - UPDATE
      - REFERENCES
    RescheduleMaintenanceMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster the maintenance operation is being rescheduled
            for.
          type: string
        delayedUntil:
          description: |-
            The time until which this maintenance operation is to be delayed.

            String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
          type: string
          format: date-time
    RescheduleMaintenanceRequest:
      type: object
      properties:
        rescheduleType:
          $ref: '#/components/schemas/RescheduleMaintenanceRequestRescheduleType_in_RescheduleMaintenanceRequest_rescheduleType'
        delayedUntil:
          description: |-
            The time until which this maintenance operation should be delayed.
            The value should be ahead of the first time when the maintenance operation has been scheduled for no more than two weeks.
            The value can also point to the past moment of time if `IMMEDIATE` reschedule type is chosen.

            String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
          type: string
          format: date-time
    RescheduleMaintenanceRequestRescheduleType:
      description: |2-
         - IMMEDIATE: Start the maintenance operation immediately.
         - NEXT_AVAILABLE_WINDOW: Start the maintenance operation within the next available maintenance window.
         - SPECIFIC_TIME: Start the maintenance operation at the specific time.
      type: string
      enum:
      - IMMEDIATE
      - NEXT_AVAILABLE_WINDOW
      - SPECIFIC_TIME
    ResourcePreset:
      description: |-
        An object that represents MySQL resource preset.
        A resource preset defines hardware configuration for cluster hosts.

        See [the documentation](/docs/managed-mysql/concepts/instance-types) for details.
      type: object
      properties:
        id:
          description: ID of the resource preset that defines available computational
            resources (vCPU, RAM, etc.) for a cluster host.
          type: string
        zoneIds:
          description: IDs of availability zones where the resource preset is available.
          type: array
          items:
            type: string
        cores:
          description: Number of CPU cores for a MySQL host created with the preset.
          type: string
          format: int64
        memory:
          description: RAM volume for a MySQL host created with the preset, in bytes.
          type: string
          format: int64
    Resources:
      description: Cluster resource preset.
      type: object
      properties:
        resourcePresetId:
          description: |-
            ID of the resource preset that defines available computational resources (vCPU, RAM, etc.) for a cluster host.

            All available presets are listed in [the documentation](/docs/managed-mysql/concepts/instance-types).
          type: string
        diskSize:
          description: Volume of the storage (for each cluster host, in bytes).
          type: string
          format: int64
        diskTypeId:
          description: |-
            Type of the storage.

            Possible values:
            * `network-hdd` - standard network storage
            * `network-ssd` - fast network storage
            * `network-ssd-nonreplicated` - fast network nonreplicated storage
            * `local-ssd` - fast local storage.

            See [the documentation](/docs/managed-mysql/concepts/storage) for details.
          type: string
    RestoreClusterMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the new cluster that is being created from a backup.
          type: string
        backupId:
          description: ID of the backup that is being used for creating a cluster.
          type: string
    RestoreClusterRequest:
      type: object
      properties:
        backupId:
          description: |-
            Required.
            ID of the backup to restore from.

            To get this ID, make a [list](/docs/managed-mysql/api-ref/Backup/list) request (lists all backups in a folder) or a [listBackups](/docs/managed-mysql/api-ref/Cluster/listBackups) request (lists all backups for an existing cluster).
          type: string
        time:
          description: |-
            Required.
            Timestamp of the moment to which the MySQL cluster should be restored.

            String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
          type: string
          format: date-time
        name:
          description: |-
            Required.
            Name of the new MySQL cluster the backup will be restored to. The name must be unique within the folder.

            Value must match the regular expression `` [a-zA-Z0-9_-]* ``.
          type: string
        description:
          description: |-
            Description of the new cluster.

            The maximum string length in characters is 256.
          type: string
        labels:
          description: |-
            Custom labels for the new cluster as `key:value` pairs.

            No more than 64 per resource.
            The string length in characters for each key must be 1-63.
            Each key must match the regular expression `` [a-z][-_0-9a-z]* ``.
            The maximum string length in characters for each value is 63.
            Each value must match the regular expression `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        environment:
          $ref: '#/components/schemas/ClusterEnvironment_in_RestoreClusterRequest_environment'
        configSpec:
          $ref: '#/components/schemas/ConfigSpec_in_RestoreClusterRequest_configSpec'
        hostSpecs:
          description: Configuration of hosts in the new cluster.
          type: array
          items:
            $ref: '#/components/schemas/HostSpec'
        networkId:
          description: |-
            ID of the network to create the new cluster in.

            The maximum string length in characters is 50.
          type: string
        folderId:
          description: |-
            ID of the folder to create the new cluster in.

            The maximum string length in characters is 50.
          type: string
        securityGroupIds:
          description: List of security group IDs to apply to the new cluster.
          type: array
          items:
            type: string
        deletionProtection:
          description: Deletion Protection inhibits deletion of the cluster
          type: boolean
          format: boolean
        hostGroupIds:
          description: Host groups hosting VMs of the cluster.
          type: array
          items:
            type: string
    RevokeUserPermissionMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster the user is being revoked a permission in.
          type: string
        userName:
          description: Name of the user whose permission is being revoked.
          type: string
    RevokeUserPermissionRequest:
      type: object
      properties:
        permission:
          $ref: '#/components/schemas/Permission_in_RevokeUserPermissionRequest_permission'
    Service:
      type: object
      properties:
        type:
          $ref: '#/components/schemas/ServiceType_in_Service_type'
        health:
          $ref: '#/components/schemas/ServiceHealth_in_Service_health'
    ServiceHealth:
      description: |2-
         - HEALTH_UNKNOWN: Health of the service is unknown.
         - ALIVE: The service is working normally.
         - DEAD: The service is dead or unresponsive.
         - READONLY: The service is in read-only mode.
      type: string
      enum:
      - HEALTH_UNKNOWN
      - ALIVE
      - DEAD
      - READONLY
      default: HEALTH_UNKNOWN
    ServiceType:
      description: ' - MYSQL: The host is a MySQL server.'
      type: string
      enum:
      - MYSQL
    StartClusterFailoverMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster that is being failovered.
          type: string
    StartClusterFailoverRequest:
      type: object
      properties:
        hostName:
          description: |-
            Host name to switch master role to.
            If not provided, then the master role is switched to the most up-to-date replica host.

            To get this name, make a [listHosts](/docs/managed-mysql/api-ref/Cluster/listHosts) request.

            The maximum string length in characters is 253.
          type: string
    StartClusterMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster that is being started.
          type: string
    StopClusterMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster that is being stopped.
          type: string
    StreamClusterLogsRequestServiceType:
      description: |2-
         - MYSQL_ERROR: MySQL error log.
         - MYSQL_GENERAL: MySQL general query log.
         - MYSQL_SLOW_QUERY: MySQL slow query log.
         - MYSQL_AUDIT: MySQL audit log.
      type: string
      enum:
      - MYSQL_ERROR
      - MYSQL_GENERAL
      - MYSQL_SLOW_QUERY
      - MYSQL_AUDIT
    StreamLogRecord:
      description: A single log record in the logs stream.
      type: object
      properties:
        record:
          $ref: '#/components/schemas/LogRecord_in_StreamLogRecord_record'
        nextRecordToken:
          description: |-
            The token that can be used to continue streaming logs starting from the exact same record.
            To continue streaming, specify value of `nextRecordToken` as the [recordToken](/docs/managed-mysql/api-ref/Cluster/streamLogs#query_params) value in the next [streamLogs](/docs/managed-mysql/api-ref/Cluster/streamLogs) request.

            This value is interchangeable with [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#responses) from [listLogs](/docs/managed-mysql/api-ref/Cluster/listLogs) method.
          type: string
    TimeOfDay:
      description: |-
        Represents a time of day. The date and time zone are either not significant
        or are specified elsewhere. An API may choose to allow leap seconds. Related
        types are [google.type.Date](https://github.com/googleapis/googleapis/blob/master/google/type/date.proto) and [google.protobuf.Timestamp](https://github.com/protocolbuffers/protobuf/blob/master/src/google/protobuf/timestamp.proto).
      type: object
      properties:
        hours:
          description: |-
            Hours of day in 24 hour format. Should be from 0 to 23. An API may choose
            to allow the value "24:00:00" for scenarios like business closing time.
          type: integer
          format: int32
        minutes:
          description: Minutes of hour of day. Must be from 0 to 59.
          type: integer
          format: int32
        seconds:
          description: |-
            Seconds of minutes of the time. Must normally be from 0 to 59. An API may
            allow the value 60 if it allows leap-seconds.
          type: integer
          format: int32
        nanos:
          description: Fractions of seconds in nanoseconds. Must be from 0 to 999,999,999.
          type: integer
          format: int32
    UpdateClusterHostsMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster in which the hosts are being updated.
          type: string
        hostNames:
          description: Names of hosts that are being updated.
          type: array
          items:
            type: string
    UpdateClusterHostsRequest:
      type: object
      properties:
        updateHostSpecs:
          description: |-
            Required.
            New configurations to apply to hosts.

            Must contain at least one element.
          type: array
          items:
            $ref: '#/components/schemas/UpdateHostSpec'
    UpdateClusterMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster that is being updated.
          type: string
    UpdateClusterRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateClusterRequest_updateMask'
        description:
          description: |-
            New description of the cluster.

            The maximum string length in characters is 256.
          type: string
        labels:
          description: |-
            New set of custom labels for the cluster as `key:value` pairs.

            This set will completely replace the current one.
            To add a label, request the current label set with the [get](/docs/managed-mysql/api-ref/Cluster/get) request, then send an [update](/docs/managed-mysql/api-ref/Cluster/update) request with the new label added to the current set.

            No more than 64 per resource.
            The string length in characters for each key must be 1-63.
            Each key must match the regular expression `` [a-z][-_0-9a-z]* ``.
            The maximum string length in characters for each value is 63.
            Each value must match the regular expression `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        configSpec:
          $ref: '#/components/schemas/ConfigSpec_in_UpdateClusterRequest_configSpec'
        name:
          description: |-
            New name of the cluster.

            The maximum string length in characters is 63.
            Value must match the regular expression `` [a-zA-Z0-9_-]* ``.
          type: string
        maintenanceWindow:
          $ref: '#/components/schemas/MaintenanceWindow_in_UpdateClusterRequest_maintenanceWindow'
        securityGroupIds:
          description: New list of security group IDs to apply to the cluster.
          type: array
          items:
            type: string
        deletionProtection:
          description: This option prevents unintended deletion of the cluster.
          type: boolean
          format: boolean
    UpdateHostSpec:
      type: object
      properties:
        hostName:
          description: |-
            Required.
            Name of the host to update.
            To get a MySQL host name, use a [listHosts](/docs/managed-mysql/api-ref/Cluster/listHosts) request.
          type: string
        replicationSource:
          description: |-
            `name` of the host to be used as the replication source (for cascading replication).
            To get a MySQL host name, use a [listHosts](/docs/managed-mysql/api-ref/Cluster/listHosts) request.
          type: string
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateHostSpec_updateMask'
        backupPriority:
          description: |-
            Host backup priority.

            Acceptable values are 0 to 100, inclusive.
          type: string
          format: int64
        assignPublicIp:
          description: Whether the host should get a public IP address on creation.
          type: boolean
          format: boolean
        priority:
          description: |-
            Host master promotion priority.

            Acceptable values are 0 to 100, inclusive.
          type: string
          format: int64
    UpdateUserMetadata:
      type: object
      properties:
        clusterId:
          description: ID of the cluster the user is being updated in.
          type: string
        userName:
          description: Name of the user that is being updated.
          type: string
    UpdateUserRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateUserRequest_updateMask'
        password:
          description: |-
            New password for the user.

            The string length in characters must be 8-128.
          type: string
        permissions:
          description: A new set of permissions that should be granted to the user.
          type: array
          items:
            $ref: '#/components/schemas/Permission'
        globalPermissions:
          description: New set of global permissions to grant to the user.
          type: array
          items:
            $ref: '#/components/schemas/GlobalPermission'
        connectionLimits:
          $ref: '#/components/schemas/ConnectionLimits_in_UpdateUserRequest_connectionLimits'
        authenticationPlugin:
          $ref: '#/components/schemas/v1AuthPlugin_in_UpdateUserRequest_authenticationPlugin'
    User:
      description: |-
        An object that represents MySQL user.

        See [the documentation](/docs/managed-mysql/operations/cluster-users) for details.
      type: object
      properties:
        name:
          description: Name of the user.
          type: string
        clusterId:
          description: ID of the cluster the user belongs to.
          type: string
        permissions:
          description: Set of permissions granted to the user.
          type: array
          items:
            $ref: '#/components/schemas/Permission'
        globalPermissions:
          description: Set of global permissions to grant to the user.
          type: array
          items:
            $ref: '#/components/schemas/GlobalPermission'
        connectionLimits:
          $ref: '#/components/schemas/ConnectionLimits_in_User_connectionLimits'
        authenticationPlugin:
          $ref: '#/components/schemas/v1AuthPlugin_in_User_authenticationPlugin'
    UserSpec:
      type: object
      properties:
        name:
          description: |-
            Required.
            Name of the user.

            The maximum string length in characters is 32.
            Value must match the regular expression `` [a-zA-Z0-9_]* ``.
          type: string
        password:
          description: |-
            Required.
            Password of the user.

            The string length in characters must be 8-128.
          type: string
        permissions:
          description: |-
            Set of permissions granted to the user to access specific databases.
            One permission per database.

            When a permission for a database is set, the user will have access to the database.
          type: array
          items:
            $ref: '#/components/schemas/Permission'
        globalPermissions:
          description: Set of global permissions to grant to the user.
          type: array
          items:
            $ref: '#/components/schemas/GlobalPermission'
        connectionLimits:
          $ref: '#/components/schemas/ConnectionLimits_in_UserSpec_connectionLimits'
        authenticationPlugin:
          $ref: '#/components/schemas/v1AuthPlugin_in_UserSpec_authenticationPlugin'
    WeeklyMaintenanceWindow:
      description: Weelky maintenance window settings.
      type: object
      properties:
        day:
          $ref: '#/components/schemas/WeeklyMaintenanceWindowWeekDay_in_WeeklyMaintenanceWindow_day'
        hour:
          description: |-
            Hour of the day in UTC (in `HH` format).

            Acceptable values are 1 to 24, inclusive.
          type: string
          format: int64
    WeeklyMaintenanceWindowWeekDay:
      type: string
      enum:
      - MON
      - TUE
      - WED
      - THU
      - FRI
      - SAT
      - SUN
    rpcStatus:
      description: The error result of the operation in case of failure or cancellation.
      type: object
      properties:
        code:
          description: Error code. An enum value of [google.rpc.Code](https://github.com/googleapis/googleapis/blob/master/google/rpc/code.proto).
          type: integer
          format: int32
        message:
          description: An error message.
          type: string
        details:
          description: A list of messages that carry the error details.
          type: array
          items:
            type: object
    v1AuthPlugin:
      description: |2-
         - MYSQL_NATIVE_PASSWORD: Use [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/native-pluggable-authentication.html).
         - CACHING_SHA2_PASSWORD: Use [Caching SHA-2 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/caching-sha2-pluggable-authentication.html).
         - SHA256_PASSWORD: Use [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    ClusterEnvironment_in_Cluster_environment:
      description: |-
        Deployment environment of the cluster.

         - PRODUCTION: Environment for stable versions of your apps.
        A conservative update policy is in effect: only bug fixes are applied during regular maintenance.
         - PRESTABLE: Environment for testing, including the Managed Service for MySQL itself.
        This environment gets new features, improvements, and bug fixes in the first place, compared to the production environment.
        However, not every update ensures backward compatibility.
      type: string
      enum:
      - PRODUCTION
      - PRESTABLE
    ClusterConfig_in_Cluster_config:
      description: Configuration of the cluster.
      type: object
      allOf:
      - type: object
        properties:
          version:
            description: Version of MySQL used in the cluster.
            type: string
          resources:
            $ref: '#/components/schemas/Resources'
            description: Resource preset for the cluster hosts.
          backupWindowStart:
            $ref: '#/components/schemas/TimeOfDay'
            description: Time to start the daily backup, in the UTC timezone.
          access:
            $ref: '#/components/schemas/Access'
            description: Access policy for external services.
          performanceDiagnostics:
            $ref: '#/components/schemas/PerformanceDiagnostics'
            description: Configuration of the performance diagnostics service.
          backupRetainPeriodDays:
            description: |-
              Retention policy of automated backups.

              Acceptable values are 7 to 60, inclusive.
            type: integer
            format: int64
      - title: mysql_config
        type: object
        maxProperties: 1
        properties:
          mysqlConfig_5_7:
            $ref: '#/components/schemas/MysqlConfigSet5_7'
            description: Configuration of a MySQL 5.7 server.
          mysqlConfig_8_0:
            $ref: '#/components/schemas/MysqlConfigSet8_0'
            description: Configuration of a MySQL 8.0 server.
    ClusterHealth_in_Cluster_health:
      description: |-
        Aggregated health of the cluster.

         - HEALTH_UNKNOWN: Health of the cluster is unknown (`health` for every host in the cluster is `UNKNOWN`).
         - ALIVE: Cluster is alive and well (`health` for every host in the cluster is `ALIVE`).
         - DEAD: Cluster is inoperable (`health` for every host in the cluster is `DEAD`).
         - DEGRADED: Cluster is degraded (`health` for at least one host in the cluster is not `ALIVE`).
      type: string
      enum:
      - HEALTH_UNKNOWN
      - ALIVE
      - DEAD
      - DEGRADED
      default: HEALTH_UNKNOWN
    ClusterStatus_in_Cluster_status:
      description: |-
        Current state of the cluster.

         - STATUS_UNKNOWN: Cluster state is unknown.
         - CREATING: Cluster is being created.
         - RUNNING: Cluster is running normally.
         - ERROR: Cluster encountered a problem and cannot operate.
         - UPDATING: Cluster is being updated.
         - STOPPING: Cluster is stopping.
         - STOPPED: Cluster is stopped.
         - STARTING: Cluster is starting.
      type: string
      enum:
      - STATUS_UNKNOWN
      - CREATING
      - RUNNING
      - ERROR
      - UPDATING
      - STOPPING
      - STOPPED
      - STARTING
      default: STATUS_UNKNOWN
    MaintenanceWindow_in_Cluster_maintenanceWindow:
      description: |-
        Maintenance window settings for the cluster.

        Configuration of a maintenance window in a MySQL cluster.
      type: object
      allOf:
      - title: policy
        type: object
        maxProperties: 1
        properties:
          anytime:
            $ref: '#/components/schemas/AnytimeMaintenanceWindow'
            description: Maintenance operation can be scheduled anytime.
          weeklyMaintenanceWindow:
            $ref: '#/components/schemas/WeeklyMaintenanceWindow'
            description: Maintenance operation can be scheduled on a weekly basis.
    MaintenanceOperation_in_Cluster_plannedOperation:
      description: |-
        Planned maintenance operation to be started for the cluster within the nearest [maintenanceWindow](/docs/managed-mysql/api-ref/Cluster#representation).

        A planned maintenance operation.
      type: object
      properties:
        info:
          description: |-
            Information about this maintenance operation.

            The maximum string length in characters is 256.
          type: string
        delayedUntil:
          description: |-
            Time until which this maintenance operation is delayed.

            String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
          type: string
          format: date-time
    ClusterEnvironment_in_CreateClusterRequest_environment:
      description: |-
        Deployment environment of the cluster.

         - PRODUCTION: Environment for stable versions of your apps.
        A conservative update policy is in effect: only bug fixes are applied during regular maintenance.
         - PRESTABLE: Environment for testing, including the Managed Service for MySQL itself.
        This environment gets new features, improvements, and bug fixes in the first place, compared to the production environment.
        However, not every update ensures backward compatibility.
      type: string
      enum:
      - PRODUCTION
      - PRESTABLE
    ConfigSpec_in_CreateClusterRequest_configSpec:
      description: Configuration of the cluster.
      type: object
      allOf:
      - type: object
        properties:
          version:
            description: |-
              Version of MySQL used in the cluster.

              Possible values: `5.7`, `8.0`.
            type: string
          resources:
            $ref: '#/components/schemas/Resources'
            description: Resource preset for the cluster hosts.
          backupWindowStart:
            $ref: '#/components/schemas/TimeOfDay'
            description: Time to start the daily backup, in the UTC timezone.
          access:
            $ref: '#/components/schemas/Access'
            description: |-
              Access policy for external services.

              If the specific services need to access the cluster, then set the necessary values in this policy.
          performanceDiagnostics:
            $ref: '#/components/schemas/PerformanceDiagnostics'
            description: Configuration of the performance diagnostics service.
          backupRetainPeriodDays:
            description: |-
              Retention policy of automated backups.

              Acceptable values are 7 to 60, inclusive.
            type: integer
            format: int64
      - title: mysql_config
        type: object
        maxProperties: 1
        properties:
          mysqlConfig_5_7:
            $ref: '#/components/schemas/MysqlConfig5_7'
            description: Configuration for a MySQL 5.7 cluster.
          mysqlConfig_8_0:
            $ref: '#/components/schemas/MysqlConfig8_0'
            description: Configuration for a MySQL 8.0 cluster.
    DatabaseSpec_in_CreateDatabaseRequest_databaseSpec:
      description: |-
        Required.
        Configuration of the database.
      type: object
      properties:
        name:
          description: |-
            Required.
            Name of the database.

            The maximum string length in characters is 63.
            Value must match the regular expression `` [a-zA-Z0-9_-]* ``.
          type: string
    UserSpec_in_CreateUserRequest_userSpec:
      description: |-
        Required.
        Configuration of the user.
      type: object
      properties:
        name:
          description: |-
            Required.
            Name of the user.

            The maximum string length in characters is 32.
            Value must match the regular expression `` [a-zA-Z0-9_]* ``.
          type: string
        password:
          description: |-
            Required.
            Password of the user.

            The string length in characters must be 8-128.
          type: string
        permissions:
          description: |-
            Set of permissions granted to the user to access specific databases.
            One permission per database.

            When a permission for a database is set, the user will have access to the database.
          type: array
          items:
            $ref: '#/components/schemas/Permission'
        globalPermissions:
          description: Set of global permissions to grant to the user.
          type: array
          items:
            $ref: '#/components/schemas/GlobalPermission'
        connectionLimits:
          $ref: '#/components/schemas/ConnectionLimits_in_UserSpec_connectionLimits'
        authenticationPlugin:
          $ref: '#/components/schemas/v1AuthPlugin_in_UserSpec_authenticationPlugin'
    Permission_in_GrantUserPermissionRequest_permission:
      description: |-
        Required.
        Permission that should be granted to the specified user.
      type: object
      properties:
        databaseName:
          description: Name of the database that the permission grants access to.
          type: string
        roles:
          description: |-
            Required.
            Roles granted to the user within the database.

            See [the documentation](/docs/managed-mysql/operations/grant) for details.

            The minimum number of elements is 1.
          type: array
          items:
            $ref: '#/components/schemas/PermissionPrivilege'
    Resources_in_Host_resources:
      description: |-
        Resources allocated to the host.

        Cluster resource preset.
      type: object
      properties:
        resourcePresetId:
          description: |-
            ID of the resource preset that defines available computational resources (vCPU, RAM, etc.) for a cluster host.

            All available presets are listed in [the documentation](/docs/managed-mysql/concepts/instance-types).
          type: string
        diskSize:
          description: Volume of the storage (for each cluster host, in bytes).
          type: string
          format: int64
        diskTypeId:
          description: |-
            Type of the storage.

            Possible values:
            * `network-hdd` - standard network storage
            * `network-ssd` - fast network storage
            * `network-ssd-nonreplicated` - fast network nonreplicated storage
            * `local-ssd` - fast local storage.

            See [the documentation](/docs/managed-mysql/concepts/storage) for details.
          type: string
    HostRole_in_Host_role:
      description: |-
        Role of the host in the cluster.

         - ROLE_UNKNOWN: Role of the host is unknown.
         - MASTER: Host is the master.
         - REPLICA: Host is a replica.
      type: string
      enum:
      - ROLE_UNKNOWN
      - MASTER
      - REPLICA
      default: ROLE_UNKNOWN
    HostHealth_in_Host_health:
      description: |-
        Aggregated health of the host.

         - HEALTH_UNKNOWN: Health of the host is unknown.
         - ALIVE: Host is performing all its functions normally.
         - DEAD: Host is inoperable, and cannot perform any of its essential functions.
         - DEGRADED: Host is degraded, and can perform only some of its essential functions.
         - READONLY: Host is alive, but in read-only mode.
      type: string
      enum:
      - HEALTH_UNKNOWN
      - ALIVE
      - DEAD
      - DEGRADED
      - READONLY
      default: HEALTH_UNKNOWN
    MysqlConfig5_7AuthPlugin_in_MysqlConfig5_7_defaultAuthenticationPlugin:
      description: |-
        Authentication plugin used in the managed MySQL cluster.

        See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_default_authentication_plugin for details.

         - MYSQL_NATIVE_PASSWORD: Using [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/5.7/en/native-pluggable-authentication.html).
         - SHA256_PASSWORD: Using [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/5.7/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    MysqlConfig5_7TransactionIsolation_in_MysqlConfig5_7_transactionIsolation:
      description: |-
        Default transaction isolation level.

        See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_transaction_isolation for details.
      type: string
      enum:
      - READ_COMMITTED
      - REPEATABLE_READ
      - SERIALIZABLE
    MysqlConfig5_7BinlogRowImage_in_MysqlConfig5_7_binlogRowImage:
      description: |-
        For MySQL row-based replication, this variable determines how row images are written to the binary log.

        See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_row_image) for details.
      type: string
      enum:
      - FULL
      - MINIMAL
      - NOBLOB
    MysqlConfig5_7SlaveParallelType_in_MysqlConfig5_7_slaveParallelType:
      description: |-
        When using a multi-threaded replica, this variable specifies the policy used to decide which transactions are allowed to execute in parallel on the replica.

        See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-replica.html#sysvar_slave_parallel_type) for details.
      type: string
      enum:
      - DATABASE
      - LOGICAL_CLOCK
    MysqlConfig5_7LogSlowRateType_in_MysqlConfig5_7_logSlowRateType:
      description: |-
        Specifies slow log granularity for `log_slow_rate_limit` values QUERY or SESSION.

        See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_rate_type) for details.
      type: string
      enum:
      - SESSION
      - QUERY
    MysqlConfig5_7BinlogTransactionDependencyTracking_in_MysqlConfig5_7_binlogTransactionDependencyTracking:
      description: |-
        Specifies how the source mysqld generates the dependency information that it writes in the binary log to help replicas determine which transactions can be executed in parallel.

        For details, see [MySQL documentation for the variabl](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_transaction_dependency_tracking).
      type: string
      enum:
      - COMMIT_ORDER
      - WRITESET
      - WRITESET_SESSION
    MysqlConfig8_0AuthPlugin_in_MysqlConfig8_0_defaultAuthenticationPlugin:
      description: |-
        Authentication plugin used in the managed MySQL cluster.

        See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_default_authentication_plugin for details.

         - MYSQL_NATIVE_PASSWORD: Using [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/native-pluggable-authentication.html).
         - CACHING_SHA2_PASSWORD: Using [Caching SHA-2 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/caching-sha2-pluggable-authentication.html).
         - SHA256_PASSWORD: Using [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    MysqlConfig8_0TransactionIsolation_in_MysqlConfig8_0_transactionIsolation:
      description: |-
        Default transaction isolation level.

        See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_transaction_isolation for details.
      type: string
      enum:
      - READ_COMMITTED
      - REPEATABLE_READ
      - SERIALIZABLE
    MysqlConfig8_0BinlogRowImage_in_MysqlConfig8_0_binlogRowImage:
      description: |-
        For MySQL row-based replication, this variable determines how row images are written to the binary log.

        See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_row_image) for details.
      type: string
      enum:
      - FULL
      - MINIMAL
      - NOBLOB
    MysqlConfig8_0SlaveParallelType_in_MysqlConfig8_0_slaveParallelType:
      description: |-
        When using a multi-threaded replica, this variable specifies the policy used to decide which transactions are allowed to execute in parallel on the replica.

        See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_slave_parallel_type) for details.
      type: string
      enum:
      - DATABASE
      - LOGICAL_CLOCK
    MysqlConfig8_0LogSlowRateType_in_MysqlConfig8_0_logSlowRateType:
      description: |-
        Specifies slow log granularity for `log_slow_rate_limit` QUERY or SESSION value.

        See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_rate_type) for details.
      type: string
      enum:
      - SESSION
      - QUERY
    MysqlConfig8_0BinlogTransactionDependencyTracking_in_MysqlConfig8_0_binlogTransactionDependencyTracking:
      description: |-
        Specifies how the source mysqld generates the dependency information that it writes in the binary log to help replicas determine which transactions can be executed in parallel.

        For details, see [MySQL documentation for the variabl](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_transaction_dependency_tracking).
      type: string
      enum:
      - COMMIT_ORDER
      - WRITESET
      - WRITESET_SESSION
    MysqlConfig5_7_in_MysqlConfigSet5_7_effectiveConfig:
      description: |-
        Effective settings for a MySQL 5.7 cluster (a combination of settings defined
        in `userConfig` and `defaultConfig`).

        Options and structure of `MysqlConfig5_7` reflects MySQL 5.7 configuration file.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Size of the InnoDB buffer pool used for caching table and index data.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size) for details.

            The minimum value is 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            The maximum permitted number of simultaneous client connections.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_connections) for details.

            Acceptable values are 10 to 16384, inclusive.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Time that it takes to process a query before it is considered slow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_long_query_time) for details.

            Acceptable values are 0 to 3600, inclusive.
          type: number
          format: double
        generalLog:
          description: |-
            Enable writing of general query log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_general_log) for details.
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Enable writing of audit log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/mysql-security-excerpt/5.6/en/audit-log-options-variables.html#option_mysqld_audit-log) for details.
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Server SQL mode of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/sql-mode.html#sql-mode-setting) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig5_7SQLMode'
        maxAllowedPacket:
          description: |-
            The maximum size in bytes of one packet.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_allowed_packet) for details.

            Acceptable values are 1024 to 1073741824, inclusive.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig5_7AuthPlugin_in_MysqlConfig5_7_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Transaction log flush behaviour.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Max time in seconds for a transaction to wait for a row lock.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout for details.

            Acceptable values are 1 to 28800, inclusive.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig5_7TransactionIsolation_in_MysqlConfig5_7_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Print information about deadlocks in error log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks for details.
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            The number of seconds to wait for more data from a connection before aborting the read.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_read_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            The number of seconds to wait for a block to be written to a connection before aborting the write.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_write_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            The maximum permitted result length in bytes for the GROUP_CONCAT() function.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_group_concat_max_len for details.

            Acceptable values are 4 to 33554432, inclusive.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            The maximum size of internal in-memory temporary tables.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_tmp_table_size for details.

            Acceptable values are 1024 to 536870912, inclusive.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            This variable sets the maximum size to which user-created MEMORY tables are permitted to grow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_heap_table_size for details.

            Acceptable values are 16384 to 536870912, inclusive.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            The servers default time zone.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-options.html#option_mysqld_default-time-zone for details.
          type: string
        characterSetServer:
          description: |-
            The servers default character set.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_character_set_server for details.
          type: string
        collationServer:
          description: |-
            The server default collation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_collation_server for details.
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Enables InnoDB adaptive hash index.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index for details.
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Enables the NUMA interleave memory policy for allocation of the InnoDB buffer pool.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_numa_interleave for details.
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            The size in bytes of the buffer that InnoDB uses to write to the log files on disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_buffer_size for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            The size in bytes of the single InnoDB Redo log file.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_file_size for details.

            Acceptable values are 268435456 to 4294967296, inclusive.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity_max for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            The number of I/O threads for read operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_read_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            The number of I/O threads for write operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_write_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            The number of background threads devoted to the InnoDB purge operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_purge_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Defines the maximum number of threads permitted inside of InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_thread_concurrency for details.

            Acceptable values are 0 to 1000, inclusive.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Limits the max size of InnoDB temp tablespace.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path for details.

            Acceptable values are 0 to 107374182400, inclusive.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            A number of threads the server should cache for reuse.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_cache_size) for details.

            Acceptable values are 10 to 10000, inclusive.
          type: integer
          format: int64
        threadStack:
          description: |-
            The stack size for each thread. The default is large enough for normal operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_stack) for details.

            Acceptable values are 131072 to 16777216, inclusive.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            The minimum size of the buffer that is used for plain index scans, range index scans, and joins that do not use indexes and thus perform full table scans.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_join_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Each session that must perform a sort allocates a buffer of this size.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_sort_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            The number of table definitions that can be stored in the definition cache.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_definition_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            The number of open tables for all threads.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            The number of open tables cache instances.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache_instances) for details.

            Acceptable values are 1 to 32, inclusive.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Determines whether the server enables certain nonstandard behaviors for default values and NULL-value handling in TIMESTAMP columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp) for details.
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_increment) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_offset) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Controls how often the MySQL server synchronizes the binary log to disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_sync_binlog) for details.

            Acceptable values are 0 to 4096, inclusive.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            The size of the cache to hold changes to the binary log during a transaction.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_cache_size) for details.

            Acceptable values are 4096 to 67108864, inclusive.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Controls how many microseconds the binary log commit waits before synchronizing the binary log file to disk.

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay) for details.

            Acceptable values are 0 to 50000, inclusive.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig5_7BinlogRowImage_in_MysqlConfig5_7_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            When enabled, it causes the server to write informational log events such as row query log events into its binary log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events) for details.
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            The number of replica acknowledgments the source must receive per transaction before proceeding.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count) for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig5_7SlaveParallelType_in_MysqlConfig5_7_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Sets the number of applier threads for executing replication transactions in parallel.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-replica.html#sysvar_slave_parallel_workers) for details.

            Acceptable values are 0 to 64, inclusive.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            The size of the binary log to hold.

            Acceptable values are 1073741824 to 1099511627776, inclusive.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            The number of seconds the server waits for activity on an interactive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_interactive_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            The number of seconds the server waits for activity on a noninteractive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_wait_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = ON' to prevent users from reading stale data.

            Acceptable values are 600 to 432000, inclusive.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = OFF'.
            Should be less than mdb_offline_mode_enable_lag value.

            Acceptable values are 60 to 86400, inclusive.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            The limit on memory consumption for the range optimizer.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size) for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        slowQueryLog:
          description: |-
            Manages slow query log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_slow_query_log) for details.
          type: boolean
          format: boolean
        slowQueryLogAlwaysWriteTime:
          description: |-
            Query execution time, after which query to be logged unconditionally, that is, `log_slow_rate_limit`` will not apply to it.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#slow_query_log_always_write_time) for details.
          type: number
          format: double
        logSlowRateType:
          $ref: '#/components/schemas/MysqlConfig5_7LogSlowRateType_in_MysqlConfig5_7_logSlowRateType'
        logSlowRateLimit:
          description: |-
            Specifies what fraction of session/query should be logged. Logging is enabled for every nth session/query.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_rate_limit) for details.

            Acceptable values are 1 to 1000, inclusive.
          type: integer
          format: int64
        logSlowSpStatements:
          description: |-
            When TRUE, statements executed by stored procedures are logged to the slow log.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_sp_statements) for details.
          type: boolean
          format: boolean
        logSlowFilter:
          description: |-
            Filters the slow log by the query's execution plan.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_filter) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig5_7LogSlowFilterType'
        mdbPriorityChoiceMaxLag:
          description: |-
            Replication lag threshold (seconds) which allows replica to be promoted to master while executing "switchover from".
            Should be less than mdb_offline_mode_disable_lag.

            Acceptable values are 0 to 86400, inclusive.
          type: integer
          format: int64
        innodbPageSize:
          description: |-
            Specifies the page size for InnoDB tablespaces.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_page_size).

            Acceptable values are 4096 to 65536, inclusive.
          type: integer
          format: int64
        innodbOnlineAlterLogMaxSize:
          description: |-
            The limit in bytes on the size of the temporary log files used during online DDL operations

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_online_alter_log_max_size).

            Acceptable values are 65536 to 107374182400, inclusive.
          type: integer
          format: int64
        innodbFtMinTokenSize:
          description: |-
            Minimum length of words that are stored in an InnoDB FULLTEXT index

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_ft_min_token_size).

            Acceptable values are 0 to 16, inclusive.
          type: integer
          format: int64
        innodbFtMaxTokenSize:
          description: |-
            Maximum length of words that are stored in an InnoDB FULLTEXT index

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_ft_max_token_size).

            Acceptable values are 10 to 84, inclusive.
          type: integer
          format: int64
        lowerCaseTableNames:
          description: |-
            Table names storage and comparison strategy

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_lower_case_table_names).

            Acceptable values are 0 to 1, inclusive.
          type: integer
          format: int64
        showCompatibility_56:
          description: |-
            Manages MySQL 5.6 compatibility

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_show_compatibility_56).
          type: boolean
          format: boolean
        maxSpRecursionDepth:
          description: |-
            The number of times that any given stored procedure may be called recursively.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_sp_recursion_depth).

            Acceptable values are 0 to 255, inclusive.
          type: integer
          format: int64
        innodbCompressionLevel:
          description: |-
            The level of zlib compression to use for InnoDB compressed tables and indexes.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_compression_level).

            Acceptable values are 0 to 9, inclusive.
          type: integer
          format: int64
        binlogTransactionDependencyTracking:
          $ref: '#/components/schemas/MysqlConfig5_7BinlogTransactionDependencyTracking_in_MysqlConfig5_7_binlogTransactionDependencyTracking'
    MysqlConfig5_7_in_MysqlConfigSet5_7_userConfig:
      description: |-
        User-defined settings for a MySQL 5.7 cluster.

        Options and structure of `MysqlConfig5_7` reflects MySQL 5.7 configuration file.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Size of the InnoDB buffer pool used for caching table and index data.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size) for details.

            The minimum value is 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            The maximum permitted number of simultaneous client connections.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_connections) for details.

            Acceptable values are 10 to 16384, inclusive.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Time that it takes to process a query before it is considered slow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_long_query_time) for details.

            Acceptable values are 0 to 3600, inclusive.
          type: number
          format: double
        generalLog:
          description: |-
            Enable writing of general query log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_general_log) for details.
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Enable writing of audit log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/mysql-security-excerpt/5.6/en/audit-log-options-variables.html#option_mysqld_audit-log) for details.
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Server SQL mode of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/sql-mode.html#sql-mode-setting) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig5_7SQLMode'
        maxAllowedPacket:
          description: |-
            The maximum size in bytes of one packet.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_allowed_packet) for details.

            Acceptable values are 1024 to 1073741824, inclusive.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig5_7AuthPlugin_in_MysqlConfig5_7_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Transaction log flush behaviour.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Max time in seconds for a transaction to wait for a row lock.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout for details.

            Acceptable values are 1 to 28800, inclusive.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig5_7TransactionIsolation_in_MysqlConfig5_7_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Print information about deadlocks in error log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks for details.
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            The number of seconds to wait for more data from a connection before aborting the read.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_read_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            The number of seconds to wait for a block to be written to a connection before aborting the write.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_write_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            The maximum permitted result length in bytes for the GROUP_CONCAT() function.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_group_concat_max_len for details.

            Acceptable values are 4 to 33554432, inclusive.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            The maximum size of internal in-memory temporary tables.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_tmp_table_size for details.

            Acceptable values are 1024 to 536870912, inclusive.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            This variable sets the maximum size to which user-created MEMORY tables are permitted to grow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_heap_table_size for details.

            Acceptable values are 16384 to 536870912, inclusive.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            The servers default time zone.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-options.html#option_mysqld_default-time-zone for details.
          type: string
        characterSetServer:
          description: |-
            The servers default character set.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_character_set_server for details.
          type: string
        collationServer:
          description: |-
            The server default collation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_collation_server for details.
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Enables InnoDB adaptive hash index.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index for details.
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Enables the NUMA interleave memory policy for allocation of the InnoDB buffer pool.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_numa_interleave for details.
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            The size in bytes of the buffer that InnoDB uses to write to the log files on disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_buffer_size for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            The size in bytes of the single InnoDB Redo log file.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_file_size for details.

            Acceptable values are 268435456 to 4294967296, inclusive.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity_max for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            The number of I/O threads for read operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_read_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            The number of I/O threads for write operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_write_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            The number of background threads devoted to the InnoDB purge operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_purge_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Defines the maximum number of threads permitted inside of InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_thread_concurrency for details.

            Acceptable values are 0 to 1000, inclusive.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Limits the max size of InnoDB temp tablespace.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path for details.

            Acceptable values are 0 to 107374182400, inclusive.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            A number of threads the server should cache for reuse.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_cache_size) for details.

            Acceptable values are 10 to 10000, inclusive.
          type: integer
          format: int64
        threadStack:
          description: |-
            The stack size for each thread. The default is large enough for normal operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_stack) for details.

            Acceptable values are 131072 to 16777216, inclusive.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            The minimum size of the buffer that is used for plain index scans, range index scans, and joins that do not use indexes and thus perform full table scans.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_join_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Each session that must perform a sort allocates a buffer of this size.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_sort_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            The number of table definitions that can be stored in the definition cache.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_definition_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            The number of open tables for all threads.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            The number of open tables cache instances.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache_instances) for details.

            Acceptable values are 1 to 32, inclusive.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Determines whether the server enables certain nonstandard behaviors for default values and NULL-value handling in TIMESTAMP columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp) for details.
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_increment) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_offset) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Controls how often the MySQL server synchronizes the binary log to disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_sync_binlog) for details.

            Acceptable values are 0 to 4096, inclusive.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            The size of the cache to hold changes to the binary log during a transaction.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_cache_size) for details.

            Acceptable values are 4096 to 67108864, inclusive.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Controls how many microseconds the binary log commit waits before synchronizing the binary log file to disk.

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay) for details.

            Acceptable values are 0 to 50000, inclusive.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig5_7BinlogRowImage_in_MysqlConfig5_7_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            When enabled, it causes the server to write informational log events such as row query log events into its binary log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events) for details.
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            The number of replica acknowledgments the source must receive per transaction before proceeding.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count) for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig5_7SlaveParallelType_in_MysqlConfig5_7_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Sets the number of applier threads for executing replication transactions in parallel.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-replica.html#sysvar_slave_parallel_workers) for details.

            Acceptable values are 0 to 64, inclusive.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            The size of the binary log to hold.

            Acceptable values are 1073741824 to 1099511627776, inclusive.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            The number of seconds the server waits for activity on an interactive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_interactive_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            The number of seconds the server waits for activity on a noninteractive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_wait_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = ON' to prevent users from reading stale data.

            Acceptable values are 600 to 432000, inclusive.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = OFF'.
            Should be less than mdb_offline_mode_enable_lag value.

            Acceptable values are 60 to 86400, inclusive.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            The limit on memory consumption for the range optimizer.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size) for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        slowQueryLog:
          description: |-
            Manages slow query log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_slow_query_log) for details.
          type: boolean
          format: boolean
        slowQueryLogAlwaysWriteTime:
          description: |-
            Query execution time, after which query to be logged unconditionally, that is, `log_slow_rate_limit`` will not apply to it.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#slow_query_log_always_write_time) for details.
          type: number
          format: double
        logSlowRateType:
          $ref: '#/components/schemas/MysqlConfig5_7LogSlowRateType_in_MysqlConfig5_7_logSlowRateType'
        logSlowRateLimit:
          description: |-
            Specifies what fraction of session/query should be logged. Logging is enabled for every nth session/query.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_rate_limit) for details.

            Acceptable values are 1 to 1000, inclusive.
          type: integer
          format: int64
        logSlowSpStatements:
          description: |-
            When TRUE, statements executed by stored procedures are logged to the slow log.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_sp_statements) for details.
          type: boolean
          format: boolean
        logSlowFilter:
          description: |-
            Filters the slow log by the query's execution plan.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_filter) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig5_7LogSlowFilterType'
        mdbPriorityChoiceMaxLag:
          description: |-
            Replication lag threshold (seconds) which allows replica to be promoted to master while executing "switchover from".
            Should be less than mdb_offline_mode_disable_lag.

            Acceptable values are 0 to 86400, inclusive.
          type: integer
          format: int64
        innodbPageSize:
          description: |-
            Specifies the page size for InnoDB tablespaces.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_page_size).

            Acceptable values are 4096 to 65536, inclusive.
          type: integer
          format: int64
        innodbOnlineAlterLogMaxSize:
          description: |-
            The limit in bytes on the size of the temporary log files used during online DDL operations

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_online_alter_log_max_size).

            Acceptable values are 65536 to 107374182400, inclusive.
          type: integer
          format: int64
        innodbFtMinTokenSize:
          description: |-
            Minimum length of words that are stored in an InnoDB FULLTEXT index

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_ft_min_token_size).

            Acceptable values are 0 to 16, inclusive.
          type: integer
          format: int64
        innodbFtMaxTokenSize:
          description: |-
            Maximum length of words that are stored in an InnoDB FULLTEXT index

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_ft_max_token_size).

            Acceptable values are 10 to 84, inclusive.
          type: integer
          format: int64
        lowerCaseTableNames:
          description: |-
            Table names storage and comparison strategy

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_lower_case_table_names).

            Acceptable values are 0 to 1, inclusive.
          type: integer
          format: int64
        showCompatibility_56:
          description: |-
            Manages MySQL 5.6 compatibility

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_show_compatibility_56).
          type: boolean
          format: boolean
        maxSpRecursionDepth:
          description: |-
            The number of times that any given stored procedure may be called recursively.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_sp_recursion_depth).

            Acceptable values are 0 to 255, inclusive.
          type: integer
          format: int64
        innodbCompressionLevel:
          description: |-
            The level of zlib compression to use for InnoDB compressed tables and indexes.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_compression_level).

            Acceptable values are 0 to 9, inclusive.
          type: integer
          format: int64
        binlogTransactionDependencyTracking:
          $ref: '#/components/schemas/MysqlConfig5_7BinlogTransactionDependencyTracking_in_MysqlConfig5_7_binlogTransactionDependencyTracking'
    MysqlConfig5_7_in_MysqlConfigSet5_7_defaultConfig:
      description: |-
        Default configuration for a MySQL 5.7 cluster.

        Options and structure of `MysqlConfig5_7` reflects MySQL 5.7 configuration file.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Size of the InnoDB buffer pool used for caching table and index data.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size) for details.

            The minimum value is 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            The maximum permitted number of simultaneous client connections.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_connections) for details.

            Acceptable values are 10 to 16384, inclusive.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Time that it takes to process a query before it is considered slow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_long_query_time) for details.

            Acceptable values are 0 to 3600, inclusive.
          type: number
          format: double
        generalLog:
          description: |-
            Enable writing of general query log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_general_log) for details.
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Enable writing of audit log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/mysql-security-excerpt/5.6/en/audit-log-options-variables.html#option_mysqld_audit-log) for details.
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Server SQL mode of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/sql-mode.html#sql-mode-setting) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig5_7SQLMode'
        maxAllowedPacket:
          description: |-
            The maximum size in bytes of one packet.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_allowed_packet) for details.

            Acceptable values are 1024 to 1073741824, inclusive.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig5_7AuthPlugin_in_MysqlConfig5_7_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Transaction log flush behaviour.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Max time in seconds for a transaction to wait for a row lock.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout for details.

            Acceptable values are 1 to 28800, inclusive.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig5_7TransactionIsolation_in_MysqlConfig5_7_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Print information about deadlocks in error log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks for details.
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            The number of seconds to wait for more data from a connection before aborting the read.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_read_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            The number of seconds to wait for a block to be written to a connection before aborting the write.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_write_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            The maximum permitted result length in bytes for the GROUP_CONCAT() function.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_group_concat_max_len for details.

            Acceptable values are 4 to 33554432, inclusive.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            The maximum size of internal in-memory temporary tables.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_tmp_table_size for details.

            Acceptable values are 1024 to 536870912, inclusive.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            This variable sets the maximum size to which user-created MEMORY tables are permitted to grow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_heap_table_size for details.

            Acceptable values are 16384 to 536870912, inclusive.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            The servers default time zone.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-options.html#option_mysqld_default-time-zone for details.
          type: string
        characterSetServer:
          description: |-
            The servers default character set.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_character_set_server for details.
          type: string
        collationServer:
          description: |-
            The server default collation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_collation_server for details.
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Enables InnoDB adaptive hash index.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index for details.
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Enables the NUMA interleave memory policy for allocation of the InnoDB buffer pool.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_numa_interleave for details.
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            The size in bytes of the buffer that InnoDB uses to write to the log files on disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_buffer_size for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            The size in bytes of the single InnoDB Redo log file.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_file_size for details.

            Acceptable values are 268435456 to 4294967296, inclusive.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity_max for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            The number of I/O threads for read operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_read_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            The number of I/O threads for write operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_write_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            The number of background threads devoted to the InnoDB purge operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_purge_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Defines the maximum number of threads permitted inside of InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_thread_concurrency for details.

            Acceptable values are 0 to 1000, inclusive.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Limits the max size of InnoDB temp tablespace.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path for details.

            Acceptable values are 0 to 107374182400, inclusive.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            A number of threads the server should cache for reuse.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_cache_size) for details.

            Acceptable values are 10 to 10000, inclusive.
          type: integer
          format: int64
        threadStack:
          description: |-
            The stack size for each thread. The default is large enough for normal operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_stack) for details.

            Acceptable values are 131072 to 16777216, inclusive.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            The minimum size of the buffer that is used for plain index scans, range index scans, and joins that do not use indexes and thus perform full table scans.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_join_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Each session that must perform a sort allocates a buffer of this size.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_sort_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            The number of table definitions that can be stored in the definition cache.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_definition_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            The number of open tables for all threads.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            The number of open tables cache instances.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache_instances) for details.

            Acceptable values are 1 to 32, inclusive.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Determines whether the server enables certain nonstandard behaviors for default values and NULL-value handling in TIMESTAMP columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp) for details.
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_increment) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_offset) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Controls how often the MySQL server synchronizes the binary log to disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_sync_binlog) for details.

            Acceptable values are 0 to 4096, inclusive.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            The size of the cache to hold changes to the binary log during a transaction.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_cache_size) for details.

            Acceptable values are 4096 to 67108864, inclusive.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Controls how many microseconds the binary log commit waits before synchronizing the binary log file to disk.

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay) for details.

            Acceptable values are 0 to 50000, inclusive.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig5_7BinlogRowImage_in_MysqlConfig5_7_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            When enabled, it causes the server to write informational log events such as row query log events into its binary log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events) for details.
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            The number of replica acknowledgments the source must receive per transaction before proceeding.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count) for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig5_7SlaveParallelType_in_MysqlConfig5_7_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Sets the number of applier threads for executing replication transactions in parallel.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/replication-options-replica.html#sysvar_slave_parallel_workers) for details.

            Acceptable values are 0 to 64, inclusive.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            The size of the binary log to hold.

            Acceptable values are 1073741824 to 1099511627776, inclusive.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            The number of seconds the server waits for activity on an interactive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_interactive_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            The number of seconds the server waits for activity on a noninteractive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_wait_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = ON' to prevent users from reading stale data.

            Acceptable values are 600 to 432000, inclusive.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = OFF'.
            Should be less than mdb_offline_mode_enable_lag value.

            Acceptable values are 60 to 86400, inclusive.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            The limit on memory consumption for the range optimizer.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size) for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        slowQueryLog:
          description: |-
            Manages slow query log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_slow_query_log) for details.
          type: boolean
          format: boolean
        slowQueryLogAlwaysWriteTime:
          description: |-
            Query execution time, after which query to be logged unconditionally, that is, `log_slow_rate_limit`` will not apply to it.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#slow_query_log_always_write_time) for details.
          type: number
          format: double
        logSlowRateType:
          $ref: '#/components/schemas/MysqlConfig5_7LogSlowRateType_in_MysqlConfig5_7_logSlowRateType'
        logSlowRateLimit:
          description: |-
            Specifies what fraction of session/query should be logged. Logging is enabled for every nth session/query.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_rate_limit) for details.

            Acceptable values are 1 to 1000, inclusive.
          type: integer
          format: int64
        logSlowSpStatements:
          description: |-
            When TRUE, statements executed by stored procedures are logged to the slow log.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_sp_statements) for details.
          type: boolean
          format: boolean
        logSlowFilter:
          description: |-
            Filters the slow log by the query's execution plan.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_filter) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig5_7LogSlowFilterType'
        mdbPriorityChoiceMaxLag:
          description: |-
            Replication lag threshold (seconds) which allows replica to be promoted to master while executing "switchover from".
            Should be less than mdb_offline_mode_disable_lag.

            Acceptable values are 0 to 86400, inclusive.
          type: integer
          format: int64
        innodbPageSize:
          description: |-
            Specifies the page size for InnoDB tablespaces.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_page_size).

            Acceptable values are 4096 to 65536, inclusive.
          type: integer
          format: int64
        innodbOnlineAlterLogMaxSize:
          description: |-
            The limit in bytes on the size of the temporary log files used during online DDL operations

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_online_alter_log_max_size).

            Acceptable values are 65536 to 107374182400, inclusive.
          type: integer
          format: int64
        innodbFtMinTokenSize:
          description: |-
            Minimum length of words that are stored in an InnoDB FULLTEXT index

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_ft_min_token_size).

            Acceptable values are 0 to 16, inclusive.
          type: integer
          format: int64
        innodbFtMaxTokenSize:
          description: |-
            Maximum length of words that are stored in an InnoDB FULLTEXT index

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_ft_max_token_size).

            Acceptable values are 10 to 84, inclusive.
          type: integer
          format: int64
        lowerCaseTableNames:
          description: |-
            Table names storage and comparison strategy

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_lower_case_table_names).

            Acceptable values are 0 to 1, inclusive.
          type: integer
          format: int64
        showCompatibility_56:
          description: |-
            Manages MySQL 5.6 compatibility

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_show_compatibility_56).
          type: boolean
          format: boolean
        maxSpRecursionDepth:
          description: |-
            The number of times that any given stored procedure may be called recursively.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_sp_recursion_depth).

            Acceptable values are 0 to 255, inclusive.
          type: integer
          format: int64
        innodbCompressionLevel:
          description: |-
            The level of zlib compression to use for InnoDB compressed tables and indexes.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_compression_level).

            Acceptable values are 0 to 9, inclusive.
          type: integer
          format: int64
        binlogTransactionDependencyTracking:
          $ref: '#/components/schemas/MysqlConfig5_7BinlogTransactionDependencyTracking_in_MysqlConfig5_7_binlogTransactionDependencyTracking'
    MysqlConfig8_0_in_MysqlConfigSet8_0_effectiveConfig:
      description: |-
        Effective settings for a MySQL 8.0 cluster (a combination of settings defined
        in `userConfig` and `defaultConfig`).

        Options and structure of `MysqlConfig8_0` reflects MySQL 8.0 configuration file.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Size of the InnoDB buffer pool used for caching table and index data.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size) for details.

            The minimum value is 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            The maximum permitted number of simultaneous client connections.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_connections) for details.

            Acceptable values are 10 to 16384, inclusive.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Time that it takes to process a query before it is considered slow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_long_query_time) for details.
          type: number
          format: double
        generalLog:
          description: |-
            Enable writing of general query log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_general_log) for details.
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Enable writing of audit log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/audit-log-reference.html#audit-log-options-variables) for details.
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Server SQL mode of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/sql-mode.html#sql-mode-setting) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig8_0SQLMode'
        maxAllowedPacket:
          description: |-
            The maximum size in bytes of one packet.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_allowed_packet) for details.

            Acceptable values are 1024 to 1073741824, inclusive.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig8_0AuthPlugin_in_MysqlConfig8_0_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Transaction log flush behaviour.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Max time in seconds for a transaction to wait for a row lock.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout for details.

            Acceptable values are 1 to 28800, inclusive.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig8_0TransactionIsolation_in_MysqlConfig8_0_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Print information about deadlocks in error log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks for details.
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            The number of seconds to wait for more data from a connection before aborting the read.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_read_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            The number of seconds to wait for a block to be written to a connection before aborting the write.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_write_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            The maximum permitted result length in bytes for the GROUP_CONCAT() function.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_group_concat_max_len for details.

            Acceptable values are 4 to 33554432, inclusive.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            The maximum size of internal in-memory temporary tables.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_tmp_table_size for details.

            Acceptable values are 1024 to 536870912, inclusive.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            This variable sets the maximum size to which user-created MEMORY tables are permitted to grow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_heap_table_size for details.

            Acceptable values are 16384 to 536870912, inclusive.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            The servers default time zone.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-options.html#option_mysqld_default-time-zone for details.
          type: string
        characterSetServer:
          description: |-
            The servers default character set.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_character_set_server for details.
          type: string
        collationServer:
          description: |-
            The server default collation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_collation_server for details.
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Enables InnoDB adaptive hash index.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index for details.
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Enables the NUMA interleave memory policy for allocation of the InnoDB buffer pool.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_numa_interleave for details.
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            The size in bytes of the buffer that InnoDB uses to write to the log files on disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_buffer_size for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            The size in bytes of the single InnoDB Redo log file.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_file_size for details.

            Acceptable values are 268435456 to 4294967296, inclusive.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity_max for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            The number of I/O threads for read operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_read_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            The number of I/O threads for write operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_write_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            The number of background threads devoted to the InnoDB purge operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_purge_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Defines the maximum number of threads permitted inside of InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_thread_concurrency for details.

            Acceptable values are 0 to 1000, inclusive.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Limits the max size of InnoDB temp tablespace.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path for details.

            Acceptable values are 0 to 107374182400, inclusive.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            How many threads the server should cache for reuse.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_cache_size) for details.

            Acceptable values are 10 to 10000, inclusive.
          type: integer
          format: int64
        threadStack:
          description: |-
            The stack size for each thread. The default is large enough for normal operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_stack) for details.

            Acceptable values are 131072 to 16777216, inclusive.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            The minimum size of the buffer that is used for plain index scans, range index scans, and joins that do not use indexes and thus perform full table scans.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_join_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Each session that must perform a sort allocates a buffer of this size.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_sort_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            The number of table definitions that can be stored in the definition cache.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_definition_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            The number of open tables for all threads.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            The number of open tables cache instances.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache_instances) for details.

            Acceptable values are 1 to 32, inclusive.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Determines whether the server enables certain nonstandard behaviors for default values and NULL-value handling in TIMESTAMP columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp) for details.
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_increment) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_offset) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Controls how often the MySQL server synchronizes the binary log to disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_sync_binlog) for details.

            Acceptable values are 0 to 4096, inclusive.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            The size of the cache to hold changes to the binary log during a transaction.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_cache_size) for details.

            Acceptable values are 4096 to 67108864, inclusive.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Controls how many microseconds the binary log commit waits before synchronizing the binary log file to disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay) for details.

            Acceptable values are 0 to 50000, inclusive.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig8_0BinlogRowImage_in_MysqlConfig8_0_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            When enabled, it causes the server to write informational log events such as row query log events into its binary log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events) for details.
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            The number of replica acknowledgments the source must receive per transaction before proceeding.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count) for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig8_0SlaveParallelType_in_MysqlConfig8_0_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Sets the number of applier threads for executing replication transactions in parallel.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_slave_parallel_workers) for details.

            Acceptable values are 0 to 64, inclusive.
          type: integer
          format: int64
        regexpTimeLimit:
          description: |-
            The time limit for regular expression matching operations performed by REGEXP_LIKE and similar functions.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_regexp_time_limit) for details.

            Acceptable values are 0 to 1048576, inclusive.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            The size of the binary log to hold.

            Acceptable values are 1073741824 to 1099511627776, inclusive.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            The number of seconds the server waits for activity on an interactive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_interactive_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            The number of seconds the server waits for activity on a noninteractive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_wait_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = ON' to prevent users from reading stale data.

            Acceptable values are 600 to 432000, inclusive.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = OFF'.
            Should be less than mdb_offline_mode_enable_lag.

            Acceptable values are 60 to 86400, inclusive.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            The limit on memory consumption for the range optimizer.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size) for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        slowQueryLog:
          description: |-
            Manages slow query log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_slow_query_log) for details.
          type: boolean
          format: boolean
        slowQueryLogAlwaysWriteTime:
          description: |-
            Query execution time, after which query to be logged unconditionally, that is, `log_slow_rate_limit` will not apply to it.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#slow_query_log_always_write_time) for details.
          type: number
          format: double
        logSlowRateType:
          $ref: '#/components/schemas/MysqlConfig8_0LogSlowRateType_in_MysqlConfig8_0_logSlowRateType'
        logSlowRateLimit:
          description: |-
            Specifies what fraction of session/query should be logged. Logging is enabled for every nth session/query.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_rate_limit) for details.

            Acceptable values are 1 to 1000, inclusive.
          type: integer
          format: int64
        logSlowSpStatements:
          description: |-
            When TRUE, statements executed by stored procedures are logged to the slow log.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_sp_statements) for details.
          type: boolean
          format: boolean
        logSlowFilter:
          description: |-
            Filters the slow log by the query's execution plan.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_filter) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig8_0LogSlowFilterType'
        mdbPriorityChoiceMaxLag:
          description: |-
            Replication lag threshold (seconds) which allows replica to be promoted to master while executing "switchover from".
            Should be less than mdb_offline_mode_disable_lag.

            Acceptable values are 0 to 86400, inclusive.
          type: integer
          format: int64
        innodbPageSize:
          description: |-
            Specifies the page size for InnoDB tablespaces.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_page_size).

            Acceptable values are 4096 to 65536, inclusive.
          type: integer
          format: int64
        innodbOnlineAlterLogMaxSize:
          description: |-
            The limit in bytes on the size of the temporary log files used during online DDL operations

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_online_alter_log_max_size) for details.

            Acceptable values are 65536 to 107374182400, inclusive.
          type: integer
          format: int64
        innodbFtMinTokenSize:
          description: |-
            Minimum length of words that are stored in an InnoDB FULLTEXT index

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_ft_min_token_size) for details.

            Acceptable values are 0 to 16, inclusive.
          type: integer
          format: int64
        innodbFtMaxTokenSize:
          description: |-
            Maximum length of words that are stored in an InnoDB FULLTEXT index

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_ft_max_token_size) for details.

            Acceptable values are 10 to 84, inclusive.
          type: integer
          format: int64
        lowerCaseTableNames:
          description: |-
            Table names storage and comparison strategy

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_lower_case_table_names) for details.

            Acceptable values are 0 to 1, inclusive.
          type: integer
          format: int64
        maxSpRecursionDepth:
          description: |-
            The number of times that any given stored procedure may be called recursively.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_sp_recursion_depth).

            Acceptable values are 0 to 255, inclusive.
          type: integer
          format: int64
        innodbCompressionLevel:
          description: |-
            The level of zlib compression to use for InnoDB compressed tables and indexes.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_compression_level).

            Acceptable values are 0 to 9, inclusive.
          type: integer
          format: int64
        binlogTransactionDependencyTracking:
          $ref: '#/components/schemas/MysqlConfig8_0BinlogTransactionDependencyTracking_in_MysqlConfig8_0_binlogTransactionDependencyTracking'
    MysqlConfig8_0_in_MysqlConfigSet8_0_userConfig:
      description: |-
        User-defined settings for a MySQL 8.0 cluster.

        Options and structure of `MysqlConfig8_0` reflects MySQL 8.0 configuration file.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Size of the InnoDB buffer pool used for caching table and index data.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size) for details.

            The minimum value is 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            The maximum permitted number of simultaneous client connections.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_connections) for details.

            Acceptable values are 10 to 16384, inclusive.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Time that it takes to process a query before it is considered slow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_long_query_time) for details.
          type: number
          format: double
        generalLog:
          description: |-
            Enable writing of general query log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_general_log) for details.
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Enable writing of audit log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/audit-log-reference.html#audit-log-options-variables) for details.
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Server SQL mode of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/sql-mode.html#sql-mode-setting) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig8_0SQLMode'
        maxAllowedPacket:
          description: |-
            The maximum size in bytes of one packet.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_allowed_packet) for details.

            Acceptable values are 1024 to 1073741824, inclusive.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig8_0AuthPlugin_in_MysqlConfig8_0_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Transaction log flush behaviour.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Max time in seconds for a transaction to wait for a row lock.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout for details.

            Acceptable values are 1 to 28800, inclusive.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig8_0TransactionIsolation_in_MysqlConfig8_0_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Print information about deadlocks in error log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks for details.
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            The number of seconds to wait for more data from a connection before aborting the read.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_read_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            The number of seconds to wait for a block to be written to a connection before aborting the write.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_write_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            The maximum permitted result length in bytes for the GROUP_CONCAT() function.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_group_concat_max_len for details.

            Acceptable values are 4 to 33554432, inclusive.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            The maximum size of internal in-memory temporary tables.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_tmp_table_size for details.

            Acceptable values are 1024 to 536870912, inclusive.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            This variable sets the maximum size to which user-created MEMORY tables are permitted to grow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_heap_table_size for details.

            Acceptable values are 16384 to 536870912, inclusive.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            The servers default time zone.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-options.html#option_mysqld_default-time-zone for details.
          type: string
        characterSetServer:
          description: |-
            The servers default character set.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_character_set_server for details.
          type: string
        collationServer:
          description: |-
            The server default collation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_collation_server for details.
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Enables InnoDB adaptive hash index.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index for details.
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Enables the NUMA interleave memory policy for allocation of the InnoDB buffer pool.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_numa_interleave for details.
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            The size in bytes of the buffer that InnoDB uses to write to the log files on disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_buffer_size for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            The size in bytes of the single InnoDB Redo log file.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_file_size for details.

            Acceptable values are 268435456 to 4294967296, inclusive.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity_max for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            The number of I/O threads for read operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_read_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            The number of I/O threads for write operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_write_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            The number of background threads devoted to the InnoDB purge operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_purge_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Defines the maximum number of threads permitted inside of InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_thread_concurrency for details.

            Acceptable values are 0 to 1000, inclusive.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Limits the max size of InnoDB temp tablespace.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path for details.

            Acceptable values are 0 to 107374182400, inclusive.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            How many threads the server should cache for reuse.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_cache_size) for details.

            Acceptable values are 10 to 10000, inclusive.
          type: integer
          format: int64
        threadStack:
          description: |-
            The stack size for each thread. The default is large enough for normal operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_stack) for details.

            Acceptable values are 131072 to 16777216, inclusive.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            The minimum size of the buffer that is used for plain index scans, range index scans, and joins that do not use indexes and thus perform full table scans.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_join_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Each session that must perform a sort allocates a buffer of this size.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_sort_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            The number of table definitions that can be stored in the definition cache.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_definition_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            The number of open tables for all threads.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            The number of open tables cache instances.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache_instances) for details.

            Acceptable values are 1 to 32, inclusive.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Determines whether the server enables certain nonstandard behaviors for default values and NULL-value handling in TIMESTAMP columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp) for details.
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_increment) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_offset) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Controls how often the MySQL server synchronizes the binary log to disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_sync_binlog) for details.

            Acceptable values are 0 to 4096, inclusive.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            The size of the cache to hold changes to the binary log during a transaction.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_cache_size) for details.

            Acceptable values are 4096 to 67108864, inclusive.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Controls how many microseconds the binary log commit waits before synchronizing the binary log file to disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay) for details.

            Acceptable values are 0 to 50000, inclusive.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig8_0BinlogRowImage_in_MysqlConfig8_0_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            When enabled, it causes the server to write informational log events such as row query log events into its binary log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events) for details.
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            The number of replica acknowledgments the source must receive per transaction before proceeding.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count) for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig8_0SlaveParallelType_in_MysqlConfig8_0_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Sets the number of applier threads for executing replication transactions in parallel.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_slave_parallel_workers) for details.

            Acceptable values are 0 to 64, inclusive.
          type: integer
          format: int64
        regexpTimeLimit:
          description: |-
            The time limit for regular expression matching operations performed by REGEXP_LIKE and similar functions.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_regexp_time_limit) for details.

            Acceptable values are 0 to 1048576, inclusive.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            The size of the binary log to hold.

            Acceptable values are 1073741824 to 1099511627776, inclusive.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            The number of seconds the server waits for activity on an interactive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_interactive_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            The number of seconds the server waits for activity on a noninteractive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_wait_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = ON' to prevent users from reading stale data.

            Acceptable values are 600 to 432000, inclusive.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = OFF'.
            Should be less than mdb_offline_mode_enable_lag.

            Acceptable values are 60 to 86400, inclusive.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            The limit on memory consumption for the range optimizer.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size) for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        slowQueryLog:
          description: |-
            Manages slow query log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_slow_query_log) for details.
          type: boolean
          format: boolean
        slowQueryLogAlwaysWriteTime:
          description: |-
            Query execution time, after which query to be logged unconditionally, that is, `log_slow_rate_limit` will not apply to it.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#slow_query_log_always_write_time) for details.
          type: number
          format: double
        logSlowRateType:
          $ref: '#/components/schemas/MysqlConfig8_0LogSlowRateType_in_MysqlConfig8_0_logSlowRateType'
        logSlowRateLimit:
          description: |-
            Specifies what fraction of session/query should be logged. Logging is enabled for every nth session/query.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_rate_limit) for details.

            Acceptable values are 1 to 1000, inclusive.
          type: integer
          format: int64
        logSlowSpStatements:
          description: |-
            When TRUE, statements executed by stored procedures are logged to the slow log.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_sp_statements) for details.
          type: boolean
          format: boolean
        logSlowFilter:
          description: |-
            Filters the slow log by the query's execution plan.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_filter) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig8_0LogSlowFilterType'
        mdbPriorityChoiceMaxLag:
          description: |-
            Replication lag threshold (seconds) which allows replica to be promoted to master while executing "switchover from".
            Should be less than mdb_offline_mode_disable_lag.

            Acceptable values are 0 to 86400, inclusive.
          type: integer
          format: int64
        innodbPageSize:
          description: |-
            Specifies the page size for InnoDB tablespaces.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_page_size).

            Acceptable values are 4096 to 65536, inclusive.
          type: integer
          format: int64
        innodbOnlineAlterLogMaxSize:
          description: |-
            The limit in bytes on the size of the temporary log files used during online DDL operations

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_online_alter_log_max_size) for details.

            Acceptable values are 65536 to 107374182400, inclusive.
          type: integer
          format: int64
        innodbFtMinTokenSize:
          description: |-
            Minimum length of words that are stored in an InnoDB FULLTEXT index

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_ft_min_token_size) for details.

            Acceptable values are 0 to 16, inclusive.
          type: integer
          format: int64
        innodbFtMaxTokenSize:
          description: |-
            Maximum length of words that are stored in an InnoDB FULLTEXT index

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_ft_max_token_size) for details.

            Acceptable values are 10 to 84, inclusive.
          type: integer
          format: int64
        lowerCaseTableNames:
          description: |-
            Table names storage and comparison strategy

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_lower_case_table_names) for details.

            Acceptable values are 0 to 1, inclusive.
          type: integer
          format: int64
        maxSpRecursionDepth:
          description: |-
            The number of times that any given stored procedure may be called recursively.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_sp_recursion_depth).

            Acceptable values are 0 to 255, inclusive.
          type: integer
          format: int64
        innodbCompressionLevel:
          description: |-
            The level of zlib compression to use for InnoDB compressed tables and indexes.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_compression_level).

            Acceptable values are 0 to 9, inclusive.
          type: integer
          format: int64
        binlogTransactionDependencyTracking:
          $ref: '#/components/schemas/MysqlConfig8_0BinlogTransactionDependencyTracking_in_MysqlConfig8_0_binlogTransactionDependencyTracking'
    MysqlConfig8_0_in_MysqlConfigSet8_0_defaultConfig:
      description: |-
        Default configuration for a MySQL 8.0 cluster.

        Options and structure of `MysqlConfig8_0` reflects MySQL 8.0 configuration file.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Size of the InnoDB buffer pool used for caching table and index data.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size) for details.

            The minimum value is 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            The maximum permitted number of simultaneous client connections.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_connections) for details.

            Acceptable values are 10 to 16384, inclusive.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Time that it takes to process a query before it is considered slow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_long_query_time) for details.
          type: number
          format: double
        generalLog:
          description: |-
            Enable writing of general query log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_general_log) for details.
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Enable writing of audit log of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/audit-log-reference.html#audit-log-options-variables) for details.
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Server SQL mode of MySQL.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/sql-mode.html#sql-mode-setting) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig8_0SQLMode'
        maxAllowedPacket:
          description: |-
            The maximum size in bytes of one packet.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_allowed_packet) for details.

            Acceptable values are 1024 to 1073741824, inclusive.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig8_0AuthPlugin_in_MysqlConfig8_0_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Transaction log flush behaviour.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Max time in seconds for a transaction to wait for a row lock.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout for details.

            Acceptable values are 1 to 28800, inclusive.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig8_0TransactionIsolation_in_MysqlConfig8_0_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Print information about deadlocks in error log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks for details.
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            The number of seconds to wait for more data from a connection before aborting the read.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_read_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            The number of seconds to wait for a block to be written to a connection before aborting the write.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_write_timeout for details.

            Acceptable values are 1 to 1200, inclusive.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            The maximum permitted result length in bytes for the GROUP_CONCAT() function.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_group_concat_max_len for details.

            Acceptable values are 4 to 33554432, inclusive.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            The maximum size of internal in-memory temporary tables.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_tmp_table_size for details.

            Acceptable values are 1024 to 536870912, inclusive.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            This variable sets the maximum size to which user-created MEMORY tables are permitted to grow.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_heap_table_size for details.

            Acceptable values are 16384 to 536870912, inclusive.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            The servers default time zone.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-options.html#option_mysqld_default-time-zone for details.
          type: string
        characterSetServer:
          description: |-
            The servers default character set.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_character_set_server for details.
          type: string
        collationServer:
          description: |-
            The server default collation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_collation_server for details.
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Enables InnoDB adaptive hash index.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index for details.
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Enables the NUMA interleave memory policy for allocation of the InnoDB buffer pool.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_numa_interleave for details.
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            The size in bytes of the buffer that InnoDB uses to write to the log files on disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_buffer_size for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            The size in bytes of the single InnoDB Redo log file.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_file_size for details.

            Acceptable values are 268435456 to 4294967296, inclusive.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Limits IO available for InnoDB background tasks.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity_max for details.

            Acceptable values are 100 to 100000, inclusive.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            The number of I/O threads for read operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_read_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            The number of I/O threads for write operations in InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_write_io_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            The number of background threads devoted to the InnoDB purge operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_purge_threads for details.

            Acceptable values are 1 to 16, inclusive.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Defines the maximum number of threads permitted inside of InnoDB.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_thread_concurrency for details.

            Acceptable values are 0 to 1000, inclusive.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Limits the max size of InnoDB temp tablespace.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path for details.

            Acceptable values are 0 to 107374182400, inclusive.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            How many threads the server should cache for reuse.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_cache_size) for details.

            Acceptable values are 10 to 10000, inclusive.
          type: integer
          format: int64
        threadStack:
          description: |-
            The stack size for each thread. The default is large enough for normal operation.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_stack) for details.

            Acceptable values are 131072 to 16777216, inclusive.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            The minimum size of the buffer that is used for plain index scans, range index scans, and joins that do not use indexes and thus perform full table scans.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_join_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Each session that must perform a sort allocates a buffer of this size.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_sort_buffer_size) for details.

            Acceptable values are 1024 to 16777216, inclusive.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            The number of table definitions that can be stored in the definition cache.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_definition_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            The number of open tables for all threads.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache) for details.

            Acceptable values are 400 to 524288, inclusive.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            The number of open tables cache instances.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache_instances) for details.

            Acceptable values are 1 to 32, inclusive.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Determines whether the server enables certain nonstandard behaviors for default values and NULL-value handling in TIMESTAMP columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp) for details.
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_increment) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Can be used to control the operation of AUTO_INCREMENT columns.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_offset) for details.

            Acceptable values are 1 to 65535, inclusive.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Controls how often the MySQL server synchronizes the binary log to disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_sync_binlog) for details.

            Acceptable values are 0 to 4096, inclusive.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            The size of the cache to hold changes to the binary log during a transaction.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_cache_size) for details.

            Acceptable values are 4096 to 67108864, inclusive.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Controls how many microseconds the binary log commit waits before synchronizing the binary log file to disk.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay) for details.

            Acceptable values are 0 to 50000, inclusive.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig8_0BinlogRowImage_in_MysqlConfig8_0_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            When enabled, it causes the server to write informational log events such as row query log events into its binary log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events) for details.
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            The number of replica acknowledgments the source must receive per transaction before proceeding.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count) for details.

            Acceptable values are 1 to 2, inclusive.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig8_0SlaveParallelType_in_MysqlConfig8_0_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Sets the number of applier threads for executing replication transactions in parallel.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_slave_parallel_workers) for details.

            Acceptable values are 0 to 64, inclusive.
          type: integer
          format: int64
        regexpTimeLimit:
          description: |-
            The time limit for regular expression matching operations performed by REGEXP_LIKE and similar functions.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_regexp_time_limit) for details.

            Acceptable values are 0 to 1048576, inclusive.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            The size of the binary log to hold.

            Acceptable values are 1073741824 to 1099511627776, inclusive.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            The number of seconds the server waits for activity on an interactive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_interactive_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            The number of seconds the server waits for activity on a noninteractive connection before closing it.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_wait_timeout) for details.

            Acceptable values are 600 to 86400, inclusive.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = ON' to prevent users from reading stale data.

            Acceptable values are 600 to 432000, inclusive.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Replication lag threshold (seconds) which will switch MySQL to 'offline_mode = OFF'.
            Should be less than mdb_offline_mode_enable_lag.

            Acceptable values are 60 to 86400, inclusive.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            The limit on memory consumption for the range optimizer.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size) for details.

            Acceptable values are 1048576 to 268435456, inclusive.
          type: integer
          format: int64
        slowQueryLog:
          description: |-
            Manages slow query log.

            See [MySQL documentation](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_slow_query_log) for details.
          type: boolean
          format: boolean
        slowQueryLogAlwaysWriteTime:
          description: |-
            Query execution time, after which query to be logged unconditionally, that is, `log_slow_rate_limit` will not apply to it.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#slow_query_log_always_write_time) for details.
          type: number
          format: double
        logSlowRateType:
          $ref: '#/components/schemas/MysqlConfig8_0LogSlowRateType_in_MysqlConfig8_0_logSlowRateType'
        logSlowRateLimit:
          description: |-
            Specifies what fraction of session/query should be logged. Logging is enabled for every nth session/query.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_rate_limit) for details.

            Acceptable values are 1 to 1000, inclusive.
          type: integer
          format: int64
        logSlowSpStatements:
          description: |-
            When TRUE, statements executed by stored procedures are logged to the slow log.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_sp_statements) for details.
          type: boolean
          format: boolean
        logSlowFilter:
          description: |-
            Filters the slow log by the query's execution plan.

            See [Percona documentation](https://www.percona.com/doc/percona-server/8.0/diagnostics/slow_extended.html#log_slow_filter) for details.
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig8_0LogSlowFilterType'
        mdbPriorityChoiceMaxLag:
          description: |-
            Replication lag threshold (seconds) which allows replica to be promoted to master while executing "switchover from".
            Should be less than mdb_offline_mode_disable_lag.

            Acceptable values are 0 to 86400, inclusive.
          type: integer
          format: int64
        innodbPageSize:
          description: |-
            Specifies the page size for InnoDB tablespaces.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_page_size).

            Acceptable values are 4096 to 65536, inclusive.
          type: integer
          format: int64
        innodbOnlineAlterLogMaxSize:
          description: |-
            The limit in bytes on the size of the temporary log files used during online DDL operations

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_online_alter_log_max_size) for details.

            Acceptable values are 65536 to 107374182400, inclusive.
          type: integer
          format: int64
        innodbFtMinTokenSize:
          description: |-
            Minimum length of words that are stored in an InnoDB FULLTEXT index

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_ft_min_token_size) for details.

            Acceptable values are 0 to 16, inclusive.
          type: integer
          format: int64
        innodbFtMaxTokenSize:
          description: |-
            Maximum length of words that are stored in an InnoDB FULLTEXT index

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_ft_max_token_size) for details.

            Acceptable values are 10 to 84, inclusive.
          type: integer
          format: int64
        lowerCaseTableNames:
          description: |-
            Table names storage and comparison strategy

            See [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_lower_case_table_names) for details.

            Acceptable values are 0 to 1, inclusive.
          type: integer
          format: int64
        maxSpRecursionDepth:
          description: |-
            The number of times that any given stored procedure may be called recursively.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_sp_recursion_depth).

            Acceptable values are 0 to 255, inclusive.
          type: integer
          format: int64
        innodbCompressionLevel:
          description: |-
            The level of zlib compression to use for InnoDB compressed tables and indexes.

            For details, see [MySQL documentation for the variable](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_compression_level).

            Acceptable values are 0 to 9, inclusive.
          type: integer
          format: int64
        binlogTransactionDependencyTracking:
          $ref: '#/components/schemas/MysqlConfig8_0BinlogTransactionDependencyTracking_in_MysqlConfig8_0_binlogTransactionDependencyTracking'
    RescheduleMaintenanceRequestRescheduleType_in_RescheduleMaintenanceRequest_rescheduleType:
      description: |-
        Required.
        The type of reschedule request.

         - IMMEDIATE: Start the maintenance operation immediately.
         - NEXT_AVAILABLE_WINDOW: Start the maintenance operation within the next available maintenance window.
         - SPECIFIC_TIME: Start the maintenance operation at the specific time.
      type: string
      enum:
      - IMMEDIATE
      - NEXT_AVAILABLE_WINDOW
      - SPECIFIC_TIME
    ClusterEnvironment_in_RestoreClusterRequest_environment:
      description: |-
        Deployment environment for the new cluster.

         - PRODUCTION: Environment for stable versions of your apps.
        A conservative update policy is in effect: only bug fixes are applied during regular maintenance.
         - PRESTABLE: Environment for testing, including the Managed Service for MySQL itself.
        This environment gets new features, improvements, and bug fixes in the first place, compared to the production environment.
        However, not every update ensures backward compatibility.
      type: string
      enum:
      - PRODUCTION
      - PRESTABLE
    ConfigSpec_in_RestoreClusterRequest_configSpec:
      description: Configuration of the new cluster.
      type: object
      allOf:
      - type: object
        properties:
          version:
            description: |-
              Version of MySQL used in the cluster.

              Possible values: `5.7`, `8.0`.
            type: string
          resources:
            $ref: '#/components/schemas/Resources'
            description: Resource preset for the cluster hosts.
          backupWindowStart:
            $ref: '#/components/schemas/TimeOfDay'
            description: Time to start the daily backup, in the UTC timezone.
          access:
            $ref: '#/components/schemas/Access'
            description: |-
              Access policy for external services.

              If the specific services need to access the cluster, then set the necessary values in this policy.
          performanceDiagnostics:
            $ref: '#/components/schemas/PerformanceDiagnostics'
            description: Configuration of the performance diagnostics service.
          backupRetainPeriodDays:
            description: |-
              Retention policy of automated backups.

              Acceptable values are 7 to 60, inclusive.
            type: integer
            format: int64
      - title: mysql_config
        type: object
        maxProperties: 1
        properties:
          mysqlConfig_5_7:
            $ref: '#/components/schemas/MysqlConfig5_7'
            description: Configuration for a MySQL 5.7 cluster.
          mysqlConfig_8_0:
            $ref: '#/components/schemas/MysqlConfig8_0'
            description: Configuration for a MySQL 8.0 cluster.
    Permission_in_RevokeUserPermissionRequest_permission:
      description: |-
        Required.
        Permission that should be revoked from the user.
      type: object
      properties:
        databaseName:
          description: Name of the database that the permission grants access to.
          type: string
        roles:
          description: |-
            Required.
            Roles granted to the user within the database.

            See [the documentation](/docs/managed-mysql/operations/grant) for details.

            The minimum number of elements is 1.
          type: array
          items:
            $ref: '#/components/schemas/PermissionPrivilege'
    ServiceType_in_Service_type:
      description: |-
        Type of the service provided by the host.

         - MYSQL: The host is a MySQL server.
      type: string
      enum:
      - MYSQL
    ServiceHealth_in_Service_health:
      description: |-
        Aggregated health of the service.

         - HEALTH_UNKNOWN: Health of the service is unknown.
         - ALIVE: The service is working normally.
         - DEAD: The service is dead or unresponsive.
         - READONLY: The service is in read-only mode.
      type: string
      enum:
      - HEALTH_UNKNOWN
      - ALIVE
      - DEAD
      - READONLY
      default: HEALTH_UNKNOWN
    LogRecord_in_StreamLogRecord_record:
      description: |-
        One of the requested log records.

        A single log record.
      type: object
      properties:
        timestamp:
          description: |-
            Timestamp of the log record.

            String in [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) text format.
          type: string
          format: date-time
        message:
          description: Contents of the log record.
          type: object
          additionalProperties:
            type: string
    FieldMask_in_UpdateClusterRequest_updateMask:
      description: |-
        Field mask that specifies which settings of the cluster should be updated.

        A comma-separated names off ALL fields to be updated.
        Only the specified fields will be changed. The others will be left untouched.
        If the field is specified in `` updateMask `` and no value for that field was sent in the request,
        the field's value will be reset to the default. The default value for most fields is null or 0.

        If `` updateMask `` is not sent in the request, all fields' values will be updated.
        Fields specified in the request will be updated to provided values.
        The rest of the fields will be reset to the default.
      type: string
    ConfigSpec_in_UpdateClusterRequest_configSpec:
      description: New configuration of the cluster.
      type: object
      allOf:
      - type: object
        properties:
          version:
            description: |-
              Version of MySQL used in the cluster.

              Possible values: `5.7`, `8.0`.
            type: string
          resources:
            $ref: '#/components/schemas/Resources'
            description: Resource preset for the cluster hosts.
          backupWindowStart:
            $ref: '#/components/schemas/TimeOfDay'
            description: Time to start the daily backup, in the UTC timezone.
          access:
            $ref: '#/components/schemas/Access'
            description: |-
              Access policy for external services.

              If the specific services need to access the cluster, then set the necessary values in this policy.
          performanceDiagnostics:
            $ref: '#/components/schemas/PerformanceDiagnostics'
            description: Configuration of the performance diagnostics service.
          backupRetainPeriodDays:
            description: |-
              Retention policy of automated backups.

              Acceptable values are 7 to 60, inclusive.
            type: integer
            format: int64
      - title: mysql_config
        type: object
        maxProperties: 1
        properties:
          mysqlConfig_5_7:
            $ref: '#/components/schemas/MysqlConfig5_7'
            description: Configuration for a MySQL 5.7 cluster.
          mysqlConfig_8_0:
            $ref: '#/components/schemas/MysqlConfig8_0'
            description: Configuration for a MySQL 8.0 cluster.
    MaintenanceWindow_in_UpdateClusterRequest_maintenanceWindow:
      description: |-
        Configuration of a maintenance window in an MySQL cluster.

        Configuration of a maintenance window in a MySQL cluster.
      type: object
      allOf:
      - title: policy
        type: object
        maxProperties: 1
        properties:
          anytime:
            $ref: '#/components/schemas/AnytimeMaintenanceWindow'
            description: Maintenance operation can be scheduled anytime.
          weeklyMaintenanceWindow:
            $ref: '#/components/schemas/WeeklyMaintenanceWindow'
            description: Maintenance operation can be scheduled on a weekly basis.
    FieldMask_in_UpdateHostSpec_updateMask:
      description: |-
        Field mask that specifies which settings of the MySQL host should be updated.

        A comma-separated names off ALL fields to be updated.
        Only the specified fields will be changed. The others will be left untouched.
        If the field is specified in `` updateMask `` and no value for that field was sent in the request,
        the field's value will be reset to the default. The default value for most fields is null or 0.

        If `` updateMask `` is not sent in the request, all fields' values will be updated.
        Fields specified in the request will be updated to provided values.
        The rest of the fields will be reset to the default.
      type: string
    FieldMask_in_UpdateUserRequest_updateMask:
      description: |-
        Field mask that specifies which settings of the user should be updated.

        A comma-separated names off ALL fields to be updated.
        Only the specified fields will be changed. The others will be left untouched.
        If the field is specified in `` updateMask `` and no value for that field was sent in the request,
        the field's value will be reset to the default. The default value for most fields is null or 0.

        If `` updateMask `` is not sent in the request, all fields' values will be updated.
        Fields specified in the request will be updated to provided values.
        The rest of the fields will be reset to the default.
      type: string
    ConnectionLimits_in_UpdateUserRequest_connectionLimits:
      description: Set of changed user connection limits.
      type: object
      properties:
        maxQuestionsPerHour:
          description: |-
            The maximum permitted number of user questions per hour.

            The minimum value is 0.
          type: integer
          format: int64
        maxUpdatesPerHour:
          description: |-
            The maximum permitted number of user updates per hour.

            The minimum value is 0.
          type: integer
          format: int64
        maxConnectionsPerHour:
          description: |-
            The maximum permitted number of simultaneous client connections per hour.

            The minimum value is 0.
          type: integer
          format: int64
        maxUserConnections:
          description: |-
            The maximum number of simultaneous connections permitted to any given MySQL user account.

            The minimum value is 0.
          type: integer
          format: int64
    v1AuthPlugin_in_UpdateUserRequest_authenticationPlugin:
      description: |-
        New user authentication plugin.

         - MYSQL_NATIVE_PASSWORD: Use [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/native-pluggable-authentication.html).
         - CACHING_SHA2_PASSWORD: Use [Caching SHA-2 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/caching-sha2-pluggable-authentication.html).
         - SHA256_PASSWORD: Use [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    ConnectionLimits_in_User_connectionLimits:
      description: Set of user connection limits.
      type: object
      properties:
        maxQuestionsPerHour:
          description: |-
            The maximum permitted number of user questions per hour.

            The minimum value is 0.
          type: integer
          format: int64
        maxUpdatesPerHour:
          description: |-
            The maximum permitted number of user updates per hour.

            The minimum value is 0.
          type: integer
          format: int64
        maxConnectionsPerHour:
          description: |-
            The maximum permitted number of simultaneous client connections per hour.

            The minimum value is 0.
          type: integer
          format: int64
        maxUserConnections:
          description: |-
            The maximum number of simultaneous connections permitted to any given MySQL user account.

            The minimum value is 0.
          type: integer
          format: int64
    v1AuthPlugin_in_User_authenticationPlugin:
      description: |-
        User authentication plugin.

         - MYSQL_NATIVE_PASSWORD: Use [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/native-pluggable-authentication.html).
         - CACHING_SHA2_PASSWORD: Use [Caching SHA-2 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/caching-sha2-pluggable-authentication.html).
         - SHA256_PASSWORD: Use [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    ConnectionLimits_in_UserSpec_connectionLimits:
      description: Set of user connection limits.
      type: object
      properties:
        maxQuestionsPerHour:
          description: |-
            The maximum permitted number of user questions per hour.

            The minimum value is 0.
          type: integer
          format: int64
        maxUpdatesPerHour:
          description: |-
            The maximum permitted number of user updates per hour.

            The minimum value is 0.
          type: integer
          format: int64
        maxConnectionsPerHour:
          description: |-
            The maximum permitted number of simultaneous client connections per hour.

            The minimum value is 0.
          type: integer
          format: int64
        maxUserConnections:
          description: |-
            The maximum number of simultaneous connections permitted to any given MySQL user account.

            The minimum value is 0.
          type: integer
          format: int64
    v1AuthPlugin_in_UserSpec_authenticationPlugin:
      description: |-
        User authentication plugin.

         - MYSQL_NATIVE_PASSWORD: Use [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/native-pluggable-authentication.html).
         - CACHING_SHA2_PASSWORD: Use [Caching SHA-2 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/caching-sha2-pluggable-authentication.html).
         - SHA256_PASSWORD: Use [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    WeeklyMaintenanceWindowWeekDay_in_WeeklyMaintenanceWindow_day:
      description: Day of the week (in `DDD` format).
      type: string
      enum:
      - MON
      - TUE
      - WED
      - THU
      - FRI
      - SAT
      - SUN
tags:
- name: Backup
  description: |-
    A set of methods for managing MySQL backups.

    See [the documentation](/docs/managed-mysql/operations/cluster-backups) for details.
- name: Cluster
  description: A set of methods for managing MySQL clusters.
- name: Database
  description: |-
    A set of methods for managing MySQL databases in a cluster.

    See [the documentation](/docs/managed-mysql/operations/databases) for details.
- name: ResourcePreset
  description: |-
    A set of methods for managing MySQL resource presets.

    See [the documentation](/docs/managed-mysql/concepts/instance-types) for details.
- name: User
  description: |-
    A set of methods for managing MySQL users.

    See [the documentation](/docs/managed-mysql/operations/cluster-users) for details.

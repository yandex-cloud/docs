# Создать колонку

Запрос позволяет создать колонку на доске задач.

## Формат запроса {#section_e4q_ptd_pfb}

Чтобы создать колонку на доске задач, используйте HTTP-запрос с методом `POST`:

```json
POST /{{ ver }}/boards/<board-id>/columns/
Host: {{ host }}
Authorization: OAuth <токен>
{{ org-id }}
If-Match: "<номер версии>"

{
  "name": "Согласовать",
  "statuses": 
      [
        "needInfo", "adjustment",...
      ]
}
```

{% include [headings](../_includes/tracker/api/headings.md) %}

- **If-Match**

    Номер текущей версии доски. При указании неактуальной версии доски в ответ на HTTP-запрос придет ответ с ошибкой `412 Precondition Failed`.

#### Ресурс {#req-resource}

- **\<board-id\>**

    Идентификатор доски.



#### Тело запроса {#req-body-params}

Тело запроса содержит обязательные параметры колонки.

Параметр | Описание | Тип данных
-------- | -------- | -----
name | Название колонки.<br/>Обязательный параметр. | Строка.
statuses | Массив содержит ключи возможных статусов задач, которые попадут в колонку.<br/>Обязательный параметр.<br/>Список всех статусов задачи: [{{ link-tracker-statuses }}]({{ link-tracker-statuses }}) | Массив.

## Формат ответа {#section_chg_hj2_pfb}

{% list tabs %}

- Запрос выполнен успешно

    В случае успешного выполнения запроса API возвращает ответ с кодом 200. Тело ответа содержит JSON-объект с параметрами созданной колонки.

    ```json
    {
        "self": "https://api.tracker.yandex.net/v2/boards/73/columns/5",
        "id": 5,
        "name": "Согласовать",
        "statuses":
         [
            {
               "self": "https://api.tracker.yandex.net/v2/statuses/2",
               "id": "2",
               "key": "needInfo",
               "display": "Требуется информация"
            },
            ...
          ]
    }
    ```

    #### Параметры ответа {#answer-params}

    Параметр | Описание | Тип данных
    -------- | -------- | ----------
    self | Адрес ресурса API, который содержит информацию о колонке доски. | Строка.
    id | Идентификатор колонки. | Число.
    name | Название колонки. | Строка.
    [statuses](#statuses) | Массив содержит ключи возможных статусов задач, которые попадут в колонку.<br/>Список всех статусов задачи: [{{ link-tracker-statuses }}]({{ link-tracker-statuses }}) | Массив.

    **Поля объекта** `statuses` {#statuses}

    Параметр | Описание | Тип данных
    -------- | -------- | ----------
    self | Адрес ресурса API, который содержит информацию о статусе. | Строка.
    id | Идентификатор статуса. | Строка.
    key | Ключ статуса. | Строка.
    display | Отображаемое название статуса. | Строка.

- Запрос выполнен с ошибкой

    Если запрос не был успешно обработан, ответное сообщение содержит информацию о возникших ошибках:

    HTTP-код ошибки | Описание ошибки
    --------------- | ---------------
    400 Bad Request | Один из параметров запроса имеет недопустимое значение или формат данных.
    403 Forbidden | У пользователя или приложения нет прав на доступ к ресурсу, запрос отклонен.
    404 Not Found | Запрашиваемый ресурс не найден.
    409 Conflict | Запрос не может быть выполнен по причине конфликта имен.
    412 Precondition Failed | Некорректное значение заголовка.
    422 Unprocessable Entity | Ошибка валидации JSON, запрос отклонен.
    428 Precondition Required | Требуется заголовок.
    500 Internal Server Error | Внутренняя ошибка сервиса. Попробуйте повторно отправить запрос через некоторое время.
    503 Service Unavailable | Сервис API временно недоступен.

{% endlist %}

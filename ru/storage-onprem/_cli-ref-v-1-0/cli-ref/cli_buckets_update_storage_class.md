# cli buckets update storage_class

Update bucket storage_class

```
cli buckets update storage_class <bucket_name> [flags]
```

## Options

```
  -h, --help                   help for storage_class
  -s, --storage_class string   Storage class
  -t, --tenant string          Tenant ID
```

## Options inherited from parent commands

```
  -c, --config-dir string   path to configuration directory
      --debug               enable debug mode
      --insecure            use if console has self-signed certificate
  -p, --profile string      configuration profile
```

## See also

* [cli buckets update](cli_buckets_update.md)	 — Update bucket settings


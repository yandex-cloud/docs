---
editable: false
---

# Правила тарификации для {{ monitoring-full-name }}

{% if product == "cloud-il" %}

{% note info %}

Цены, указанные ниже, начнут действовать 6 декабря 2022 года. До этого использование сервиса {{ monitoring-short-name }} не тарифицируется

{% endnote %}

{% endif %}

## Из чего складывается стоимость использования {{ monitoring-short-name }} {#rules}

На данный момент в рамках сервиса {{ monitoring-short-name }} тарифицируется запись пользовательских метрик через [API](api-ref/index.md), а также чтение любых метрик через [API](api-ref/index.md).

Особенности тарификации:
* После записи или чтения первых 50 млн значений стоимость записи снижается. См. [Цены](#prices).
* Запись метрик ресурсов {{ yandex-cloud }}, собираемых автоматически, не тарифицируется.
* Чтение метрик при помощи интерфейса {{ monitoring-short-name }} и при помощи консоли {{ yandex-cloud }} не тарифицируется.
* Входящий и исходящий трафик в {{ monitoring-short-name }} не тарифицируются.

### Пример расчета стоимости {#example}

Стоимость пользования сервисом {{ monitoring-short-name }} в течение 30 дней при записи 20 метрик с частотой **1 значение в минуту**:

{% if product == "yandex-cloud" %}

> 20 × 1 × (60 × 24 × 30) = 864 000 значений = 0,864 млн значений
> {% if region == "ru" %}0,864 × 9,80 ₽ = 8,4672 ₽ = 8,47 ₽{% endif %}
> {% if region == "kz" %}0,864 × 49,00 ₸ = 42,336 ₸ = 42,34 ₸{% endif %}
> {% if region == "int" %}0,864 × 0,0784 $ = 0,0677376 $ = 0,07 ${% endif %}
> 
> Итого: {% if region == "ru" %}8,47 ₽{% endif %}{% if region == "kz" %}42,34 ₸{% endif %}{% if region == "int" %}0,07 ${% endif %}

{% endif %}

{% if product == "cloud-il" %}

> 20 × 1 × (60 × 24 × 30) = 864 000 значений = 0,864 млн значений
> 0,864 × 0,4976 ₪ = 0,4299 ₪ = 0,43 ₪
>
> Итого: 0,43 ₪

{% endif %}

Где:

* 20 — количество метрик.
* 1 — количество записанных значений в минуту.
* (60 × 24 × 30) — количество минут в 30 днях.
* {% if product == "yandex-cloud" %}{% if region == "ru" %}9,80 ₽{% endif %}{% if region == "kz" %}49,00 ₸{% endif %}{% if region == "int" %}0,0784 ${% endif %}{% endif %}{% if product == "cloud-il" %}0,4976 ₪{% endif %} — стоимость записи 1 млн значений (до 50 млн значений).

Стоимость пользования сервисом {{ monitoring-short-name }} в течение 30 дней при записи 20 метрик с частотой **1 значение в секунду**:

{% if product == "yandex-cloud" %}

> 20 × 1 × (60 × 60 × 24 × 30) = 51 840 000 значений = 51,84 млн значений
> {% if region == "ru" %}50 × 9,80 ₽ + (51,84 - 50) × 5,60 ₽ = 500,30 ₽{% endif %}
> {% if region == "kz" %}50 × 49,00 ₸ + (51,84 - 50) × 28,00 ₸ = 2501,52 ₸{% endif %}
> {% if region == "int" %}50 × 0,0784 $ + (51,84 - 50) × 0,0448 $ = 4,00 ${% endif %}
> 
> Итого: {% if region == "ru" %}500,30 ₽{% endif %}{% if region == "kz" %}2501,52 ₸{% endif %}{% if region == "int" %}4,00 ${% endif %}

{% endif %}

{% if product == "cloud-il" %}

> 20 × 1 × (60 × 60 × 24 × 30) = 51 840 000 значений = 51,84 млн значений
> 50 × 0,4976 ₪ + (51,84 - 50) × 0,2844 ₪ = 25,40 ₪
>
> Итого: 25,40 ₪

{% endif %}

Где:

* 20 — количество метрик.
* 1 — количество записанных значений в секунду.
* (60 × 60 × 24 × 30) — количество секунд в 30 днях.
* {% if product == "yandex-cloud" %}{% if region == "ru" %}9,80 ₽{% endif %}{% if region == "kz" %}49,00 ₸{% endif %}{% if region == "int" %}0,0784 ${% endif %}{% endif %}{% if product == "cloud-il" %}0,4976 ₪{% endif %} — стоимость записи 1 млн значений (до 50 млн значений).
* {% if product == "yandex-cloud" %}{% if region == "ru" %}5,60 ₽{% endif %}{% if region == "kz" %}28,00 ₸{% endif %}{% if region == "int" %}0,0448 ${% endif %}{% endif %}{% if product == "cloud-il" %}0,2844 ₪{% endif %} — стоимость записи 1 млн значений (сверх 50 млн значений).

Стоимость экспорта 100 метрик из {{ monitoring-short-name }} в собственную инсталляцию системы мониторинга {{ prometheus-name }} с периодом сбора **15 секунд** в течение 30 дней:

{% if product == "yandex-cloud" %}

> 100 × (60 / 15) × (60 × 24 × 30) = 17 280 000 значений = 17,28 млн значений
> {% if region == "ru" %}17,28 × 7,00 ₽ = 120,96 ₽{% endif %}
> {% if region == "kz" %}17,28 × 35,00 ₸ = 604,80 ₸{% endif %}
> {% if region == "int" %}17,28 × 0,0560 $ = 0,97 ${% endif %}
> 
> Итого: {% if region == "ru" %}120,96 ₽{% endif %}{% if region == "kz" %}604,80 ₸{% endif %}{% if region == "int" %}0,97 ${% endif %}

{% endif %}

{% if product == "cloud-il" %}

> 100 × (60 / 15) × (60 × 24 × 30) = 17 280 000 значений = 17,28 млн значений
> 17,28 × 0,3554 ₪ = 6,14 ₪
>
> Итого: 6,14 ₪

{% endif %}

Где:

* 100 — количество метрик.
* (60 / 15) — количество считанных значений в минуту.
* (60 × 24 × 30) — количество минут в 30 днях.
* {% if product == "yandex-cloud" %}{% if region == "ru" %}7,00 ₽{% endif %}{% if region == "kz" %}35,00 ₸{% endif %}{% if region == "int" %}0,0560 ${% endif %}{% endif %}{% if product == "cloud-il" %}0,3554 ₪{% endif %} — стоимость чтения 1 млн значений (до 50 млн значений).

## Цены {% if product == "cloud-il" %}c 6 декабря 2022 года{% endif %} {#prices}

{% if product == "yandex-cloud" %}

{% if region == "ru" %}

Минимальная единица тарификации — 1 значение метрики. Стоимость округляется до копейки.

Например, стоимость записи первых 86 400 значений составит `(86 400 значений / 1 млн) × 9,8 ₽ = 0,8467 ₽` и будет округлена до `0,85 ₽`. При этом стоимость записи 87 000 значений составит `(87 000 значений / 1 млн) × 9,8 ₽ = 0,8526 ₽` и будет округлена до `0,85 ₽`. Где `9,80 ₽` — цена за 1 млн значений (при записи до 50 млн значений).

{% include [rub.md](../_pricing/monitoring/rub.md) %}

{% endif %}

{% if region == "kz" %}

Минимальная единица тарификации — 1 значение метрики. Стоимость округляется до тиына.

Например, стоимость записи первых 86 500 значений составит `(86 500 значений / 1 млн) × 49,00 ₸ = 4,2385 ₸` и будет округлена до `4,24 ₸`. При этом стоимость записи 86 600 значений составит `(86 600 значений / 1 млн) × 49,00 ₸ = 4,2434 ₸` и будет округлена до `4,24 ₸`. Где `49,00 ₸` — цена за 1 млн значений (при записи до 50 млн значений).

{% include [kzt.md](../_pricing/monitoring/kzt.md) %}

{% endif %}

{% if region == "int" %}

Минимальная единица тарификации — 1 значение метрики. Стоимость округляется до цента.

Например, стоимость записи первых 100 000 значений составит `(100 000 значений / 1 млн) × 0,0784 $ = 0,00784 $` и будет округлена до `0,01 $`. При этом стоимость записи 150 000 значений составит `(150 000 значений / 1 млн) × 0,0784 $ = 0,01176 $` и будет округлена до `0,01 $`. Где `0,0784 $` — цена за 1 млн значений (при записи до 50 млн значений).

{% include [usd.md](../_pricing/monitoring/usd.md) %}

{% endif %}

{% endif %}

{% if product == "cloud-il" %}

Минимальная единица тарификации — 1 значение метрики. Стоимость округляется до агоры.

{% include [ils.md](../_pricing/monitoring/ils.md) %}

{% endif %}

---
title: Целевые группы сетевых балансировщиков в {{ managed-k8s-full-name }}
description: Как {{ managed-k8s-name }} управляет целевыми группами сетевых балансировщиков и как сократить число проверяемых узлов в большом кластере.
---

# Целевые группы сетевых балансировщиков

{% include [load-balancer-target-groups-intro](../../_includes/managed-kubernetes/load-balancer-target-groups-intro.md) %}

{% include [load-balancer-v2-availability](../../_includes/managed-kubernetes/load-balancer-v2-availability.md) %}

Чтобы включить режим `v2` для нового или существующего сетевого балансировщика, выполните действия из раздела [{#T}](../operations/configure-load-balancer-target-groups.md).

## Комбинации режимов управления и политик {#management-modes}

От сочетания режима управления и политики зависит состав целевой группы.

#|
|| **Режим управления** | **Политика трафика** | **Состав целевой группы** ||
|| `legacy` | `Local` или `Cluster` | Общая группа со всеми узлами кластера ||
|| `v2` | `Local` | Отдельная группа с узлами, на которых размещены поды нужного приложения ||
|| `v2` | `Cluster` | Общая группа со всеми узлами кластера ||
|#

Учитывайте следующие особенности:

* Возможность уменьшать целевую группу появляется только при включении политики `Local`.
* Если поды сервиса размещены на всех узлах, группа может не уменьшиться и при включении политики `Local`.
* Для каждого сервиса с `Local` создается отдельная группа. Проверьте [квоты и лимиты {{ network-load-balancer-name }}](../../network-load-balancer/concepts/limits.md) на группы и цели.
* Режим `v2` не увеличивает [квоты {{ managed-k8s-name }}](limits.md), адресное пространство или ресурсы мастера.
* При отсутствии аннотации или неизвестном значении используется режим `legacy`.

## Обновление целевой группы {#target-group-updates}

В режиме `v2` с политикой `Local` контроллер использует объекты `EndpointSlice`, чтобы определить узлы с подами приложения. Если под нужного приложения появляется на новом узле, контроллер добавляет узел в группу. Если на узле больше нет подов, контроллер исключает его из группы.

{{ network-load-balancer-name }} выполняет [проверки доступности](../../network-load-balancer/concepts/health-check.md) узлов в группе. Наличие узла в группе не означает, что он успешно проходит проверку. Контроллер определяет состав группы, а проверки определяют доступность определенного узла.

Изменение `EndpointSlice`, обновление облачных ресурсов и проверки доступности выполняются асинхронно. Состав группы может не сразу отразить новое размещение подов.

Не изменяйте вручную состав групп, которыми управляет контроллер. Для изменения режима используйте аннотацию объекта `Service`.

## Примеры использования {#examples}

В кластере со ста узлами у приложения три пода на трех узлах. В режиме `legacy` целевая группа балансировщика включает все сто узлов, но проверки успешно проходят только на трех узлах с подами приложения. В режиме `v2` группа балансировщика содержит три узла.

Режим полезен, когда в большом кластере работает много приложений, каждое из которых занимает небольшую часть узлов. Добавление узлов для других приложений не расширяет целевую группу такого сервиса, пока на новых узлах не появятся его поды.

#### Полезные ссылки {#see-also}
* [{#T}](../operations/configure-load-balancer-target-groups.md)
* [{#T}](../operations/create-load-balancer.md)
* [{#T}](../nlb-ref/service.md)

{% note info %}

В рамках закрытого тестирования можно организовать сетевую связность между приватными подсетями {{ baremetal-name }} и ресурсами в on-premises с помощью [{{ cr-name }} v2](../../cloud-router/index.yaml).

Такая архитектура обеспечивает прямое сетевое взаимодействие между серверами {{ baremetal-name }} и ресурсами в on-premises, оптимизирует маршрутизацию трафика за счет исключения избыточных промежуточных узлов, а также поддерживает анонсирование маршрута по умолчанию (`0.0.0.0/0`).

{% endnote %}

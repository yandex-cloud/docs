{% note warning %}

При выборе метрик для расчета SLI обратите внимание на [TTL](../../monium/concepts/ttl.md). Значение TTL-метрик не должно быть меньше, чем выбранное окно вычисления показателей. Иначе нужные метрики могут быть удалены из хранилища из-за прекращения их поставки.

{% endnote %}
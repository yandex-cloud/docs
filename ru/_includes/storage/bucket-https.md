При использовании URL вида `http://<bucket>.{{ s3-storage-host }}?<parameters>` протокол HTTPS не доступен.

О поддержке HTTPS при хостинге сайтов в {{ objstorage-name }} читайте в разделе [{#T}](../../storage/concepts/hosting.md).
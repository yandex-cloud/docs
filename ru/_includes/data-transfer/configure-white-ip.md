{% if audience != "internal" %}

Убедитесь, что настройки сети, в которой размещен кластер, разрешают подключение к нему из интернета с IP-адресов, [используемых сервисом {{ data-transfer-name }}]({{ dt-white-ip-list-uri }}).

{% else %}

Убедитесь, что группы безопасности сети, в которой размещен кластер, разрешают входящий и исходящий трафик из сети `_YTVANGANETS_`. Для этого необходимо настроить правило, которое позволило бы инициировать подключение из `_YTVANGANETS_` в `_USER_`.

{% endif %}

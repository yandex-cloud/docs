Убедиться, что метки API-шлюза изменились, можно в [консоли управления]({{ link-console-main }}) или с помощью команды [CLI](../../cli/index.yaml):

```bash
yc serverless api-gateway get <имя_API-шлюза>
```
## Сервис {{ compute-full-name }} {#compute}

Все метрики сервиса {{ compute-name }} имеют общую метку `service=compute`.

### Метрики виртуальных машин и дисков {#vm-disk-metrics}

Имя метрики<br>Тип, единицы измерения | Описание<br>Метки
--- | ---
`cpu_usage`<br>`DGAUGE`, % | Утилизация процессора ВМ в процентах. Значение может превысить 100%, если ВМ потребляет свободные ресурсы процесса сверх гарантированных.<br>Метки:<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`cpu_utilization`<br>`DGAUGE`, % | Утилизация процессорных ядер ВМ (vCPU) в процентах. Принимает значения от 0% до [уровня производительности vCPU](../../../compute/concepts/performance-levels.md).<br>Метки:<br>- *cpu_name* — идентификатор процессорного ядра.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`disk.io_quota_utilization_percentage`<br>`RATE`, % | Процент используемой квоты диска.<br>Метки:<br>- *disk* — идентификатор диска.
`disk.read_bytes`<br>`RATE`, байты/с | Количество байт, прочитанных с диска ВМ.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.read_bytes_in_flight`<br>`DGAUGE`, байты/с | Количество байт, читаемых с диска ВМ в данный момент времени.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.read_errors`<br>`RATE`, операции/с | Количество операций чтения с диска ВМ, завершившихся ошибкой.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.read_latency`<br>`HIST_RATE`, миллисекунды | Гистограмма распределения времени обработки запросов на чтение с диска ВМ.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.read_ops`<br>`RATE`, операции/с | Количество операций чтения с диска ВМ.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.read_ops_in_flight`<br>`DGAUGE`, операции/с | Количество операций чтения с диска ВМ, выполняемых в данный момент времени.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.read_throttler_delay`<br>`HIST_RATE`, миллисекунды | Гистограмма задержки, вносимой в длительность выполнения операций чтения при превышении квоты диска ВМ.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.write_bytes`<br>`RATE`, байты/с | Количество байт, записанных на диск ВМ.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.write_bytes_in_flight`<br>`DGAUGE`, байты/с | Количество байт, записываемых на диск ВМ в данный момент времени.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.write_errors`<br>`RATE`, операции/с | Количество операций записи на диск ВМ, завершившихся ошибкой.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.write_latency`<br>`HIST_RATE`, миллисекунды | Гистограмма распределения времени обработки запросов на запись на диск ВМ.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.write_ops`<br>`RATE`, операции/с | Количество операций записи на диск ВМ.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.write_ops_in_flight`<br>`DGAUGE`, операции/с | Количество операций записи на диск ВМ, выполняемых в данный момент времени.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk.write_throttler_delay`<br>`HIST_RATE`, миллисекунды | Гистограмма задержки, вносимой в длительность выполнения операций записи при превышении квоты диска ВМ.<br>Метки:<br>- *disk* — идентификатор диска.<br>- *instance* — идентификатор ВМ.
`disk_read_bytes`<br>`DGAUGE`, байты/с | Количество байт в секунду, прочитанных с диска ВМ.<br>Метки:<br>- *device* — идентификатор диска.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`disk_read_latency`<br>`HIST_RATE`, миллисекунды | Гистограмма распределения времени обработки запросов на чтение с диска ВМ.<br>Метки:<br>- *bin* — бакеты гистограммы.<br>- *device* — идентификатор диска.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`disk_read_ops`<br>`DGAUGE`, операции/с | Количество операций чтения с диска ВМ в секунду.<br>Метки:<br>- *device* — идентификатор диска.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`disk_read_ops_failed`<br>`DGAUGE`, операции/с | Количество ошибочных операций чтения с диска ВМ в секунду.<br>Метки:<br>- *device* — идентификатор диска.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`disk_read_ops_in_flight`<br>`DGAUGE`, операции/с | Количество одновременно выполняющихся операций чтения с диска ВМ в секунду.<br>Метки:<br>- *device* — идентификатор диска.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`disk_write_bytes`<br>`DGAUGE`, байты/с | Количество байт в секунду, записанных на диск ВМ.<br>Метки:<br>- *device* — идентификатор диска.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`disk_write_latency`<br>`HIST_RATE`, миллисекунды | Гистограмма распределения времени обработки запросов на запись на диск ВМ.<br>Метки:<br>- *bin* — бакеты гистограммы.<br>- *device* — идентификатор диска.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`disk_write_ops`<br>`DGAUGE`, операции/с | Количество операций записи на диск ВМ в секунду.<br>Метки:<br>- *device* — идентификатор диска.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`disk_write_ops_failed`<br>`DGAUGE`, операции/с | Количество ошибочных операций записи на диск ВМ в секунду.<br>Метки:<br>- *device* — идентификатор диска.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`disk_write_ops_in_flight`<br>`DGAUGE`, операции/с | Количество одновременно выполняющихся операций записи на диск ВМ в секунду.<br>Метки:<br>- *device* — идентификатор диска.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`network_connections.quota_utilization`<br>`DGAUGE`, % | Утилизация [квоты]({{ link-console-quotas }}) на количество сетевых соединений ВМ от 0% до 100%.<br>Метки:<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`network_received_bytes`<br>`DGAUGE`, байты/с | Количество байт в секунду, полученных на сетевом интерфейсе ВМ.<br>Метки:<br>- *interface_number* — идентификатор сетевого интерфейса ВМ.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`network_received_packets`<br>`DGAUGE`, пакеты/с | Количество пакетов в секунду, полученных на сетевом интерфейсе ВМ.<br>Метки:<br>- *interface_number* — идентификатор сетевого интерфейса ВМ.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`network_sent_bytes`<br>`DGAUGE`, байты/с | Количество байт в секунду, переданных через сетевой интерфейс ВМ.<br>Метки:<br>- *interface_number* — идентификатор сетевого интерфейса ВМ.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.
`network_sent_packets`<br>`DGAUGE`, пакеты/с | Количество пакетов в секунду, переданных через сетевой интерфейс ВМ.<br>Метки:<br>- *interface_number* — идентификатор сетевого интерфейса ВМ.<br>- *resource_id* — идентификатор ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `vm`.


### Метрики групп виртуальных машин {#ig-metrics}

| Имя метрики<br>Тип, единицы измерения | Описание<br>Метки
--- | ---                                                                
`instances_count`<br>`IGAUGE`, штуки | Количество ВМ в группе.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.
`target_instances_count`<br>`IGAUGE`, штуки | Целевое количество ВМ в группе.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.
`target_utilization`<br>`DGAUGE` | Целевое потребление ресурсов для одной ВМ.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.<br>- *source_metric* — имя метрики.
`summary_capacity`<br>`DGAUGE` | Суммарное потребление ресурсов для всех ВМ, при котором увеличится размер группы.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.<br>- *source_metric* — имя метрики.
`summary_utilization`<br>`DGAUGE` | Суммарное потребление ресурсов для всех ВМ.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.<br>- *source_metric* — имя метрики.
`average_utilization`<br>`DGAUGE` | Среднее потребление ресурсов для всех ВМ.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.<br>- *source_metric* — имя метрики.
`target_workload`<br>`DGAUGE` | Целевая нагрузка на одну ВМ в группе или зоне доступности.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.<br>- *source_metric* — имя метрики.
`instances_count_in_zone`<br>`IGAUGE`, штуки | Количество ВМ в зоне доступности.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.<br>- *zone_id* — идентификатор зоны доступности.
`target_instances_count_in_zone`<br>`IGAUGE`, штуки | Целевое количество ВМ в зоне доступности.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.<br>- *zone_id* — идентификатор зоны доступности.
`average_utilization_in_zone`<br>`DGAUGE` | Среднее потребление ресурсов для всех ВМ в зоне доступности.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.<br>- *zone_id* — идентификатор зоны доступности.<br>- *source_metric* — имя метрики.
`utilization_in_zone`<br>`DGAUGE` | Суммарное потребление ресурсов для всех ВМ в зоне доступности.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.<br>- *zone_id* — идентификатор зоны доступности.<br>- *source_metric* — имя метрики.
`summary_capacity_in_zone`<br>`DGAUGE` | Суммарное потребление ресурсов для всех ВМ в зоне доступности, при котором увеличится размер группы.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.<br>- *zone_id* — идентификатор зоны доступности.<br>- *source_metric* — имя метрики.
`zone_workload`<br>`DGAUGE` | Нагрузка на одну ВМ в зоне доступности.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.<br>- *zone_id* — идентификатор зоны доступности.<br>- *source_metric* — имя метрики.
`region_workload`<br>`DGAUGE` | Нагрузка на одну ВМ в группе.<br>Метки:<br>- *resource_id* — идентификатор группы ВМ.<br>- *resource_type* — тип ресурса. Единственное значение — `instance_group`.<br>- *source_metric* — имя метрики.

Подробнее о сервисе в документации [{{ compute-name }}](../../../compute/).

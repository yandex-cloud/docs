Имя метрики пишется в метку `name`.

Общие метки для всех метрик сервиса {{ myt-name }}: 

#|
|| **Метка** | **Значение** ||
|| service | Идентификатор сервиса: `managed-ytsaurus` ||
|| cluster_id | Идентификатор кластера ||
|#


## Метрики ресурсов кластера {#managed-ytsaurus-cluster-resources-metrics}

### Метрики вычислительных ресурсов {#managed-ytsaurus-cluster-compute-resources-metrics}

#|
|| **Имя**<br/>**Тип, единицы измерения** | **Описание** ||
|| `yt_scheduler_pools_resource_limits_cpu`<br/>``DGAUGE``, штуки | Максимальное количество CPU, доступное в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_distributed_resources_cpu`<br/>``DGAUGE``, штуки | Количество CPU, которое планировщик выделил под текущие задачи. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `yt-controller-agent` \| `*`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `undefined_tree` \| `*` \| `-`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_resource_limits_gpu`<br/>``DGAUGE``, штуки | Максимальное количество GPU, доступное в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_distributed_resources_gpu`<br/>``DGAUGE``, штуки | Количество GPU, которое планировщик выделил под текущие задачи. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `yt-controller-agent` \| `*`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `undefined_tree` \| `*` \| `-`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_resource_limits_user_memory`<br/>``DGAUGE``, байты | Максимальный размер памяти для пользовательских задач, который может быть выделен в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_distributed_resources_user_memory`<br/>``DGAUGE``, байты | Размер памяти для пользовательских задач, который выделил планировщик. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `yt-controller-agent` \| `*`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `undefined_tree` \| `*` \| `-`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_undistributed_resources_cpu`<br/>``DGAUGE``, штуки | Количество нераспределенных CPU, доступное планировщику. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `yt-controller-agent` \| `*`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `undefined_tree` \| `*` \| `-`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_undistributed_resources_user_memory`<br/>``DGAUGE``, байты | Размер нераспределенной памяти для пользовательских задач, который доступен планировщику. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `yt-controller-agent` \| `*`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `undefined_tree` \| `*` \| `-`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|#

### Метрики планировщика пулов {#managed-ytsaurus-cluster-scheduler-pools-metrics}

#|
|| **Имя**<br/>**Тип, единицы измерения** | **Описание** ||
|| `yt_scheduler_pools_running_operation_count`<br/>`DGAUGE`, штуки | Количество операций со статусом `running` в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_lightweight_running_operation_count`<br/>`DGAUGE`, штуки | Количество lightweight-операций со статусом `running` в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_max_running_operation_count`<br/>`DGAUGE`, штуки | Максимально допустимое количество операций, которые могут одновременно находиться в статусе `running` в рамках пула планировщика.
Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_total_operation_count`<br/>`DGAUGE`, штуки | Суммарное количество всех операций в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_max_operation_count`<br/>`DGAUGE`, штуки | Максимально допустимое количество операций в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| **CPU** | > ||
|| `yt_scheduler_pools_resource_usage_cpu`<br/>`DGAUGE`, штуки | Количество CPU, которое в данный момент используется для задач в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_resource_demand_cpu`<br/>`DGAUGE`, штуки | Количество CPU, которое необходимо для запуска всех ожидающих задач в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_effective_strong_guarantee_resources_cpu`<br/>`DGAUGE`, штуки | Количество CPU, которое гарантированно доступно в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_specified_resource_limits_cpu`<br/>`DGAUGE`, штуки | Максимальное количество CPU, которое задано в конфигурации пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| **RAM** | > ||
|| `yt_scheduler_pools_resource_usage_user_memory`<br/>`DGAUGE`, байты | Размер памяти для пользовательских задач, который в данный момент используется в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_resource_demand_user_memory`<br/>`DGAUGE`, байты | Размер памяти для пользовательских задач, который необходим для запуска всех ожидающих задач в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_effective_strong_guarantee_resources_user_memory`<br/>`DGAUGE`, байты | Размер памяти для пользовательских задач, который гарантированно доступен в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_specified_resource_limits_user_memory`<br/>`DGAUGE`, байты | Размер памяти для пользовательских задач, который задан в конфигурации пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| **GPU** | > ||
|| `yt_scheduler_pools_resource_usage_gpu`<br/>`DGAUGE`, штуки | Количество GPU, которое в данный момент используется для задач в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_resource_demand_gpu`<br/>`DGAUGE`, штуки | Количество GPU, которое необходимо для запуска всех ожидающих задач в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_effective_strong_guarantee_resources_gpu`<br/>`DGAUGE`, штуки | Количество GPU, которое гарантированно доступно в рамках пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_scheduler_pools_specified_resource_limits_gpu`<br/>`DGAUGE`, штуки | Количество GPU, которое задано в конфигурации пула планировщика. Дополнительные метки:
* `service_name` — имя компонента кластера. Может принимать значения: `yt-scheduler` \| `*`;
* `pool` — название пула. Может принимать значения: `<Root>` \| `research` \| `robot-odin` \| `*` \| `-`;
* `tree` — идентификатор дерева пулов. Может принимать значения: `default` \| `*`;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|#

### Метрики возможных неполадок {#managed-ytsaurus-cluster-problems-metrics}

#|
|| **Имя**<br/>**Тип, единицы измерения** | **Описание** ||
|| `yt_node_tracker_offline_node_count`<br/>`DGAUGE`, штуки | Количество нод в статусе `offline`. ||
|| `yt_node_tracker_banned_node_count`<br/>`DGAUGE`, штуки | Количество нод в статусе `banned`. ||
|| `yt_node_tracker_decommissioned_node_count`<br/>`DGAUGE`, штуки | Количество нод в состоянии `decommissioned`. ||
|| `yt_cluster_node_alerts`<br/>`DGAUGE`, штуки | Количество активных предупреждений о проблемах на нодах кластера {{ ytsaurus-name }}. Дополнительная метка `error_code` может принимать следующие значения:
* `*`
* `-`
* `NYT::NBus::EErrorCode::SslError`
* `NYT::NRpc::EErrorCode::Unavailable`
* `NYT::NSecurityClient::EErrorCode::AccountLimitExceeded`
* `NYT::NTabletClient::EErrorCode::NoSuchCell`
* `cell_did_not_appear_within_timeout`
||
|#

## Метрики CPU и памяти {#managed-ytsaurus-cluster-cpu-memory-metrics}

#|
|| **Имя**<br/>**Тип, единицы измерения** | **Описание** ||
|| `yt_resource_tracker_total_cpu`<br/>`DGAUGE`, штуки | Суммарное количество CPU в кластере. Дополнительные метки:
* `service_name` — имя компонента кластера. 
* `thread` — имя потока;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_security_user_write_time`<br/>`DGAUGE`, миллисекунды | Суммарное время, затраченное системой безопасности на проверку прав при операциях записи от имени пользователя. Дополнительные метки:
* `user` — имя пользователя.
* `service_name` — имя компонента кластера;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_security_user_read_time`<br/>`DGAUGE`, миллисекунды | Суммарное время, затраченное системой безопасности на проверку прав при операциях чтения от имени пользователя. Дополнительные метки:
* `user` — имя пользователя.
* `service_name` — имя компонента кластера;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
||` yt_resource_tracker_memory_usage_rss`<br/>`DGAUGE`, байты | Суммарное фактическое использование памяти RSS (Resident Set Size) в кластере. Дополнительные метки:
* `service_name` — имя компонента кластера;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_security_user_write_request_count`<br/>`DGAUGE`, штуки в секунду | Количество запросов на операции записи от имени пользователя, обработанных системой безопасности. Дополнительные метки:
* `user` — имя пользователя.
* `service_name` — имя компонента кластера;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_security_user_read_request_count`<br/>`DGAUGE`, штуки в секунду | Количество запросов на операции чтения от имени пользователя, обработанных системой безопасности. Дополнительные метки:
* `user` — имя пользователя.
* `service_name` — имя компонента кластера;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|| `yt_chunk_service_weight_throttler_value`<br/>`DGAUGE`, число | Текущее значение весового ограничителя (weight throttler). Дополнительные метки:
* `user` — имя пользователя.
* `service_name` — имя компонента кластера;
* `host` — имя хоста. Может принимать значения: `<имя_хоста>`, `*` \| `-`. ||
|#

# Column used in join expression is not a key column

`ERR.DS_API.DB.CHYT.INVALID_SORTED_JOIN.NOT_KEY_PREFIX_COLUMN`

Используемая в `JOIN` колонка неявляется префиксом ключа таблицы.
См. ошибку выше.

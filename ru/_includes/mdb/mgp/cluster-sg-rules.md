
[Настройте группы безопасности кластера](../../../vpc/operations/security-group-update.md#add-rule) так, чтобы они разрешали любой входящий и исходящий трафик по любым протоколам:

* Диапазон портов — `{{ port-any }}`.
* Протокол — `Любой` (`Any`).
* Источник — `CIDR`.
* CIDR блоки — `0.0.0.0/0`.


Тип хранилища выбирается при создании кластера:

* Хранилище на сетевых HDD-дисках (`network-hdd`) — самый экономичный вариант. Подходит для кластеров, не требовательных к скорости записи и чтения.
* Хранилище на локальных SSD-дисках (`local-ssd`) — использует самые быстрые диски. Объем локального хранилища можно увеличивать:
    * для платформ Intel Broadwell и Intel Cascade Lake — только с шагом 100 ГБ;
    * для платформы Intel Ice Lake — только с шагом {{ local-ssd-v3-step }}.
* Хранилище на сетевых SSD-дисках (`network-ssd`) — компромиссный вариант: медленнее, чем хранилище на локальных SSD-дисках, но, в отличие от него, обеспечивает сохранность данных при выходе из строя оборудования {{ yandex-cloud }}.

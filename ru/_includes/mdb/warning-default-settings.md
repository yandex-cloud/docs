{% note warning %}

Этот метод API сбросит на значения по умолчанию все параметры изменяемого объекта, которые не были явно переданы в запросе. Чтобы избежать этого, перечислите в параметре `updateMask` те настройки, которые необходимо изменить (одной строкой через запятую).

{% endnote %}

---

__system: {"dislikeVariants":["Нет ответа на мой вопрос","Рекомендации не помогли","Содержание не соответствует заголовку","Другое"]}
---
# Модели распознавания

Модель распознавания — модель, которая обучена распознавать речь на определенном языке. Для обучения моделей используются массивы данных из сервисов и приложений Яндекса. Это позволяет постоянно улучшать качество распознавания.

Основная поддерживаемая модель для каждого типа распознавания — модель `general`. Она распознает речь на любую тему на заданном языке: короткие и длинные фразы, а также имена, адреса, даты и числа.

## Теги версий {#tags}

Одновременно могут быть доступны три версии модели `general`. Выбрать нужную версию можно по тегам:

* `general` — основная версия модели.
* `general:rc` — версия-кандидат для релиза, которую вы можете тестировать.
* `general:deprecated` — предыдущая версия модели.

{% note info %}

 Версии, доступные по тегу `general:deprecated`, с выходом новых моделей перестают поддерживаться: мы гарантируем две недели поддержки предыдущей версии со дня обновления версии по тегу `general`.
 
{% endnote %}

{% list tabs %}

- Потоковое распознавание

   Модель `general` доступна для русского и казахского языков. Чтобы выбрать язык, в запросе API укажите значение параметра `config .specification.languageCode`. Доступные значения не чувствительны к регистру:
   * `ru-RU` (по умолчанию) — русский язык;
   * `kk-KK` — казахский язык.
   
   Подробнее о настройке запросов API для потокового распознавания читайте на странице [Потоковое распознавание](streaming.md#specification-msg).
    
- Распознавание коротких аудио
   
   Модель `general` доступна для русского языка. В запросе API укажите значение параметра `lang`. Доступные значения не чувствительны к регистру:
   * `ru-RU` (по умолчанию) — русский язык.
   
   Подробнее о настройке запросов API для распознавания коротких аудио читайте на странице [Распознавание коротких аудио](request.md#query_params).

- Распознавание длинных аудио

   Для [распознавания длинных аудио](transcribation.md) кроме модели `general` также доступна модель `deferred-general`. Это модель отложенной транскрибации по [специальным тарифам](../pricing.md#prices-stt). Во время отложенной транскрибации аудио попадает в очередь с более низким приоритетом и обрабатывается в наименее нагруженное время, но не более 24 часов.

   Обе модели доступны для русского языка — `ru-RU`. 

  Подробнее о настройке запросов API для распознавания длинных аудио читайте на странице [Распознавание длинных аудио](transcribation.md#sendfile-params).

   {% note info %}
   
   Для распознавания по тегу `deferred-general` используется та же версия модели, которая доступна и по тегу `general`.
   
   {% endnote %}

{% endlist %}


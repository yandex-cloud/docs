# Создание подключения к файлу CSV


{% note warning %}

Вы можете загрузить CSV-файл только один раз. Чтобы загрузить обновленный CSV-файл повторно, создайте новое подключение.

{% endnote %}

Чтобы создать подключение к CSV-файлу:


1. Перейдите на [страницу подключений](https://datalens.yandex.ru/connections).

1. Нажмите кнопку **Создать подключение**.
1. Выберите подключение **CSV**.
1. Нажмите кнопку **Выбрать CSV-файл** и укажите файл или перетащите CSV-файл на экран. Информация о добавленном файле отобразится в нижней части диалогового окна.
    
    {% note info %}

    Размер CSV-файла не должен превышать 200 МБ.

    {% endnote %}
    
1. Нажмите **Загрузить**.
1. Выберите параметры загрузки файла:
    - **Имя подключения**. Задайте имя подключения. Имя может быть произвольным.
    - **Кодировка**. Укажите кодировку данных из файла. Доступны значения `utf-8`, `windows-1251` и `utf-8-sig`.
    - **Разделитель**. Укажите разделитель, который используется в CSV-файле. Доступны значения `;`, `,` и `\t`.
    - **Заголовок**. Выберите опцию заголовка. Если выберете **Есть**, то {{ datalens-short-name }} использует первую строку данных в качестве заголовка.
1. Нажмите **Подключить**. Подключение появится в списке со статусом `materializing`. Это значит, что процесс загрузки данных из CSV-файла в хранилище {{ datalens-short-name }} еще не завершен.
Чтобы создать датасет на основе подключения к CSV-файлу, дождитесь, когда статус подключения изменится на `materialized`.
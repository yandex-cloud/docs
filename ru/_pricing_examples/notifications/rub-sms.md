Стоимость отправки 100 уведомлений абонентам МТС:

> {% calc [currency=RUB] 5,35 × 1 %} × 100 = {% calc [currency=RUB] 5,35 × 1 × 100 %}

Где:

* {% calc [currency=RUB] 5,35 × 1 %} — стоимость обработки одного рекламного события для абонентов МТС, до 400 000 шт.
* 100 — количество уведомлений.

Стоимость отправки 70 уведомлений, отправленных разным абонентам Билайн:

> {% calc [currency=RUB] 7,75 × 1 %} × 70 = {% calc [currency=RUB] 7,75 × 1 × 70 %}

Где:

* {% calc [currency=RUB] 7,75 × 1 %} — стоимость обработки одного рекламного события для абонентов Билайн, до двух событий на одного получателя.
* 70 — количество уведомлений.

30 уведомлений, отправленных одному абоненту Билайн, будут тарифицированы следующим образом:

> Обработка первого события — {% calc [currency=RUB] 7,75 × 1 %}.
> Обработка второго события — {% calc [currency=RUB] 7,75 × 1 %}.
> При обработке третьего события активируется пакет. Стоимость пакета от 3 до 5 событий на получателя — {% calc [currency=RUB] 18,90 × 1 %}.
> Четвертое и пятое события включены в пакет выше.
> При обработке шестого события активируется пакет. Стоимость пакета от 6 до 10 событий на получателя — {% calc [currency=RUB] 14,02 × 1 %}.
> С седьмого по десятое события включены в пакет выше.
> При обработке одиннадцатого события активируется пакет. Стоимость пакета от 11 до 20 событий на получателя — {% calc [currency=RUB] 26,76 × 1 %}.
> С двенадцатого по двадцатое события включены в пакет выше.
> При обработке событий более 20 тарифицируется каждое событие. Стоимость каждого из них — {% calc [currency=RUB] 7,66 × 1 %}.
> Итого, за 30 уведомлений данному абоненту нужно заплатить:
> {% calc [currency=RUB] 7,75 × 1 %} × 2 + {% calc [currency=RUB] 18,90 × 1 %} + {% calc [currency=RUB] 14,02 × 1 %} + {% calc [currency=RUB] 26,76 × 1 %} + {% calc [currency=RUB] 7,66 × 1 %} × 10 = {% calc [currency=RUB] (7,75 × 2 + 18,90 + 14,02 + 26,76 + 7,66 × 10) × 1 %}

Всего за 200 уведомлений нужно заплатить:

> {% calc [currency=RUB] 535,00 × 1 %} + {% calc [currency=RUB] 542,50 × 1 %} + {% calc [currency=RUB] 151,78 × 1 %} = {% calc [currency=RUB] (535,00 + 542,50 + 151,78) × 1 %}
> 
> Итого: {% calc [currency=RUB] (535,00 + 542,50 + 151,78) × 1 %}

Где:

* {% calc [currency=RUB] 535,00 × 1 %} — стоимость отправки 100 уведомлений абонентам МТС.
* {% calc [currency=RUB] 542,50 × 1 %} — стоимость отправки 70 уведомлений, отправленных разным абонентам Билайн.
* {% calc [currency=RUB] 151,78 × 1 %} — стоимость отправки 30 уведомлений, отправленных одному абоненту Билайн.
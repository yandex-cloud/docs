# Релизы YC CLI

## Версия 0.XX.X (ДД.ММ.ГГ) {#latest-release}

### Важные изменения {important}

<!-- Раздел для описания изменений, которые касаются изменений в логике работы, переименований, удалений и т.д. То есть всего, что ломает обратную совместимость. -->

### Изменения в CLI {#cli}

<!-- Раздел для описания изменений в интерфейсе командной строки, которые касаются параметров, глобальных флагов и прочих подобных штук в CLI. -->

**Улучшено**

<!-- Например, лучше стали тексты ошибок, что-то сделали симпатичнее). -->

**Исправлено**

<!-- Например, команда `yc init` завершалась с ошибкой, если у пользователя не было роли `viewer` в {{ yandex-cloud }}. -->

### Изменения в сервисах {{ yandex-cloud }} {#services}

<!-- Раздел для перечисления изменений в сервисах {{ yandex-cloud }}. Перед сервисами пишутся общие изменения. Если минорный фикс и ничего не поменялось, писать: "- Небольшие исправления и обновления." -->

#### {{ service-name }} {#container-registry}

- Команда <листиг команды в cli>.

    <!-- Описание изменения команды, примеры можно посмотреть в доке. В основном добавлябтся флаги. -->
    
#### {{ service-name }} {#container-registry}

#### Сервисы управляемых баз данных {#managed-db}

**Все сервисы управляемых БД**

- Команда `yc <имя сервиса управляемой БД> листинг команды`.

    <!-- Описание изменения команды, примеры можно посмотреть в доке. В основном добавлябтся флаги. -->

**{{ <конкретного сервиса управляемых БД>-name }}**

- Команда `yc <имя сервиса управляемой БД> листинг команды`.

    <!-- Описание изменения команды, примеры можно посмотреть в доке. В основном добавлябтся флаги. -->
    
## Предыдущие релизы {#previous-releases}

### Версия X.XX.X (ДД.ММ.ГГ) {#versionХ.ХХ.Х}

<!-- Сюда переносится то, что было последним релизом. 
- Поменять якоря: у последнего релиза: {#latest-release}, у остальных {#version<номер релиза>}.
- Поменять уровни заголовков: у заголовка версии ## -> ###. Все остальные загловки должны быть 4 уровня, чтобы в токе слева было отражено подробное описание последнего релиза и ссылки на остальные заголовки. -->

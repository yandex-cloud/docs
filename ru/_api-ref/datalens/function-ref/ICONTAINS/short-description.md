**Синтаксис:**`ICONTAINS( string, substring )`

Регистронезависимый вариант [CONTAINS](../CONTAINS/short-description.md). Возвращает `TRUE`, если строка `string` содержит в себе подстроку `substring`.
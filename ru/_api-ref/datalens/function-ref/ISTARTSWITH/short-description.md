**Синтаксис:**`ISTARTSWITH( string, substring )`

Регистронезависимый вариант [STARTSWITH](../STARTSWITH/short-description.md). Возвращает `TRUE`, если строка `string` начинается на подстроку `substring`.
openapi: "3.0"
info:
  title: Yandex Managed Service for MySQL API
  description: Методы справочника API сгруппированы по ресурсам. Чтобы выполнить операцию
    над ресурсом, отправьте HTTP-запрос к соответствующему URL или выполните RPC-вызов.
    Подробнее об архитектуре API Яндекс.Облака, см. [Концепции API Яндекс.Облака](/docs/api-design-guide/).
  version: v1
  x-y-docUri: api-ref
  x-y-baseUrl: https://mdb.api.cloud.yandex.net
paths:
  /managed-mysql/v1/backups/{backupId}:
    get:
      tags:
      - Backup
      summary: Возвращает указанную резервную копию MySQL.
      description: Чтобы получить список доступных резервных копий MySQL, выполните
        запрос [list](/docs/managed-mysql/api-ref/Backup/list).
      operationId: BackupGet
      parameters:
      - name: backupId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор резервной копии, сведения о которой запрашиваются. Чтобы получить
          идентификатор резервной копии, используйте запрос [listBackups](/docs/managed-mysql/api-ref/Cluster/listBackups).
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Backup'
      x-y-navtitle: get
  /managed-mysql/v1/backups:
    get:
      tags:
      - Backup
      summary: Возвращает список резервных копий MySQL, доступных для указанного каталога.
      operationId: BackupList
      parameters:
      - name: folderId
        in: query
        description: |-
          Обязательное поле.
          Идентификатор каталога для вывода списка резервных копий. Чтобы получить
          идентификатор каталога, используйте запрос [list](/docs/resource-manager/api-ref/Folder/list).

          Максимальная длина строки в символах — 50.
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          Максимальное количество результатов на странице ответа на запрос. Если количество
          результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Backup/list#query_params), сервис вернет значение [nextPageToken](/docs/managed-mysql/api-ref/Backup/list#responses), которое
          можно использовать для получения следующей страницы.

          Максимальное значение — 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение [pageToken](/docs/managed-mysql/api-ref/Backup/list#query_params) равным значению поля
          [nextPageToken](/docs/managed-mysql/api-ref/Backup/list#responses) предыдущего запроса, чтобы получить следующую страницу
          результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListBackupsResponse'
      x-y-navtitle: list
  /managed-mysql/v1/clusters/{clusterId}:
    get:
      tags:
      - Cluster
      summary: Возвращает указанный кластер MySQL.
      description: Чтобы получить список доступных кластеров MySQL, выполните запрос
        [list](/docs/managed-mysql/api-ref/Cluster/list).
      operationId: ClusterGet
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор возвращаемого кластера MySQL. Чтобы получить идентификатор кластера,
          используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cluster'
      x-y-navtitle: get
    delete:
      tags:
      - Cluster
      summary: Удаляет указанный кластер MySQL.
      operationId: ClusterDelete
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, который следует удалить. Чтобы получить идентификатор
          MySQL кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteClusterMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - Cluster
      summary: Изменяет указанный кластер MySQL.
      operationId: ClusterUpdate
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, который следует изменить. Чтобы получить идентификатор
          MySQL кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateClusterRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters:
    get:
      tags:
      - Cluster
      summary: Возвращает список кластеров MySQL, принадлежащих указанному каталогу.
      operationId: ClusterList
      parameters:
      - name: folderId
        in: query
        description: |-
          Обязательное поле.
          Идентификатор каталога, для которого нужно получить список кластеров MySQL. Чтобы
          получить идентификатор каталога, используйте запрос [list](/docs/resource-manager/api-ref/Folder/list).

          Максимальная длина строки в символах — 50.
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          Максимальное количество результатов на странице ответа на запрос. Если количество
          результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Cluster/list#query_params), сервис вернет значение [nextPageToken](/docs/managed-mysql/api-ref/Cluster/list#responses), которое
          можно использовать для получения следующей страницы.

          Допустимые значения — от 0 до 1000 включительно.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение [pageToken](/docs/managed-mysql/api-ref/Cluster/list#query_params) равным значению поля
          [nextPageToken](/docs/managed-mysql/api-ref/Cluster/list#responses) предыдущего запроса, чтобы получить следующую страницу
          результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      - name: filter
        in: query
        description: |-
          Параметры фильтрации ресурсов в ответе. В параметрах фильтрации указываются:
          1. Имя поля. В настоящее время фильтрацию можно использовать только с полем [Cluster.name](/docs/managed-mysql/api-ref/Cluster#representation).
          2. Оператор. Операторы `=` или `!=` для одиночных значений, `IN` или `NOT IN` для списков значений.
          3. Значение. Должен содержать от 1 до 63 символов и соответствовать регулярному выражению `^[a-zA-Z0-9_-]+$`.

          Максимальная длина строки в символах — 1000.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListClustersResponse'
      x-y-navtitle: list
    post:
      tags:
      - Cluster
      summary: Создает кластер MySQL в указанном каталоге.
      operationId: ClusterCreate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateClusterRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:start:
    post:
      tags:
      - Cluster
      summary: Запускает указанный кластер MySQL.
      operationId: ClusterStart
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, который следует запустить.

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: start
      x-y-operation-metadata:
        $ref: '#/components/schemas/StartClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:stop:
    post:
      tags:
      - Cluster
      summary: Останавливает указанный кластер MySQL.
      operationId: ClusterStop
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, который следует остановить.

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: stop
      x-y-operation-metadata:
        $ref: '#/components/schemas/StopClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:move:
    post:
      tags:
      - Cluster
      summary: Перемещает кластер MySQL в указанный каталог.
      operationId: ClusterMove
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, который следует переместить.

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveClusterRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: move
      x-y-operation-metadata:
        $ref: '#/components/schemas/MoveClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:backup:
    post:
      tags:
      - Cluster
      summary: Создает резервную копию для указанного кластера MySQL.
      operationId: ClusterBackup
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, для которого следует создать резервную копию. Чтобы
          получить идентификатор MySQL кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: backup
      x-y-operation-metadata:
        $ref: '#/components/schemas/BackupClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters:restore:
    post:
      tags:
      - Cluster
      summary: Создает новый кластер MySQL с использованием указанной резервной копии.
      operationId: ClusterRestore
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RestoreClusterRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: restore
      x-y-operation-metadata:
        $ref: '#/components/schemas/RestoreClusterMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:startFailover:
    post:
      tags:
      - Cluster
      summary: Запускает ручное переключение мастера для указанного кластера MySQL.
      operationId: ClusterStartFailover
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL.

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartClusterFailoverRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: startFailover
      x-y-operation-metadata:
        $ref: '#/components/schemas/StartClusterFailoverMetadata'
      x-y-operation-response: '[Cluster](/docs/managed-mysql/api-ref/Cluster#representation)'
  /managed-mysql/v1/clusters/{clusterId}:logs:
    get:
      tags:
      - Cluster
      summary: Получает логи для указанного кластера MySQL.
      operationId: ClusterListLogs
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, для которого следует запросить логи. Чтобы получить
          идентификатор MySQL кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: columnFilter
        in: query
        description: |-
          Столбцы из таблицы логов для запроса. Если столбцы не указаны, записи логов
          возвращаются целиком.
        schema:
          type: array
          items:
            type: string
      - name: serviceType
        in: query
        description: Тип сервиса, для которого следует запросить логи.
        schema:
          $ref: '#/components/schemas/ListClusterLogsRequestServiceType'
      - name: fromTime
        in: query
        description: |-
          Начало периода, для которого следует запросить логи.

          Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
        schema:
          type: string
          format: date-time
      - name: toTime
        in: query
        description: |-
          Конец периода, для которого следует запросить логи.

          Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
        schema:
          type: string
          format: date-time
      - name: pageSize
        in: query
        description: |-
          Максимальное количество результатов на странице ответа на запрос. Если количество
          результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Cluster/listLogs#query_params), сервис вернет значение [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#responses),
          которое можно использовать для получения следующей страницы.

          Допустимые значения — от 0 до 1000 включительно.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение [pageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#query_params) равным значению поля
          [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#responses) предыдущего запроса, чтобы получить следующую страницу
          результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      - name: alwaysNextPageToken
        in: query
        description: Всегда возвращать `next_page_token`, даже если текущая страница
          пуста.
        schema:
          type: boolean
          format: boolean
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListClusterLogsResponse'
      x-y-navtitle: listLogs
  /managed-mysql/v1/clusters/{clusterId}/operations:
    get:
      tags:
      - Cluster
      summary: Возвращает список операций для указанного кластера MySQL.
      operationId: ClusterListOperations
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, для которого запрашивается список операций.

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          Максимальное количество результатов на странице ответа на запрос. Если количество
          результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Cluster/listOperations#query_params), сервис вернет значение [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listOperations#responses),
          которое можно использовать для получения следующей страницы.

          Допустимые значения — от 0 до 1000 включительно.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение [pageToken](/docs/managed-mysql/api-ref/Cluster/listOperations#query_params) равным значению поля
          [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listOperations#responses) предыдущего запроса, чтобы получить следующую страницу
          результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListClusterOperationsResponse'
      x-y-navtitle: listOperations
  /managed-mysql/v1/clusters/{clusterId}/backups:
    get:
      tags:
      - Cluster
      summary: Получает список доступных резервных копий для указанного кластера MySQL.
      operationId: ClusterListBackups
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL. Чтобы получить идентификатор MySQL кластера, используйте
          запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          Максимальное количество результатов на странице ответа на запрос. Если количество
          результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Cluster/listBackups#query_params), сервис вернет значение [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listBackups#responses),
          которое можно использовать для получения следующей страницы.

          Максимальное значение — 1000.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение [pageToken](/docs/managed-mysql/api-ref/Cluster/listBackups#query_params) равным значению поля
          [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listBackups#responses) предыдущего запроса, чтобы получить следующую страницу
          результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListClusterBackupsResponse'
      x-y-navtitle: listBackups
  /managed-mysql/v1/clusters/{clusterId}/hosts:
    get:
      tags:
      - Cluster
      summary: Получает список хостов для указанного кластера MySQL.
      operationId: ClusterListHosts
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL. Чтобы получить идентификатор MySQL кластера, используйте
          запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          Максимальное количество результатов на странице ответа на запрос. Если количество
          результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Cluster/listHosts#query_params), сервис вернет значение [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listHosts#responses),
          которое можно использовать для получения следующей страницы.

          Допустимые значения — от 0 до 1000 включительно.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение [pageToken](/docs/managed-mysql/api-ref/Cluster/listHosts#query_params) равным значению поля
          [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listHosts#responses) предыдущего запроса, чтобы получить следующую страницу
          результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListClusterHostsResponse'
      x-y-navtitle: listHosts
  /managed-mysql/v1/clusters/{clusterId}/hosts:batchCreate:
    post:
      tags:
      - Cluster
      summary: Создает новые хосты для кластера.
      operationId: ClusterAddHosts
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, для которого следует добавить хосты. Чтобы получить
          идентификатор MySQL кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddClusterHostsRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: addHosts
      x-y-operation-metadata:
        $ref: '#/components/schemas/AddClusterHostsMetadata'
      x-y-operation-response: Empty
  /managed-mysql/v1/clusters/{clusterId}/hosts:batchDelete:
    post:
      tags:
      - Cluster
      summary: Удаляет указанные хосты кластера.
      operationId: ClusterDeleteHosts
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, из которого следует удалить хосты. Чтобы получить
          идентификатор MySQL кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteClusterHostsRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: deleteHosts
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteClusterHostsMetadata'
      x-y-operation-response: Empty
  /managed-mysql/v1/clusters/{clusterId}/databases/{databaseName}:
    get:
      tags:
      - Database
      summary: Возвращает указанную базу данных MySQL.
      description: Чтобы получить список доступных баз данных MySQL, выполните запрос
        [list](/docs/managed-mysql/api-ref/Database/list).
      operationId: DatabaseGet
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, которому принадлежит база данных. Чтобы получить
          идентификатор кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: databaseName
        in: path
        description: |-
          Обязательное поле.
          Имя базы данных MySQL, данные о которой нужно запросить. Чтобы получить имя базы данных,
          используйте запрос [list](/docs/managed-mysql/api-ref/Database/list).

          Максимальная длина строки в символах — 63.
          Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_-]* ``.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Database'
      x-y-navtitle: get
    delete:
      tags:
      - Database
      summary: Удаляет указанную базу данных MySQL.
      operationId: DatabaseDelete
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, в котором следует удалить базу данных. Чтобы получить
          идентификатор кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: databaseName
        in: path
        description: |-
          Обязательное поле.
          Имя удаляемой базы данных. Чтобы получить имя базы данных, используйте запрос
          [list](/docs/managed-mysql/api-ref/Database/list).

          Максимальная длина строки в символах — 63.
          Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_-]* ``.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteDatabaseMetadata'
      x-y-operation-response: Empty
  /managed-mysql/v1/clusters/{clusterId}/databases:
    get:
      tags:
      - Database
      summary: Получает список баз данных MySQL в указанном кластере.
      operationId: DatabaseList
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, для которого нужно вывести список баз данных. Чтобы
          получить идентификатор кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          Максимальное количество результатов на странице ответа на запрос. Если количество
          результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Database/list#query_params), сервис вернет значение [nextPageToken](/docs/managed-mysql/api-ref/Database/list#responses), которое
          можно использовать для получения следующей страницы.

          Допустимые значения — от 0 до 1000 включительно.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение [pageToken](/docs/managed-mysql/api-ref/Database/list#query_params) равным значению поля
          [nextPageToken](/docs/managed-mysql/api-ref/Database/list#responses) предыдущего запроса, чтобы получить следующую страницу
          результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListDatabasesResponse'
      x-y-navtitle: list
    post:
      tags:
      - Database
      summary: Создает новую базу данных MySQL в указанном кластере.
      operationId: DatabaseCreate
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, в котором следует создать базу данных. Чтобы получить
          идентификатор кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDatabaseRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateDatabaseMetadata'
      x-y-operation-response: '[Database](/docs/managed-mysql/api-ref/Database#representation)'
  /managed-mysql/v1/resourcePresets/{resourcePresetId}:
    get:
      tags:
      - ResourcePreset
      summary: Возвращает указанный набор ресурсов.
      description: Чтобы получить список доступных наборов ресурсов, используйте запрос
        [list](/docs/managed-mysql/api-ref/ResourcePreset/list).
      operationId: ResourcePresetGet
      parameters:
      - name: resourcePresetId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор набора ресурсов, данные о котором запрашиваются. Чтобы получить
          идентификатор набора ресурсов, используйте запрос [list](/docs/managed-mysql/api-ref/ResourcePreset/list).
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourcePreset'
      x-y-navtitle: get
  /managed-mysql/v1/resourcePresets:
    get:
      tags:
      - ResourcePreset
      summary: Возвращает список доступных наборов ресурсов.
      operationId: ResourcePresetList
      parameters:
      - name: pageSize
        in: query
        description: |-
          Максимальное количество результатов на странице ответа на запрос. Если количество
          результатов больше чем [pageSize](/docs/managed-mysql/api-ref/ResourcePreset/list#query_params), сервис вернет значение [nextPageToken](/docs/managed-mysql/api-ref/ResourcePreset/list#responses),
          которое можно использовать для получения следующей страницы.

          Допустимые значения — от 0 до 1000 включительно.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение [pageToken](/docs/managed-mysql/api-ref/ResourcePreset/list#query_params) равным значению поля
          [nextPageToken](/docs/managed-mysql/api-ref/ResourcePreset/list#responses) предыдущего запроса, чтобы получить следующую страницу
          результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListResourcePresetsResponse'
      x-y-navtitle: list
  /managed-mysql/v1/clusters/{clusterId}/users/{userName}:
    get:
      tags:
      - User
      summary: Возвращает указанного пользователя MySQL.
      description: Чтобы получить список доступных пользователей MySQL, выполните
        запрос [list](/docs/managed-mysql/api-ref/User/list).
      operationId: UserGet
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL.

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: userName
        in: path
        description: |-
          Обязательное поле.
          Обязательное поле.

          Максимальная длина строки в символах — 63.
          Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_]* ``.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
      x-y-navtitle: get
    delete:
      tags:
      - User
      summary: Удаляет указанного пользователя MySQL.
      operationId: UserDelete
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, которому принадлежит пользователь. Чтобы получить
          идентификатор кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: userName
        in: path
        description: |-
          Обязательное поле.
          Имя удаляемого пользователя. Чтобы получить имя пользователя, используйте запрос
          [list](/docs/managed-mysql/api-ref/User/list).

          Максимальная длина строки в символах — 63.
          Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_]* ``.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteUserMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - User
      summary: Изменяет указанного пользователя MySQL.
      operationId: UserUpdate
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, которому принадлежит пользователь. Чтобы получить
          идентификатор кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: userName
        in: path
        description: |-
          Обязательное поле.
          Имя пользователя, которого следует изменить. Чтобы получить имя пользователя,
          используйте запрос [list](/docs/managed-mysql/api-ref/User/list).

          Максимальная длина строки в символах — 63.
          Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_]* ``.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateUserMetadata'
      x-y-operation-response: '[User](/docs/managed-mysql/api-ref/User#representation)'
  /managed-mysql/v1/clusters/{clusterId}/users:
    get:
      tags:
      - User
      summary: Получает список пользователей MySQL в указанном кластере.
      operationId: UserList
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера для вывода списка пользователей MySQL. Чтобы получить
          идентификатор кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: |-
          Максимальное количество результатов на странице ответа на запрос. Если количество
          результатов больше чем [pageSize](/docs/managed-mysql/api-ref/User/list#query_params), сервис вернет значение [nextPageToken](/docs/managed-mysql/api-ref/User/list#responses), которое
          можно использовать для получения следующей страницы.

          Допустимые значения — от 0 до 1000 включительно.
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение [pageToken](/docs/managed-mysql/api-ref/User/list#query_params) равным значению поля [nextPageToken](/docs/managed-mysql/api-ref/User/list#responses)
          предыдущего запроса, чтобы получить следующую страницу результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListUsersResponse'
      x-y-navtitle: list
    post:
      tags:
      - User
      summary: Создает пользователя MySQL в указанном кластере.
      operationId: UserCreate
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, для которого следует создать пользователя. Чтобы
          получить идентификатор кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateUserMetadata'
      x-y-operation-response: '[User](/docs/managed-mysql/api-ref/User#representation)'
  /managed-mysql/v1/clusters/{clusterId}/users/{userName}:grantPermission:
    post:
      tags:
      - User
      summary: Предоставляет разрешение указанному пользователю MySQL.
      operationId: UserGrantPermission
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, которому принадлежит пользователь. Чтобы получить
          идентификатор кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: userName
        in: path
        description: |-
          Обязательное поле.
          Имя пользователя, которому следует предоставить разрешение. Чтобы получить имя
          пользователя, используйте запрос [list](/docs/managed-mysql/api-ref/User/list).

          Максимальная длина строки в символах — 63.
          Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_]* ``.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GrantUserPermissionRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: grantPermission
      x-y-operation-metadata:
        $ref: '#/components/schemas/GrantUserPermissionMetadata'
      x-y-operation-response: '[User](/docs/managed-mysql/api-ref/User#representation)'
  /managed-mysql/v1/clusters/{clusterId}/users/{userName}:revokePermission:
    post:
      tags:
      - User
      summary: Отзывает разрешение у указанного пользователя MySQL.
      operationId: UserRevokePermission
      parameters:
      - name: clusterId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор кластера MySQL, которому принадлежит пользователь. Чтобы получить
          идентификатор кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: userName
        in: path
        description: |-
          Обязательное поле.
          Имя пользователя, у которого следует отозвать разрешение. Чтобы получить имя
          пользователя, используйте запрос [list](/docs/managed-mysql/api-ref/User/list).

          Максимальная длина строки в символах — 63.
          Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_]* ``.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RevokeUserPermissionRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: revokePermission
      x-y-operation-metadata:
        $ref: '#/components/schemas/RevokeUserPermissionMetadata'
      x-y-operation-response: '[User](/docs/managed-mysql/api-ref/User#representation)'
components:
  schemas:
    Access:
      type: object
      properties:
        dataLens:
          description: Разрешить доступ для DataLens
          type: boolean
          format: boolean
    AddClusterHostsMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор кластера MySQL, в который добавляются хосты.
          type: string
        hostNames:
          description: Имена хостов, добавляемых в кластер.
          type: array
          items:
            type: string
    AddClusterHostsRequest:
      type: object
      properties:
        hostSpecs:
          description: |-
            Обязательное поле.
            Конфигурации для хостов MySQL, которые должны быть добавлены в кластер.

            Должен содержать хотя бы один элемент.
          type: array
          items:
            $ref: '#/components/schemas/HostSpec'
    Backup:
      description: Резервная копия MySQL. Подробнее см. в разделе [Резервные копии](/docs/managed-mysql/concepts/backup).
      type: object
      properties:
        id:
          description: |-
            Обязательное поле.
            Идентификатор резервной копии.
          type: string
        folderId:
          description: Идентификатор каталога, которому принадлежит резервная копия.
          type: string
        createdAt:
          description: Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
          type: string
          format: date-time
        sourceClusterId:
          description: Идентификатор кластера MySQL, для которого была создана резервная
            копия.
          type: string
        startedAt:
          description: |-
            Время запуска операции резервного копирования.

            Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
          type: string
          format: date-time
    BackupClusterMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор кластера MySQL, для которого выполняется резервное
            копирование.
          type: string
    Cluster:
      description: Кластер MySQL. Подробнее см. в [документации](/docs/managed-mysql/concepts).
      type: object
      properties:
        id:
          description: |-
            Идентификатор кластера MySQL. Этот идентификатор Managed Service for MySQL генерирует при
            создании.
          type: string
        folderId:
          description: Идентификатор каталога, которому принадлежит кластер MySQL.
          type: string
        createdAt:
          description: Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
          type: string
          format: date-time
        name:
          description: |-
            Имя кластера MySQL. Имя должно быть уникальным в каталоге, соответствовать RFC 1035 и иметь
            длину 1-63 символа.
          type: string
        description:
          description: Описание кластера MySQL. Длина описания должна быть от 0 до
            256 символов.
          type: string
        labels:
          description: Пользовательские метки для кластера MySQL в виде пар `key:value`.
            Максимум 64 на ресурс.
          type: object
          additionalProperties:
            type: string
        environment:
          $ref: '#/components/schemas/ClusterEnvironment_in_Cluster_environment'
        monitoring:
          description: Описание систем мониторинга, относящихся к данному кластеру
            MySQL.
          type: array
          items:
            $ref: '#/components/schemas/Monitoring'
        config:
          $ref: '#/components/schemas/ClusterConfig_in_Cluster_config'
        networkId:
          description: Идентификатор сети, к которой принадлежит кластер.
          type: string
        health:
          $ref: '#/components/schemas/ClusterHealth_in_Cluster_health'
        status:
          $ref: '#/components/schemas/ClusterStatus_in_Cluster_status'
    ClusterConfig:
      type: object
      allOf:
      - type: object
        properties:
          version:
            description: Версия серверного программного обеспечения MySQL.
            type: string
          resources:
            $ref: '#/components/schemas/Resources'
            description: Ресурсы, выделенные хостам MySQL.
          backupWindowStart:
            $ref: '#/components/schemas/TimeOfDay'
            description: Время запуска ежедневного резервного копирования, в часовом
              поясе UTC.
          access:
            $ref: '#/components/schemas/Access'
            description: Политика доступа к БД
      - title: mysql_config
        type: object
        maxProperties: 1
        properties:
          mysqlConfig_5_7:
            $ref: '#/components/schemas/MysqlConfigSet5_7'
            description: Конфигурация сервера MySQL 5.7.
          mysqlConfig_8_0:
            $ref: '#/components/schemas/MysqlConfigSet8_0'
            description: Конфигурация сервера MySQL 8.0.
    ClusterEnvironment:
      description: |2-
         - PRODUCTION: Стабильная среда с осторожной политикой обновления: во время регулярного
        обслуживания применяются только срочные исправления.
         - PRESTABLE: Среда с более агрессивной политикой обновления: новые версии развертываются
        независимо от обратной совместимости.
      type: string
      enum:
      - PRODUCTION
      - PRESTABLE
    ClusterHealth:
      description: |2-
         - HEALTH_UNKNOWN: Состояние кластера неизвестно (`health` для каждого хоста в кластере — UNKNOWN).
         - ALIVE: Кластер работает нормально (`health` для каждого хоста в кластере — ALIVE).
         - DEAD: Кластер не работает (`health` для каждого узла в кластере — DEAD).
         - DEGRADED: Кластер работает неоптимально (`health` по крайней мере для одного узла в кластере
        не ALIVE).
      type: string
      enum:
      - HEALTH_UNKNOWN
      - ALIVE
      - DEAD
      - DEGRADED
      default: HEALTH_UNKNOWN
    ClusterStatus:
      description: |2-
         - STATUS_UNKNOWN: Состояние кластера неизвестно.
         - CREATING: Кластер создается.
         - RUNNING: Кластер работает нормально.
         - ERROR: На кластере произошла ошибка, блокирующая работу.
         - UPDATING: Кластер изменяется.
         - STOPPING: Кластер останавливается.
         - STOPPED: Кластер остановлен.
         - STARTING: Кластер запускается.
      type: string
      enum:
      - STATUS_UNKNOWN
      - CREATING
      - RUNNING
      - ERROR
      - UPDATING
      - STOPPING
      - STOPPED
      - STARTING
      default: STATUS_UNKNOWN
    ConfigSpec:
      type: object
      allOf:
      - type: object
        properties:
          version:
            description: |-
              Версия MySQL, используемая в кластере. Возможные значения:
              * 5.7
              * 8.0
            type: string
          resources:
            $ref: '#/components/schemas/Resources'
            description: Ресурсы, выделенные хостам MySQL.
          backupWindowStart:
            $ref: '#/components/schemas/TimeOfDay'
            description: Время запуска ежедневного резервного копирования, в часовом
              поясе UTC.
          access:
            $ref: '#/components/schemas/Access'
            description: Политика доступа к БД.
      - title: mysql_config
        type: object
        maxProperties: 1
        properties:
          mysqlConfig_5_7:
            $ref: '#/components/schemas/MysqlConfig5_7'
            description: Конфигурация для кластера MySQL 5.7.
          mysqlConfig_8_0:
            $ref: '#/components/schemas/MysqlConfig8_0'
            description: Конфигурация для кластера MySQL 8.0.
    ConnectionLimits:
      type: object
      properties:
        maxQuestionsPerHour:
          description: |-
            Максимально допустимое число вопросов в час.

            Минимальное значение — 0.
          type: integer
          format: int64
        maxUpdatesPerHour:
          description: |-
            Максимально допустимое число апдейтов в час.

            Минимальное значение — 0.
          type: integer
          format: int64
        maxConnectionsPerHour:
          description: |-
            Максимально допустимое число коннектов в час.

            Минимальное значение — 0.
          type: integer
          format: int64
        maxUserConnections:
          description: |-
            Максимально допустимое число коннектов.

            Минимальное значение — 0.
          type: integer
          format: int64
    CreateClusterMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор создаваемого кластера MySQL.
          type: string
    CreateClusterRequest:
      type: object
      properties:
        folderId:
          description: |-
            Обязательное поле.
            Идентификатор каталога, в котором нужно создать кластер MySQL.

            Максимальная длина строки в символах — 50.
          type: string
        name:
          description: |-
            Обязательное поле.
            Имя кластера MySQL. Имя должно быть уникальным в каталоге.

            Максимальная длина строки в символах — 63.
            Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_-]* ``.
          type: string
        description:
          description: |-
            Описание кластера MySQL.

            Максимальная длина строки в символах — 256.
          type: string
        labels:
          description: |-
            Пользовательские метки для кластера MySQL в виде пар `key:value`. Максимум 64 на ресурс.
            Например, "project": "mvp" или "source": "dictionary".

            Не более 64 на ресурс.
            Длина строки в символах для каждого ключа должна быть от 1 до 63.
            Каждый ключ должен соответствовать регулярному выражению `` [a-z][-_0-9a-z]* ``.
            Максимальная длина строки в символах для каждого значения — 63.
            Каждое значение должно соответствовать регулярному выражению `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        environment:
          $ref: '#/components/schemas/ClusterEnvironment_in_CreateClusterRequest_environment'
        configSpec:
          $ref: '#/components/schemas/ConfigSpec_in_CreateClusterRequest_configSpec'
        databaseSpecs:
          description: Описания баз данных, которые нужно создать в кластере MySQL.
          type: array
          items:
            $ref: '#/components/schemas/DatabaseSpec'
        userSpecs:
          description: Описания пользователей базы данных, которых нужно создать в
            кластере MySQL.
          type: array
          items:
            $ref: '#/components/schemas/UserSpec'
        hostSpecs:
          description: Конфигурации для отдельных хостов, которые должны быть созданы
            для кластера MySQL.
          type: array
          items:
            $ref: '#/components/schemas/HostSpec'
        networkId:
          description: |-
            Идентификатор сети, в которой нужно создать кластер.

            Максимальная длина строки в символах — 50.
          type: string
    CreateDatabaseMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор кластера MySQL, в котором создается база данных.
          type: string
        databaseName:
          description: Имя создаваемой базы данных MySQL.
          type: string
    CreateDatabaseRequest:
      type: object
      properties:
        databaseSpec:
          $ref: '#/components/schemas/DatabaseSpec_in_CreateDatabaseRequest_databaseSpec'
    CreateUserMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор кластера MySQL, в котором создается пользователь.
          type: string
        userName:
          description: Имя создаваемого пользователя.
          type: string
    CreateUserRequest:
      type: object
      properties:
        userSpec:
          $ref: '#/components/schemas/UserSpec_in_CreateUserRequest_userSpec'
    Database:
      description: База данных MySQL. Подробнее см. в [документации](/docs/managed-mysql/concepts).
      type: object
      properties:
        name:
          description: Имя базы данных.
          type: string
        clusterId:
          description: Идентификатор кластера MySQL, которому принадлежит база данных.
          type: string
    DatabaseSpec:
      type: object
      properties:
        name:
          description: |-
            Обязательное поле.
            Имя базы данных MySQL.

            Максимальная длина строки в символах — 63.
            Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_-]* ``.
          type: string
    DeleteClusterHostsMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор кластера MySQL, из которого следует удалить хосты.
          type: string
        hostNames:
          description: Имена удаляемых хостов.
          type: array
          items:
            type: string
    DeleteClusterHostsRequest:
      type: object
      properties:
        hostNames:
          description: |-
            Обязательное поле.
            Имена хостов, которые следует удалить.

            Должен содержать хотя бы один элемент.
            Максимальная длина строки в символах для каждого значения — 253.
          type: array
          items:
            type: string
    DeleteClusterMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор удаляемого кластера MySQL.
          type: string
    DeleteDatabaseMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор кластера MySQL, в котором удаляется база данных.
          type: string
        databaseName:
          description: Имя удаляемой базы данных MySQL.
          type: string
    DeleteUserMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор кластера MySQL, которому принадлежит пользователь.
          type: string
        userName:
          description: Имя удаляемого пользователя.
          type: string
    Empty:
      description: Пустой JSON объект `` {} ``.
      type: object
      maxProperties: 0
    FieldMask:
      description: |-
        Имена всех обновляемых полей, разделенные запятыми.
        Только значения указанных полей будут изменены. Остальные останутся нетронутыми.
        Если поле указано в параметре `` updateMask `` и в запросе не было отправлено значение для этого поля,
        значение поля будет сброшено на значение по умолчанию. Значение по умолчанию для большинства полей — null или 0.

        Если в запросе не передается `` updateMask ``, значения всех полей будут обновлены.
        Для полей, указанных в запросе, будут использованы переданные значения.
        Значения остальных полей будут сброшены на значения по умолчанию.
      type: string
    GlobalPermission:
      description: |2-
         - REPLICATION_CLIENT: Разрешает использование SHOW MASTER STATUS, SHOW SLAVE STATUS, и SHOW BINARY LOGS.
         - REPLICATION_SLAVE: Разрешает использование SHOW SLAVE HOSTS, SHOW RELAYLOG EVENTS и SHOW BINLOG EVENTS.
         - PROCESS: Разрешает использование SHOW ENGINE INNODB STATUS.
      type: string
      enum:
      - REPLICATION_CLIENT
      - REPLICATION_SLAVE
      - PROCESS
    GrantUserPermissionMetadata:
      type: object
      properties:
        clusterId:
          description: |-
            Идентификатор кластера MySQL, которому принадлежит пользователь. Чтобы получить
            идентификатор кластера, используйте запрос [list](/docs/managed-mysql/api-ref/Cluster/list).
          type: string
        userName:
          description: Имя пользователя, которому предоставляется разрешение.
          type: string
    GrantUserPermissionRequest:
      type: object
      properties:
        permission:
          $ref: '#/components/schemas/Permission_in_GrantUserPermissionRequest_permission'
    Host:
      type: object
      properties:
        name:
          description: |-
            Имя хоста MySQL. Имя хоста Managed Service for MySQL назначает во время создания, его нельзя
            изменить. Длина 1-63 символов.

            Имя уникально для всех существующих хостов баз данных в Яндекс.Облаке, так как оно
            определяет полное доменное имя (FQDN) хоста.
          type: string
        clusterId:
          description: Идентификатор хоста MySQL. Этот идентификатор Managed Service
            for MySQL генерирует при создании.
          type: string
        zoneId:
          description: Идентификатор зоны доступности, в которой находится хост MySQL.
          type: string
        resources:
          $ref: '#/components/schemas/Resources_in_Host_resources'
        role:
          $ref: '#/components/schemas/HostRole_in_Host_role'
        health:
          $ref: '#/components/schemas/HostHealth_in_Host_health'
        services:
          description: Сервисы, предоставляемые хостом.
          type: array
          items:
            $ref: '#/components/schemas/Service'
        subnetId:
          description: Идентификатор подсети, к которой принадлежит хост.
          type: string
        assignPublicIp:
          description: Флаг, показывающий статус публичного IP-адреса для этого хоста.
          type: boolean
          format: boolean
    HostHealth:
      description: |2-
         - HEALTH_UNKNOWN: Состояние хоста неизвестно.
         - ALIVE: Хозяин выполняет все свои функции нормально.
         - DEAD: Хост не работает и не может выполнять свои основные функции.
         - DEGRADED: Хост деградировал, и может выполнять только некоторые из своих основных функций.
      type: string
      enum:
      - HEALTH_UNKNOWN
      - ALIVE
      - DEAD
      - DEGRADED
      default: HEALTH_UNKNOWN
    HostRole:
      description: |2-
         - ROLE_UNKNOWN: Роль хоста в кластере неизвестна.
         - MASTER: Хост является мастером в кластере MySQL.
         - REPLICA: Хост является репликой в кластере MySQL.
      type: string
      enum:
      - ROLE_UNKNOWN
      - MASTER
      - REPLICA
      default: ROLE_UNKNOWN
    HostSpec:
      type: object
      properties:
        zoneId:
          description: |-
            Идентификатор зоны доступности, в которой находится хост. Чтобы получить список
            доступных зон, используйте запрос [list](/docs/compute/api-ref/Zone/list).

            Максимальная длина строки в символах — 50.
          type: string
        subnetId:
          description: |-
            Идентификатор подсети, к которой должен принадлежать хост. Эта подсеть должна быть
            частью сети, к которой принадлежит кластер. Идентификатор сети задан в поле
            [Cluster.networkId](/docs/managed-mysql/api-ref/Cluster#representation).

            Максимальная длина строки в символах — 50.
          type: string
        assignPublicIp:
          description: |-
            Должен ли хост получить публичный IP-адрес при создании.

            После создания узла этот параметр изменить нельзя. Чтобы удалить назначенный
            публичный IP-адрес или назначить публичный IP уже созданному хосту, пересоздайте хост
            с нужным значением поля `assignPublicIp`.

            Возможные значения:
            * false — не назначать хосту публичный IP-адрес.
            * true — у хоста должен быть публичный IP-адрес.
          type: boolean
          format: boolean
    ListBackupsResponse:
      type: object
      properties:
        backups:
          description: Список резервных копий MySQL.
          type: array
          items:
            $ref: '#/components/schemas/Backup'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество
            результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Backup/list#query_params), используйте [nextPageToken](/docs/managed-mysql/api-ref/Backup/list#responses) в качестве значения
            параметра [pageToken](/docs/managed-mysql/api-ref/Backup/list#query_params) в следующем запросе списка ресурсов. Все последующие
            запросы будут получать свои значения [nextPageToken](/docs/managed-mysql/api-ref/Backup/list#responses) для перебора страниц результатов.

            Максимальная длина строки в символах — 100.
          type: string
    ListClusterBackupsResponse:
      type: object
      properties:
        backups:
          description: Список резервных копий MySQL.
          type: array
          items:
            $ref: '#/components/schemas/Backup'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество
            результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Cluster/listBackups#query_params), используйте [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listBackups#responses) в качестве
            значения параметра [pageToken](/docs/managed-mysql/api-ref/Cluster/listBackups#query_params) в следующем запросе списка ресурсов. Все
            последующие запросы будут получать свои значения [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listBackups#responses) для перебора страниц
            результатов.
          type: string
    ListClusterHostsResponse:
      type: object
      properties:
        hosts:
          description: Список хостов MySQL.
          type: array
          items:
            $ref: '#/components/schemas/Host'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество
            результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Cluster/listHosts#query_params), используйте [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listHosts#responses) в качестве
            значения параметра [pageToken](/docs/managed-mysql/api-ref/Cluster/listHosts#query_params) в следующем запросе списка ресурсов. Все
            последующие запросы будут получать свои значения [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listHosts#responses) для перебора страниц
            результатов.
          type: string
    ListClusterLogsRequestServiceType:
      description: |2-
         - MYSQL_ERROR: Лог ошибок MySQL.
         - MYSQL_GENERAL: Общий лог запросов MySQL.
         - MYSQL_SLOW_QUERY: Лог медленных запросов MySQL.
         - MYSQL_AUDIT: Лог аудита MySQL.
      type: string
      enum:
      - MYSQL_ERROR
      - MYSQL_GENERAL
      - MYSQL_SLOW_QUERY
      - MYSQL_AUDIT
    ListClusterLogsResponse:
      type: object
      properties:
        logs:
          description: Запрошенные записи логов.
          type: array
          items:
            $ref: '#/components/schemas/LogRecord'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество
            результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Cluster/listLogs#query_params), используйте [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#responses) в качестве
            значения параметра [pageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#query_params) в следующем запросе списка ресурсов. Все
            последующие запросы будут получать свои значения [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listLogs#responses) для перебора страниц
            результатов.
          type: string
    ListClusterOperationsResponse:
      type: object
      properties:
        operations:
          description: Список операций для указанного кластера MySQL.
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество
            результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Cluster/listOperations#query_params), используйте [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listOperations#responses) в качестве
            значения параметра [pageToken](/docs/managed-mysql/api-ref/Cluster/listOperations#query_params) в следующем запросе списка ресурсов. Все
            последующие запросы будут получать свои значения [nextPageToken](/docs/managed-mysql/api-ref/Cluster/listOperations#responses) для перебора страниц
            результатов.
          type: string
    ListClustersResponse:
      type: object
      properties:
        clusters:
          description: Список кластеров MySQL.
          type: array
          items:
            $ref: '#/components/schemas/Cluster'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество
            результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Cluster/list#query_params), используйте [nextPageToken](/docs/managed-mysql/api-ref/Cluster/list#responses) в качестве значения
            параметра [pageToken](/docs/managed-mysql/api-ref/Cluster/list#query_params) в следующем запросе списка ресурсов. Все последующие
            запросы будут получать свои значения [nextPageToken](/docs/managed-mysql/api-ref/Cluster/list#responses) для перебора страниц результатов.
          type: string
    ListDatabasesResponse:
      type: object
      properties:
        databases:
          description: Список баз данных MySQL.
          type: array
          items:
            $ref: '#/components/schemas/Database'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество
            результатов больше чем [pageSize](/docs/managed-mysql/api-ref/Database/list#query_params), используйте [nextPageToken](/docs/managed-mysql/api-ref/Database/list#responses) в качестве значения
            параметра [pageToken](/docs/managed-mysql/api-ref/Database/list#query_params) в следующем запросе списка ресурсов. Все последующие
            запросы будут получать свои значения [nextPageToken](/docs/managed-mysql/api-ref/Database/list#responses) для перебора страниц результатов.
          type: string
    ListResourcePresetsResponse:
      type: object
      properties:
        resourcePresets:
          description: Список наборов ресурсов.
          type: array
          items:
            $ref: '#/components/schemas/ResourcePreset'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество
            результатов больше чем [pageSize](/docs/managed-mysql/api-ref/ResourcePreset/list#query_params), используйте [nextPageToken](/docs/managed-mysql/api-ref/ResourcePreset/list#responses) в качестве
            значения параметра [pageToken](/docs/managed-mysql/api-ref/ResourcePreset/list#query_params) в следующем запросе списка ресурсов. Все
            последующие запросы будут получать свои значения [nextPageToken](/docs/managed-mysql/api-ref/ResourcePreset/list#responses) для перебора страниц
            результатов.

            Максимальная длина строки в символах — 100.
          type: string
    ListUsersResponse:
      type: object
      properties:
        users:
          description: Запрошенный список пользователей MySQL.
          type: array
          items:
            $ref: '#/components/schemas/User'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество
            результатов больше чем [pageSize](/docs/managed-mysql/api-ref/User/list#query_params), используйте [nextPageToken](/docs/managed-mysql/api-ref/User/list#responses) в качестве значения
            параметра [pageToken](/docs/managed-mysql/api-ref/User/list#query_params) в следующем запросе списка ресурсов. Все последующие
            запросы будут получать свои значения [nextPageToken](/docs/managed-mysql/api-ref/User/list#responses) для перебора страниц результатов.
          type: string
    LogRecord:
      type: object
      properties:
        timestamp:
          description: |-
            Отметка времени для записи журнала в текстовом формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).

            Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
          type: string
          format: date-time
        message:
          description: Содержание записи журнала.
          type: object
          additionalProperties:
            type: string
    Monitoring:
      type: object
      properties:
        name:
          description: Название системы мониторинга.
          type: string
        description:
          description: Описание системы мониторинга.
          type: string
        link:
          description: Ссылка на графики системы мониторинга для кластера MySQL.
          type: string
    MoveClusterMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор перемещаемого кластера MySQL.
          type: string
        sourceFolderId:
          description: Идентификатор исходного каталога.
          type: string
        destinationFolderId:
          description: Идентификатор каталога назначения.
          type: string
    MoveClusterRequest:
      type: object
      properties:
        destinationFolderId:
          description: |-
            Обязательное поле.
            Идентификатор каталога, в который следует переместить кластер.

            Максимальная длина строки в символах — 50.
          type: string
    MysqlConfig5_7:
      description: Поля и структура `MysqlConfig5_7` отражает параметры конфигурации
        MySQL 5.7.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Размер буфера InnoDB, который используется для кэширования данных таблиц и индексов.

            Подробнее см. в [описании параметра в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size).

            Минимальное значение — 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            Максимальное количество одновременных подключений, которые принимает MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_connections).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Время, в течение которого запрос должен обрабатываться, прежде чем он начинает
            считаться медленным.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_long_query_time).
          type: number
          format: double
        generalLog:
          description: |-
            Включить запись общего лога запросов MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_general_log).
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Включить запись лога аудита MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/mysql-security-excerpt/5.6/en/audit-log-options-variables.html#option_mysqld_audit-log).
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Режим SQL для сервера MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/sql-mode.html#sql-mode-setting).
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig5_7SQLMode'
        maxAllowedPacket:
          description: |-
            Максимальный размер одного пакета в байтах.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_allowed_packet).

            Допустимые значения — от 1024 до 1073741824 включительно.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig5_7AuthPlugin_in_MysqlConfig5_7_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Частота записи лога транзакций на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit)

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Максимальное время ожидания блокировки строки для транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout)

            Допустимые значения — от 1 до 28800 включительно.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig5_7TransactionIsolation_in_MysqlConfig5_7_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Выводить всю информацию о взаимных блокировках в журнал ошибок.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks)
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            Максимальное время ожидания чтения при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_read_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            Максимальное время ожидания записи при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_write_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            Максимальный размер результата для функции GROUP_CONCAT()

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_group_concat_max_len)

            Допустимые значения — от 4 до 33554432 включительно.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            Максимальный размер временных таблиц в памяти, прежде чем они будут помещены на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_tmp_table_size)

            Допустимые значения — от 1024 до 134217728 включительно.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            Максимальный размер пользователских MEMORY таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_heap_table_size)

            Допустимые значения — от 16384 до 134217728 включительно.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            Часовой пояс сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-options.html#option_mysqld_default-time-zone)
          type: string
        characterSetServer:
          description: |-
            Кодировка сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_character_set_server)
          type: string
        collationServer:
          description: |-
            Алгоритм сравнения символов сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_collation_server)
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Включает адаптивный хэш индекс Innodb

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index)
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Включает NUMA interleave политику при выделении памяти для буфера InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_numa_interleave)
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            Размер в байтах буфера, который InnoDB использует при записи логов на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_buffer_size)

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            Рамер в байтах одного файла redo логов InnnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_file_size)

            Допустимые значения — от 268435456 до 4294967296 включительно.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity_max)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций чтения.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_read_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций записи.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_write_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций очистки.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_purge_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Максимальное число потоков, которые могут исполняться внутри InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_thread_concurrency)

            Допустимые значения — от 0 до 1000 включительно.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Максимальный размер временного табличного пространства InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path)

            Допустимые значения — от 0 до 107374182400 включительно.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            Количество потоков, которые кешируются для обработки новых сетевых соединений.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_cache_size).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        threadStack:
          description: |-
            Размер стэка для потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_stack).

            Допустимые значения — от 131072 до 16777216 включительно.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            Минимальный размер буфера, который используется для сканирования простого индекса, сканирования индекса диапазона и объединений таблиц, не использующих индексы.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_join_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Размер буфера, который используется для сортировок в памяти.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_sort_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            Размер кеша определений таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_definition_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            Размер кеша открытых таблиц для всех потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            Количество сегментов кеша открытых таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache_instances).

            Допустимые значения — от 1 до 32 включительно.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Эта переменная определяет, разрешает ли сервер определенные нестандартные варианты поведения для значений по умолчанию и обработку значений NULL в столбцах TIMESTAMP.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp).
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_increment).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_offset).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Управляет тем, как часто сервер MySQL синхронизирует бинлог с диском.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_sync_binlog).

            Допустимые значения — от 0 до 4096 включительно.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            Размер кеша для хранения изменений бинлога во время транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_cache_size).

            Допустимые значения — от 4096 до 67108864 включительно.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Определяет, сколько микросекунд операция COMMIT ожидает перед синхронизацией бинлога на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay).

            Допустимые значения — от 0 до 1000000 включительно.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig5_7BinlogRowImage_in_MysqlConfig5_7_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            Когда он включен, он заставляет сервер записывать информационные события журнала, такие как события журнала запросов строк, в бинлог.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events).
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            Количество реплик которых ожидает мастер при синхронной репликации прежде чем подтвердить фиксацию транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count).

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig5_7SlaveParallelType_in_MysqlConfig5_7_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Устанавливает количество потоков приложения для параллельного выполнения транзакций репликации.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-replica.html#sysvar_slave_parallel_workers).

            Допустимые значения — от 0 до 64 включительно.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            Устанавливает размер хранимого на диске бинлога.

            Допустимые значения — от 1073741824 до 107374182400 включительно.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_interactive_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с не интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_wait_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = ON' чтобы предотвратить чтение устаревших данных.

            Допустимые значения — от 600 до 432000 включительно.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = OFF'.
            Значение должно быть меньше чем mdb_offline_mode_enable_lag.

            Допустимые значения — от 60 до 86400 включительно.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            Ограничение на потребление памяти для range optimizer.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size).

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
    MysqlConfig5_7AuthPlugin:
      description: |2-
         - MYSQL_NATIVE_PASSWORD: Использует [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/5.7/en/native-pluggable-authentication.html).
         - SHA256_PASSWORD: Использует [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/5.7/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    MysqlConfig5_7BinlogRowImage:
      type: string
      enum:
      - FULL
      - MINIMAL
      - NOBLOB
    MysqlConfig5_7SQLMode:
      type: string
      enum:
      - ALLOW_INVALID_DATES
      - ANSI_QUOTES
      - ERROR_FOR_DIVISION_BY_ZERO
      - HIGH_NOT_PRECEDENCE
      - IGNORE_SPACE
      - NO_AUTO_VALUE_ON_ZERO
      - NO_BACKSLASH_ESCAPES
      - NO_ENGINE_SUBSTITUTION
      - NO_UNSIGNED_SUBTRACTION
      - NO_ZERO_DATE
      - NO_ZERO_IN_DATE
      - NO_FIELD_OPTIONS
      - NO_KEY_OPTIONS
      - NO_TABLE_OPTIONS
      - ONLY_FULL_GROUP_BY
      - PAD_CHAR_TO_FULL_LENGTH
      - PIPES_AS_CONCAT
      - REAL_AS_FLOAT
      - STRICT_ALL_TABLES
      - STRICT_TRANS_TABLES
      - ANSI
      - TRADITIONAL
      - DB2
      - MAXDB
      - MSSQL
      - MYSQL323
      - MYSQL40
      - ORACLE
      - POSTGRESQL
      - NO_AUTO_CREATE_USER
      - NO_DIR_IN_CREATE
    MysqlConfig5_7SlaveParallelType:
      type: string
      enum:
      - DATABASE
      - LOGICAL_CLOCK
    MysqlConfig5_7TransactionIsolation:
      type: string
      enum:
      - READ_COMMITTED
      - REPEATABLE_READ
      - SERIALIZABLE
    MysqlConfig8_0:
      description: Поля и структура `MysqlConfig8_0` отражает параметры конфигурации
        MySQL 8.0.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Размер буфера InnoDB, который используется для кэширования данных таблиц и индексов.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size).

            Минимальное значение — 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            Максимальное количество одновременных подключений, которые принимает MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_connections).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Время, в течение которого запрос должен обрабатываться, прежде чем он начинает
            считаться медленным.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_long_query_time).
          type: number
          format: double
        generalLog:
          description: |-
            Включить запись общего лога запросов MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_general_log).
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Включить запись лога аудита MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/audit-log-reference.html#audit-log-options-variables).
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Режим SQL для сервера MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/sql-mode.html#sql-mode-setting).
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig8_0SQLMode'
        maxAllowedPacket:
          description: |-
            Максимальный размер одного пакета в байтах.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_allowed_packet).

            Допустимые значения — от 1024 до 1073741824 включительно.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig8_0AuthPlugin_in_MysqlConfig8_0_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Частота записи лога транзакций на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit)

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Максимальное время ожидания блокировки строки для транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout)

            Допустимые значения — от 1 до 28800 включительно.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig8_0TransactionIsolation_in_MysqlConfig8_0_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Выводить всю информацию о взаимных блокировках в журнал ошибок.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks)
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            Максимальное время ожидания чтения при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_read_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            Максимальное время ожидания записи при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_write_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            Максимальный размер результата для функции GROUP_CONCAT()

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_group_concat_max_len)

            Допустимые значения — от 4 до 33554432 включительно.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            Максимальный размер временных таблиц в памяти, прежде чем они будут помещены на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_tmp_table_size)

            Допустимые значения — от 1024 до 134217728 включительно.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            Максимальный размер пользователских MEMORY таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_heap_table_size)

            Допустимые значения — от 16384 до 134217728 включительно.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            Часовой пояс сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-options.html#option_mysqld_default-time-zone)
          type: string
        characterSetServer:
          description: |-
            Кодировка сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_character_set_server)
          type: string
        collationServer:
          description: |-
            Алгоритм сравнения символов сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_collation_server)
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Включает адаптивный хэш индекс Innodb

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index)
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Включает NUMA interleave политику при выделении памяти для буфера InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_numa_interleave)
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            Размер в байтах буфера, который InnoDB использует при записи логов на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_buffer_size)

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            Рамер в байтах одного файла redo логов InnnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_file_size)

            Допустимые значения — от 268435456 до 4294967296 включительно.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity_max)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций чтения.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_read_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций записи.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_write_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций очистки.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_purge_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Максимальное число потоков, которые могут исполняться внутри InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_thread_concurrency)

            Допустимые значения — от 0 до 1000 включительно.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Максимальный размер временного табличного пространства InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path)

            Допустимые значения — от 0 до 107374182400 включительно.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            Количество потоков, которые кешируются для обработки новых сетевых соединений.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_cache_size).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        threadStack:
          description: |-
            Размер стэка для потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_stack).

            Допустимые значения — от 131072 до 16777216 включительно.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            Минимальный размер буфера, который используется для сканирования простого индекса, сканирования индекса диапазона и объединений таблиц, не использующих индексы.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_join_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Размер буфера, который используется для сортировок в памяти.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_sort_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            Размер кеша определений таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_definition_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            Размер кеша открытых таблиц для всех потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            Количество сегментов кеша открытых таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache_instances).

            Допустимые значения — от 1 до 32 включительно.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Эта переменная определяет, разрешает ли сервер определенные нестандартные варианты поведения для значений по умолчанию и обработку значений NULL в столбцах TIMESTAMP.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp).
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_increment).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_offset).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Управляет тем, как часто сервер MySQL синхронизирует бинлог с диском.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_sync_binlog).

            Допустимые значения — от 0 до 4096 включительно.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            Размер кеша для хранения изменений бинлога во время транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_cache_size).

            Допустимые значения — от 4096 до 67108864 включительно.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Определяет, сколько микросекунд операция COMMIT ожидает перед синхронизацией бинлога на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay).

            Допустимые значения — от 0 до 1000000 включительно.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig8_0BinlogRowImage_in_MysqlConfig8_0_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            Когда он включен, он заставляет сервер записывать информационные события журнала, такие как события журнала запросов строк, в бинлог.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events).
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            Количество реплик которых ожидает мастер при синхронной репликации прежде чем подтвердить фиксацию транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count).

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig8_0SlaveParallelType_in_MysqlConfig8_0_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Устанавливает количество потоков приложения для параллельного выполнения транзакций репликации.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_slave_parallel_workers).

            Допустимые значения — от 0 до 64 включительно.
          type: integer
          format: int64
        regexpTimeLimit:
          description: |-
            Ограничение времени выполнения регулярных выражений выполняемых REGEXP_LIKE и похожими функциями

            Подробнее см. в [описании переменной в документации MySQL]((https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_regexp_time_limit).

            Допустимые значения — от 0 до 1048576 включительно.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            Устанавливает размер хранимого на диске бинлога.

            Допустимые значения — от 1073741824 до 107374182400 включительно.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_interactive_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с не интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_wait_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = ON' чтобы предотвратить чтение устаревших данных.

            Допустимые значения — от 600 до 432000 включительно.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = OFF'.
            Значение должно быть меньше чем mdb_offline_mode_enable_lag.

            Допустимые значения — от 60 до 86400 включительно.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            Ограничение на потребление памяти для range optimizer.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size).

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
    MysqlConfig8_0AuthPlugin:
      description: |2-
         - MYSQL_NATIVE_PASSWORD: Использует [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/native-pluggable-authentication.html).
         - CACHING_SHA2_PASSWORD: Использует [Caching SHA-2 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/caching-sha2-pluggable-authentication.html).
         - SHA256_PASSWORD: Использует [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    MysqlConfig8_0BinlogRowImage:
      type: string
      enum:
      - FULL
      - MINIMAL
      - NOBLOB
    MysqlConfig8_0SQLMode:
      type: string
      enum:
      - ALLOW_INVALID_DATES
      - ANSI_QUOTES
      - ERROR_FOR_DIVISION_BY_ZERO
      - HIGH_NOT_PRECEDENCE
      - IGNORE_SPACE
      - NO_AUTO_VALUE_ON_ZERO
      - NO_BACKSLASH_ESCAPES
      - NO_ENGINE_SUBSTITUTION
      - NO_UNSIGNED_SUBTRACTION
      - NO_ZERO_DATE
      - NO_ZERO_IN_DATE
      - ONLY_FULL_GROUP_BY
      - PAD_CHAR_TO_FULL_LENGTH
      - PIPES_AS_CONCAT
      - REAL_AS_FLOAT
      - STRICT_ALL_TABLES
      - STRICT_TRANS_TABLES
      - TIME_TRUNCATE_FRACTIONAL
      - ANSI
      - TRADITIONAL
      - NO_DIR_IN_CREATE
    MysqlConfig8_0SlaveParallelType:
      type: string
      enum:
      - DATABASE
      - LOGICAL_CLOCK
    MysqlConfig8_0TransactionIsolation:
      type: string
      enum:
      - READ_COMMITTED
      - REPEATABLE_READ
      - SERIALIZABLE
    MysqlConfigSet5_7:
      type: object
      properties:
        effectiveConfig:
          $ref: '#/components/schemas/MysqlConfig5_7_in_MysqlConfigSet5_7_effectiveConfig'
        userConfig:
          $ref: '#/components/schemas/MysqlConfig5_7_in_MysqlConfigSet5_7_userConfig'
        defaultConfig:
          $ref: '#/components/schemas/MysqlConfig5_7_in_MysqlConfigSet5_7_defaultConfig'
    MysqlConfigSet8_0:
      type: object
      properties:
        effectiveConfig:
          $ref: '#/components/schemas/MysqlConfig8_0_in_MysqlConfigSet8_0_effectiveConfig'
        userConfig:
          $ref: '#/components/schemas/MysqlConfig8_0_in_MysqlConfigSet8_0_userConfig'
        defaultConfig:
          $ref: '#/components/schemas/MysqlConfig8_0_in_MysqlConfigSet8_0_defaultConfig'
    Operation:
      description: |-
        Ресурс Operation. Дополнительные сведения см. в разделе
        [Объект Operation](/docs/api-design-guide/concepts/operation).
      type: object
      allOf:
      - type: object
        properties:
          id:
            description: Идентификатор операции.
            type: string
          description:
            description: |-
              Описание операции. Длина описания должна быть от 0 до 256
              символов.
            type: string
          createdAt:
            description: |-
              Время создания ресурса в формате в
              [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).

              Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
            type: string
            format: date-time
          createdBy:
            description: |-
              Идентификатор пользователя или сервисного
              аккаунта, инициировавшего операцию.
            type: string
          modifiedAt:
            description: |-
              Время, когда ресурс Operation последний раз
              обновлялся. Значение в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).

              Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
            type: string
            format: date-time
          done:
            description: |-
              Если значение равно `false` — операция еще
              выполняется. Если `true` — операция завершена, и задано значение
              одного из полей `error` или `response`.
            type: boolean
            format: boolean
          metadata:
            description: |-
              Метаданные операции. Обычно в поле содержится
              идентификатор ресурса, над которым выполняется операция. Если
              метод возвращает ресурс Operation, в описании метода приведена
              структура соответствующего ему поля `metadata`.
            type: object
      - title: result
        type: object
        maxProperties: 1
        properties:
          error:
            $ref: '#/components/schemas/rpcStatus'
            description: Описание ошибки в случае сбоя или отмены операции.
          response:
            description: |-
              Результат операции в случае успешного завершения. Если
              исходный метод не возвращает никаких данных при успешном
              завершении, например метод Delete, поле содержит объект
              [google.protobuf.Empty](https://developers.google.com/protocol-buffers/docs/reference/google.protobuf#empty). Если исходный метод — это стандартный метод Create
              / Update, поле содержит целевой ресурс операции. Если метод
              возвращает ресурс Operation, в описании метода приведена
              структура соответствующего ему поля `response`.
            type: object
    Permission:
      type: object
      properties:
        databaseName:
          description: Имя базы данных, к которой предоставляет доступ разрешение.
          type: string
        roles:
          description: |-
            Обязательное поле.
            Роли, предоставленные пользователю в базе данных.

            Минимальное количество элементов — 1.
          type: array
          items:
            $ref: '#/components/schemas/PermissionPrivilege'
    PermissionPrivilege:
      description: |2-
         - ALL_PRIVILEGES: Все привилегии, которые могут быть предоставлены пользователю.
         - ALTER: Изменение таблиц.
         - ALTER_ROUTINE: Изменение хранимых процедур и функций.
         - CREATE: Создание таблиц или индексов.
         - CREATE_ROUTINE: Создание хранимых процедур.
         - CREATE_TEMPORARY_TABLES: Создание временных таблиц.
         - CREATE_VIEW: Создание представлений.
         - DELETE: Удаление таблиц.
         - DROP: Удаление таблиц или представлений.
         - EVENT: Создание, изменение, удаление или отображение событий для планировщика событий.
         - EXECUTE: Выполнение хранимых процедур.
         - INDEX: Создание и удаление индексов.
         - INSERT: Вставка строк в базу данных.
         - LOCK_TABLES: Использование инструкции LOCK TABLES для таблиц, доступных с привилегией SELECT.
         - SELECT: Получение строк из таблиц.

        Некоторые операторы SELECT могут быть разрешены без привилегии SELECT. Привилегия SELECT
        необходима для всех операторов, считывающих значения столбцов. Подробнее в [документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/privileges-provided.html#priv_select).
         - SHOW_VIEW: Использование инструкции SHOW CREATE VIEW. Также необходимо для представлений,
        используемых с EXPLAIN.
         - TRIGGER: Создание, удаление, выполнение или отображение триггеров для таблицы.
         - UPDATE: Обновление строк в базе данных.
         - REFERENCES: Создание внешних ключей для таблиц.
      type: string
      enum:
      - ALL_PRIVILEGES
      - ALTER
      - ALTER_ROUTINE
      - CREATE
      - CREATE_ROUTINE
      - CREATE_TEMPORARY_TABLES
      - CREATE_VIEW
      - DELETE
      - DROP
      - EVENT
      - EXECUTE
      - INDEX
      - INSERT
      - LOCK_TABLES
      - SELECT
      - SHOW_VIEW
      - TRIGGER
      - UPDATE
      - REFERENCES
    ResourcePreset:
      description: Набор ресурсов для хоста MySQL.
      type: object
      properties:
        id:
          description: Идентификатор набора ресурсов.
          type: string
        zoneIds:
          description: Идентификаторы зон доступности, в которых доступен данный набор
            ресурсов.
          type: array
          items:
            type: string
        cores:
          description: Количество vCPU для хоста MySQL, созданного с данным набором
            ресурсов.
          type: string
          format: int64
        memory:
          description: |-
            Объем оперативной памяти для хоста MySQL, созданного с данным набором ресурсов, в
            байтах.
          type: string
          format: int64
    Resources:
      type: object
      properties:
        resourcePresetId:
          description: |-
            Идентификатор набора вычислительных ресурсов, доступных хосту (процессор, память и т. д.).
            Все доступные наборы ресурсов перечислены в [документации](/docs/managed-mysql/concepts/instance-types).
          type: string
        diskSize:
          description: Объем хранилища, доступного хосту.
          type: string
          format: int64
        diskTypeId:
          description: |-
            Тип хранилища для хоста. Возможные значения:
            * network-ssd — сетевой SSD-диск;
            * local-ssd — локальное SSD-хранилище.
          type: string
    RestoreClusterMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор нового кластера MySQL, создаваемого из резервной
            копии.
          type: string
        backupId:
          description: Идентификатор резервной копии, используемой для создания кластера.
          type: string
    RestoreClusterRequest:
      type: object
      properties:
        backupId:
          description: |-
            Обязательное поле.
            Идентификатор резервной копии, из которой следует создать кластер. Чтобы получить
            идентификатор резервной копии, используйте запрос [listBackups](/docs/managed-mysql/api-ref/Cluster/listBackups).
          type: string
        time:
          description: |-
            Обязательное поле.
            Момент времени, на который должен быть восстановлен кластер MySQL.

            Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
          type: string
          format: date-time
        name:
          description: |-
            Обязательное поле.
            Имя нового кластера MySQL. Имя должно быть уникальным в каталоге.

            Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_-]* ``.
          type: string
        description:
          description: |-
            Описание нового кластера MySQL.

            Максимальная длина строки в символах — 256.
          type: string
        labels:
          description: |-
            Пользовательские метки для кластера MySQL в виде пар `key:value`. Максимум 64 на ресурс.
            Например, "project": "mvp" или "source": "dictionary".

            Не более 64 на ресурс.
            Длина строки в символах для каждого ключа должна быть от 1 до 63.
            Каждый ключ должен соответствовать регулярному выражению `` [a-z][-_0-9a-z]* ``.
            Максимальная длина строки в символах для каждого значения — 63.
            Каждое значение должно соответствовать регулярному выражению `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        environment:
          $ref: '#/components/schemas/ClusterEnvironment_in_RestoreClusterRequest_environment'
        configSpec:
          $ref: '#/components/schemas/ConfigSpec_in_RestoreClusterRequest_configSpec'
        hostSpecs:
          description: |-
            Конфигурации для хостов MySQL, которые должны быть добавлены к кластеру, создаваемогму
            из резервной копии.
          type: array
          items:
            $ref: '#/components/schemas/HostSpec'
        networkId:
          description: |-
            Идентификатор сети, в которой нужно создать кластер MySQL.

            Максимальная длина строки в символах — 50.
          type: string
        folderId:
          description: |-
            Идентификатор каталога, в котором нужно создать кластер MySQL.

            Максимальная длина строки в символах — 50.
          type: string
    RevokeUserPermissionMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор кластера MySQL, которому принадлежит пользователь.
          type: string
        userName:
          description: Имя пользователя, чье разрешение отзывается.
          type: string
    RevokeUserPermissionRequest:
      type: object
      properties:
        permission:
          $ref: '#/components/schemas/Permission_in_RevokeUserPermissionRequest_permission'
    Service:
      type: object
      properties:
        type:
          $ref: '#/components/schemas/ServiceType_in_Service_type'
        health:
          $ref: '#/components/schemas/ServiceHealth_in_Service_health'
    ServiceHealth:
      description: |2-
         - HEALTH_UNKNOWN: Работоспособность сервера неизвестна.
         - ALIVE: Сервер работает нормально.
         - DEAD: Сервер отключен или не отвечает.
      type: string
      enum:
      - HEALTH_UNKNOWN
      - ALIVE
      - DEAD
      default: HEALTH_UNKNOWN
    ServiceType:
      description: ' - MYSQL: Хост является сервером MySQL.'
      type: string
      enum:
      - MYSQL
    StartClusterFailoverMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор кластера MySQL, для которого переключается мастер.
          type: string
    StartClusterFailoverRequest:
      type: object
      properties:
        hostName:
          description: |-
            Новый хост-мастер. Если хост не указан, кластер переключается на реплику с самыми
            свежими данными.

            Максимальная длина строки в символах — 253.
          type: string
    StartClusterMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор запускаемого кластера MySQL.
          type: string
    StopClusterMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор останавливаемого кластера MySQL.
          type: string
    TimeOfDay:
      description: |-
        Время суток.
        Дата и часовой пояс либо не учитываются, либо задаются в других местах.

        API может разрешить использование високосной секунды.

        Связанные типы: [google.type.Date](https://github.com/googleapis/googleapis/blob/master/google/type/date.proto) и [google.protobuf.Timestamp](https://github.com/protocolbuffers/protobuf/blob/master/src/google/protobuf/timestamp.proto).
      type: object
      properties:
        hours:
          description: |-
            Часы.
            Допустимые значения: от 0 до 23.

            API может разрешить использовать значение в формате "24:00:00" в требующих этого сценариях (например, для указания времени закрытия учреждения).
          type: integer
          format: int32
        minutes:
          description: |-
            Минуты.
            Допустимые значения: от 0 до 59.
          type: integer
          format: int32
        seconds:
          description: |-
            Секунды.
            Стандартные допустимые значения: от 0 до 59.

            API может разрешить использовать значение 60, если также разрешено использование високосной секунды.
          type: integer
          format: int32
        nanos:
          description: |-
            Доли секунды (в наносекундах).
            Допустимые значения: от 0 до 999999999.
          type: integer
          format: int32
    UpdateClusterMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор изменяемого кластера MySQL.
          type: string
    UpdateClusterRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateClusterRequest_updateMask'
        description:
          description: |-
            Новое описание кластера MySQL.

            Максимальная длина строки в символах — 256.
          type: string
        labels:
          description: |-
            Пользовательские метки для кластера MySQL в виде пар `key:value`. Максимум 64 на ресурс.
            Например, "project": "mvp" или "source": "dictionary".

            Новый набор меток полностью заменит старый. Чтобы добавить метку, запросите текущий
            набор меток с помощью метода [get](/docs/managed-mysql/api-ref/Cluster/get), затем отправьте запрос [update](/docs/managed-mysql/api-ref/Cluster/update),
            добавив новую метку в этот набор.

            Не более 64 на ресурс.
            Длина строки в символах для каждого ключа должна быть от 1 до 63.
            Каждый ключ должен соответствовать регулярному выражению `` [a-z][-_0-9a-z]* ``.
            Максимальная длина строки в символах для каждого значения — 63.
            Каждое значение должно соответствовать регулярному выражению `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        configSpec:
          $ref: '#/components/schemas/ConfigSpec_in_UpdateClusterRequest_configSpec'
        name:
          description: |-
            Новое имя кластера.

            Максимальная длина строки в символах — 63.
            Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_-]* ``.
          type: string
    UpdateUserMetadata:
      type: object
      properties:
        clusterId:
          description: Идентификатор кластера MySQL, которому принадлежит пользователь.
          type: string
        userName:
          description: Имя изменяемого пользователя.
          type: string
    UpdateUserRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateUserRequest_updateMask'
        password:
          description: |-
            Новый пароль для пользователя.

            Длина строки в символах должна быть от 8 до 128.
          type: string
        permissions:
          description: Новый набор разрешений для пользователя.
          type: array
          items:
            $ref: '#/components/schemas/Permission'
        globalPermissions:
          description: Новый набор глобальных разрешений, которые следует предоставить
            пользователю.
          type: array
          items:
            $ref: '#/components/schemas/GlobalPermission'
        connectionLimits:
          $ref: '#/components/schemas/ConnectionLimits_in_UpdateUserRequest_connectionLimits'
        authenticationPlugin:
          $ref: '#/components/schemas/v1AuthPlugin_in_UpdateUserRequest_authenticationPlugin'
    User:
      description: Пользователь MySQL. Подробнее см. в [документации](/docs/managed-mysql/concepts).
      type: object
      properties:
        name:
          description: Имя пользователя MySQL.
          type: string
        clusterId:
          description: Идентификатор кластера MySQL, которому принадлежит пользователь.
          type: string
        permissions:
          description: Набор разрешений, предоставленных пользователю.
          type: array
          items:
            $ref: '#/components/schemas/Permission'
        globalPermissions:
          description: Набор глобальных разрешений, которые следует предоставить пользователю.
          type: array
          items:
            $ref: '#/components/schemas/GlobalPermission'
        connectionLimits:
          $ref: '#/components/schemas/ConnectionLimits_in_User_connectionLimits'
        authenticationPlugin:
          $ref: '#/components/schemas/v1AuthPlugin_in_User_authenticationPlugin'
    UserSpec:
      type: object
      properties:
        name:
          description: |-
            Обязательное поле.
            Имя пользователя MySQL.

            Максимальная длина строки в символах — 32.
            Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_]* ``.
          type: string
        password:
          description: |-
            Обязательное поле.
            Пароль пользователя MySQL.

            Длина строки в символах должна быть от 8 до 128.
          type: string
        permissions:
          description: Набор разрешений, которые следует предоставить пользователю.
          type: array
          items:
            $ref: '#/components/schemas/Permission'
        globalPermissions:
          description: Набор глобальных разрешений, которые следует предоставить пользователю.
          type: array
          items:
            $ref: '#/components/schemas/GlobalPermission'
        connectionLimits:
          $ref: '#/components/schemas/ConnectionLimits_in_UserSpec_connectionLimits'
        authenticationPlugin:
          $ref: '#/components/schemas/v1AuthPlugin_in_UserSpec_authenticationPlugin'
    rpcStatus:
      description: Описание ошибки в случае сбоя или отмены операции.
      type: object
      properties:
        code:
          description: Код ошибки. Значение из списка [google.rpc.Code](https://github.com/googleapis/googleapis/blob/master/google/rpc/code.proto).
          type: integer
          format: int32
        message:
          description: Текст ошибки.
          type: string
        details:
          description: Список сообщений с подробными сведениями об ошибке.
          type: array
          items:
            type: object
    v1AuthPlugin:
      description: |2-
         - MYSQL_NATIVE_PASSWORD: Использует [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/native-pluggable-authentication.html).
         - CACHING_SHA2_PASSWORD: Использует [Caching SHA-2 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/caching-sha2-pluggable-authentication.html).
         - SHA256_PASSWORD: Использует [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    ClusterEnvironment_in_Cluster_environment:
      description: |-
        Среда развертывания кластера MySQL.

         - PRODUCTION: Стабильная среда с осторожной политикой обновления: во время регулярного
        обслуживания применяются только срочные исправления.
         - PRESTABLE: Среда с более агрессивной политикой обновления: новые версии развертываются
        независимо от обратной совместимости.
      type: string
      enum:
      - PRODUCTION
      - PRESTABLE
    ClusterConfig_in_Cluster_config:
      description: Конфигурация кластера MySQL.
      type: object
      allOf:
      - type: object
        properties:
          version:
            description: Версия серверного программного обеспечения MySQL.
            type: string
          resources:
            $ref: '#/components/schemas/Resources'
            description: Ресурсы, выделенные хостам MySQL.
          backupWindowStart:
            $ref: '#/components/schemas/TimeOfDay'
            description: Время запуска ежедневного резервного копирования, в часовом
              поясе UTC.
          access:
            $ref: '#/components/schemas/Access'
            description: Политика доступа к БД
      - title: mysql_config
        type: object
        maxProperties: 1
        properties:
          mysqlConfig_5_7:
            $ref: '#/components/schemas/MysqlConfigSet5_7'
            description: Конфигурация сервера MySQL 5.7.
          mysqlConfig_8_0:
            $ref: '#/components/schemas/MysqlConfigSet8_0'
            description: Конфигурация сервера MySQL 8.0.
    ClusterHealth_in_Cluster_health:
      description: |-
        Агрегированная работоспособность кластера.

         - HEALTH_UNKNOWN: Состояние кластера неизвестно (`health` для каждого хоста в кластере — UNKNOWN).
         - ALIVE: Кластер работает нормально (`health` для каждого хоста в кластере — ALIVE).
         - DEAD: Кластер не работает (`health` для каждого узла в кластере — DEAD).
         - DEGRADED: Кластер работает неоптимально (`health` по крайней мере для одного узла в кластере
        не ALIVE).
      type: string
      enum:
      - HEALTH_UNKNOWN
      - ALIVE
      - DEAD
      - DEGRADED
      default: HEALTH_UNKNOWN
    ClusterStatus_in_Cluster_status:
      description: |-
        Текущее состояние кластера.

         - STATUS_UNKNOWN: Состояние кластера неизвестно.
         - CREATING: Кластер создается.
         - RUNNING: Кластер работает нормально.
         - ERROR: На кластере произошла ошибка, блокирующая работу.
         - UPDATING: Кластер изменяется.
         - STOPPING: Кластер останавливается.
         - STOPPED: Кластер остановлен.
         - STARTING: Кластер запускается.
      type: string
      enum:
      - STATUS_UNKNOWN
      - CREATING
      - RUNNING
      - ERROR
      - UPDATING
      - STOPPING
      - STOPPED
      - STARTING
      default: STATUS_UNKNOWN
    ClusterEnvironment_in_CreateClusterRequest_environment:
      description: |-
        Среда развертывания кластера MySQL.

         - PRODUCTION: Стабильная среда с осторожной политикой обновления: во время регулярного
        обслуживания применяются только срочные исправления.
         - PRESTABLE: Среда с более агрессивной политикой обновления: новые версии развертываются
        независимо от обратной совместимости.
      type: string
      enum:
      - PRODUCTION
      - PRESTABLE
    ConfigSpec_in_CreateClusterRequest_configSpec:
      description: Конфигурация и ресурсы для хостов, которые должны быть созданы
        для кластера MySQL.
      type: object
      allOf:
      - type: object
        properties:
          version:
            description: |-
              Версия MySQL, используемая в кластере. Возможные значения:
              * 5.7
              * 8.0
            type: string
          resources:
            $ref: '#/components/schemas/Resources'
            description: Ресурсы, выделенные хостам MySQL.
          backupWindowStart:
            $ref: '#/components/schemas/TimeOfDay'
            description: Время запуска ежедневного резервного копирования, в часовом
              поясе UTC.
          access:
            $ref: '#/components/schemas/Access'
            description: Политика доступа к БД.
      - title: mysql_config
        type: object
        maxProperties: 1
        properties:
          mysqlConfig_5_7:
            $ref: '#/components/schemas/MysqlConfig5_7'
            description: Конфигурация для кластера MySQL 5.7.
          mysqlConfig_8_0:
            $ref: '#/components/schemas/MysqlConfig8_0'
            description: Конфигурация для кластера MySQL 8.0.
    DatabaseSpec_in_CreateDatabaseRequest_databaseSpec:
      description: |-
        Обязательное поле.
        Конфигурация создаваемой базы данных.
      type: object
      properties:
        name:
          description: |-
            Обязательное поле.
            Имя базы данных MySQL.

            Максимальная длина строки в символах — 63.
            Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_-]* ``.
          type: string
    UserSpec_in_CreateUserRequest_userSpec:
      description: |-
        Обязательное поле.
        Свойства создаваемого пользователя.
      type: object
      properties:
        name:
          description: |-
            Обязательное поле.
            Имя пользователя MySQL.

            Максимальная длина строки в символах — 32.
            Значение должно соответствовать регулярному выражению `` [a-zA-Z0-9_]* ``.
          type: string
        password:
          description: |-
            Обязательное поле.
            Пароль пользователя MySQL.

            Длина строки в символах должна быть от 8 до 128.
          type: string
        permissions:
          description: Набор разрешений, которые следует предоставить пользователю.
          type: array
          items:
            $ref: '#/components/schemas/Permission'
        globalPermissions:
          description: Набор глобальных разрешений, которые следует предоставить пользователю.
          type: array
          items:
            $ref: '#/components/schemas/GlobalPermission'
        connectionLimits:
          $ref: '#/components/schemas/ConnectionLimits_in_UserSpec_connectionLimits'
        authenticationPlugin:
          $ref: '#/components/schemas/v1AuthPlugin_in_UserSpec_authenticationPlugin'
    Permission_in_GrantUserPermissionRequest_permission:
      description: |-
        Обязательное поле.
        Разрешение, которое должно быть предоставлено указанному пользователю.
      type: object
      properties:
        databaseName:
          description: Имя базы данных, к которой предоставляет доступ разрешение.
          type: string
        roles:
          description: |-
            Обязательное поле.
            Роли, предоставленные пользователю в базе данных.

            Минимальное количество элементов — 1.
          type: array
          items:
            $ref: '#/components/schemas/PermissionPrivilege'
    Resources_in_Host_resources:
      description: Ресурсы, выделенные хосту.
      type: object
      properties:
        resourcePresetId:
          description: |-
            Идентификатор набора вычислительных ресурсов, доступных хосту (процессор, память и т. д.).
            Все доступные наборы ресурсов перечислены в [документации](/docs/managed-mysql/concepts/instance-types).
          type: string
        diskSize:
          description: Объем хранилища, доступного хосту.
          type: string
          format: int64
        diskTypeId:
          description: |-
            Тип хранилища для хоста. Возможные значения:
            * network-ssd — сетевой SSD-диск;
            * local-ssd — локальное SSD-хранилище.
          type: string
    HostRole_in_Host_role:
      description: |-
        Роль хоста в кластере.

         - ROLE_UNKNOWN: Роль хоста в кластере неизвестна.
         - MASTER: Хост является мастером в кластере MySQL.
         - REPLICA: Хост является репликой в кластере MySQL.
      type: string
      enum:
      - ROLE_UNKNOWN
      - MASTER
      - REPLICA
      default: ROLE_UNKNOWN
    HostHealth_in_Host_health:
      description: |-
        Код работоспособности хоста.

         - HEALTH_UNKNOWN: Состояние хоста неизвестно.
         - ALIVE: Хозяин выполняет все свои функции нормально.
         - DEAD: Хост не работает и не может выполнять свои основные функции.
         - DEGRADED: Хост деградировал, и может выполнять только некоторые из своих основных функций.
      type: string
      enum:
      - HEALTH_UNKNOWN
      - ALIVE
      - DEAD
      - DEGRADED
      default: HEALTH_UNKNOWN
    MysqlConfig5_7AuthPlugin_in_MysqlConfig5_7_defaultAuthenticationPlugin:
      description: |-
        Плагин аутентификации, используемый в управляемом кластере MySQL.

        Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_default_authentication_plugin).

         - MYSQL_NATIVE_PASSWORD: Использует [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/5.7/en/native-pluggable-authentication.html).
         - SHA256_PASSWORD: Использует [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/5.7/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    MysqlConfig5_7TransactionIsolation_in_MysqlConfig5_7_transactionIsolation:
      description: |-
        Уровень изоляции транзакций по-умолчанию.

        Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_transaction_isolation)
      type: string
      enum:
      - READ_COMMITTED
      - REPEATABLE_READ
      - SERIALIZABLE
    MysqlConfig5_7BinlogRowImage_in_MysqlConfig5_7_binlogRowImage:
      description: |-
        Для репликации на основе строк в MySQL эта переменная определяет, как образы строк записываются в бинлог.

        Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_row_image).
      type: string
      enum:
      - FULL
      - MINIMAL
      - NOBLOB
    MysqlConfig5_7SlaveParallelType_in_MysqlConfig5_7_slaveParallelType:
      description: |-
        При использовании многопоточной реплики эта переменная определяет политику, используемую для определения того, какие транзакции могут выполняться параллельно на реплике.

        Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-replica.html#sysvar_slave_parallel_type).
      type: string
      enum:
      - DATABASE
      - LOGICAL_CLOCK
    MysqlConfig8_0AuthPlugin_in_MysqlConfig8_0_defaultAuthenticationPlugin:
      description: |-
        Плагин аутентификации, используемый в управляемом кластере MySQL.

        Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_default_authentication_plugin).

         - MYSQL_NATIVE_PASSWORD: Использует [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/native-pluggable-authentication.html).
         - CACHING_SHA2_PASSWORD: Использует [Caching SHA-2 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/caching-sha2-pluggable-authentication.html).
         - SHA256_PASSWORD: Использует [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    MysqlConfig8_0TransactionIsolation_in_MysqlConfig8_0_transactionIsolation:
      description: |-
        Уровень изоляции транзакций по-умолчанию.

        Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_transaction_isolation)
      type: string
      enum:
      - READ_COMMITTED
      - REPEATABLE_READ
      - SERIALIZABLE
    MysqlConfig8_0BinlogRowImage_in_MysqlConfig8_0_binlogRowImage:
      description: |-
        Для репликации на основе строк в MySQL эта переменная определяет, как образы строк записываются в бинлог.

        Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_row_image).
      type: string
      enum:
      - FULL
      - MINIMAL
      - NOBLOB
    MysqlConfig8_0SlaveParallelType_in_MysqlConfig8_0_slaveParallelType:
      description: |-
        При использовании многопоточной реплики эта переменная определяет политику, используемую для определения того, какие транзакции могут выполняться параллельно на реплике.

        Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_slave_parallel_type).
      type: string
      enum:
      - DATABASE
      - LOGICAL_CLOCK
    MysqlConfig5_7_in_MysqlConfigSet5_7_effectiveConfig:
      description: |-
        Действующие параметры для кластера MySQL 5.7 (сочетание параметров, определенных в
        `userConfig` и `defaultConfig`).

        Поля и структура `MysqlConfig5_7` отражает параметры конфигурации MySQL 5.7.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Размер буфера InnoDB, который используется для кэширования данных таблиц и индексов.

            Подробнее см. в [описании параметра в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size).

            Минимальное значение — 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            Максимальное количество одновременных подключений, которые принимает MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_connections).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Время, в течение которого запрос должен обрабатываться, прежде чем он начинает
            считаться медленным.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_long_query_time).
          type: number
          format: double
        generalLog:
          description: |-
            Включить запись общего лога запросов MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_general_log).
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Включить запись лога аудита MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/mysql-security-excerpt/5.6/en/audit-log-options-variables.html#option_mysqld_audit-log).
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Режим SQL для сервера MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/sql-mode.html#sql-mode-setting).
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig5_7SQLMode'
        maxAllowedPacket:
          description: |-
            Максимальный размер одного пакета в байтах.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_allowed_packet).

            Допустимые значения — от 1024 до 1073741824 включительно.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig5_7AuthPlugin_in_MysqlConfig5_7_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Частота записи лога транзакций на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit)

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Максимальное время ожидания блокировки строки для транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout)

            Допустимые значения — от 1 до 28800 включительно.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig5_7TransactionIsolation_in_MysqlConfig5_7_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Выводить всю информацию о взаимных блокировках в журнал ошибок.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks)
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            Максимальное время ожидания чтения при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_read_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            Максимальное время ожидания записи при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_write_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            Максимальный размер результата для функции GROUP_CONCAT()

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_group_concat_max_len)

            Допустимые значения — от 4 до 33554432 включительно.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            Максимальный размер временных таблиц в памяти, прежде чем они будут помещены на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_tmp_table_size)

            Допустимые значения — от 1024 до 134217728 включительно.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            Максимальный размер пользователских MEMORY таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_heap_table_size)

            Допустимые значения — от 16384 до 134217728 включительно.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            Часовой пояс сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-options.html#option_mysqld_default-time-zone)
          type: string
        characterSetServer:
          description: |-
            Кодировка сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_character_set_server)
          type: string
        collationServer:
          description: |-
            Алгоритм сравнения символов сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_collation_server)
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Включает адаптивный хэш индекс Innodb

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index)
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Включает NUMA interleave политику при выделении памяти для буфера InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_numa_interleave)
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            Размер в байтах буфера, который InnoDB использует при записи логов на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_buffer_size)

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            Рамер в байтах одного файла redo логов InnnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_file_size)

            Допустимые значения — от 268435456 до 4294967296 включительно.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity_max)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций чтения.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_read_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций записи.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_write_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций очистки.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_purge_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Максимальное число потоков, которые могут исполняться внутри InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_thread_concurrency)

            Допустимые значения — от 0 до 1000 включительно.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Максимальный размер временного табличного пространства InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path)

            Допустимые значения — от 0 до 107374182400 включительно.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            Количество потоков, которые кешируются для обработки новых сетевых соединений.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_cache_size).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        threadStack:
          description: |-
            Размер стэка для потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_stack).

            Допустимые значения — от 131072 до 16777216 включительно.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            Минимальный размер буфера, который используется для сканирования простого индекса, сканирования индекса диапазона и объединений таблиц, не использующих индексы.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_join_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Размер буфера, который используется для сортировок в памяти.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_sort_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            Размер кеша определений таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_definition_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            Размер кеша открытых таблиц для всех потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            Количество сегментов кеша открытых таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache_instances).

            Допустимые значения — от 1 до 32 включительно.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Эта переменная определяет, разрешает ли сервер определенные нестандартные варианты поведения для значений по умолчанию и обработку значений NULL в столбцах TIMESTAMP.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp).
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_increment).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_offset).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Управляет тем, как часто сервер MySQL синхронизирует бинлог с диском.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_sync_binlog).

            Допустимые значения — от 0 до 4096 включительно.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            Размер кеша для хранения изменений бинлога во время транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_cache_size).

            Допустимые значения — от 4096 до 67108864 включительно.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Определяет, сколько микросекунд операция COMMIT ожидает перед синхронизацией бинлога на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay).

            Допустимые значения — от 0 до 1000000 включительно.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig5_7BinlogRowImage_in_MysqlConfig5_7_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            Когда он включен, он заставляет сервер записывать информационные события журнала, такие как события журнала запросов строк, в бинлог.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events).
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            Количество реплик которых ожидает мастер при синхронной репликации прежде чем подтвердить фиксацию транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count).

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig5_7SlaveParallelType_in_MysqlConfig5_7_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Устанавливает количество потоков приложения для параллельного выполнения транзакций репликации.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-replica.html#sysvar_slave_parallel_workers).

            Допустимые значения — от 0 до 64 включительно.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            Устанавливает размер хранимого на диске бинлога.

            Допустимые значения — от 1073741824 до 107374182400 включительно.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_interactive_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с не интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_wait_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = ON' чтобы предотвратить чтение устаревших данных.

            Допустимые значения — от 600 до 432000 включительно.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = OFF'.
            Значение должно быть меньше чем mdb_offline_mode_enable_lag.

            Допустимые значения — от 60 до 86400 включительно.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            Ограничение на потребление памяти для range optimizer.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size).

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
    MysqlConfig5_7_in_MysqlConfigSet5_7_userConfig:
      description: |-
        Пользовательские настройки для кластера MySQL 5.7.

        Поля и структура `MysqlConfig5_7` отражает параметры конфигурации MySQL 5.7.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Размер буфера InnoDB, который используется для кэширования данных таблиц и индексов.

            Подробнее см. в [описании параметра в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size).

            Минимальное значение — 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            Максимальное количество одновременных подключений, которые принимает MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_connections).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Время, в течение которого запрос должен обрабатываться, прежде чем он начинает
            считаться медленным.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_long_query_time).
          type: number
          format: double
        generalLog:
          description: |-
            Включить запись общего лога запросов MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_general_log).
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Включить запись лога аудита MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/mysql-security-excerpt/5.6/en/audit-log-options-variables.html#option_mysqld_audit-log).
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Режим SQL для сервера MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/sql-mode.html#sql-mode-setting).
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig5_7SQLMode'
        maxAllowedPacket:
          description: |-
            Максимальный размер одного пакета в байтах.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_allowed_packet).

            Допустимые значения — от 1024 до 1073741824 включительно.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig5_7AuthPlugin_in_MysqlConfig5_7_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Частота записи лога транзакций на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit)

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Максимальное время ожидания блокировки строки для транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout)

            Допустимые значения — от 1 до 28800 включительно.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig5_7TransactionIsolation_in_MysqlConfig5_7_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Выводить всю информацию о взаимных блокировках в журнал ошибок.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks)
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            Максимальное время ожидания чтения при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_read_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            Максимальное время ожидания записи при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_write_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            Максимальный размер результата для функции GROUP_CONCAT()

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_group_concat_max_len)

            Допустимые значения — от 4 до 33554432 включительно.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            Максимальный размер временных таблиц в памяти, прежде чем они будут помещены на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_tmp_table_size)

            Допустимые значения — от 1024 до 134217728 включительно.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            Максимальный размер пользователских MEMORY таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_heap_table_size)

            Допустимые значения — от 16384 до 134217728 включительно.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            Часовой пояс сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-options.html#option_mysqld_default-time-zone)
          type: string
        characterSetServer:
          description: |-
            Кодировка сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_character_set_server)
          type: string
        collationServer:
          description: |-
            Алгоритм сравнения символов сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_collation_server)
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Включает адаптивный хэш индекс Innodb

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index)
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Включает NUMA interleave политику при выделении памяти для буфера InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_numa_interleave)
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            Размер в байтах буфера, который InnoDB использует при записи логов на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_buffer_size)

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            Рамер в байтах одного файла redo логов InnnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_file_size)

            Допустимые значения — от 268435456 до 4294967296 включительно.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity_max)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций чтения.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_read_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций записи.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_write_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций очистки.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_purge_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Максимальное число потоков, которые могут исполняться внутри InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_thread_concurrency)

            Допустимые значения — от 0 до 1000 включительно.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Максимальный размер временного табличного пространства InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path)

            Допустимые значения — от 0 до 107374182400 включительно.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            Количество потоков, которые кешируются для обработки новых сетевых соединений.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_cache_size).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        threadStack:
          description: |-
            Размер стэка для потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_stack).

            Допустимые значения — от 131072 до 16777216 включительно.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            Минимальный размер буфера, который используется для сканирования простого индекса, сканирования индекса диапазона и объединений таблиц, не использующих индексы.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_join_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Размер буфера, который используется для сортировок в памяти.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_sort_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            Размер кеша определений таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_definition_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            Размер кеша открытых таблиц для всех потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            Количество сегментов кеша открытых таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache_instances).

            Допустимые значения — от 1 до 32 включительно.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Эта переменная определяет, разрешает ли сервер определенные нестандартные варианты поведения для значений по умолчанию и обработку значений NULL в столбцах TIMESTAMP.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp).
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_increment).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_offset).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Управляет тем, как часто сервер MySQL синхронизирует бинлог с диском.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_sync_binlog).

            Допустимые значения — от 0 до 4096 включительно.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            Размер кеша для хранения изменений бинлога во время транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_cache_size).

            Допустимые значения — от 4096 до 67108864 включительно.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Определяет, сколько микросекунд операция COMMIT ожидает перед синхронизацией бинлога на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay).

            Допустимые значения — от 0 до 1000000 включительно.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig5_7BinlogRowImage_in_MysqlConfig5_7_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            Когда он включен, он заставляет сервер записывать информационные события журнала, такие как события журнала запросов строк, в бинлог.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events).
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            Количество реплик которых ожидает мастер при синхронной репликации прежде чем подтвердить фиксацию транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count).

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig5_7SlaveParallelType_in_MysqlConfig5_7_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Устанавливает количество потоков приложения для параллельного выполнения транзакций репликации.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-replica.html#sysvar_slave_parallel_workers).

            Допустимые значения — от 0 до 64 включительно.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            Устанавливает размер хранимого на диске бинлога.

            Допустимые значения — от 1073741824 до 107374182400 включительно.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_interactive_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с не интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_wait_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = ON' чтобы предотвратить чтение устаревших данных.

            Допустимые значения — от 600 до 432000 включительно.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = OFF'.
            Значение должно быть меньше чем mdb_offline_mode_enable_lag.

            Допустимые значения — от 60 до 86400 включительно.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            Ограничение на потребление памяти для range optimizer.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size).

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
    MysqlConfig5_7_in_MysqlConfigSet5_7_defaultConfig:
      description: |-
        Конфигурация по умолчанию для кластера MySQL 5.7.

        Поля и структура `MysqlConfig5_7` отражает параметры конфигурации MySQL 5.7.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Размер буфера InnoDB, который используется для кэширования данных таблиц и индексов.

            Подробнее см. в [описании параметра в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size).

            Минимальное значение — 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            Максимальное количество одновременных подключений, которые принимает MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_connections).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Время, в течение которого запрос должен обрабатываться, прежде чем он начинает
            считаться медленным.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_long_query_time).
          type: number
          format: double
        generalLog:
          description: |-
            Включить запись общего лога запросов MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_general_log).
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Включить запись лога аудита MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/mysql-security-excerpt/5.6/en/audit-log-options-variables.html#option_mysqld_audit-log).
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Режим SQL для сервера MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/sql-mode.html#sql-mode-setting).
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig5_7SQLMode'
        maxAllowedPacket:
          description: |-
            Максимальный размер одного пакета в байтах.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_allowed_packet).

            Допустимые значения — от 1024 до 1073741824 включительно.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig5_7AuthPlugin_in_MysqlConfig5_7_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Частота записи лога транзакций на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit)

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Максимальное время ожидания блокировки строки для транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout)

            Допустимые значения — от 1 до 28800 включительно.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig5_7TransactionIsolation_in_MysqlConfig5_7_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Выводить всю информацию о взаимных блокировках в журнал ошибок.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks)
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            Максимальное время ожидания чтения при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_read_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            Максимальное время ожидания записи при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_net_write_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            Максимальный размер результата для функции GROUP_CONCAT()

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_group_concat_max_len)

            Допустимые значения — от 4 до 33554432 включительно.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            Максимальный размер временных таблиц в памяти, прежде чем они будут помещены на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_tmp_table_size)

            Допустимые значения — от 1024 до 134217728 включительно.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            Максимальный размер пользователских MEMORY таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_max_heap_table_size)

            Допустимые значения — от 16384 до 134217728 включительно.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            Часовой пояс сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-options.html#option_mysqld_default-time-zone)
          type: string
        characterSetServer:
          description: |-
            Кодировка сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_character_set_server)
          type: string
        collationServer:
          description: |-
            Алгоритм сравнения символов сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_collation_server)
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Включает адаптивный хэш индекс Innodb

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index)
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Включает NUMA interleave политику при выделении памяти для буфера InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_numa_interleave)
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            Размер в байтах буфера, который InnoDB использует при записи логов на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_buffer_size)

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            Рамер в байтах одного файла redo логов InnnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_log_file_size)

            Допустимые значения — от 268435456 до 4294967296 включительно.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_io_capacity_max)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций чтения.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_read_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций записи.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_write_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций очистки.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_purge_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Максимальное число потоков, которые могут исполняться внутри InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_thread_concurrency)

            Допустимые значения — от 0 до 1000 включительно.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Максимальный размер временного табличного пространства InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path)

            Допустимые значения — от 0 до 107374182400 включительно.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            Количество потоков, которые кешируются для обработки новых сетевых соединений.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_cache_size).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        threadStack:
          description: |-
            Размер стэка для потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_thread_stack).

            Допустимые значения — от 131072 до 16777216 включительно.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            Минимальный размер буфера, который используется для сканирования простого индекса, сканирования индекса диапазона и объединений таблиц, не использующих индексы.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_join_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Размер буфера, который используется для сортировок в памяти.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_sort_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            Размер кеша определений таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_definition_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            Размер кеша открытых таблиц для всех потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            Количество сегментов кеша открытых таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_table_open_cache_instances).

            Допустимые значения — от 1 до 32 включительно.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Эта переменная определяет, разрешает ли сервер определенные нестандартные варианты поведения для значений по умолчанию и обработку значений NULL в столбцах TIMESTAMP.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp).
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_increment).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_auto_increment_offset).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Управляет тем, как часто сервер MySQL синхронизирует бинлог с диском.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_sync_binlog).

            Допустимые значения — от 0 до 4096 включительно.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            Размер кеша для хранения изменений бинлога во время транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_cache_size).

            Допустимые значения — от 4096 до 67108864 включительно.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Определяет, сколько микросекунд операция COMMIT ожидает перед синхронизацией бинлога на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay).

            Допустимые значения — от 0 до 1000000 включительно.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig5_7BinlogRowImage_in_MysqlConfig5_7_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            Когда он включен, он заставляет сервер записывать информационные события журнала, такие как события журнала запросов строк, в бинлог.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events).
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            Количество реплик которых ожидает мастер при синхронной репликации прежде чем подтвердить фиксацию транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count).

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig5_7SlaveParallelType_in_MysqlConfig5_7_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Устанавливает количество потоков приложения для параллельного выполнения транзакций репликации.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/replication-options-replica.html#sysvar_slave_parallel_workers).

            Допустимые значения — от 0 до 64 включительно.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            Устанавливает размер хранимого на диске бинлога.

            Допустимые значения — от 1073741824 до 107374182400 включительно.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_interactive_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с не интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_wait_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = ON' чтобы предотвратить чтение устаревших данных.

            Допустимые значения — от 600 до 432000 включительно.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = OFF'.
            Значение должно быть меньше чем mdb_offline_mode_enable_lag.

            Допустимые значения — от 60 до 86400 включительно.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            Ограничение на потребление памяти для range optimizer.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/5.7/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size).

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
    MysqlConfig8_0_in_MysqlConfigSet8_0_effectiveConfig:
      description: |-
        Действующие параметры для кластера MySQL 8.0 (сочетание параметров, определенных в
        `userConfig` и `defaultConfig`).

        Поля и структура `MysqlConfig8_0` отражает параметры конфигурации MySQL 8.0.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Размер буфера InnoDB, который используется для кэширования данных таблиц и индексов.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size).

            Минимальное значение — 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            Максимальное количество одновременных подключений, которые принимает MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_connections).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Время, в течение которого запрос должен обрабатываться, прежде чем он начинает
            считаться медленным.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_long_query_time).
          type: number
          format: double
        generalLog:
          description: |-
            Включить запись общего лога запросов MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_general_log).
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Включить запись лога аудита MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/audit-log-reference.html#audit-log-options-variables).
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Режим SQL для сервера MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/sql-mode.html#sql-mode-setting).
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig8_0SQLMode'
        maxAllowedPacket:
          description: |-
            Максимальный размер одного пакета в байтах.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_allowed_packet).

            Допустимые значения — от 1024 до 1073741824 включительно.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig8_0AuthPlugin_in_MysqlConfig8_0_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Частота записи лога транзакций на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit)

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Максимальное время ожидания блокировки строки для транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout)

            Допустимые значения — от 1 до 28800 включительно.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig8_0TransactionIsolation_in_MysqlConfig8_0_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Выводить всю информацию о взаимных блокировках в журнал ошибок.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks)
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            Максимальное время ожидания чтения при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_read_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            Максимальное время ожидания записи при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_write_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            Максимальный размер результата для функции GROUP_CONCAT()

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_group_concat_max_len)

            Допустимые значения — от 4 до 33554432 включительно.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            Максимальный размер временных таблиц в памяти, прежде чем они будут помещены на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_tmp_table_size)

            Допустимые значения — от 1024 до 134217728 включительно.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            Максимальный размер пользователских MEMORY таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_heap_table_size)

            Допустимые значения — от 16384 до 134217728 включительно.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            Часовой пояс сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-options.html#option_mysqld_default-time-zone)
          type: string
        characterSetServer:
          description: |-
            Кодировка сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_character_set_server)
          type: string
        collationServer:
          description: |-
            Алгоритм сравнения символов сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_collation_server)
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Включает адаптивный хэш индекс Innodb

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index)
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Включает NUMA interleave политику при выделении памяти для буфера InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_numa_interleave)
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            Размер в байтах буфера, который InnoDB использует при записи логов на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_buffer_size)

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            Рамер в байтах одного файла redo логов InnnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_file_size)

            Допустимые значения — от 268435456 до 4294967296 включительно.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity_max)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций чтения.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_read_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций записи.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_write_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций очистки.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_purge_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Максимальное число потоков, которые могут исполняться внутри InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_thread_concurrency)

            Допустимые значения — от 0 до 1000 включительно.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Максимальный размер временного табличного пространства InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path)

            Допустимые значения — от 0 до 107374182400 включительно.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            Количество потоков, которые кешируются для обработки новых сетевых соединений.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_cache_size).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        threadStack:
          description: |-
            Размер стэка для потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_stack).

            Допустимые значения — от 131072 до 16777216 включительно.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            Минимальный размер буфера, который используется для сканирования простого индекса, сканирования индекса диапазона и объединений таблиц, не использующих индексы.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_join_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Размер буфера, который используется для сортировок в памяти.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_sort_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            Размер кеша определений таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_definition_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            Размер кеша открытых таблиц для всех потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            Количество сегментов кеша открытых таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache_instances).

            Допустимые значения — от 1 до 32 включительно.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Эта переменная определяет, разрешает ли сервер определенные нестандартные варианты поведения для значений по умолчанию и обработку значений NULL в столбцах TIMESTAMP.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp).
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_increment).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_offset).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Управляет тем, как часто сервер MySQL синхронизирует бинлог с диском.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_sync_binlog).

            Допустимые значения — от 0 до 4096 включительно.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            Размер кеша для хранения изменений бинлога во время транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_cache_size).

            Допустимые значения — от 4096 до 67108864 включительно.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Определяет, сколько микросекунд операция COMMIT ожидает перед синхронизацией бинлога на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay).

            Допустимые значения — от 0 до 1000000 включительно.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig8_0BinlogRowImage_in_MysqlConfig8_0_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            Когда он включен, он заставляет сервер записывать информационные события журнала, такие как события журнала запросов строк, в бинлог.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events).
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            Количество реплик которых ожидает мастер при синхронной репликации прежде чем подтвердить фиксацию транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count).

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig8_0SlaveParallelType_in_MysqlConfig8_0_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Устанавливает количество потоков приложения для параллельного выполнения транзакций репликации.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_slave_parallel_workers).

            Допустимые значения — от 0 до 64 включительно.
          type: integer
          format: int64
        regexpTimeLimit:
          description: |-
            Ограничение времени выполнения регулярных выражений выполняемых REGEXP_LIKE и похожими функциями

            Подробнее см. в [описании переменной в документации MySQL]((https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_regexp_time_limit).

            Допустимые значения — от 0 до 1048576 включительно.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            Устанавливает размер хранимого на диске бинлога.

            Допустимые значения — от 1073741824 до 107374182400 включительно.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_interactive_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с не интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_wait_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = ON' чтобы предотвратить чтение устаревших данных.

            Допустимые значения — от 600 до 432000 включительно.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = OFF'.
            Значение должно быть меньше чем mdb_offline_mode_enable_lag.

            Допустимые значения — от 60 до 86400 включительно.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            Ограничение на потребление памяти для range optimizer.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size).

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
    MysqlConfig8_0_in_MysqlConfigSet8_0_userConfig:
      description: |-
        Пользовательские настройки для кластера MySQL 8.0.

        Поля и структура `MysqlConfig8_0` отражает параметры конфигурации MySQL 8.0.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Размер буфера InnoDB, который используется для кэширования данных таблиц и индексов.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size).

            Минимальное значение — 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            Максимальное количество одновременных подключений, которые принимает MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_connections).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Время, в течение которого запрос должен обрабатываться, прежде чем он начинает
            считаться медленным.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_long_query_time).
          type: number
          format: double
        generalLog:
          description: |-
            Включить запись общего лога запросов MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_general_log).
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Включить запись лога аудита MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/audit-log-reference.html#audit-log-options-variables).
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Режим SQL для сервера MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/sql-mode.html#sql-mode-setting).
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig8_0SQLMode'
        maxAllowedPacket:
          description: |-
            Максимальный размер одного пакета в байтах.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_allowed_packet).

            Допустимые значения — от 1024 до 1073741824 включительно.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig8_0AuthPlugin_in_MysqlConfig8_0_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Частота записи лога транзакций на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit)

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Максимальное время ожидания блокировки строки для транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout)

            Допустимые значения — от 1 до 28800 включительно.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig8_0TransactionIsolation_in_MysqlConfig8_0_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Выводить всю информацию о взаимных блокировках в журнал ошибок.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks)
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            Максимальное время ожидания чтения при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_read_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            Максимальное время ожидания записи при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_write_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            Максимальный размер результата для функции GROUP_CONCAT()

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_group_concat_max_len)

            Допустимые значения — от 4 до 33554432 включительно.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            Максимальный размер временных таблиц в памяти, прежде чем они будут помещены на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_tmp_table_size)

            Допустимые значения — от 1024 до 134217728 включительно.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            Максимальный размер пользователских MEMORY таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_heap_table_size)

            Допустимые значения — от 16384 до 134217728 включительно.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            Часовой пояс сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-options.html#option_mysqld_default-time-zone)
          type: string
        characterSetServer:
          description: |-
            Кодировка сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_character_set_server)
          type: string
        collationServer:
          description: |-
            Алгоритм сравнения символов сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_collation_server)
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Включает адаптивный хэш индекс Innodb

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index)
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Включает NUMA interleave политику при выделении памяти для буфера InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_numa_interleave)
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            Размер в байтах буфера, который InnoDB использует при записи логов на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_buffer_size)

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            Рамер в байтах одного файла redo логов InnnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_file_size)

            Допустимые значения — от 268435456 до 4294967296 включительно.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity_max)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций чтения.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_read_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций записи.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_write_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций очистки.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_purge_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Максимальное число потоков, которые могут исполняться внутри InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_thread_concurrency)

            Допустимые значения — от 0 до 1000 включительно.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Максимальный размер временного табличного пространства InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path)

            Допустимые значения — от 0 до 107374182400 включительно.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            Количество потоков, которые кешируются для обработки новых сетевых соединений.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_cache_size).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        threadStack:
          description: |-
            Размер стэка для потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_stack).

            Допустимые значения — от 131072 до 16777216 включительно.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            Минимальный размер буфера, который используется для сканирования простого индекса, сканирования индекса диапазона и объединений таблиц, не использующих индексы.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_join_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Размер буфера, который используется для сортировок в памяти.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_sort_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            Размер кеша определений таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_definition_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            Размер кеша открытых таблиц для всех потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            Количество сегментов кеша открытых таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache_instances).

            Допустимые значения — от 1 до 32 включительно.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Эта переменная определяет, разрешает ли сервер определенные нестандартные варианты поведения для значений по умолчанию и обработку значений NULL в столбцах TIMESTAMP.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp).
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_increment).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_offset).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Управляет тем, как часто сервер MySQL синхронизирует бинлог с диском.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_sync_binlog).

            Допустимые значения — от 0 до 4096 включительно.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            Размер кеша для хранения изменений бинлога во время транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_cache_size).

            Допустимые значения — от 4096 до 67108864 включительно.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Определяет, сколько микросекунд операция COMMIT ожидает перед синхронизацией бинлога на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay).

            Допустимые значения — от 0 до 1000000 включительно.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig8_0BinlogRowImage_in_MysqlConfig8_0_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            Когда он включен, он заставляет сервер записывать информационные события журнала, такие как события журнала запросов строк, в бинлог.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events).
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            Количество реплик которых ожидает мастер при синхронной репликации прежде чем подтвердить фиксацию транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count).

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig8_0SlaveParallelType_in_MysqlConfig8_0_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Устанавливает количество потоков приложения для параллельного выполнения транзакций репликации.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_slave_parallel_workers).

            Допустимые значения — от 0 до 64 включительно.
          type: integer
          format: int64
        regexpTimeLimit:
          description: |-
            Ограничение времени выполнения регулярных выражений выполняемых REGEXP_LIKE и похожими функциями

            Подробнее см. в [описании переменной в документации MySQL]((https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_regexp_time_limit).

            Допустимые значения — от 0 до 1048576 включительно.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            Устанавливает размер хранимого на диске бинлога.

            Допустимые значения — от 1073741824 до 107374182400 включительно.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_interactive_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с не интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_wait_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = ON' чтобы предотвратить чтение устаревших данных.

            Допустимые значения — от 600 до 432000 включительно.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = OFF'.
            Значение должно быть меньше чем mdb_offline_mode_enable_lag.

            Допустимые значения — от 60 до 86400 включительно.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            Ограничение на потребление памяти для range optimizer.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size).

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
    MysqlConfig8_0_in_MysqlConfigSet8_0_defaultConfig:
      description: |-
        Конфигурация по умолчанию для кластера MySQL 8.0.

        Поля и структура `MysqlConfig8_0` отражает параметры конфигурации MySQL 8.0.
      type: object
      properties:
        innodbBufferPoolSize:
          description: |-
            Размер буфера InnoDB, который используется для кэширования данных таблиц и индексов.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_buffer_pool_size).

            Минимальное значение — 5242880.
          type: integer
          format: int64
        maxConnections:
          description: |-
            Максимальное количество одновременных подключений, которые принимает MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_connections).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        longQueryTime:
          description: |-
            Время, в течение которого запрос должен обрабатываться, прежде чем он начинает
            считаться медленным.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_long_query_time).
          type: number
          format: double
        generalLog:
          description: |-
            Включить запись общего лога запросов MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_general_log).
          type: boolean
          format: boolean
        auditLog:
          description: |-
            Включить запись лога аудита MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/audit-log-reference.html#audit-log-options-variables).
          type: boolean
          format: boolean
        sqlMode:
          description: |-
            Режим SQL для сервера MySQL.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/sql-mode.html#sql-mode-setting).
          type: array
          items:
            $ref: '#/components/schemas/MysqlConfig8_0SQLMode'
        maxAllowedPacket:
          description: |-
            Максимальный размер одного пакета в байтах.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_allowed_packet).

            Допустимые значения — от 1024 до 1073741824 включительно.
          type: integer
          format: int64
        defaultAuthenticationPlugin:
          $ref: '#/components/schemas/MysqlConfig8_0AuthPlugin_in_MysqlConfig8_0_defaultAuthenticationPlugin'
        innodbFlushLogAtTrxCommit:
          description: |-
            Частота записи лога транзакций на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_flush_log_at_trx_commit)

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        innodbLockWaitTimeout:
          description: |-
            Максимальное время ожидания блокировки строки для транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_lock_wait_timeout)

            Допустимые значения — от 1 до 28800 включительно.
          type: integer
          format: int64
        transactionIsolation:
          $ref: '#/components/schemas/MysqlConfig8_0TransactionIsolation_in_MysqlConfig8_0_transactionIsolation'
        innodbPrintAllDeadlocks:
          description: |-
            Выводить всю информацию о взаимных блокировках в журнал ошибок.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_print_all_deadlocks)
          type: boolean
          format: boolean
        netReadTimeout:
          description: |-
            Максимальное время ожидания чтения при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_read_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        netWriteTimeout:
          description: |-
            Максимальное время ожидания записи при передаче данных по сети.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_net_write_timeout)

            Допустимые значения — от 1 до 1200 включительно.
          type: integer
          format: int64
        groupConcatMaxLen:
          description: |-
            Максимальный размер результата для функции GROUP_CONCAT()

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_group_concat_max_len)

            Допустимые значения — от 4 до 33554432 включительно.
          type: integer
          format: int64
        tmpTableSize:
          description: |-
            Максимальный размер временных таблиц в памяти, прежде чем они будут помещены на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_tmp_table_size)

            Допустимые значения — от 1024 до 134217728 включительно.
          type: integer
          format: int64
        maxHeapTableSize:
          description: |-
            Максимальный размер пользователских MEMORY таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_max_heap_table_size)

            Допустимые значения — от 16384 до 134217728 включительно.
          type: integer
          format: int64
        defaultTimeZone:
          description: |-
            Часовой пояс сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-options.html#option_mysqld_default-time-zone)
          type: string
        characterSetServer:
          description: |-
            Кодировка сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_character_set_server)
          type: string
        collationServer:
          description: |-
            Алгоритм сравнения символов сервера.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_collation_server)
          type: string
        innodbAdaptiveHashIndex:
          description: |-
            Включает адаптивный хэш индекс Innodb

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_adaptive_hash_index)
          type: boolean
          format: boolean
        innodbNumaInterleave:
          description: |-
            Включает NUMA interleave политику при выделении памяти для буфера InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_numa_interleave)
          type: boolean
          format: boolean
        innodbLogBufferSize:
          description: |-
            Размер в байтах буфера, который InnoDB использует при записи логов на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_buffer_size)

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
        innodbLogFileSize:
          description: |-
            Рамер в байтах одного файла redo логов InnnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_log_file_size)

            Допустимые значения — от 268435456 до 4294967296 включительно.
          type: integer
          format: int64
        innodbIoCapacity:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbIoCapacityMax:
          description: |-
            Ограничивает потребление IO InnoDB в фоновых операциях.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_io_capacity_max)

            Допустимые значения — от 100 до 100000 включительно.
          type: integer
          format: int64
        innodbReadIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций чтения.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_read_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbWriteIoThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций записи.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_write_io_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbPurgeThreads:
          description: |-
            Количество I/O потоков Innodb, используемых для операций очистки.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_purge_threads)

            Допустимые значения — от 1 до 16 включительно.
          type: integer
          format: int64
        innodbThreadConcurrency:
          description: |-
            Максимальное число потоков, которые могут исполняться внутри InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_thread_concurrency)

            Допустимые значения — от 0 до 1000 включительно.
          type: integer
          format: int64
        innodbTempDataFileMaxSize:
          description: |-
            Максимальный размер временного табличного пространства InnoDB.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/innodb-parameters.html#sysvar_innodb_temp_data_file_path)

            Допустимые значения — от 0 до 107374182400 включительно.
          type: integer
          format: int64
        threadCacheSize:
          description: |-
            Количество потоков, которые кешируются для обработки новых сетевых соединений.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_cache_size).

            Допустимые значения — от 10 до 10000 включительно.
          type: integer
          format: int64
        threadStack:
          description: |-
            Размер стэка для потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_thread_stack).

            Допустимые значения — от 131072 до 16777216 включительно.
          type: integer
          format: int64
        joinBufferSize:
          description: |-
            Минимальный размер буфера, который используется для сканирования простого индекса, сканирования индекса диапазона и объединений таблиц, не использующих индексы.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_join_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        sortBufferSize:
          description: |-
            Размер буфера, который используется для сортировок в памяти.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_sort_buffer_size).

            Допустимые значения — от 1024 до 16777216 включительно.
          type: integer
          format: int64
        tableDefinitionCache:
          description: |-
            Размер кеша определений таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_definition_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCache:
          description: |-
            Размер кеша открытых таблиц для всех потоков.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache).

            Допустимые значения — от 400 до 524288 включительно.
          type: integer
          format: int64
        tableOpenCacheInstances:
          description: |-
            Количество сегментов кеша открытых таблиц.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_table_open_cache_instances).

            Допустимые значения — от 1 до 32 включительно.
          type: integer
          format: int64
        explicitDefaultsForTimestamp:
          description: |-
            Эта переменная определяет, разрешает ли сервер определенные нестандартные варианты поведения для значений по умолчанию и обработку значений NULL в столбцах TIMESTAMP.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_explicit_defaults_for_timestamp).
          type: boolean
          format: boolean
        autoIncrementIncrement:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_increment).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        autoIncrementOffset:
          description: |-
            Может использоваться для управления работой столбцов AUTO_INCREMENT.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_auto_increment_offset).

            Допустимые значения — от 1 до 65535 включительно.
          type: integer
          format: int64
        syncBinlog:
          description: |-
            Управляет тем, как часто сервер MySQL синхронизирует бинлог с диском.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_sync_binlog).

            Допустимые значения — от 0 до 4096 включительно.
          type: integer
          format: int64
        binlogCacheSize:
          description: |-
            Размер кеша для хранения изменений бинлога во время транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_cache_size).

            Допустимые значения — от 4096 до 67108864 включительно.
          type: integer
          format: int64
        binlogGroupCommitSyncDelay:
          description: |-
            Определяет, сколько микросекунд операция COMMIT ожидает перед синхронизацией бинлога на диск.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_group_commit_sync_delay).

            Допустимые значения — от 0 до 1000000 включительно.
          type: integer
          format: int64
        binlogRowImage:
          $ref: '#/components/schemas/MysqlConfig8_0BinlogRowImage_in_MysqlConfig8_0_binlogRowImage'
        binlogRowsQueryLogEvents:
          description: |-
            Когда он включен, он заставляет сервер записывать информационные события журнала, такие как события журнала запросов строк, в бинлог.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-binary-log.html#sysvar_binlog_rows_query_log_events).
          type: boolean
          format: boolean
        rplSemiSyncMasterWaitForSlaveCount:
          description: |-
            Количество реплик которых ожидает мастер при синхронной репликации прежде чем подтвердить фиксацию транзакции.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-master.html#sysvar_rpl_semi_sync_master_wait_for_slave_count).

            Допустимые значения — от 1 до 2 включительно.
          type: integer
          format: int64
        slaveParallelType:
          $ref: '#/components/schemas/MysqlConfig8_0SlaveParallelType_in_MysqlConfig8_0_slaveParallelType'
        slaveParallelWorkers:
          description: |-
            Устанавливает количество потоков приложения для параллельного выполнения транзакций репликации.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_slave_parallel_workers).

            Допустимые значения — от 0 до 64 включительно.
          type: integer
          format: int64
        regexpTimeLimit:
          description: |-
            Ограничение времени выполнения регулярных выражений выполняемых REGEXP_LIKE и похожими функциями

            Подробнее см. в [описании переменной в документации MySQL]((https://dev.mysql.com/doc/refman/8.0/en/replication-options-replica.html#sysvar_regexp_time_limit).

            Допустимые значения — от 0 до 1048576 включительно.
          type: integer
          format: int64
        mdbPreserveBinlogBytes:
          description: |-
            Устанавливает размер хранимого на диске бинлога.

            Допустимые значения — от 1073741824 до 107374182400 включительно.
          type: integer
          format: int64
        interactiveTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_interactive_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        waitTimeout:
          description: |-
            Количество секунд без активности которые ожидает сервер до закрытия соединения с не интерактивным клиентом.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_wait_timeout).

            Допустимые значения — от 600 до 86400 включительно.
          type: integer
          format: int64
        mdbOfflineModeEnableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = ON' чтобы предотвратить чтение устаревших данных.

            Допустимые значения — от 600 до 432000 включительно.
          type: integer
          format: int64
        mdbOfflineModeDisableLag:
          description: |-
            Пороговое значение лага репликации (в секундах), после которого MySQL переключится в 'offline_mode = OFF'.
            Значение должно быть меньше чем mdb_offline_mode_enable_lag.

            Допустимые значения — от 60 до 86400 включительно.
          type: integer
          format: int64
        rangeOptimizerMaxMemSize:
          description: |-
            Ограничение на потребление памяти для range optimizer.

            Подробнее см. в [описании переменной в документации MySQL](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_range_optimizer_max_mem_size).

            Допустимые значения — от 1048576 до 268435456 включительно.
          type: integer
          format: int64
    ClusterEnvironment_in_RestoreClusterRequest_environment:
      description: |-
        Среда развертывания для нового кластера MySQL.

         - PRODUCTION: Стабильная среда с осторожной политикой обновления: во время регулярного
        обслуживания применяются только срочные исправления.
         - PRESTABLE: Среда с более агрессивной политикой обновления: новые версии развертываются
        независимо от обратной совместимости.
      type: string
      enum:
      - PRODUCTION
      - PRESTABLE
    ConfigSpec_in_RestoreClusterRequest_configSpec:
      description: Конфигурация для создаваемого кластера MySQL.
      type: object
      allOf:
      - type: object
        properties:
          version:
            description: |-
              Версия MySQL, используемая в кластере. Возможные значения:
              * 5.7
              * 8.0
            type: string
          resources:
            $ref: '#/components/schemas/Resources'
            description: Ресурсы, выделенные хостам MySQL.
          backupWindowStart:
            $ref: '#/components/schemas/TimeOfDay'
            description: Время запуска ежедневного резервного копирования, в часовом
              поясе UTC.
          access:
            $ref: '#/components/schemas/Access'
            description: Политика доступа к БД.
      - title: mysql_config
        type: object
        maxProperties: 1
        properties:
          mysqlConfig_5_7:
            $ref: '#/components/schemas/MysqlConfig5_7'
            description: Конфигурация для кластера MySQL 5.7.
          mysqlConfig_8_0:
            $ref: '#/components/schemas/MysqlConfig8_0'
            description: Конфигурация для кластера MySQL 8.0.
    Permission_in_RevokeUserPermissionRequest_permission:
      description: |-
        Обязательное поле.
        Разрешение, которое должно быть отозвано у указанного пользователя.
      type: object
      properties:
        databaseName:
          description: Имя базы данных, к которой предоставляет доступ разрешение.
          type: string
        roles:
          description: |-
            Обязательное поле.
            Роли, предоставленные пользователю в базе данных.

            Минимальное количество элементов — 1.
          type: array
          items:
            $ref: '#/components/schemas/PermissionPrivilege'
    ServiceType_in_Service_type:
      description: |-
        Тип сервиса, предоставляемого хостом.

         - MYSQL: Хост является сервером MySQL.
      type: string
      enum:
      - MYSQL
    ServiceHealth_in_Service_health:
      description: |-
        Код состояния доступности сервера.

         - HEALTH_UNKNOWN: Работоспособность сервера неизвестна.
         - ALIVE: Сервер работает нормально.
         - DEAD: Сервер отключен или не отвечает.
      type: string
      enum:
      - HEALTH_UNKNOWN
      - ALIVE
      - DEAD
      default: HEALTH_UNKNOWN
    FieldMask_in_UpdateClusterRequest_updateMask:
      description: |-
        Маска, которая указывает, какие атрибуты кластера MySQL должны быть изменены.

        Имена всех обновляемых полей, разделенные запятыми.
        Только значения указанных полей будут изменены. Остальные останутся нетронутыми.
        Если поле указано в параметре `` updateMask `` и в запросе не было отправлено значение для этого поля,
        значение поля будет сброшено на значение по умолчанию. Значение по умолчанию для большинства полей — null или 0.

        Если в запросе не передается `` updateMask ``, значения всех полей будут обновлены.
        Для полей, указанных в запросе, будут использованы переданные значения.
        Значения остальных полей будут сброшены на значения по умолчанию.
      type: string
    ConfigSpec_in_UpdateClusterRequest_configSpec:
      description: Новая конфигурация и ресурсы для хостов кластера.
      type: object
      allOf:
      - type: object
        properties:
          version:
            description: |-
              Версия MySQL, используемая в кластере. Возможные значения:
              * 5.7
              * 8.0
            type: string
          resources:
            $ref: '#/components/schemas/Resources'
            description: Ресурсы, выделенные хостам MySQL.
          backupWindowStart:
            $ref: '#/components/schemas/TimeOfDay'
            description: Время запуска ежедневного резервного копирования, в часовом
              поясе UTC.
          access:
            $ref: '#/components/schemas/Access'
            description: Политика доступа к БД.
      - title: mysql_config
        type: object
        maxProperties: 1
        properties:
          mysqlConfig_5_7:
            $ref: '#/components/schemas/MysqlConfig5_7'
            description: Конфигурация для кластера MySQL 5.7.
          mysqlConfig_8_0:
            $ref: '#/components/schemas/MysqlConfig8_0'
            description: Конфигурация для кластера MySQL 8.0.
    FieldMask_in_UpdateUserRequest_updateMask:
      description: |-
        Маска, которая указывает, какие атрибуты пользователя MySQL должны быть обновлены.

        Имена всех обновляемых полей, разделенные запятыми.
        Только значения указанных полей будут изменены. Остальные останутся нетронутыми.
        Если поле указано в параметре `` updateMask `` и в запросе не было отправлено значение для этого поля,
        значение поля будет сброшено на значение по умолчанию. Значение по умолчанию для большинства полей — null или 0.

        Если в запросе не передается `` updateMask ``, значения всех полей будут обновлены.
        Для полей, указанных в запросе, будут использованы переданные значения.
        Значения остальных полей будут сброшены на значения по умолчанию.
      type: string
    ConnectionLimits_in_UpdateUserRequest_connectionLimits:
      description: Набор изменившихся ограничений на коннекты пользователя.
      type: object
      properties:
        maxQuestionsPerHour:
          description: |-
            Максимально допустимое число вопросов в час.

            Минимальное значение — 0.
          type: integer
          format: int64
        maxUpdatesPerHour:
          description: |-
            Максимально допустимое число апдейтов в час.

            Минимальное значение — 0.
          type: integer
          format: int64
        maxConnectionsPerHour:
          description: |-
            Максимально допустимое число коннектов в час.

            Минимальное значение — 0.
          type: integer
          format: int64
        maxUserConnections:
          description: |-
            Максимально допустимое число коннектов.

            Минимальное значение — 0.
          type: integer
          format: int64
    v1AuthPlugin_in_UpdateUserRequest_authenticationPlugin:
      description: |-
        Новый аутентификационный плагин пользователя.

         - MYSQL_NATIVE_PASSWORD: Использует [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/native-pluggable-authentication.html).
         - CACHING_SHA2_PASSWORD: Использует [Caching SHA-2 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/caching-sha2-pluggable-authentication.html).
         - SHA256_PASSWORD: Использует [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    ConnectionLimits_in_User_connectionLimits:
      description: Набор ограничений на коннекты пользователя.
      type: object
      properties:
        maxQuestionsPerHour:
          description: |-
            Максимально допустимое число вопросов в час.

            Минимальное значение — 0.
          type: integer
          format: int64
        maxUpdatesPerHour:
          description: |-
            Максимально допустимое число апдейтов в час.

            Минимальное значение — 0.
          type: integer
          format: int64
        maxConnectionsPerHour:
          description: |-
            Максимально допустимое число коннектов в час.

            Минимальное значение — 0.
          type: integer
          format: int64
        maxUserConnections:
          description: |-
            Максимально допустимое число коннектов.

            Минимальное значение — 0.
          type: integer
          format: int64
    v1AuthPlugin_in_User_authenticationPlugin:
      description: |-
        Аутентификационный плагин пользователя.

         - MYSQL_NATIVE_PASSWORD: Использует [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/native-pluggable-authentication.html).
         - CACHING_SHA2_PASSWORD: Использует [Caching SHA-2 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/caching-sha2-pluggable-authentication.html).
         - SHA256_PASSWORD: Использует [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
    ConnectionLimits_in_UserSpec_connectionLimits:
      description: Набор ограничений на коннекты пользователя.
      type: object
      properties:
        maxQuestionsPerHour:
          description: |-
            Максимально допустимое число вопросов в час.

            Минимальное значение — 0.
          type: integer
          format: int64
        maxUpdatesPerHour:
          description: |-
            Максимально допустимое число апдейтов в час.

            Минимальное значение — 0.
          type: integer
          format: int64
        maxConnectionsPerHour:
          description: |-
            Максимально допустимое число коннектов в час.

            Минимальное значение — 0.
          type: integer
          format: int64
        maxUserConnections:
          description: |-
            Максимально допустимое число коннектов.

            Минимальное значение — 0.
          type: integer
          format: int64
    v1AuthPlugin_in_UserSpec_authenticationPlugin:
      description: |-
        Аутентификационный плагин пользователя.

         - MYSQL_NATIVE_PASSWORD: Использует [Native Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/native-pluggable-authentication.html).
         - CACHING_SHA2_PASSWORD: Использует [Caching SHA-2 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/caching-sha2-pluggable-authentication.html).
         - SHA256_PASSWORD: Использует [SHA-256 Pluggable Authentication](https://dev.mysql.com/doc/refman/8.0/en/sha256-pluggable-authentication.html).
      type: string
      enum:
      - MYSQL_NATIVE_PASSWORD
      - CACHING_SHA2_PASSWORD
      - SHA256_PASSWORD
tags:
- name: Backup
  description: Набор методов для управления резервными копиями MySQL.
- name: Cluster
  description: Набор методов для управления кластерами MySQL.
- name: Database
  description: Набор методов для управления базами данных MySQL.
- name: ResourcePreset
  description: Методы для работы с наборами ресурсов.
- name: User
  description: Набор методов для управления пользователями MySQL.

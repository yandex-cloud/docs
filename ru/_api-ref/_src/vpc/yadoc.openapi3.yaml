openapi: "3.0"
info:
  title: Yandex Virtual Private Cloud API
  description: Методы справочника API сгруппированы по ресурсам. Чтобы выполнить операцию
    над ресурсом, отправьте HTTP-запрос к соответствующему URL или выполните RPC-вызов.
    Подробнее об архитектуре API Яндекс.Облака, см. [Концепции API Яндекс.Облака](/docs/api-design-guide/).
  version: v1
  x-y-docUri: api-ref
  x-y-baseUrl: https://vpc.api.cloud.yandex.net
paths:
  /vpc/v1/addresses/{addressId}:
    get:
      tags:
      - Address
      summary: Возвращает информацию об указанном адресе.
      description: Чтобы получить список доступных адресов, используйте запрос [list](/docs/vpc/api-ref/Address/list).
      operationId: AddressGet
      parameters:
      - name: addressId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор возвращаемого адреса.

          Чтобы получить идентификатор адреса, используйте запрос [list](/docs/vpc/api-ref/Address/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Address'
      x-y-navtitle: get
    delete:
      tags:
      - Address
      summary: Удаляет указанный адрес.
      operationId: AddressDelete
      parameters:
      - name: addressId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор адреса, который следует удалить.

          Чтобы получить идентификатор адреса, используйте запрос [list](/docs/vpc/api-ref/Address/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteAddressMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - Address
      summary: Обновляет параметры указанного адреса.
      operationId: AddressUpdate
      parameters:
      - name: addressId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор обновляемого адреса.

          Для получения идентификатора адреса используйте запрос [list](/docs/vpc/api-ref/Address/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAddressRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateAddressMetadata'
      x-y-operation-response: '[Address](/docs/vpc/api-ref/Address#representation)'
  /vpc/v1/addresses:byValue:
    get:
      tags:
      - Address
      summary: Возвращает информацию об указанном адресе по его значению.
      description: Чтобы получить список доступных адресов, используйте запрос [list](/docs/vpc/api-ref/Address/list).
      operationId: AddressGetByValue
      parameters:
      - name: externalIpv4Address
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Address'
      x-y-navtitle: getByValue
  /vpc/v1/addresses:
    get:
      tags:
      - Address
      summary: Возвращает список доступных адресов в указанном каталоге.
      operationId: AddressList
      parameters:
      - name: folderId
        in: query
        description: |-
          Обязательное поле.
          Идентификатор каталога, которому принадлежит адрес.

          Чтобы получить идентификатор каталога, используйте запрос [list](/docs/resource-manager/api-ref/Folder/list).

          Максимальная длина строки в символах — 50.
        schema:
          type: string
      - name: pageSize
        in: query
        description: "Максимальное количество результатов на странице ответа на запрос.
          Если количество результатов \nбольше чем `page_size`, сервис вернет значение
          [nextPageToken](/docs/vpc/api-ref/Address/list#responses),\nкоторое можно
          использовать для получения следующей страницы.\nЗначение по умолчанию: 100.\n\nМаксимальное
          значение — 1000."
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение `page_token` равным значению поля [nextPageToken](/docs/vpc/api-ref/Address/list#responses)
          предыдущего запроса, чтобы получить следующую страницу результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      - name: filter
        in: query
        description: "Параметры фильтрации адресов в ответе.\n\nВ параметрах фильтрации
          указываются: \n1. Имя поля. В настоящее время фильтрация осуществляется
          только по полю [Address.name](/docs/vpc/api-ref/Address#representation).
          \n2. Оператор. Поддерживаются операторы `=` и `!=` для одиночных значений,
          `IN` и `NOT IN` для списков значений.\n3. Значение. Значение длиной от 3
          до 63 символов, совпадающее с регулярным выражением `^[a-z][-a-z0-9]{1,61}[a-z0-9]`.
          \nПример фильтра: \"name=my-address\"."
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListAddressesResponse'
      x-y-navtitle: list
    post:
      tags:
      - Address
      summary: Создает адрес в указанном каталоге и сети.
      operationId: AddressCreate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAddressRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateAddressMetadata'
      x-y-operation-response: '[Address](/docs/vpc/api-ref/Address#representation)'
  /vpc/v1/addresses/{addressId}/operations:
    get:
      tags:
      - Address
      summary: Возвращает список операций указанного адреса.
      operationId: AddressListOperations
      parameters:
      - name: addressId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор адреса для которого запрашивается список операций.

          Чтобы получить идентификатор адреса, используйте запрос [list](/docs/vpc/api-ref/Address/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: "Максимальное количество результатов на странице ответа на запрос.
          Если количество результатов \nбольше чем [pageSize](/docs/vpc/api-ref/Address/listOperations#query_params),
          сервис вернет значение [nextPageToken](/docs/vpc/api-ref/Address/listOperations#responses),\nкоторое
          можно использовать для получения следующей страницы.\nЗначение по умолчанию:
          100.\n\nМаксимальное значение — 1000."
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение [pageToken](/docs/vpc/api-ref/Address/listOperations#query_params) равным значению поля [nextPageToken](/docs/vpc/api-ref/Address/listOperations#responses)
          предыдущего запроса, чтобы получить следующую страницу результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListAddressOperationsResponse'
      x-y-navtitle: listOperations
  /vpc/v1/networks/{networkId}:
    get:
      tags:
      - Network
      summary: Возвращает указанный ресурс Network.
      description: |-
        Чтобы получить список доступных ресурсов Network, используйте
        запрос [list](/docs/vpc/api-ref/Network/list).
      operationId: NetworkGet
      parameters:
      - name: networkId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор возвращаемого ресурса Network. Чтобы получить
          идентификатор облачной сети, используйте запрос [list](/docs/vpc/api-ref/Network/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Network'
      x-y-navtitle: get
    delete:
      tags:
      - Network
      summary: Удаляет указанную облачную сеть.
      operationId: NetworkDelete
      parameters:
      - name: networkId
        in: path
        description: "Обязательное поле.\nИдентификатор обновляемого ресурса Network.
          \nЧтобы получить идентификатор облачной сети, используйте запрос [list](/docs/vpc/api-ref/Network/list).\n\nМаксимальная
          длина строки в символах — 50."
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteNetworkMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - Network
      summary: |-
        Обновляет параметры указанной облачной сети. Метод запускает
        асинхронную операцию, которую можно отменить перед тем, как
        она завершится.
      operationId: NetworkUpdate
      parameters:
      - name: networkId
        in: path
        description: "Обязательное поле.\nИдентификатор обновляемого ресурса Network.
          \nДля получения идентификатора облачной сети используйте запрос [list](/docs/vpc/api-ref/Network/list).\n\nМаксимальная
          длина строки в символах — 50."
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateNetworkRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateNetworkMetadata'
      x-y-operation-response: '[Network](/docs/vpc/api-ref/Network#representation)'
  /vpc/v1/networks:
    get:
      tags:
      - Network
      summary: |-
        Возвращает список доступных ресурсов Network в указанном
        каталоге.
      operationId: NetworkList
      parameters:
      - name: folderId
        in: query
        description: |-
          Обязательное поле.
          Идентификатор каталога для получения списка облачных сетей.
          Чтобы получить идентификатор каталога, используйте запрос
          [list](/docs/resource-manager/api-ref/Folder/list).

          Максимальная длина строки в символах — 50.
        schema:
          type: string
      - name: pageSize
        in: query
        description: "Максимальное количество результатов на странице ответа на запрос.
          Если количество результатов\nбольше чем [pageSize](/docs/vpc/api-ref/Network/list#query_params),
          сервис вернет значение [nextPageToken](/docs/vpc/api-ref/Network/list#responses),
          \nкоторое можно использовать для получения следующей страницы. \nЗначение
          по умолчанию: 100.\n\nМаксимальное значение — 1000."
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение [pageToken](/docs/vpc/api-ref/Network/list#query_params) равным значению
          поля [nextPageToken](/docs/vpc/api-ref/Network/list#responses) прошлого запроса, чтобы получить
          следующую страницу результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      - name: filter
        in: query
        description: |-
          Параметры фильтрации ресурсов в ответе. В параметрах
          фильтрации указываются:
          1. Имя поля. В настоящее время фильтрация осуществляется только по полю [Network.name](/docs/vpc/api-ref/Network#representation).
          2. Оператор. Операторы `=` или `!=` для одиночных значений, `IN` или `NOT IN` для списков значений.
          3. Значение. Значение длиной от 3 до 63 символов, совпадающее с регулярным выражением `^[a-z][-a-z0-9]{1,61}[a-z0-9]$`.

          Максимальная длина строки в символах — 1000.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListNetworksResponse'
      x-y-navtitle: list
    post:
      tags:
      - Network
      summary: |-
        Создает облачную сеть в указанном каталоге. Метод запускает
        асинхронную операцию, которую можно отменить перед тем, как
        она завершится.
      operationId: NetworkCreate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateNetworkRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateNetworkMetadata'
      x-y-operation-response: '[Network](/docs/vpc/api-ref/Network#representation)'
  /vpc/v1/networks/{networkId}/subnets:
    get:
      tags:
      - Network
      summary: Возвращает список доступных подсетей в указанной облачной сети.
      operationId: NetworkListSubnets
      parameters:
      - name: networkId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор ресурса Network для получения списка подсетей.

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: "Максимальное количество результатов на странице ответа на запрос.
          Если количество результатов\nбольше чем [pageSize](/docs/vpc/api-ref/Network/listSubnets#query_params),
          \nсервис вернет значение [nextPageToken](/docs/vpc/api-ref/Network/listSubnets#responses),
          \nкоторое можно использовать для получения следующей страницы. \nЗначение
          по умолчанию: 100.\n\nМаксимальное значение — 1000."
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: "Токен страницы. Установите значение [pageToken](/docs/vpc/api-ref/Network/listSubnets#query_params)
          равным значению поля [nextPageToken](/docs/vpc/api-ref/Network/listSubnets#responses)
          \nпрошлого запроса, чтобы получить следующую страницу результатов.\n\nМаксимальная
          длина строки в символах — 100."
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListNetworkSubnetsResponse'
      x-y-navtitle: listSubnets
  /vpc/v1/networks/{networkId}/operations:
    get:
      tags:
      - Network
      summary: Возвращает список операций для указанной облачной сети.
      operationId: NetworkListOperations
      parameters:
      - name: networkId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор ресурса Network, для которого запрашивается список операций.

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: "Максимальное количество результатов на странице ответа на запрос.
          Если количество результатов \nбольше чем [pageSize](/docs/vpc/api-ref/Network/listOperations#query_params),
          сервис вернет значение [nextPageToken](/docs/vpc/api-ref/Network/listOperations#responses),
          \nкоторое можно использовать для получения следующей страницы. \nЗначение
          по умолчанию: 100.\n\nМаксимальное значение — 1000."
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: "Токен страницы. Установите значение [pageToken](/docs/vpc/api-ref/Network/listOperations#query_params)
          равным значению поля [nextPageToken](/docs/vpc/api-ref/Network/listOperations#responses)
          \nпрошлого запроса, чтобы получить следующую страницу результатов.\n\nМаксимальная
          длина строки в символах — 100."
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListNetworkOperationsResponse'
      x-y-navtitle: listOperations
  /vpc/v1/routeTables/{routeTableId}:
    get:
      tags:
      - RouteTable
      summary: Возвращает указанную таблицу маршрутизации.
      description: Чтобы получить список доступных таблиц маршрутизации, используйте
        запрос [list](/docs/vpc/api-ref/RouteTable/list).
      operationId: RouteTableGet
      parameters:
      - name: routeTableId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор возвращаемой таблицы маршрутизации. Для получения идентификатора
          таблицы маршрутизации используйте запрос [list](/docs/vpc/api-ref/RouteTable/list) .

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteTable'
      x-y-navtitle: get
    delete:
      tags:
      - RouteTable
      summary: Удаляет указанную таблицу маршрутизации.
      operationId: RouteTableDelete
      parameters:
      - name: routeTableId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор удаляемой таблицы маршрутизации. Для получения идентификатора
          таблицы маршрутизации используйте запрос [list](/docs/vpc/api-ref/RouteTable/list).

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteRouteTableMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - RouteTable
      summary: |-
        Обновляет указанную таблицу маршрутизации. Метод запускает асинхронную операцию,
        которую можно отменить перед тем, как она завершится.
      operationId: RouteTableUpdate
      parameters:
      - name: routeTableId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор обновляемой таблицы маршрутизации.

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRouteTableRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateRouteTableMetadata'
      x-y-operation-response: '[RouteTable](/docs/vpc/api-ref/RouteTable#representation)'
  /vpc/v1/routeTables:
    get:
      tags:
      - RouteTable
      summary: Возвращает список доступных таблиц маршрутизации в указанном каталоге.
      operationId: RouteTableList
      parameters:
      - name: folderId
        in: query
        description: |-
          Обязательное поле.
          Идентификатор каталога, к которому принадлежит таблица маршрутизации. Чтобы
          получить идентификатор каталога, используйте запрос [list](/docs/resource-manager/api-ref/Folder/list).

          Максимальная длина строки в символах — 50.
        schema:
          type: string
      - name: pageSize
        in: query
        description: "Максимальное количество результатов на странице ответа на запрос.
          Если количество\nрезультатов больше чем [pageSize](/docs/vpc/api-ref/RouteTable/list#query_params)
          , сервис вернет значение [nextPageToken](/docs/vpc/api-ref/RouteTable/list#responses),\nкоторое
          можно использовать для получения следующей страницы. \nЗначение по умолчанию:
          100.\n\nМаксимальное значение — 1000."
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: |-
          Токен страницы. Установите значение [pageToken](/docs/vpc/api-ref/RouteTable/list#query_params) равным значению поля
          [nextPageToken](/docs/vpc/api-ref/RouteTable/list#responses) предыдущего запроса, чтобы получить следующую страницу
          результатов.

          Максимальная длина строки в символах — 100.
        schema:
          type: string
      - name: filter
        in: query
        description: |-
          Параметры фильтрации ресурсов в ответе. В параметрах фильтрации указываются:
          1. Имя поля. В настоящее время фильтрация осуществляется только по полю [RouteTable.name](/docs/vpc/api-ref/RouteTable#representation).
          2. Оператор. Операторы `=` или `!=` для одиночных значений, `IN` или `NOT IN` для списков значений.
          3. Значение. Значение длиной от 3 до 63 символов, совпадающее с регулярным выражением `^[a-z][-a-z0-9]{1,61}[a-z0-9]$`.

          Максимальная длина строки в символах — 1000.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListRouteTablesResponse'
      x-y-navtitle: list
    post:
      tags:
      - RouteTable
      summary: |-
        Создает таблицу маршрутизации в указанных каталоге и сети. Метод запускает
        асинхронную операцию, которую можно отменить перед тем, как она завершится.
      operationId: RouteTableCreate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRouteTableRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateRouteTableMetadata'
      x-y-operation-response: '[RouteTable](/docs/vpc/api-ref/RouteTable#representation)'
  /vpc/v1/routeTables/{routeTableId}/operations:
    get:
      tags:
      - RouteTable
      summary: Возвращает список операций для указанной таблицы маршрутизации.
      operationId: RouteTableListOperations
      parameters:
      - name: routeTableId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор таблицы маршрутизации, для которой запрашивается список операций.

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: "Максимальное число возвращаемых результатов на странице. Если
          количество\nрезультатов больше чем [pageSize](/docs/vpc/api-ref/RouteTable/listOperations#query_params)
          , сервис вернет значение [nextPageToken](/docs/vpc/api-ref/RouteTable/listOperations#responses),\nкоторое
          можно использовать для получения следующей страницы. \nЗначение по умолчанию:
          100.\n\nМаксимальное значение — 1000."
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: "Токен страницы. Установите значение [pageToken](/docs/vpc/api-ref/RouteTable/listOperations#query_params)
          равным значению поля\n[nextPageToken](/docs/vpc/api-ref/RouteTable/listOperations#responses)
          предыдущего запроса, \nчтобы получить следующую страницу результатов.\n\nМаксимальная
          длина строки в символах — 100."
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListRouteTableOperationsResponse'
      x-y-navtitle: listOperations
  /vpc/v1/subnets/{subnetId}:
    get:
      tags:
      - Subnet
      summary: Возвращает указанный ресурс Subnet.
      description: Чтобы получить список доступных подсетей, используйте запрос [list](/docs/vpc/api-ref/Subnet/list).
      operationId: SubnetGet
      parameters:
      - name: subnetId
        in: path
        description: "Обязательное поле.\nИдентификатор возвращаемого ресурса Subnet.
          \n\nЧтобы получить идентификатор подсети, используйте запрос [list](/docs/vpc/api-ref/Subnet/list).\n\nМаксимальная
          длина строки в символах — 50."
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subnet'
      x-y-navtitle: get
    delete:
      tags:
      - Subnet
      summary: Удаляет указанную подсеть.
      operationId: SubnetDelete
      parameters:
      - name: subnetId
        in: path
        description: "Обязательное поле.\nИдентификатор удаляемой подсети. \nЧтобы
          получить идентификатор подсети, используйте запрос [list](/docs/vpc/api-ref/Subnet/list).\n\nМаксимальная
          длина строки в символах — 50."
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: delete
      x-y-operation-metadata:
        $ref: '#/components/schemas/DeleteSubnetMetadata'
      x-y-operation-response: Empty
    patch:
      tags:
      - Subnet
      summary: "Обновляет параметры указанной подсети. \nМетод запускает асинхронную
        операцию, которую можно отменить перед тем, как она завершится."
      operationId: SubnetUpdate
      parameters:
      - name: subnetId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор обновляемого ресурса Subnet.

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSubnetRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: update
      x-y-operation-metadata:
        $ref: '#/components/schemas/UpdateSubnetMetadata'
      x-y-operation-response: '[Subnet](/docs/vpc/api-ref/Subnet#representation)'
  /vpc/v1/subnets:
    get:
      tags:
      - Subnet
      summary: Возвращает список доступных подсетей в указанном каталоге.
      operationId: SubnetList
      parameters:
      - name: folderId
        in: query
        description: "Обязательное поле.\nИдентификатор каталога для получения списка
          подсетей. \nЧтобы получить идентификатор каталога, используйте запрос [list](/docs/resource-manager/api-ref/Folder/list).\n\nМаксимальная
          длина строки в символах — 50."
        schema:
          type: string
      - name: pageSize
        in: query
        description: "Максимальное количество результатов на странице ответа на запрос.
          Если количество результатов \nбольше чем [pageSize](/docs/vpc/api-ref/Subnet/list#query_params),
          сервис вернет значение [nextPageToken](/docs/vpc/api-ref/Subnet/list#responses),
          \nкоторое можно использовать для получения следующей страницы. \nЗначение
          по умолчанию: 100.\n\nМаксимальное значение — 1000."
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: "Токен страницы. Установите значение [pageToken](/docs/vpc/api-ref/Subnet/list#query_params)
          равным значению поля [nextPageToken](/docs/vpc/api-ref/Subnet/list#responses)
          \nпрошлого запроса, чтобы получить следующую страницу результатов.\n\nМаксимальная
          длина строки в символах — 100."
        schema:
          type: string
      - name: filter
        in: query
        description: |-
          Параметры фильтрации ресурсов в ответе. В параметрах
          фильтрации указываются:
          1. Имя поля. В настоящее время фильтрация осуществляется только по полю [Subnet.name](/docs/vpc/api-ref/Subnet#representation).
          2. Оператор. Операторы `=` или `!=` для одиночных значений, `IN` или `NOT IN` для списков значений.
          3. Значение. Значение длиной от 3 до 63 символов, совпадающее с регулярным выражением `^[a-z][-a-z0-9]{1,61}[a-z0-9]$`.

          Максимальная длина строки в символах — 1000.
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSubnetsResponse'
      x-y-navtitle: list
    post:
      tags:
      - Subnet
      summary: "Создает подсеть в указанных каталоге и сети. \nМетод запускает асинхронную
        операцию, которую можно отменить перед тем, как она завершится."
      operationId: SubnetCreate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSubnetRequest'
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Operation'
      x-y-navtitle: create
      x-y-operation-metadata:
        $ref: '#/components/schemas/CreateSubnetMetadata'
      x-y-operation-response: '[Subnet](/docs/vpc/api-ref/Subnet#representation)'
  /vpc/v1/subnets/{subnetId}/operations:
    get:
      tags:
      - Subnet
      summary: Возвращает список операций для указанной подсети.
      operationId: SubnetListOperations
      parameters:
      - name: subnetId
        in: path
        description: |-
          Обязательное поле.
          Идентификатор ресурса Subnet, для которого запрашивается список операций.

          Максимальная длина строки в символах — 50.
        required: true
        schema:
          type: string
      - name: pageSize
        in: query
        description: "Максимальное количество результатов на странице ответа на запрос.
          Если количество результатов \nбольше чем [pageSize](/docs/vpc/api-ref/Subnet/listOperations#query_params),
          сервис вернет значение [nextPageToken](/docs/vpc/api-ref/Network/listOperations#responses),
          \nкоторое можно использовать для получения следующей страницы. \nЗначение
          по умолчанию: 100.\n\nМаксимальное значение — 1000."
        schema:
          type: string
          format: int64
      - name: pageToken
        in: query
        description: "Токен страницы. Установите значение [pageToken](/docs/vpc/api-ref/Subnet/listOperations#query_params)
          равным значению поля [nextPageToken](/docs/vpc/api-ref/Network/listSubnets#responses)
          \nпрошлого запроса, чтобы получить следующую страницу результатов.\n\nМаксимальная
          длина строки в символах — 100."
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListSubnetOperationsResponse'
      x-y-navtitle: listOperations
components:
  schemas:
    Address:
      description: Ресурс Адрес. Подробнее см. [документацию](/docs/vpc/concepts/address).
      type: object
      allOf:
      - type: object
        properties:
          id:
            description: Идентификатор адреса. Генерируется при создании.
            type: string
          folderId:
            description: Идентификатор каталога, которому принадлежит адрес.
            type: string
          createdAt:
            description: |-
              Время создания ресурса.

              Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
            type: string
            format: date-time
          name:
            description: |-
              Имя адреса.
              Имя должно быть уникальным в каталоге.
            type: string
          description:
            description: Описание адреса.
            type: string
          labels:
            description: Метки адреса в формате `ключ:значение`.
            type: object
            additionalProperties:
              type: string
          reserved:
            description: Определяет, зарезервирован ли адрес (статический ли он).
            type: boolean
            format: boolean
          used:
            description: Определяет, используется ли адрес.
            type: boolean
            format: boolean
      - title: address
        type: object
        maxProperties: 1
        properties:
          externalIpv4Address:
            $ref: '#/components/schemas/ExternalIpv4Address'
    AddressRequirements:
      type: object
      properties:
        ddosProtectionProvider:
          description: Идентификатор провайдера защиты от DDoS-атак.
          type: string
        outgoingSmtpCapability:
          description: Возможность отправки SMTP-трафика.
          type: string
    CreateAddressMetadata:
      type: object
      properties:
        addressId:
          description: Идентификатор создаваемого адреса.
          type: string
    CreateAddressRequest:
      type: object
      allOf:
      - type: object
        properties:
          folderId:
            description: |-
              Обязательное поле.
              Идентификатор каталога, в котором создается адрес.

              Чтобы получить идентификатор каталога, используйте запрос [list](/docs/resource-manager/api-ref/Folder/list).

              Максимальная длина строки в символах — 50.
            type: string
          name:
            description: "Имя адреса. \nИмя должно быть уникальным в каталоге.\n\nЗначение
              должно соответствовать регулярному выражению `` |[a-z][-a-z0-9]{1,61}[a-z0-9]
              ``."
            type: string
          description:
            description: |-
              Описание адреса.

              Максимальная длина строки в символах — 256.
            type: string
          labels:
            description: |-
              Метки адресов в формате `ключ:значение`.

              Не более 64 на ресурс.
              Длина строки в символах для каждого ключа должна быть от 1 до 63.
              Каждый ключ должен соответствовать регулярному выражению `` [a-z][-_0-9a-z]* ``.
              Максимальная длина строки в символах для каждого значения — 63.
              Каждое значение должно соответствовать регулярному выражению `` [-_0-9a-z]* ``.
            type: object
            additionalProperties:
              type: string
      - title: address_spec
        type: object
        maxProperties: 1
        properties:
          externalIpv4AddressSpec:
            $ref: '#/components/schemas/ExternalIpv4AddressSpec'
    CreateNetworkMetadata:
      type: object
      properties:
        networkId:
          description: Идентификатор создаваемой облачной сети.
          type: string
    CreateNetworkRequest:
      type: object
      properties:
        folderId:
          description: |-
            Обязательное поле.
            Идентификатор каталога, в котором создается облачная сеть.
            Чтобы получить идентификатор каталога, используйте запрос [list](/docs/resource-manager/api-ref/Folder/list).

            Максимальная длина строки в символах — 50.
          type: string
        name:
          description: |-
            Имя облачной сети. Имя должно быть уникальным в каталоге.

            Значение должно соответствовать регулярному выражению `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            Описание облачной сети.

            Максимальная длина строки в символах — 256.
          type: string
        labels:
          description: |-
            Метки ресурса в формате `` ключ:значение ``.

            Не более 64 на ресурс.
            Длина строки в символах для каждого ключа должна быть от 1 до 63.
            Каждый ключ должен соответствовать регулярному выражению `` [a-z][-_0-9a-z]* ``.
            Максимальная длина строки в символах для каждого значения — 63.
            Каждое значение должно соответствовать регулярному выражению `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
    CreateRouteTableMetadata:
      type: object
      properties:
        routeTableId:
          description: Идентификатор создаваемой таблицы маршрутизации.
          type: string
    CreateRouteTableRequest:
      type: object
      properties:
        folderId:
          description: |-
            Обязательное поле.
            Идентификатор каталога, к которому принадлежит таблица маршрутизации. Чтобы
            получить идентификатор каталога, используйте запрос [list](/docs/resource-manager/api-ref/Folder/list) .

            Максимальная длина строки в символах — 50.
          type: string
        name:
          description: |-
            Имя таблицы маршрутизации. Имя должно быть уникальным в каталоге.

            Значение должно соответствовать регулярному выражению `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            Описание таблицы маршрутизации.

            Максимальная длина строки в символах — 256.
          type: string
        labels:
          description: |-
            Метки ресурса в формате `` ключ:значение ``.

            Не более 64 на ресурс.
            Длина строки в символах для каждого ключа должна быть от 1 до 63.
            Каждый ключ должен соответствовать регулярному выражению `` [a-z][-_0-9a-z]* ``.
            Максимальная длина строки в символах для каждого значения — 63.
            Каждое значение должно соответствовать регулярному выражению `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        networkId:
          description: |-
            Обязательное поле.
            Идентификатор облачной сети, к которой принадлежит таблица маршрутизации.

            Максимальная длина строки в символах — 50.
          type: string
        staticRoutes:
          description: Список статических маршрутов в таблице маршрутизации.
          type: array
          items:
            $ref: '#/components/schemas/StaticRoute'
    CreateSubnetMetadata:
      type: object
      properties:
        subnetId:
          description: Идентификатор создаваемой подсети.
          type: string
    CreateSubnetRequest:
      type: object
      properties:
        folderId:
          description: "Обязательное поле.\nИдентификатор каталога, в котором создается
            подсеть. \nЧтобы получить идентификатор каталога, используйте запрос [list](/docs/resource-manager/api-ref/Folder/list).\n\nМаксимальная
            длина строки в символах — 50."
          type: string
        name:
          description: |-
            Имя подсети. Имя должно быть уникальным в каталоге.

            Значение должно соответствовать регулярному выражению `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            Описание подсети.

            Максимальная длина строки в символах — 256.
          type: string
        labels:
          description: |-
            Метки ресурса в формате `` ключ:значение ``.

            Не более 64 на ресурс.
            Длина строки в символах для каждого ключа должна быть от 1 до 63.
            Каждый ключ должен соответствовать регулярному выражению `` [a-z][-_0-9a-z]* ``.
            Максимальная длина строки в символах для каждого значения — 63.
            Каждое значение должно соответствовать регулярному выражению `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        networkId:
          description: |-
            Обязательное поле.
            Идентификатор облачной сети, в которой создается подсеть.

            Максимальная длина строки в символах — 50.
          type: string
        zoneId:
          description: |-
            Идентификатор зоны доступности, где находится подсеть. Чтобы
            получить список доступных зон, используйте запрос
            [list](/docs/compute/api-ref/Zone/list).

            Максимальная длина строки в символах — 50.
          type: string
        v4CidrBlocks:
          description: "CIDR. Диапазон внутренних адресов, определенных для этой подсети.
            \nЭто поле может быть задано только во время создания\nресурса Subnet
            и не может быть изменено. Например, 10.0.0.0/22 или 192.168.0.0/24. \nМинимальный
            размер подсети /28, максимальный размер подсети /16."
          type: array
          items:
            type: string
        v6CidrBlocks:
          description: Диапазоны IPv6 еще не доступны.
          type: array
          items:
            type: string
        routeTableId:
          description: |-
            ID of route table the subnet is linked to.

            Максимальная длина строки в символах — 50.
          type: string
    DeleteAddressMetadata:
      type: object
      properties:
        addressId:
          description: Идентификатор удаляемого адреса.
          type: string
    DeleteNetworkMetadata:
      type: object
      properties:
        networkId:
          description: Идентификатор удаляемой облачной сети.
          type: string
    DeleteRouteTableMetadata:
      type: object
      properties:
        routeTableId:
          description: Идентификатор удаляемой таблицы маршрутизации.
          type: string
    DeleteSubnetMetadata:
      type: object
      properties:
        subnetId:
          description: Идентификатор удаляемого ресурса Subnet.
          type: string
    Empty:
      description: Пустой JSON объект `` {} ``.
      type: object
      maxProperties: 0
    ExternalIpv4Address:
      type: object
      properties:
        address:
          description: Значение адреса.
          type: string
        zoneId:
          description: Зона доступности, из которой будет выделен адрес.
          type: string
        requirements:
          $ref: '#/components/schemas/AddressRequirements_in_ExternalIpv4Address_requirements'
    ExternalIpv4AddressSpec:
      type: object
      properties:
        address:
          description: Значение адреса.
          type: string
        zoneId:
          description: Зона доступности, из которой будет выделен адрес.
          type: string
        requirements:
          $ref: '#/components/schemas/AddressRequirements_in_ExternalIpv4AddressSpec_requirements'
    FieldMask:
      description: |-
        Имена всех обновляемых полей, разделенные запятыми.
        Только значения указанных полей будут изменены. Остальные останутся нетронутыми.
        Если поле указано в параметре `` updateMask `` и в запросе не было отправлено значение для этого поля,
        значение поля будет сброшено на значение по умолчанию. Значение по умолчанию для большинства полей — null или 0.

        Если в запросе не передается `` updateMask ``, значения всех полей будут обновлены.
        Для полей, указанных в запросе, будут использованы переданные значения.
        Значения остальных полей будут сброшены на значения по умолчанию.
      type: string
    ListAddressOperationsResponse:
      type: object
      properties:
        operations:
          description: Возвращает список операций для указанного адреса.
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество результатов
            больше чем [pageSize](/docs/vpc/api-ref/Address/listOperations#query_params), используйте `next_page_token` в качестве значения
            параметра [pageToken](/docs/vpc/api-ref/Address/listOperations#query_params) в следующем запросе списка ресурсов.

            Каждая следующая страница будет иметь свой `next_page_token` для продолжения перебора страниц результатов.
          type: string
    ListAddressesResponse:
      type: object
      properties:
        addresses:
          description: Список адресов.
          type: array
          items:
            $ref: '#/components/schemas/Address'
        nextPageToken:
          description: "Токен для получения следующей страницы результатов в ответе.
            Если количество результатов \nбольше чем [pageSize](/docs/vpc/api-ref/Address/list#query_params),
            используйте `next_page_token` в качестве значения\nпараметра [pageToken](/docs/vpc/api-ref/Address/list#query_params)
            в следующем запросе списка ресурсов.\n\nКаждая следующая страница будет
            иметь свой `next_page_token` для продолжения перебора страниц результатов."
          type: string
    ListNetworkOperationsResponse:
      type: object
      properties:
        operations:
          description: Список операций для указанной облачной сети.
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextPageToken:
          description: "Токен для получения следующей страницы результатов в ответе.
            Если количество результатов \nбольше чем [pageSize](/docs/vpc/api-ref/Network/listOperations#query_params),
            используйте [nextPageToken](/docs/vpc/api-ref/Network/listOperations#responses)
            в качестве значения \nпараметра [pageToken](/docs/vpc/api-ref/Network/listOperations#query_params)
            в следующем запросе списка ресурсов. Все последующие \nзапросы будут получать
            свои значения [nextPageToken](/docs/vpc/api-ref/Network/listOperations#responses)
            для перебора страниц результатов."
          type: string
    ListNetworkSubnetsResponse:
      type: object
      properties:
        subnets:
          description: Список подсетей, принадлежащих облачной сети, указанной в запросе.
          type: array
          items:
            $ref: '#/components/schemas/Subnet'
        nextPageToken:
          description: "Токен для получения следующей страницы результатов в ответе.
            Если количество результатов \nбольше чем [pageSize](/docs/vpc/api-ref/Network/listSubnets#query_params),
            используйте [nextPageToken](/docs/vpc/api-ref/Network/listSubnets#responses)
            в качестве значения \nпараметра [pageToken](/docs/vpc/api-ref/Network/listSubnets#query_params)
            в следующем запросе списка ресурсов. Все последующие \nзапросы будут получать
            свои значения [nextPageToken](/docs/vpc/api-ref/Network/listSubnets#responses),
            для перебора страниц результатов."
          type: string
    ListNetworksResponse:
      type: object
      properties:
        networks:
          description: Список ресурсов Network.
          type: array
          items:
            $ref: '#/components/schemas/Network'
        nextPageToken:
          description: "Токен для получения следующей страницы результатов в ответе.
            Если количество результатов \nбольше чем [pageSize](/docs/vpc/api-ref/Network/listSubnets#query_params),
            используйте [nextPageToken](/docs/vpc/api-ref/Network/list#responses)
            в качестве значения \nпараметра [pageToken](/docs/vpc/api-ref/Network/listSubnets#query_params)
            в следующем запросе списка ресурсов. Все последующие \nзапросы будут получать
            свои значения [nextPageToken](/docs/vpc/api-ref/Network/list#responses),
            для перебора страниц результатов."
          type: string
    ListRouteTableOperationsResponse:
      type: object
      properties:
        operations:
          description: Список операций для указанной таблицы маршрутизации.
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество
            результатов больше чем [pageSize](/docs/vpc/api-ref/RouteTable/listOperations#query_params), используйте [nextPageToken](/docs/vpc/api-ref/RouteTable/listOperations#responses) в качестве
            значения параметра [pageToken](/docs/vpc/api-ref/RouteTable/listOperations#query_params) в следующем запросе списка ресурсов. Все
            последующие запросы будут получать свои значения [nextPageToken](/docs/vpc/api-ref/RouteTable/listOperations#responses) для перебора страниц результатов.
          type: string
    ListRouteTablesResponse:
      type: object
      properties:
        routeTables:
          description: Список таблиц маршрутизации.
          type: array
          items:
            $ref: '#/components/schemas/RouteTable'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество
            результатов больше чем [pageSize](/docs/vpc/api-ref/RouteTable/list#query_params), используйте [nextPageToken](/docs/vpc/api-ref/RouteTable/list#responses) в качестве
            значения параметра [pageToken](/docs/vpc/api-ref/RouteTable/list#query_params) в следующем запросе списка ресурсов. Все
            последующие запросы будут получать свои значения [nextPageToken](/docs/vpc/api-ref/RouteTable/list#responses), для перебора страниц результатов.
          type: string
    ListSubnetOperationsResponse:
      type: object
      properties:
        operations:
          description: Список операций для указанного ресурса Subnet.
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextPageToken:
          description: "Токен для получения следующей страницы результатов в ответе.
            Если количество результатов \nбольше чем [pageSize](/docs/vpc/api-ref/Network/listSubnets#query_params),
            используйте [nextPageToken](/docs/vpc/api-ref/Subnet/listOperations#responses)
            в качестве значения \nпараметра [pageToken](/docs/vpc/api-ref/Network/listSubnets#query_params)
            в следующем запросе списка ресурсов. Все последующие \nзапросы будут получать
            свои значения [nextPageToken](/docs/vpc/api-ref/Subnet/listOperations#responses),
            для перебора страниц результатов."
          type: string
    ListSubnetsResponse:
      type: object
      properties:
        subnets:
          description: Список подсетей.
          type: array
          items:
            $ref: '#/components/schemas/Subnet'
        nextPageToken:
          description: |-
            Токен для получения следующей страницы результатов в ответе. Если количество результатов
            больше чем [pageSize](/docs/vpc/api-ref/Subnet/list#query_params), используйте [nextPageToken](/docs/vpc/api-ref/Subnet/list#responses) в качестве значения
            параметра [pageToken](/docs/vpc/api-ref/Subnet/list#query_params) в следующем запросе списка ресурсов. Все последующие
            запросы будут получать свои значения [nextPageToken](/docs/vpc/api-ref/Subnet/list#responses), для перебора страниц результатов.
          type: string
    Network:
      description: Ресурс Network. Подробнее см. [Сети](/vpc/concepts/network).
      type: object
      properties:
        id:
          description: Идентификатор облачной сети.
          type: string
        folderId:
          description: Идентификатор каталога, которому принадлежит сеть.
          type: string
        createdAt:
          description: |-
            Время создания ресурса в формате в [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).

            Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
          type: string
          format: date-time
        name:
          description: |-
            Имя облачной сети. Имя должно быть уникальным в каталоге.
            Длина имени должна быть от 3 до 63 символов.
          type: string
        description:
          description: |-
            Описание облачной сети. Длина описания должна быть от 0 до 256
            символов.
          type: string
        labels:
          description: Метки ресурса в формате `` ключ:значение ``. Максимум 64 метки
            на ресурс.
          type: object
          additionalProperties:
            type: string
        defaultSecurityGroupId:
          description: Идентификатор группы безопасности по умолчанию.
          type: string
    Operation:
      description: |-
        Ресурс Operation. Дополнительные сведения см. в разделе
        [Объект Operation](/docs/api-design-guide/concepts/operation).
      type: object
      allOf:
      - type: object
        properties:
          id:
            description: Идентификатор операции.
            type: string
          description:
            description: |-
              Описание операции. Длина описания должна быть от 0 до 256
              символов.
            type: string
          createdAt:
            description: |-
              Время создания ресурса в формате в
              [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).

              Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
            type: string
            format: date-time
          createdBy:
            description: |-
              Идентификатор пользователя или сервисного
              аккаунта, инициировавшего операцию.
            type: string
          modifiedAt:
            description: |-
              Время, когда ресурс Operation последний раз
              обновлялся. Значение в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).

              Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
            type: string
            format: date-time
          done:
            description: |-
              Если значение равно `false` — операция еще
              выполняется. Если `true` — операция завершена, и задано значение
              одного из полей `error` или `response`.
            type: boolean
            format: boolean
          metadata:
            description: |-
              Метаданные операции. Обычно в поле содержится
              идентификатор ресурса, над которым выполняется операция. Если
              метод возвращает ресурс Operation, в описании метода приведена
              структура соответствующего ему поля `metadata`.
            type: object
      - title: result
        type: object
        maxProperties: 1
        properties:
          error:
            $ref: '#/components/schemas/Status'
            description: Описание ошибки в случае сбоя или отмены операции.
          response:
            description: |-
              Результат операции в случае успешного завершения. Если
              исходный метод не возвращает никаких данных при успешном
              завершении, например метод Delete, поле содержит объект
              [google.protobuf.Empty](https://developers.google.com/protocol-buffers/docs/reference/google.protobuf#empty). Если исходный метод — это стандартный метод Create
              / Update, поле содержит целевой ресурс операции. Если метод
              возвращает ресурс Operation, в описании метода приведена
              структура соответствующего ему поля `response`.
            type: object
    RouteTable:
      description: "Таблица маршрутизации. \nДополнительные сведения см. в разделе
        [Статическая маршрутизация](/docs/vpc/concepts/static-routes)."
      type: object
      properties:
        id:
          description: Идентификатор таблицы маршрутизации.
          type: string
        folderId:
          description: Идентификатор каталога, к которому принадлежит таблица маршрутизации.
          type: string
        createdAt:
          description: |-
            Время создания в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt) .

            Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
          type: string
          format: date-time
        name:
          description: |-
            Имя таблицы маршрутизации. Имя должно быть уникальным в каталоге. Длина имени должна
            быть от 3 до 63 символов.
          type: string
        description:
          description: Описание таблицы маршрутизации. Длина описания должна быть
            от 0 до 256 символов.
          type: string
        labels:
          description: Метки ресурса в формате `` ключ:значение ``. Максимум 64 метки
            на ресурс.
          type: object
          additionalProperties:
            type: string
        networkId:
          description: Идентификатор облачной сети, к которой принадлежит таблица
            маршрутизации.
          type: string
        staticRoutes:
          description: Список статических маршрутов в таблице маршрутизации.
          type: array
          items:
            $ref: '#/components/schemas/StaticRoute'
    StaticRoute:
      description: Статический маршрут. Дополнительные сведения см. в разделе [Статическая
        маршрутизация](/docs/vpc/concepts/static-routes).
      type: object
      allOf:
      - title: destination
        type: object
        maxProperties: 1
        properties:
          destinationPrefix:
            description: Подсеть назначения в нотации CIDR.
            type: string
      - title: next_hop
        type: object
        maxProperties: 1
        properties:
          nextHopAddress:
            description: IP-адрес next hop.
            type: string
    Status:
      description: Описание ошибки в случае сбоя или отмены операции.
      type: object
      properties:
        code:
          description: Код ошибки. Значение из списка [google.rpc.Code](https://github.com/googleapis/googleapis/blob/master/google/rpc/code.proto).
          type: integer
          format: int32
        message:
          description: Текст ошибки.
          type: string
        details:
          description: Список сообщений с подробными сведениями об ошибке.
          type: array
          items:
            type: object
    Subnet:
      description: Ресурс Subnet. Подробнее см. [Подсети](/vpc/concepts/subnets).
      type: object
      properties:
        id:
          description: Идентификатор подсети.
          type: string
        folderId:
          description: Идентификатор каталога, которому принадлежит подсеть.
          type: string
        createdAt:
          description: |-
            Время создания ресурса в формате в [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).

            Строка в формате [RFC3339](https://www.ietf.org/rfc/rfc3339.txt).
          type: string
          format: date-time
        name:
          description: |-
            Имя подсети. Имя должно быть уникальным в каталоге.
            Длина имени должна быть от 3 до 63 символов.
          type: string
        description:
          description: Описание подсети. Длина описания должна быть от 0 до 256 символов.
          type: string
        labels:
          description: Метки ресурса в формате `` ключ:значение ``. Максимум 64 метки
            на ресурс.
          type: object
          additionalProperties:
            type: string
        networkId:
          description: Идентификатор облачной сети, к которой принадлежит подсеть.
          type: string
        zoneId:
          description: Идентификатор зоны доступности, где находится подсеть.
          type: string
        v4CidrBlocks:
          description: |-
            CIDR. Диапазон внутренних адресов, определенных для этой подсети.
            Это поле может быть задано только во время создания
            ресурса Subnet и не может быть изменено. Например, 10.0.0.0/22 или 192.168.0.0/24.
            Минимальный размер подсети /28, максимальный размер подсети /16.
          type: array
          items:
            type: string
        v6CidrBlocks:
          description: IPv6 еще не доступен.
          type: array
          items:
            type: string
    UpdateAddressMetadata:
      type: object
      properties:
        addressId:
          description: Идентификатор обновляемого адреса.
          type: string
    UpdateAddressRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateAddressRequest_updateMask'
        name:
          description: "Новое имя для адреса. \nИмя должно быть уникальным в каталоге.\n\nЗначение
            должно соответствовать регулярному выражению `` |[a-z][-a-z0-9]{1,61}[a-z0-9]
            ``."
          type: string
        description:
          description: |-
            Новое описание адреса.

            Максимальная длина строки в символах — 256.
          type: string
        labels:
          description: |-
            Метки адресов в формате `ключ:значение`.

            Имеющийся набор меток полностью перезаписывается переданным набором, поэтому если вы просто хотите
            добавить или удалить метку:
            1. Запросите текущий набор меток с помощью запроса [get](/docs/vpc/api-ref/Address/get).
            2. Добавьте или удалите метку в этом наборе.
            3. Отправьте новый набор в этом поле.

            Не более 64 на ресурс.
            Длина строки в символах для каждого ключа должна быть от 1 до 63.
            Каждый ключ должен соответствовать регулярному выражению `` [a-z][-_0-9a-z]* ``.
            Максимальная длина строки в символах для каждого значения — 63.
            Каждое значение должно соответствовать регулярному выражению `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        reserved:
          description: Определяет, зарезервирован ли адрес (статический ли он).
          type: boolean
          format: boolean
    UpdateNetworkMetadata:
      type: object
      properties:
        networkId:
          description: Идентификатор обновляемого ресурса Network.
          type: string
    UpdateNetworkRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateNetworkRequest_updateMask'
        name:
          description: |-
            Имя облачной сети. Имя должно быть уникальным в каталоге.

            Значение должно соответствовать регулярному выражению `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            Описание облачной сети.

            Максимальная длина строки в символах — 256.
          type: string
        labels:
          description: |-
            Метки ресурса в формате `` ключ:значение ``.

            Не более 64 на ресурс.
            Длина строки в символах для каждого ключа должна быть от 1 до 63.
            Каждый ключ должен соответствовать регулярному выражению `` [a-z][-_0-9a-z]* ``.
            Максимальная длина строки в символах для каждого значения — 63.
            Каждое значение должно соответствовать регулярному выражению `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
    UpdateRouteTableMetadata:
      type: object
      properties:
        routeTableId:
          description: Идентификатор обновляемой таблицы маршрутизации.
          type: string
    UpdateRouteTableRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateRouteTableRequest_updateMask'
        name:
          description: |-
            Имя таблицы маршрутизации. Имя должно быть уникальным в каталоге.

            Значение должно соответствовать регулярному выражению `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            Описание таблицы маршрутизации.

            Максимальная длина строки в символах — 256.
          type: string
        labels:
          description: |-
            Метки ресурса в формате `` ключ:значение ``.

            Не более 64 на ресурс.
            Длина строки в символах для каждого ключа должна быть от 1 до 63.
            Каждый ключ должен соответствовать регулярному выражению `` [a-z][-_0-9a-z]* ``.
            Максимальная длина строки в символах для каждого значения — 63.
            Каждое значение должно соответствовать регулярному выражению `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        staticRoutes:
          description: Список статических маршрутов.
          type: array
          items:
            $ref: '#/components/schemas/StaticRoute'
    UpdateSubnetMetadata:
      type: object
      properties:
        subnetId:
          description: Идентификатор обновляемого ресурса Subnet.
          type: string
    UpdateSubnetRequest:
      type: object
      properties:
        updateMask:
          $ref: '#/components/schemas/FieldMask_in_UpdateSubnetRequest_updateMask'
        name:
          description: |-
            Имя подсети. Имя должно быть уникальным в каталоге.

            Значение должно соответствовать регулярному выражению `` |[a-z][-a-z0-9]{1,61}[a-z0-9] ``.
          type: string
        description:
          description: |-
            Описание подсети.

            Максимальная длина строки в символах — 256.
          type: string
        labels:
          description: |-
            Метки ресурса в формате `` ключ:значение ``.

            Не более 64 на ресурс.
            Длина строки в символах для каждого ключа должна быть от 1 до 63.
            Каждый ключ должен соответствовать регулярному выражению `` [a-z][-_0-9a-z]* ``.
            Максимальная длина строки в символах для каждого значения — 63.
            Каждое значение должно соответствовать регулярному выражению `` [-_0-9a-z]* ``.
          type: object
          additionalProperties:
            type: string
        routeTableId:
          description: |-
            ID of route table the subnet is linked to.

            Максимальная длина строки в символах — 50.
          type: string
    AddressRequirements_in_ExternalIpv4Address_requirements:
      description: Параметры выделяемого адреса, например защита от DDoS-атак.
      type: object
      properties:
        ddosProtectionProvider:
          description: Идентификатор провайдера защиты от DDoS-атак.
          type: string
        outgoingSmtpCapability:
          description: Возможность отправки SMTP-трафика.
          type: string
    AddressRequirements_in_ExternalIpv4AddressSpec_requirements:
      description: Параметры выделяемого адреса, например защита от DDoS-атак.
      type: object
      properties:
        ddosProtectionProvider:
          description: Идентификатор провайдера защиты от DDoS-атак.
          type: string
        outgoingSmtpCapability:
          description: Возможность отправки SMTP-трафика.
          type: string
    FieldMask_in_UpdateAddressRequest_updateMask:
      description: |-
        Маска, определяющая, какие атрибуты адреса должны быть обновлены.

        Имена всех обновляемых полей, разделенные запятыми.
        Только значения указанных полей будут изменены. Остальные останутся нетронутыми.
        Если поле указано в параметре `` updateMask `` и в запросе не было отправлено значение для этого поля,
        значение поля будет сброшено на значение по умолчанию. Значение по умолчанию для большинства полей — null или 0.

        Если в запросе не передается `` updateMask ``, значения всех полей будут обновлены.
        Для полей, указанных в запросе, будут использованы переданные значения.
        Значения остальных полей будут сброшены на значения по умолчанию.
      type: string
    FieldMask_in_UpdateNetworkRequest_updateMask:
      description: |-
        Маска, определяющая, какие поля облачной сети будут обновлены.

        Имена всех обновляемых полей, разделенные запятыми.
        Только значения указанных полей будут изменены. Остальные останутся нетронутыми.
        Если поле указано в параметре `` updateMask `` и в запросе не было отправлено значение для этого поля,
        значение поля будет сброшено на значение по умолчанию. Значение по умолчанию для большинства полей — null или 0.

        Если в запросе не передается `` updateMask ``, значения всех полей будут обновлены.
        Для полей, указанных в запросе, будут использованы переданные значения.
        Значения остальных полей будут сброшены на значения по умолчанию.
      type: string
    FieldMask_in_UpdateRouteTableRequest_updateMask:
      description: |-
        Маска, определяющая, какие поля ресурса RouteTable будут обновлены.

        Имена всех обновляемых полей, разделенные запятыми.
        Только значения указанных полей будут изменены. Остальные останутся нетронутыми.
        Если поле указано в параметре `` updateMask `` и в запросе не было отправлено значение для этого поля,
        значение поля будет сброшено на значение по умолчанию. Значение по умолчанию для большинства полей — null или 0.

        Если в запросе не передается `` updateMask ``, значения всех полей будут обновлены.
        Для полей, указанных в запросе, будут использованы переданные значения.
        Значения остальных полей будут сброшены на значения по умолчанию.
      type: string
    FieldMask_in_UpdateSubnetRequest_updateMask:
      description: |-
        Маска, определяющая, какие поля ресурса Subnet будут обновлены.

        Имена всех обновляемых полей, разделенные запятыми.
        Только значения указанных полей будут изменены. Остальные останутся нетронутыми.
        Если поле указано в параметре `` updateMask `` и в запросе не было отправлено значение для этого поля,
        значение поля будет сброшено на значение по умолчанию. Значение по умолчанию для большинства полей — null или 0.

        Если в запросе не передается `` updateMask ``, значения всех полей будут обновлены.
        Для полей, указанных в запросе, будут использованы переданные значения.
        Значения остальных полей будут сброшены на значения по умолчанию.
      type: string
tags:
- name: Address
  description: Набор методов для управления адресами.
- name: Network
  description: Набор методов для управления облачными сетями.
- name: RouteTable
  description: Набор методов для управления таблицами маршрутизации.
- name: Subnet
  description: Набор методов для управления подсетями.

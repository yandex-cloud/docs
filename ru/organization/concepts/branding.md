---
title: Брендирование в {{ org-full-name }}
description: '{{ org-name }} позволяет настраивать внешний вид страниц аутентификации и формы входа, чтобы они соответствовали вашему корпоративному стилю и предпочтениям.'
---

# Брендирование


{% include [note-preview](../../_includes/note-preview.md) %}

В {{ org-name }} вы можете настраивать внешний вид страниц аутентификации и формы входа, чтобы они соответствовали вашему корпоративному стилю и предпочтениям.

Брендирование страниц аутентификации работает только при входе через SSO во внешние [приложения](./applications.md). Страницы для входа в другие сервисы {{ yandex-cloud }} не поддерживают брендирование.

{% note info %}

Функциональность доступна только для организаций, в которых [оплачен лимит](../pricing.md) пользователей {{ org-name }}, использующих SSO для внешних приложений. Если лимит был оплачен ранее, но в новом месяце оплаты не было, то брендирование прекратит работать.

{% endnote %}

Настроить брендирование могут только пользователи с [ролью](../security/index.md) `organization-manager.admin` или `organization-manager.organizations.owner`. Доступны следующие настройки отдельно для светлой и темной цветовой темы:

* загрузка собственных изображений в формате WebP, PNG, JPEG, JPG:
  * фоновое изображение (до 3840×2160 px, до 4 МБ);
  * логотип для верхней части формы (от 1920×1920 px, до 512 КБ);
  * фавиконка (до 512×512 px, до 512 КБ);
* цвет кнопок и рамок для текстовых полей;
* текст приветствия;
* шаблон почты для подсказки пользователю в поле ввода;
* включение и отключение способов входа с помощью Яндекс ID и [SSO](../../glossary/sso.md).

#### См. также {#see-also}

* [{#T}](../operations/manage-branding.md)
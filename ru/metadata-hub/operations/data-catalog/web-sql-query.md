---
title: Запросы к наборам данных с помощью {{ websql-full-name }}
description: С помощью сервиса {{ websql-name }} вы можете отправлять SQL-запросы к базам данных в кластерах, которые являются источниками данных для {{ data-catalog-name }}.
---

# Запросы к наборам данных с помощью {{ websql-full-name }}


{% note info %}

{% include [preview-pp](../../../_includes/preview-pp.md) %}

{% endnote %}


С помощью сервиса [{{ websql-full-name }}](../../../websql/index.yaml) вы можете отправлять SQL-запросы к базам данных в кластерах, которые являются источниками данных для {{ data-catalog-name }}.

{{ websql-name }} — это сервис {{ yandex-cloud }}, который позволяет подключаться к кластерам управляемых баз данных, работать с БД, таблицами и схемами и выполнять запросы. Сервис работает в браузере, не требует дополнительной авторизации и предлагает удобные подсказки для работы с SQL-командами.

Подробнее о работе с {{ websql-name }} см. в [документации сервиса](../../../websql/operations/index.md).

Чтобы выполнить SQL-запрос:

{% list tabs group=instructions %}

- Консоль управления {#console}

    1. В [консоли управления]({{ link-console-main }}) выберите [каталог ресурсов](../../../resource-manager/concepts/resources-hierarchy.md#folder), в котором создан каталог метаданных.
    1. Выберите сервис **{{ ui-key.yacloud.iam.folder.dashboard.label_metadata-hub }}**.
    1. На панели слева выберите ![image](../../../_assets/console-icons/folder-magnifier.svg) **{{ ui-key.yacloud.iam.folder.dashboard.label_data-catalog }}**.
    1. В открывшемся списке выберите каталог метаданных.
    1. На панели слева выберите ![image](../../../_assets/console-icons/database-magnifier.svg) **{{ ui-key.yacloud.data-catalog.title_search-data }}**.
    1. Перейдите в объект метаданных с типом **Набор данных** одним из способов:

        * Нажмите на имя объекта с типом **Набор данных**.
        * Нажмите на имя объекта с типом, отличным от **Набор данных**. С помощью вкладки **Дочерние** переходите к дочерним объектам, пока не будет найден объект с типом **Набор данных**. Нажмите на имя этого объекта.

    1. В поле **Данные** нажмите ссылку **Открыть в WebSQL**.
    1. Выберите нужное подключение к базе данных кластера-источника. Будет выполнен переход в сервис {{ websql-name }}.
    1. В правой верхней панели введите SQL-запрос и нажмите **Выполнить**.

        С помощью команд SQL можно, например:

        * [выполнять мультизапросы](../../../websql/operations/query-executor.md#multiple-queries);
        * [выгружать результаты выполнения запроса](../../../websql/operations/query-executor.md#export-results);
        * [использовать AI-ассистента для анализа и оптимизации запросов](../../../websql/operations/ai-assistant.md);
        * [сохранять запрос в качестве шаблона и делиться им с другими пользователями](../../../websql/operations/templates.md#save-query);
        * [использовать запросы из истории запросов](../../../websql/operations/history.md).

{% endlist %}

{% note info %}

Если источником данных является кластер {{ mpg-full-name }}, то c помощью команд SQL нельзя выполнять действия, требующие прав суперпользователя.

{% endnote %}

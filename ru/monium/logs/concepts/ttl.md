# Время хранения логов (TTL)

Срок хранения логов в {{ monium-name }} (TTL) — 31 день.

Начало хранения отсчитывается от времени появления события `time_unix_nano`, и по истечении `TTL` строчки с логами удаляются безвозвратно.
